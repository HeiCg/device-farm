// Package mmap — POSIX shared-memory reader for the emulator MMAP image
// transport (one shm region per display, lock-free seq ring).
// (Wave 2 fills in Region; Wave 0 ships scaffolds only.)
package mmap
