node_modules/
dist/
.vendor/
# Live-run outputs are regenerated by the harness; keep RESULTS.md tracked if you
# choose to commit a run, but ignore the raw per-config dumps by default.
results/*.capture.json
results/*.jsonl
