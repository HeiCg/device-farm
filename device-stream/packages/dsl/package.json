{
  "name": "@device-stream/dsl",
  "version": "0.1.0",
  "description": "High-level selector + orchestration DSL on top of @device-stream/android-server (HTTP) and WDA (iOS). Designed to be executed inside device-farm hook-setup scripts and to power IDE autocomplete via its .d.ts surface.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "vitest run --config ../../vitest.config.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@device-stream/core": "1.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
