# Ticket: adversarial review of phase 3h, phase 3i and screen-graph C.3 before any number enters the scoreboard

Repo: ARGENT FORK (`HeiCg/argent`). READ-ONLY review; you do not edit code.
Read each branch in its own throwaway worktree under your scratch dir
(`git worktree add <scratch>/rev-<name> <branch>`), never in `argent-*`
worktrees. NO local emulator, NO adb, no physical device. Evidence comes from
the branches, the unit tests (run them offline), and the CI artifacts
(`gh run download <id> --repo HeiCg/argent -D <scratch>`).

Rules of evidence (owner's): a number is valid only if it names the
statistic (p50/p95), the block, N and the run id; OFF and ON must be
like-for-like in the same run; a claim without a log line is a claim, not a
result; hidden failures (`continue-on-error`, `|| true`, `| tee` without
pipefail) void the run. Previous retractions in this effort were all
artifacts of this kind (0 ms vs 50 ms hold; prune that never fired; needles
matching the launch screen; scrcpy tap timed while never landing).

## A. Phase 3h — branch `feat/android-open-server-p3h` (993aa70e+), CI branch `feat/bench-ci-3h`

Claim under review: scrcpy fast-inject tap now lands because the timeline
became DOWN → MOVE(same x,y, hold midpoint) → UP; root cause stated as "async
UP arrives before the async DOWN has established a touch target for a static
press".

1. Root cause is suspect. `InputManager.injectInputEvent` in async mode
   enqueues in order; ordering inversion between DOWN and UP from one
   injector thread should not happen. Look for the real reason a static
   DOWN/UP is dropped: scrcpy server's `Controller` / `PointersState` /
   `Device.injectEvent` handling of a DOWN with `pressure`, `buttons`,
   `actionButton`, and `pointerId` when `videoWidth/videoHeight` are 0
   (control-only), and the `PositionMapper`/`ScreenInfo` path. Compare with
   scrcpy 3.3.1 client's click (which sends no MOVE). Either confirm the
   agent's mechanism with a logcat/scrcpy-server line from the CI artifact
   or state the correct mechanism. A fix that works for the wrong reason is
   allowed to ship, but the doc must say "mechanism unconfirmed" if it is.
2. Like-for-like: the scrcpy tap is now 3 frames; Kotlin and proprietary taps
   are 2. Same-point MOVE has zero displacement so app semantics are the same
   tap; verify `holdMs` is identical across blocks (parity record in the
   block JSON) and that the MOVE adds no wall-clock beyond the hold.
3. Effect check: confirm the before/after describe-label fingerprint is
   taken OUTSIDE the timed window in every block (OFF and ON), that the
   navigating target is derived identically per block, that the harness
   returns to the origin screen between iterations, and that `effectZero`
   really fails the block (read the code path and the run log).
4. Workflow: `continue-on-error` removed from the device-test step; the run
   is green with the device test enforced; the device test asserts the
   scrcpy tap lands (pngDiffRatio > 0 and destination seen).
5. Numbers: tap and tap+describe p50/p95 for all four blocks from the 3h
   run; OFF-1 vs OFF-2 drift; `fastInjectFallbacks` per block must be 0 in
   the ON-scrcpy block.

## B. Phase 3i — branch `feat/android-open-server-p3i`, CI branch `feat/bench-ci-3i`

Claim under review: idle describe ON ≤ OFF + 10 ms after cutting host/
transport cost.

1. Goldens: `test/open-server-trim-golden.test.ts` and
   `open-server-window-goldens.test.ts` pass byte-identical; tokens remain
   657 = 657 in the run's scoreboard.
2. Micro-bench honesty: fixture is committed, N and p50 per stage reported,
   stages sum to something close to the measured host time (if the stages
   sum to 20 ms and the residual is 70 ms, the map is incomplete — say so).
3. Each cut has a before/after number from the SAME method (micro-bench for
   host stages, CI run for end-to-end). `isFlagEnabled` cache: invalidation
   on setFlag/unsetFlag covered by a test; no stale-flag behaviour for the
   `open-device-server` flag toggled mid-process.
4. Kotlin `tcpNoDelay`: present on the accepted socket; `ping` p50 reported.
5. Numbers: idle describe OFF vs ON p50/p95 all four blocks, same run id;
   after-tap describe unchanged or better; no regression in any other row of
   the scoreboard vs run 33743850196 beyond noise (state OFF-1/OFF-2 drift as
   the noise floor).

## C. Screen-graph C.3 — branches `feat/screen-graph-c3`, `feat/bench-ci-c3`

Claim under review: H4 measured with a valid oracle; pre-flight gates the
matrix; O5 locateFailed explained.

1. Workflow pins the screen-graph checkout to the head of
   `feat/screen-graph-c3` (not 5b1e6e98); pre-flight and matrix steps run
   with `set -euo pipefail`; `preflightVerdict` exits non-zero on BAD/MISSING
   and has unit tests.
2. The final run's `sg-preflight.log` contains `PROBLEM needles: 0`. Every
   changed needle has a justification line from capture/oracle output; spot
   check 5 needles against the launch-screen and destination-screen dumps —
   a needle present on BOTH screens is a false pass.
3. B1 validity: taps located via the tree (no centre taps, no silent
   `locateViaDump` failure); per-config exclusions accounted, not dropped.
4. O5: the explanation matches the per-step reasons in the matrix JSON; the
   count after the change is reported; if the harness was changed, the change
   does not advantage O5 (same oracle, same task list, same steps).
5. Numbers: per-config success, exclusions, fallbacks, tokens/step, RTT/step,
   H1–H4 with statistic and run id; the oracle-independent tokens/step must
   still be ~B1 473, B2 447, O1 67, O2 32, O4 63 (same tier code, so a large
   change means the harness changed what it measures).

## Output

Write `/Users/heicg/Desktop/projects/device-farm/docs/specs/2026-09-03-review-3h-3i-c3-findings.md`
with, per section A/B/C: verdict ACCEPT / ACCEPT-WITH-CAVEATS / REJECT, the
findings as one line each (`file:line` or run id + log line, severity,
what is wrong, what would fix it), and the exact numbers you consider
scoreboard-grade (statistic, block, N, run id). Do not soften: if a number
is void, say void and why. Report the same in your final message.
