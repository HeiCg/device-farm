---
gsd_state_version: 1.0
milestone: v3.0
milestone_name: Spec-Driven Architecture
current_plan: 37-05 phase-close — COMPLETE; v3.0 PROVISIONAL
status: unknown
stopped_at: Completed 37-05-PLAN.md
last_updated: "2026-05-17T16:16:59.980Z"
last_activity: 2026-05-17
progress:
  total_phases: 19
  completed_phases: 19
  total_plans: 128
  completed_plans: 128
  percent: 75
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-16)

**Core value:** QAs e pipelines CI conseguem rodar testes Maestro em emuladores gerenciados automaticamente e acompanhar a execucao em tempo real
**Current focus:** v3.0 — Spec-Driven Architecture (Phase 18 CLOSED; Phase 19 Reporting Migration Webhooks+DLQ CLOSED; Phase 20 Pool Module CLOSED; Phase 21 Artifacts Module CLOSED; Phase 22 Streaming Module CLOSED; Phase 23 Jobs Module Keystone CLOSED; Phase 24 Maestro Module CLOSED; Phase 25 Pipelines Module CLOSED; Phase 26 Auth Module CLOSED; Phase 31 Quick Wins CLOSED; Phase 32 SimulatorKit Private Bridge CLOSED; Phase 33 Android gRPC EmulatorController CLOSED; Phase 34 Session API + MCP Server CLOSED; Phase 35 App Explorer + Atlas Graph CLOSED; Phase 36 Physical Devices + Discovery + ⌘K CommandPalette CLOSED; Phase 37 Platform Extensions CLOSED — **v3.0 PROVISIONALLY COMPLETE** pending milestone retrospective)

## Current Position

Milestone: v3.0 Spec-Driven Architecture — PROVISIONALLY COMPLETE
Phase: 37 Platform Extensions — COMPLETE 2026-05-16
Plan: 6 of 6 complete (37-05 phase-close shipped 2026-05-16)
Current Plan: 37-05 phase-close — COMPLETE; v3.0 PROVISIONAL
Total Plans in Phase: 6

**Phase 37 closed roll-up (2026-05-16):** Platform Extensions — 4 independent tracks closing v3.0. Plan 37-05 ships the phase-close artifacts. Tracks delivered:

- **Track A (EXT-IOS-SKELETON):** Go Mach-O parser + RN-Hermes scan (`cli/internal/macho/`), `device-farm analyze` CLI, server/analysis module with `POST /api/builds/:id/skeleton`, web viewer at `/builds/[id]/skeleton` with confidence-graded screen table + deep-link list + CTA to Phase 35 exploration seeding.
- **Track B (EXT-PREFLIGHT):** server/preflight module with rule engine + 6-rule pack covering 4 ITMS-91053 categories + ATT/IDFA + encryption-export warning, `POST /api/preflight` multipart endpoint, web drag-drop UI with red/yellow/green checklist surfacing rules_updated date.
- **Track C (EXT-GITHUB-PR):** server/integrations/github plugin mirroring server/azure 1:1 with `@octokit/app` + `@octokit/auth-app` + `@octokit/webhooks-methods.verify` (constant-time HMAC), CLI `--github-pr` flag, idempotent comment upsert via `pipeline_runs.github_pr_comment_id`, sandbox-repo manual verification documented in 37-VALIDATION.md (operator round-trip remains a deferred manual step — runbook + automated tests cover algorithmic correctness).
- **Track D (EXT-INPUT-BROADCAST + EXT-BUILD-ONCE):** InputBroadcaster + BuildOnceDeployN ported from kittyfarm 1:1 via `Promise.allSettled` (no rollback), `POST /api/sessions/broadcast` endpoint, parallel-deploy job mode with config-driven parallelism cap (503 + Retry-After on overflow), CLI `--parallel` + `--broadcast-input` flags.

Wave 2 cross-cutting: webhook payload extension (`preflight` + `analysis` + `parallelDeploy` optional fields, additive per SPEC-08 — 6 vitest specs prove backward compat); 4 MODULE.md files polished to MOD-01 9-section convention with Phase 37 deferrals subsections; deferred-items catalog (Android preflight rules v3.1, GitLab MR mirror Azure, bitcode analysis, Figma checker, push testing, security scanner, OAuth PAT fallback, rule-pack update mechanism, Hermes precision tuning). All 5 EXT-* pseudo-IDs traceable in REQUIREMENTS.md out-of-band section. **v3.0 milestone PROVISIONALLY COMPLETE pending milestone retrospective.**

**Phase 36 CLOSED roll-up (2026-05-16):** Physical Devices + Discovery + ⌘K CommandPalette shipped across 5 plans / 4 waves same-day. Six requirements landed: DISC-SVC (DeviceDiscoveryService — sole bare adb/simctl caller, 5s poll, fingerprint-dedup, 3 event streams via uuidv5 aggregate id); PAIR-WIRELESS (wireless ADB pairing service — bonjour-service mDNS browse + argv-array adb pair/connect + QR builder + 60s TTL session state machine + cross-subnet rejection); PHYS-ANDROID-DRIVER (PhysicalAndroidDriver under `android-physical` DriverKey extension); CMD-PALETTE (hand-rolled ⌘K Svelte 5 palette, ~150 LOC, fuzzysort, recents, ≥7 actions); DISC-WS (`WS /api/devices/stream` + web `deviceStore.applyFrame` + auto-reconnect); PAIR-WIZARD-UI (3-step `/devices/pair` Svelte 5 wizard — discriminated-union $state, fetch POST `/api/devices/pair/start`, WS `/api/devices/pair/stream`, 60s countdown ring, error/retry handlers, unmount cleanup). External Dependencies Policy upheld — zero `simvyn`/`simutil`/`kittyfarm` imports added; 4 npm deps pinned through `36-00` (bonjour-service + qrcode + @types/qrcode + fuzzysort). Operator runbook `docs/runbooks/wireless-android.md` (196 lines / 1089 words / 5 H2 sections / 7 troubleshooting cases / 6-item Pixel-8 verification checklist) closes the surface. Pool event registry: 5 → 9 entries (4 new), all TRACE-08-correct (`device.discovered.added` + `device.discovered.removed` + `device.pair.attempted` PERSISTED; `device.discovered.changed` transient). dep-cruiser rule 13 `no-bare-adb-list-outside-discovery` shipped with 7-test runtime grep-guard (negative-lookbehind exempts `this.execFile` DI wrappers; 2 production simctl callers migrated to per-UDID form rather than relax the regex). createPoolModule factory 6 → 8 keys (discoveryService + pairingService added). Wizard tests: 18 vitest specs (file existence × 6 + state-machine reducer × 8 + computeRemainingSec × 3 + isTerminal × 1) — pure-logic extraction pattern (`pairing-wizard-logic.ts`) mirrors `palette-logic.ts` (web/ has no vite-plugin-svelte test integration). Baseline gates preserved: tsc 24 errors / dep-check 5 violations / svelte-check 21 errors / 13 warnings. Plan 36-04 ships 2 task commits (RED `test(36-04): add failing PairingWizard state-machine spec` + GREEN `feat(36-04): implement pairing wizard UI`) + 1 metadata commit (this SUMMARY). Manual real-Pixel-8 verification is deferred — auto-mode chain runs without a real device available; the runbook documents the operator checklist for first-deployment validation. Phase 37 (Platform Extensions — iOS skeleton + Preflight + GitHub PR + InputBroadcaster) unblocked.

**Phase 36 Plan 00 (Wave 0 substrate) roll-up (2026-05-16):** Substrate complete in ~27 min / 3 task commits (0bd124a + 41958d4 + 2045957). 20 new files + 13 modified. Pool event registry extended additively 5 → 9 entries: 3 discovery events (`device.discovered.added` PERSISTED + `device.discovered.removed` PERSISTED + `device.discovered.changed` transient — TRACE-08 split: added/removed audit-relevant for physical devices joining/leaving, changed derivable from added/removed pairs) + 1 pairing audit (`device.pair.attempted` PERSISTED with outcome enum auth-fail/timeout/cross-subnet/unknown). `server/pool/schemas.ts` gains `discoveredDevicePayloadSchema` (id + name + platform + state booted|shutdown|unauthorized|offline + deviceType Emulator|Simulator|Physical + osVersion/model nullable). `server/pool/index.ts` barrel re-exports the 4 new event names + payload schemas + DiscoveredDevicePayload type. `.dependency-cruiser.cjs` gains rule 13 `no-bare-adb-list-outside-discovery` (path-based: only `server/pool/internal/discovery/adapters/**` may shell out to bare `adb devices` / `simctl list devices`; runtime grep-guard spec stub lands in Plan 36-01). 14 server stub files: discovery (types + fingerprint + poller + index barrel + android/ios adapters), wireless (mdns + pair + connect + qr + session.ts), physical-android driver implementing DeviceDriver, 2 API route stubs (devices-stream WS + pairing REST/WS). 6 web stub files: CommandPalette.svelte (Svelte 5 with openPalette/closePalette exports for 36-03 ⌘K wiring), command-palette/{registry,recent.svelte,fuzzy}.ts, devices.svelte.ts store, /devices/pair route. 11 vitest spec stubs (8 server + 3 web) — all `it.todo` rows matching VALIDATION.md. 4 new npm deps pinned: bonjour-service@^1.3.0 + qrcode@^1.5.4 + @types/qrcode@^1.5.5 (server) + fuzzysort@^3.1.0 (web). STUDY-ONLY policy honored: zero `simvyn` / `simutil` / `kittyfarm` strings in package.json diff. 3 auto-fixed deviations (1 Rule 2 missing-test-coverage in events.spec for new entries + 2 Rule 1 structural-type-completeness fixes in NOOP_POOL_EMIT and capture stubs). tsc baseline 24 errors maintained; dep-check 5 baseline violations maintained (zero new from 25 added modules); svelte-check 21 baseline maintained (zero new in 6 web files). Plans 36-01 (DISC-SVC body) + 36-02 (PAIR-WIRELESS + PHYS-ANDROID-DRIVER + DISC-WS) + 36-03 (CMD-PALETTE) + 36-04 (PAIR-WIZARD-UI) all unblocked with exact stub-file write targets.

Previous status preserved below for Phase 35 archival. **Phase 35 CLOSED roll-up (2026-05-16):** App Explorer + Atlas Graph fully shipped across 7 plans / 7 waves same-day. 3 Drizzle tables (`explorations`, `exploration_screens`, `exploration_transitions`) via migration `0010_explorations.sql` (3 CREATE TABLE + 2 UNIQUE INDEX + 9 secondary indexes + 4 FKs). Claude Agent SDK driven BFS via in-process MCP server (`exploration-state` with 4 explore_* tools: save_screen / save_transition / mark_element_explored / get_unexplored / finish) + external `@device-stream/mcp` stdio server (7 device_* tools); inline sharp-phash (Hamming<8) + grayscale RMSE (<0.02) for loop detection — fixtures confirm wide margins (Hamming 0/29, RMSE 0/0.043); StuckDetector sliding window of 3 emits `exploration.stuck` on 3rd consecutive same-screen + carries `{stuckCount}` back through tool return for agent adaptation; budget caps enforced server-side (tap counter intercept → watchdog.tripBudget → queryInstance.return; screen counter pre-check; wall-clock setTimeout Watchdog). WS event stream at `GET /api/explorations/:id/events` with 6 discriminated-union frame variants (screen-discovered / transition / tool-call / stuck / finished / error) + 200-event ring buffer + 30s heartbeat + correlationId TRACE-06 propagation. REST surface: POST/GET-list/GET-by-id/DELETE/GET-report.md + WS upgrade. CLI `device-farm explore` (Cobra) with 8 flags (--apk required + --bundle-id auto-detect via aapt/plutil + 6 budget/model flags) + global --json + 4 exit codes (0/1/2/3/4). Web UI port of `_reference/app-explorer/frontend/src/components/AtlasGraph.tsx` (459 LoC React → ~140 LoC Svelte 5) + ScreenPanel + JourneyPanel to @xyflow/svelte + @dagrejs/dagre with BFS-aware dagre layout (tree edges only feed dagre; back-edges drawn as dashed overlays). Markdown + Mermaid reports via `GET /api/explorations/:id/report.md` — port of `_reference/app-explorer/app_explorer/report.py` (152 lines) with sidSafe + describeAction + buildMermaid + enumerateJourneys + buildReport. MODULE.md full 9 sections + Runnable Example H3 (Purpose / Public API / 7 Events Emitted / Events Consumed / exploration.run Queue Produced / Queue Consumed / 7 Invariants / 7 Non-Goals / Dependencies). Barrel `server/explorations/index.ts` 92 lines full surface — 0 export * (MOD-02 strict); re-exports factory + plugin + events surface (registry + emitters + 7 payload schemas + 7 payload types) + REST/row-decoder schemas + queue + WS surface. plugin-order.spec extended with Phase 35 additive block (6 sub-assertions: dep-order after auth + after websocket-plugin + before static + structural deps-literal extraction matching canonical 5-entry `['config','db','event-bus','queue','auth']` + grep-friendly literal + MODULE.md exactly 9 H2 sections). dep-cruiser 12th rule `no-deep-imports-into-explorations-internal` shipped Plan 35-00. 12TH persistEnvelope sample point landed (DEFERRED-35-A inherits DEFERRED-26-B chain → Phase 27+). 7 EXP-* requirements closed (EXP-SCHEMA 35-01, EXP-AGENT + EXP-LOOP 35-02, EXP-WS 35-03, EXP-CLI 35-04, EXP-UI 35-05, EXP-REPORT 35-06). Final sweep gates: lint 0 NEW (1 pre-existing `_helpers.js` no-explicit-any baseline); tsc 24 errors total all baseline (DEFERRED-15-A inherited, 0 NEW in explorations); dep-check 5 baseline violations preserved; npm test 103 baseline failures unchanged (pre-existing Plan 33 emulator/grpcPort breaks + sessions/pipelines/azure inheritance); CLI `make test` clean modulo pre-existing `cli/internal/types/unions.go` errors logged DEFERRED; svelte-check 21 baseline (Plan 35-05 reduced 22→21 fixing atlas-graph type errors); nyquist gate exit 0 with delta +3.01pp (improvement). 17 DEFERRED-35-* items + 3 carry-forwards catalogued. Plan 35-06 ships 2 task commits (642de14 report.ts + route + fixtures + spec / 85f8192 MODULE.md + barrel + plugin-order extension) in ~25 min. Phase 36 (Physical Devices + Command Palette) unblocked.

**Phase 35 Plan 01 (Wave 1 REST routes + factory body) roll-up (2026-05-16):** Complete in ~24 min / 4 task commits (b0eaec1 + 749693a + 6efdbd8 + 1800b3c). `POST/GET/DELETE /api/explorations` operational end-to-end (sessions integration opportunistic, falls back to direct pool allocation + sentinel sessionId). `createExplorationsModule` factory replaces 35-00 throw-stub: per-module TypedBus + 12TH persistEnvelope sample (DEFERRED-26-B carry to Phase 27+) + 7 typed emit helpers + idempotent `enqueueRun(runId)` + idempotent `shutdown()`. Events registry filled — TRACE-08 split: 4 persisted (started/stuck/finished/failed for audit + terminal) + 3 transient (screen/transition/toolCall for high-frequency derivable). `EXPLORATIONS_AGGREGATE_ID` v5 UUID re-derived at runtime via uuidv5 + asserted equal at boot. SPEC-04 Zod row decoders applied on all read paths (`explorationRowSchema`/`explorationScreenRowSchema`/`explorationTransitionRowSchema`). Plugin order valid: `explorationsPlugin` registered between `sessionsPlugin` and `websocketPlugin` (sessions integration available + WS endpoint follows). 5 auto-fixed deviations: (1) Rule 1 — `explorationRowSchema` missing `ownerApiKeyId`+`ownerActor` (Wave-0 stub oversight); (2) Rule 3 — `actorSchema` deep import → switched to `auth/index.js` barrel for MOD-02 compliance; (3) Rule 3 — DELETE 204 `z.void()` dropped from Zod schema (DEFERRED-15-A `fastify-zod-openapi` v5 cannot serialize empty body); (4) Rule 3 — routes.spec test seed missing `sessions` row, FK fails; (5) Rule 3 — `artifacts` table column mismatch with plan pseudocode (correct: `type/filePath/fileName/mimeType/fileSizeBytes` + non-null `jobId` FK). All 5 essential, no scope creep. EXP-SCHEMA requirement fully verified including FK cascade-delete behavior. Plan 35-02 (agent runner) unblocked — can call `fastify.explorationsModule.emit.*` directly; `enqueueRun` graceful no-op pattern means POST→agent flow lights up the moment 35-02 lands without further 35-01 changes.

**Phase 35 Plan 00 (Wave 0 substrate) roll-up (2026-05-16):** Substrate complete in ~19 min / 4 task commits (bd4672e + 2e5785a + ba118d4 + 8f01ad7). 22 new files + 6 modified. New `server/explorations/` module skeleton (events.ts 7-name stub + internal/module.ts throw-stub + queue.ts EXPLORATION_RUN alias + schemas.ts FULL 7-schema REST surface with .meta() OpenAPI ids + MODULE.md Purpose-only placeholder + index.ts 1-line MOD-02 barrel). `server/db/schema.ts` extended with explorationStatusEnum + 3 pgTables (explorations / exploration_screens / exploration_transitions). Drizzle migration 0010_explorations.sql (renamed from drizzle-kit slug `0010_dashing_ezekiel_stane.sql` + journal idx-10 retagged) — 3 CREATE TABLE, 2 UNIQUE INDEX, 9 secondary indexes, 4 FKs (devices, sessions, artifacts ×2, api_keys). `server/queue/names.ts` QUEUE_NAMES extended 13 → 14 entries with `EXPLORATION_RUN: 'exploration.run'`. `.dependency-cruiser.cjs` gains 12th forbidden rule `no-deep-imports-into-explorations-internal` mirroring rule 11 verbatim. `__fixtures__/dep-cruiser/bad-explorations-deep-import.ts` fires the new rule. `server/hooks/__tests__/dep-cruiser.spec.ts` extended with `[MOD-02 explorations extension]` it-block (12 it-blocks now total). 11 vitest spec stubs (events.spec substantive + 10 skip-stubs). 3 PNG fixtures (~11KB each, sharp-generated for pHash crossover tests). New `prompts/` top-level directory with `exploration.md` stub. `cli/cmd/explore.go` Cobra skeleton + test passes. 3 web stubs (list + detail routes + atlas-graph component + atlas spec). npm install added 3 deps (@anthropic-ai/claude-agent-sdk ^0.3.143, sharp-phash ^2.2.0, sharp ^0.34.5) + bumped @anthropic-ai/sdk to ^0.96.0 (peer-dep satisfaction, all 120 sessions tests still pass under new SDK). 2 auto-fixed deviations (Rule 3): EventRegistry import path corrected to ../bus/types.js; SDK peer-dep bump. 2 pre-existing issues logged to deferred-items.md (Go unions.go missing Job*Message types + pipeline-schema.ts Zod argcount — both pre-existing, out of scope). Plans 35-01..35-06 unblocked for parallel authoring.

**Phase 34 SHIPPED (2026-05-16):** 9 plans across 7 waves (0,1,2,3,4,5,5,5,6) shipped same-day. REST POST/DELETE/GET `/api/sessions` with Zod-typed schemas + OpenAPI emission; WS `/ws/sessions/:id?token=` with 11 client + 4 server envelope types; action dispatch into device-stream/android + iOS simctl; NL resolver chain (MaestroAi default + ClaudeVision opt-in via SESSION_RESOLVER=claude-vision); rate limit 30/10s + pg-boss sweeper (30s tick with 6-field-cron preferred / 5-field+setInterval fallback per RESEARCH Open Q #1); `@device-stream/mcp` npm workspace with 12 tools + `device-farm://devices` resource; `device-farm session [lease|tap|type|swipe|key|screenshot|release]` Cobra subcommands with `~/.device-farm/session.json` persist; web UI `/sessions` list + `/sessions/[id]` detail with click-to-tap canvas overlay; TLS-first WS scheme guard (force-upgrade non-loopback plaintext); 4 events emitted (session.leased/released/expired/device.lost — all persisted, aggregateType=`session`); device.health.failed subscriber wired; 3 operator runbooks (session-api.md / mcp.md / session-resolver-costs.md) + 2 agent skeletons (pr-bot.md / exploration.md); MODULE.md 10 H2 sections + runnable example; plugin-order.spec extended with 8 additive Phase 34 assertions; deferred-items.md catalogs 7 items (DEFERRED-34-A persistEnvelope 11TH SAMPLE → Phase 27+; DEFERRED-34-B multi-session-per-device → v3.1; DEFERRED-34-C iOS resolver parity → Phase 36/37; DEFERRED-34-D per-session resolver cost cap → Phase 37; DEFERRED-34-E MCP resource expansion → Phase 35; DEFERRED-34-F live /sessions WS updates → Phase 36 CommandPalette; DEFERRED-34-G Maestro `--ai-prompt` shell-out fallback — cleared as never-needed pending 30-day prod verification). Phase 35 (App Explorer) unblocked.

**Phase 34 Plan 00 (Wave 0 substrate) roll-up (2026-05-16):** Substrate complete in ~12 min / 3 task commits (e2c0181 + 0e608ee + f45e4d2). 31 new files + 6 modified. New `server/sessions/` module skeleton (events.ts 4-name stub + internal/module.ts throw-stub + internal/protocol.ts ping/pong-only discriminatedUnion stub + schemas.ts FULL 5-schema REST surface with .meta() OpenAPI ids + MODULE.md Purpose-only placeholder + index.ts 1-line MOD-02 barrel). `server/db/schema.ts` extended with sessionStatusEnum + sessions pgTable (partial unique index `WHERE status='active'` for single-active-session-per-device race protection + 3 secondary indexes on leaseUntil/status/ownerApiKeyId + FK to devices + nullable FK to apiKeys). Drizzle migration 0009_sessions.sql (renamed from drizzle-kit slug `0009_new_ironclad.sql` + journal idx-9 retagged). `server/queue/names.ts` QUEUE_NAMES extended 12 → 13 entries with `SESSION_SWEEP: 'session.sweep'`. `.dependency-cruiser.cjs` gains 11th forbidden rule `no-deep-imports-into-sessions-internal` mirroring rule 10 verbatim. `__fixtures__/dep-cruiser/bad-sessions-deep-import.ts` fires the new rule. `server/hooks/__tests__/dep-cruiser.spec.ts` extended with `[MOD-02 sessions extension]` it-block (11 it-blocks now total). 8 vitest skip-stubs (events.spec + routes.spec + protocol.spec with 2 it.todo + ws.spec + dispatch.spec + resolver-maestro.spec + resolver-claude.spec + auth-rate-sweeper.spec). 2 fixtures: android-hierarchy.xml (real uiautomator dump shape with `Sign In` Button + `Settings` content-desc + bottom-nav + form fields; 12-leaf tree) + screenshot.png (1080x1920 solid-gray 11KB PNG generated via inline raw-PNG writer — no third-party deps). `mcp/` workspace skeleton (package.json declaring `@device-stream/mcp` + bin + 3 deps (@modelcontextprotocol/sdk + ws + zod) + tsconfig + vitest.config + src/index.ts shebang placeholder + 2 spec stubs + README; root package.json workspaces array gains `"mcp"`; NO `npm install` runs — Plan 34-05 owns). `cli/cmd/session.go` Cobra skeleton (Use=`session` registered under rootCmd via init; RunE returns `Plan 34-06 not yet implemented` error). `cli/cmd/session_test.go` TestSessionCommandExists compile + registration assertion (passes). `web/src/routes/sessions/{+page,[id]/+page}.svelte` placeholders + `web/src/lib/sessions/__tests__/sessions-list.spec.ts` route-existence assertions. Verification: npx tsc --noEmit zero NEW errors (24 pre-existing DEFERRED-15-A inherited); npm run dep-check 5 pre-existing violations unchanged (rule 11 contributes 0 in `server/` since fixture in `__fixtures__/`); 8 sessions specs / 26 tests (9 pass + 15 skipped + 2 todo) in 149ms; dep-cruiser.spec 11/11 pass; Go session_test 1/1 pass; mcp vitest list shows both stub specs. Open Question #1 (sub-minute cron) carried to Plan 34-04. Plans 34-01 (REST lease/release) + 34-02 (WS protocol body) + 34-03 (NL resolvers) + 34-04 (auth + rate + sweeper) + 34-05 (MCP server body) + 34-06 (CLI + web) + 34-07 (phase close) all unblocked.

Previous status preserved below for Phase 33 archival. **Phase 33 CLOSED roll-up (2026-05-16):** 6 plans across 6 waves shipped same-day. Wave 0 substrate (33-00) — Go module `github.com/device-farm/device-stream/native-servers/android-grpc` + subset `emulator_controller.proto` + 17 `t.Skip` test placeholders + TS Vitest scaffolds + Wave 0 build script stub + postinstall extension + daily Android matrix workflow scaffold. Wave 1 (33-01) — proto codegen + `auth.FindToken` (per-instance `pid_*.ini` + global fallback) — 7 Go auth tests. Wave 2 (33-02) — daemon body: mmap RW shared region (64 MiB) + 2-slot Ring + gRPC client + IPC framer for all 8 kinds (Phase 32 inherited 0x01/0x02/0x09/0xC0/0xC1/0xC9 + Phase 33 additive 0x03/0xC2) + VideoToolbox cgo encoder (encoder config byte-identical to Phase 32) + Unix-socket accept-one-client server + `bin/android-grpc-stream` daemon — 18 Go tests across mmap/ipc/client. Wave 3 (33-03) — `allocateGrpcPort(usedPorts)` helper (port band 8554-8650) + `boot()` argv injects `-grpc <port>` between `-port` and conditional flags + `BootResult.grpcPort` + `DeviceInfo.grpcPort` propagation — 4 Vitest tests. Wave 4 (33-04) — `GrpcEmuClient` TS adapter (spawn lifecycle + parse loop + outbound framer for 0xC1 touch / 0xC2 key) + `AndroidStreamingService` 4-stage selection rule with scrcpy fallback + `device-service.ts` tap/pressKey routing branches (`typeText` UNCHANGED on ADB) — 20 Vitest tests. Wave 5 (33-05, this plan) — real `build-android-grpc.sh` (replaces Wave 0 stub; preflight protoc/plugins; `make proto && make build`; stages `device-stream/bin/android-grpc-stream`; verified build succeeded — 14.5 MB darwin/arm64 Mach-O) + scrcpy-setup.ts top-of-file comment block documenting conditional usage (physical Android still uses scrcpy) + 4 verification scripts mirroring Phase 32 shape (`smoke-android-grpc.sh` boot+probe+30-frame teardown; `android-grpc-soak.sh` 30-min default RSS-growth ≤50MB gate; `android-grpc-touch.sh` 10-sample latency median ≤80ms gate; `android-grpc-visual.sh` ffmpeg SSIM ≥0.99 vs `adb screenrecord` baseline) + activated `.github/workflows/android-grpc-matrix.yml` (real steps replace Wave 0 TODO echoes: checkout + setup-go 1.26.1 + brew protoc + go install plugins + build-android-grpc.sh + reactivecircus/android-emulator-runner@v2 with api-level matrix [34, 35], arch=x86_64, target=google_apis_playstore, smoke script invocation, upload-artifact on failure) + 221-line operator runbook `docs/runbooks/android-grpc.md` (9 H2 sections + 8 H3 troubleshooting cases mirroring sim-capture-private.md structure) + REQUIREMENTS.md out-of-band §Phase 33 Android gRPC section with 10-row AND-GRPC-* traceability table (8 functional pseudo-IDs + 2 REF-* faithful-port/wire-compat IDs) + `.planning/phases/33-android-grpc/deferred-items.md` catalog with 8 DEFERRED-33-* items (linux token path, physical Android, audio capture, anti-frame-loss backpressure, doctor check, bitrate adaptation, x264 linux build tag, zombie-aware grpcPort reclaim) plus carry-forward DEFERRED-33-04-A semgrep CWE-134 → ROADMAP.md Phase 33 row `6/6 Complete 2026-05-16`; STATE.md frontmatter `current_plan: 6 status: phase-complete completed_phases: 15`; 33-VALIDATION.md `nyquist_compliant: true wave_0_complete: true status: complete completed: 2026-05-16` with all 8 task-status cells `✅ green`. External Dependencies Policy honored across all 6 plans (zero `import` / `require` / `link` of kittyfarm; all kittyfarm citations in `//` comments with file:line); scrcpy fallback proven (env opt-out + spawn-failure tests in Plan 33-04); CI matrix active daily 09:00 UTC on `macos-14`. Phase 34 Session API + MCP Server unblocked (depends on Phase 26 auth — already complete).

Previous status preserved below for Phase 33-00 archival. **Phase 33 Plan 00 (Wave 0 substrate) roll-up (2026-05-16):** Substrate complete in ~11 min / 4 task commits (a96f306 + 0572f71 + b422326 + 06f4674). 21 new files + 1 modified. New Go module `github.com/device-farm/device-stream/native-servers/android-grpc` (go 1.26.1, stdlib-only — `require` lines come in Wave 1). Subset `emulator_controller.proto` translated from upstream AOSP (5 RPCs: streamScreenshot/sendTouch/sendKey/setClipboard/getStatus + 8 messages; 119 lines; package literal `android.emulation.control` matches upstream byte-for-byte for wire compat). Daemon entrypoint `cmd/android-grpc-stream/main.go` — `--help` prints usage and exits 0; flagged invocation exits 2 with "not implemented Wave 2". 6 Go test files with 17 `t.Skip("Wave N — TODO")` placeholders mapping row-for-row to 33-RESEARCH.md §Unit tests (auth/token_test.go 4, auth/ini_test.go 2, mmap/mmap_test.go 2, client/client_test.go 4, client/ring_test.go 1, ipc/framer_test.go 4). TS Vitest scaffold `device-stream/packages/android/tests/grpc-emu-client.spec.ts` with 8 `it.todo` across fallback-path + framer describes. Server-side Vitest scaffold `server/pool/__tests__/emulator-grpc.spec.ts` with 4 `it.todo` matching Plan 33-03 Task 3.2 verbatim. Build wrapper stub `device-stream/scripts/build-android-grpc.sh` exits 1 with Wave-5 TODO. Postinstall hook `device-stream/scripts/postinstall.js` extended additively (already darwin+arm64-gated via `isSupportedHost`; warning-not-fatal on failure). Daily Android emulator workflow `.github/workflows/android-grpc-matrix.yml` — `[34, 35]` matrix on `macos-14` runner (API 36.1 excluded per CLAUDE.md macOS Tahoe mprotect/hvf incompat).

Previous status preserved below for Phase 32 archival. **Phase 32 closed roll-up (2026-05-16):** SimulatorKit private bridge fully operational. Plan 32-05 ships the phase-close artifacts: `docs/runbooks/sim-capture-private.md` (173-line operator runbook covering Prerequisites + Install + Configuration env table + Manual TCC-prompt verification (SIM-PRIV-01) + visual-diff/touch-latency/soak verification commands + CI reference + 6-case Troubleshooting); 4 new shell scripts (`smoke-sim-private.sh` real implementation replacing the Wave-0 stub — probe + simctl boot + spawn daemon + read 30 frames via nc -U + python3 length-prefix framer + clean teardown via 0xC9 quit frame; `sim-visual-diff.sh` SSIM ≥ 0.995 gate; `sim-touch-latency.sh` 10-sample latency measurement; `sim-soak.sh` 50 MB RSS-growth gate + leaks); `.github/workflows/sim-private-matrix.yml` activated (build step + boot-simulator step + smoke step — soft-fail echo removed; `matrix.xcode: [15.4, 16.0, 16.1, 17.0]`; daily 09:00 UTC + manual dispatch); REQUIREMENTS.md gains out-of-band "Phase 32 SimulatorKit" section with 7 SIM-PRIV-* pseudo-IDs traceability table; ROADMAP.md Phase 32 row marked `6/6 Complete 2026-05-16`; deferred-items.md catalogs 13 items (code-signing post-canary, multi-display, iOS 18+ regressions, Open Question #1 fallback, probe extension, doctor integration, x86_64 path, sandboxing/SIP, visual-diff/touch-latency/soak CI gating, orientation API extension, kittyfarm chrome bridge). VALIDATION.md frontmatter advanced `nyquist_compliant: true, wave_0_complete: true`. Phase 32 acceptance criteria all green: SIM-PRIV-01 manual procedure documented, SIM-PRIV-02 probe `OK: 8/8` Plan 32-01, SIM-PRIV-03/04 verification scripts shipped, SIM-PRIV-05 fallback tests in Plan 32-04, SIM-PRIV-06 CI matrix active, SIM-PRIV-REF byte-for-byte IPC wire compat (Plan 32-03+04).

Previous status preserved below for Phase 26 archival. Status: Plan 26-05 complete — Phase 26 CLOSED. Auth Module fully migrated to MOD-01..09 conventions:

- **SC1 closed:** Every auth route ships Zod request + 200-response schemas (Plan 26-03: POST/GET/DELETE `/admin/keys` + POST `/api/keys/:id/claims/admin` — 4 routes, 9 `.meta({id:...})` promoted schemas in openapi.json components.schemas). `auth.key.created` + `auth.key.revoked` events publish on the typed bus with correct correlationId + actor populated (BOTH persisted per TRACE-08 — security-notable audit events). Two-layer proof from `subscriber.spec` (bus envelope listener + DB row inspection): emit fires, persistEnvelope writes events row with `actor='apikey:{requesterId}'` from ALS, aggregateType='auth', aggregateId=apiKeyId. 10TH persistEnvelope sample point reached (DEFERRED-26-B → Phase 27+ consolidation; supersedes DEFERRED-25-A).
- **SC2 closed:** 4 actor sources resolve correctly on `events.actor`. Entry point #1 (HTTP authenticated) writes `actor:'apikey:{matched.id}'` via bearer-auth callback (Plan 26-03 thin-wirer; Pitfall 6 fiber inheritance proven by `als-actor.spec` Test 1). Entry point #2 (HTTP unauthenticated) inherits `'system'` from the createEventHelpers fallback (Plan 26-02 migration `'anonymous' → 'system'` at bus/helpers.ts:100 + Plan 26-05 correlation/plugin.ts:42 defaultStoreValues actor migration). Entry point #3 (boot-time onReady) wraps `asyncLocalStorage.run({correlationId: randomUUID(), actor:'system'}, ...)` (Plan 26-02 server/index.ts wrap). Entry point #4 (pg-boss worker) stamps `actor:'cron'` (already done Phase 18 lifecycle; grep-guard at queue/plugin.ts:199 in `lifecycle-ownership.spec` Test 3 from Plan 26-02). `als-actor.spec` proves entries #1-3 runtime; entry #4 is `it.todo` with explicit Phase 18 cross-reference. Pitfall 1 concurrent-fiber actor isolation proven by `als-actor.spec` Test 5 (Promise.all parallel inject with different Bearer keys writes per-fiber actor without bleed).
- **SC3 substrate ready:** Phase 26 lays the substrate (events.actor TEXT column stable + populated from ALS-driven envelope for every persisted event). Phase 27 API Aggregator owns the `GET /api/events?correlationId=...&aggregateType=auth` trace-tree endpoint that consumes this column. No cross-coordination needed beyond keeping `actor` column shape stable (TEXT, regex per actorSchema).
- **SC4 closed:** Auth module follows Phase 16 conventions — `MODULE.md` 9 H2 sections + Runnable Example (Plan 26-05); `index.ts` full barrel (MOD-02 strict 1-line `./internal/module.js` re-export FIRST + AuthService + MatchedApiKey + requireAdmin + keyRoutes + events surface + actor surface; NO `export *`); 2 `.test → .spec` renames (MOD-04) — `auth-plugin.test.ts → .spec.ts` + `auth-service.test.ts → .spec.ts` via `git mv` 100% similarity (blame preserved); `plugin-order.spec` extension (3 positional + 1 structural deps literal + 1 positional auth<jobs + 1 MODULE.md 9-section count = 6 additive Phase 26 assertions); Nyquist gate exit 0 with delta +3.01pp (baseline UNCHANGED since Phase 15 commit).

**DEFERRED-23-A RESOLVED:** `requireAdmin` middleware gates `/admin/drain` + `/admin/drain/resume` (Plan 26-03 routes.ts chain `[requireAuth, requireAdmin]`); `admin-claim.spec` proves 403 RFC 7807 problem+json without `claims.admin === true` + 2xx with; `apiKeys.claims` JSONB column shipped via Drizzle migration 0026 (Plan 26-00); first admin bootstrapped via SQL runbook at `docs/runbooks/admin-bootstrap.md` (Plan 26-03 — chicken-and-egg solved by operator one-shot, subsequent admins via POST `/api/keys/:id/claims/admin` route gated on requireAdmin). `grantAdminClaim` uses `jsonb_set` semantics (Plan 26-03 — preserves any other claim keys across merge; admin-claim.spec Test 6 proves preservation).

**Plan 26-05 phase-close artifacts:** `server/auth/MODULE.md` (~155 lines, 9 fixed H2 sections + Runnable Example with SQL bootstrap + HTTP curl flow + TypeScript bus subscribe sample, MOD-01); `server/auth/index.ts` (~42 lines, full barrel — factory + AuthService + MatchedApiKey + requireAdmin + keyRoutes + events surface (3 constants + 2 schemas + 5 types) + actor surface (regex + 4 helpers), MOD-02); 2 `*.test.ts → *.spec.ts` renames via `git mv` 100% similarity; `plugin-order.spec` extended with Phase 26 additive block (existing Phase 17-25 assertions byte-preserved); `deferred-items.md` catalog (5 Phase 26-specific + 2 carry-forwards = 7 items). Auto-fix during 5.5: `server/correlation/plugin.ts:42` defaultStoreValues `actor:'anonymous' → 'system'` (Plan 26-02 missed this substrate site; Rule 1 bug fix during phase-close sweep — SC4 production-code grep `! grep -rE "actor:\s*'anonymous'" server/` clean post-fix).

**Phase 26 deferrals (5):** DEFERRED-26-A bootstrap CLI `device-farm admin-grant <keyId>` → Phase 28 Go-side; DEFERRED-26-B persistEnvelope 10TH SAMPLE POINT consolidation → Phase 27+ (supersedes DEFERRED-25-A); DEFERRED-26-C OIDC/OAuth integration → v2 territory (actorSchema accepts `user:<id>` for forward-compat); DEFERRED-26-D audit log UI → Phase 29 Web Refactor (Phase 27 events trace API exposes programmatically); DEFERRED-26-E `fastify-zod-openapi` v5 `required`-emission bug in auth-plugin.spec.ts harness → Phase 27+ (v6 not released through 2026-05-08; supersedes DEFERRED-17-A scope for auth-plugin.spec.ts specifically — 8 tests fail post-rename with `event-bus` dep not registered, same count as pre-26-03, different cause). Carry-forwards (2): DEFERRED-15-A Map-vs-RequestContext tsc inheritance (9 of 24 baseline errors); DEFERRED-17-A non-auth scope inheritance (api/routes.test.ts + artifacts/artifact-routes.test.ts; unchanged by Phase 26).

**Phase 26 CLOSED roll-up:** 6 plans across 6 waves (26-00 substrate / 26-01 events body + actorSchema spec / 26-02 ALS actor stamp wiring + grep-guards / 26-03 factory + bearer-auth + Zod routes + drain gate + admin-bootstrap runbook / 26-04 DB-gated proofs / 26-05 phase close). Sweep results: `npm run lint` clean (ESLint: No issues found); `npx tsc --noEmit` 24 pre-existing errors (DEFERRED-15-A inherited — ZERO new from Phase 26); `npm run dep-check` 5 pre-existing violations (artifacts→streaming/internal + api→pipelines/internal, all out-of-scope per Plan 23-04 SUMMARY, unchanged); `npm run nyquist:check` exit 0 with delta +3.01pp; `find server/auth/__tests__ -name '*.test.ts' | wc -l` = 0 (MOD-04 closed); production-code `'anonymous'` literal count = 0 (only docblock anti-pattern citations remain in actor.ts + correlation/plugin.ts). **Phase 27 API Aggregator + Events API explicitly unblocked.** TRACE-10 fully shipped (4 actor sources resolve correctly on every persisted events.actor row).

Previous status preserved below for Phase 25 archival. Plan 25-05 complete — Phase 25 CLOSED. Pipelines Module fully migrated to MOD-01..09 conventions:

- **SC1 closed:** `node-cron` NO LONGER imported anywhere in `server/` (verified by `lifecycle-ownership.spec` readFileSync grep walk + `! grep -rE "from 'node-cron'" server/` final sweep clean; `package.json` + `package-lock.json` regenerated in Plan 25-03 to drop both `node-cron` and `@types/node-cron`). Pipeline schedules upserted via `boss.schedule(name, cron, data, {key: scheduleId, tz: 'UTC'})` (NOT `singletonKey`, NOT `pipelineId` — researcher-corrected from CONTEXT draft per Pitfall 1); per-`pipelineSchedules.id` rows mirror to `pgboss.schedule` via `reconcileSchedules` at boot (Pitfall 6 idempotent upsert proven by DB-gated `queue.spec` `idempotent upsert` test). Pitfall 3 cascade enforced: `service.deletePipeline` calls `boss.unschedule` for every child schedule before DB delete (DB-gated `queue.spec` `unschedule on delete` proves zero pgboss rows for the pipeline post-delete).
- **SC2 closed:** Pipeline stage advancement is driven entirely by a `job.completed` bus subscriber. The local Promise chain in `service.executeRun` (lines 198-380 pre-rewrite) was DELETED in Plan 25-03; the polling loop (lines 523-539) was DELETED; `executor.ts` is now pure (executeScript + evaluateCondition only). DB-gated `subscriber.spec` proves a 3-stage sequential pipeline completes via bus events (Pitfall 7-aware test, no synchronous Promise hold). Matrix N-of-N gate (Pitfall 8) holds until ALL matrix entries terminal — proven by `subscriber.spec -t "matrix"`. DB-driven join on `pipeline_stage_jobs.jobId` is the routing primitive (Pitfall 4 — never envelope correlationId). DEFERRED-25-C cancellation preserved IN-PHASE via `runningRuns: Map<runId, AbortController>` for in-process script-stage abort + `pipelineRuns.status='cancelled'` check in `subscribers.advanceRunOrComplete`.
- **SC3 closed:** Pipelines module follows Phase 16 conventions — `MODULE.md` 9 H2 sections + Runnable Example (Plan 25-05); `index.ts` full barrel (MOD-02 strict 1-line `./internal/module.js` re-export FIRST + back-compat surface re-exports of `PipelineService`, `PipelineScheduler`, `PipelineBroadcaster`, `SecretsService`, `GitService` + events surface + queue surface); `.test → .spec` renames (MOD-04) — 5 files renamed via `git mv` 100% similarity (executor + git-service + integration + parser + secrets; blame preserved); `plugin-order.spec` extension (3 positional + 1 structural deps literal + 1 grep-friendly literal + 1 MODULE.md 9-section count = 6 additive Phase 25 assertions); Nyquist gate exit 0 with delta +3.01pp (well within -2pp budget; baseline UNCHANGED since Phase 15 commit). 9TH persistEnvelope sample point reached (DEFERRED-25-A; Phase 27+ owns consolidation).

**Plan 25-05 phase-close artifacts:** `server/pipelines/MODULE.md` (~225 lines, 9 fixed H2 sections + Runnable Example, MOD-01); `server/pipelines/index.ts` (~62 lines, full barrel — factory + 5 back-compat classes + events surface + queue surface, MOD-02); 5 `*.test.ts → *.spec.ts` renames via `git mv` 100% similarity; `plugin-order.spec` extended with Phase 25 additive block (existing Phase 17-24 assertions byte-preserved; reuses `pipelinesIdx` const declared in Phase 24 block — no duplicate declaration); `deferred-items.md` catalog (8 Phase 25-specific + 2 carry-forwards = 10 items).

**Phase 25 deferrals (8):** DEFERRED-25-A persistEnvelope 9TH SAMPLE POINT consolidation → Phase 27+ (supersedes DEFERRED-24-B); DEFERRED-25-B Pipeline DAG/parallel stages → v2/future feature; DEFERRED-25-C Pipeline-run cancellation RESOLVED IN-PHASE via `runningRuns` Map + DB status check (no carry-forward); DEFERRED-25-D maestro test rewrite carry-forward (inherited from DEFERRED-24-A) → Phase 30; DEFERRED-25-E `pipelineRuns.correlationId` column → Phase 27 (events-trace API decision); DEFERRED-25-F per-schedule timezone column → future feature phase; DEFERRED-25-G script-stage durability across server restart → future resilience phase; DEFERRED-25-H `integration.spec.ts` rewrite cost-balloon fallback → Phase 30 (PATH B taken — `it.skip` with TODO; subscriber.spec already proves bus flow). Carry-forwards (2): DEFERRED-17-A fastify-zod-openapi v5 inherited; DEFERRED-15-A Map-vs-RequestContext tsc inherited (10 errors total — 9 Map-vs-RequestContext + 1 pipeline-schema.ts:17 Zod v4 arg-count, both pre-Phase-25).

**Phase 25 CLOSED roll-up:** 6 plans across 6 waves (25-00 substrate / 25-01 events+queue body / 25-02 scheduler.ts boss.schedule + DB-gated SC1 idempotency / 25-03 factory + service rewrite + plugin replacement + node-cron drop from package.json + lockfile regen / 25-04 DB-gated SC2 subscriber+correlation+lifecycle-ownership / 25-05 phase close). Sweep results: `npm run lint` clean (ESLint: No issues found); `npx tsc --noEmit` 10 pre-existing errors (9 DEFERRED-15-A Map-vs-RequestContext + 1 pipeline-schema.ts:17 Zod v4 arg-count — ZERO new from Phase 25); `npm run dep-check` 3 pre-existing violations (artifacts→streaming/internal, out-of-scope per Plan 23-04 SUMMARY, unchanged); `npm run nyquist:check` exit 0 with delta +3.01pp; `find server/pipelines/__tests__ -name '*.test.ts' | wc -l` = 0 (MOD-04 closed); `! grep -rE "from 'node-cron'" server/` succeeds (only matches in lifecycle-ownership.spec test bodies + MODULE.md/deferred-items doc citations); `! grep "node-cron" package.json` succeeds. **Pipeline migration is the LAST node-cron removal in v3.0 — pg-boss is now the sole scheduler in the project.** Phase 26 Auth Module unblocked.

Previous status preserved below for Phase 24 archival. Plan 24-05 complete — Phase 24 CLOSED. Maestro Module fully migrated to MOD-01..09 conventions:

- **SC1 closed:** `HierarchyService`, `DeviceInfoCollector`, `AppiumService` all live in `server/maestro/internal/` behind a barrel; routes file consumes `fastify.maestroModule.*` decorators only; dep-cruiser rule `no-deep-imports-into-maestro-internal` (8th forbidden rule) blocks external `server/maestro/internal/**` imports structurally; lifecycle-ownership.spec readFileSync grep-guards prove zero `imperative onReady metadata-collection loop` callsites in `server/maestro/plugin.ts`.
- **SC2 closed:** Device metadata refresh is triggered by bus subscription on `device.booted` (4 emit sites in `server/pool/pool-manager.ts` after Booting→Idle transitions). The legacy imperative onReady loop at OLD `server/maestro/plugin.ts:55-72` was DELETED in Plan 24-03; the `app.hookExecutor.execute('device.booted', ...)` loop at `server/index.ts:167-191` is RETAINED (different surface — user-facing shell hooks, NOT bus refresh; DEFERRED-24-E).
- **SC3 closed:** Maestro module follows Phase 16 conventions — MODULE.md 9 H2 sections + Runnable Example (Plan 24-05); barrel index.ts (factory + 3 back-compat classes + events surface, MOD-02 strict 1-line internal/module.js re-export); events.ts (2 transient events: maestro.hierarchy.fetched + maestro.device-info.collected; both persisted:false per TRACE-08); .test→.spec renames (hierarchy-service + appium-service via `git mv` 100% similarity, blame preserved); plugin-order.spec extension (5 new Phase 24 assertions: maestro-plugin>pool-plugin position + maestro-plugin<pipelines-plugin position + 4-entry deps literal `['config','db','event-bus','pool-plugin']` + MODULE.md 9-section count); Nyquist gate exit 0 with delta +3.01pp (well within -2pp budget; baseline UNCHANGED since Phase 15 commit 55ff8ac). 8th persistEnvelope sample point reached (DEFERRED-24-B; Phase 27+ owns consolidation).

**Plan 24-05 phase-close artifacts:** server/maestro/MODULE.md (135 lines, 9 fixed H2 sections + Runnable Example, MOD-01 closure); server/maestro/index.ts (50-line full barrel — strict 1-line internal/module.js + back-compat re-exports of HierarchyService/AppiumService/DeviceInfoCollector + events surface — MOD-02); 2 *.test.ts → *.spec.ts renames via `git mv` 100% similarity (hierarchy-service + appium-service; MOD-04 closed for maestro); plugin-order.spec extended with Phase 24 additive block (2 positional + 1 structural readFileSync regex-extract + 1 grep-friendly literal + 1 MODULE.md 9-section count); deferred-items.md catalog (5 Phase 24-specific + 2 carry-forwards = 7 tracked items; superseded the legacy Plan-24-03 ReDoS-warnings entry).

**Phase 24 deferrals (5):** DEFERRED-24-A maestro test rewrite to tests-as-spec → Phase 30 Test Migration Cleanup; DEFERRED-24-B persistEnvelope 8TH SAMPLE POINT consolidation → Phase 27+; DEFERRED-24-C Appium driver queue-managed lifecycle → future phase; DEFERRED-24-D cross-module causationId thread on subscriber-side re-emit → Phase 27+; DEFERRED-24-E hookExecutor 'device.booted' loop in server/index.ts:167-191 RETAINED (different surface) — Phase 27+ may consolidate via hooks→bus migration. Carry-forwards (2): DEFERRED-17-A fastify-zod-openapi v5 inherited; DEFERRED-15-A Map-vs-RequestContext tsc inherited.

**Phase 24 CLOSED roll-up:** 6 plans across 6 waves (24-00 substrate / 24-01 events bodies / 24-02 pool emission wiring / 24-03 factory + git-mv + plugin rewrite + routes extraction + onReady-loop deletion / 24-04 DB-gated proofs / 24-05 phase close). Sweep results: `npm run nyquist:check` exit 0 with delta +3.01pp; `.planning/nyquist-baseline.json` UNCHANGED; renamed test files all green (25/25 in <2s); plugin-order.spec extension structurally validates 4-entry deps array + 9-section MODULE.md count. Phase 25 Pipelines Module unblocked.

Previous status preserved below for Phase 23 archival. Plan 23-07 complete — Phase 23 CLOSED. Jobs Module Keystone fully migrated:

- **EVENTS-10 SC1:** chained-subscriber saga (queued → allocated → running → completed → recording → webhook → cleanup) replaces imperative coupling. server/jobs/internal/{executor.ts (pure run loop), module.ts (MOD-06 factory + 7TH SAMPLE POINT for persistEnvelope), subscribers.ts (saga chain)} ship; server/jobs/{job-service.ts (back-compat shim), job-queue.ts (deleted)}; lifecycle-ownership.spec proves zero `.catch(() => {})` / `setTimeout(...broadcaster.cleanup)` / streaming/internal imports / direct bus.emit() outside events.ts (4 grep-guards all 0).
- **QUEUE-03 SC2:** `policy:'stately'` + per-send `singletonKey:jobId` blocks duplicate enqueue at the queue layer (idempotency.spec queue-layer 3 tests + saga-level SC2 strict gated on PHASE23_FULL_STACK_TEST). Pitfall 2 corrected (policy:'standard' silently ignores singletonKey); Pitfall 3 confirmed (collision returns null, not throws).
- **CLI-05 / DEBT-02 SC3:** `jobResponseSchema` ships with deviceName cross-field refinement + `.meta({id:'Job'})` emit; `internal/repo.ts` applies `leftJoin(devices)` projecting `deviceName: schema.devices.name`; contract-devicename.spec proves Zod refinement (5 cases) + `openapi.json components.schemas.Job.properties.deviceName` exists. Cross-tier Go proof attempted; falls back to DEFERRED-23-C (Phase 28 owns).
- **SC4 (drain + JobQueue removal + Nyquist):** server/jobs/job-queue.ts deleted; SC4 grep contract `! grep -rE "from .*jobs/job-queue" server/` holds (TS-only matches = 0). POST /admin/drain + POST /admin/drain/resume routes ship via Pitfall 1 corrected mechanism: `boss.offWork(JOB_EXECUTE)` + `system_state.drain_requested_at` row + admission check on `enqueueJob` (throws 503 'system_draining'). `honorDrainOnBoot` reads system_state on plugin onReady → calls offWork immediately if drain row present (restart safety). docs/runbooks/drain.md exercises operational procedure. Nyquist delta +3.01pp (baseline 48.29% → current 51.3%; well within -2pp budget; .planning/nyquist-baseline.json unchanged since Phase 15 commit 55ff8ac).

**Plan 23-07 phase-close artifacts:** server/jobs/MODULE.md (170 lines, 9 fixed H2 sections + Runnable Example, MOD-01 closure); 3 *.test.ts → *.spec.ts renames via `git mv` 100% similarity (job-executor + maestro-parser; job-service.test.ts already deleted in Plan 23-04; job-queue.test.ts already deleted in Plan 23-04 → MOD-04 closed for jobs); plugin-order.spec extended with Phase 23 additive block (4 positional + 1 structural readFileSync regex-extract verifying canonical 6-entry dependencies literal `['config','db','queue','event-bus','pool-plugin','auth']` from server/jobs/plugin.ts; existing Phase 17-22 assertions byte-for-byte preserved); deferred-items.md catalog (4 Phase 23 + 3 carry-forwards + 1 logged pre-existing test failure = 8 tracked items); openapi.json components.schemas.Job re-inlined to repair Plan 23-05 drain regen regression (contract-devicename.spec test (f) all 7 tests green).

**DEFERRED-21 RESOLVED:** server/jobs/plugin.ts no longer imports server/bus/bus.ts directly (factory routes through event-bus plugin dependency); npm run dep-check shows 3 pre-existing artifacts→streaming/internal violations (out-of-scope per Plan 23-04 SUMMARY).

**DEFERRED-22-D RESOLVED:** server/streaming/internal/module.ts subscribes to `job.cleanup.requested` event → calls jobBroadcaster.cleanup; replaces the legacy setTimeout(...broadcaster.cleanup, 5000) imperative pattern.

**DEFERRED-22-F RESOLVED:** server/jobs/internal/executor.ts has zero `from '../streaming/internal/'` imports; cross-module type imports cleared.

**Phase 23 deferrals (4):** DEFERRED-23-A admin-claim gate → Phase 26; DEFERRED-23-B system.drain.* aggregateType placement → Phase 27+; DEFERRED-23-C Go cross-tier deviceName proof → Phase 28; DEFERRED-23-D pgboss schema isolation per drain test (track via flake count). Carry-forwards (3): DEFERRED-22-E persistEnvelope consolidation reaching 7TH SAMPLE POINT → Phase 27+; DEFERRED-17-A fastify-zod-openapi v5 inherited; DEFERRED-15-A Map-vs-RequestContext tsc inherited.

**Phase 23 CLOSED roll-up:** 8 plans across 6 waves (23-00 substrate / 23-01 events body / 23-02 queue+idempotency / 23-03 deviceName contract / 23-04 saga rewrite + JobQueue deletion / 23-05 drain endpoint / 23-06 DB-gated proofs / 23-07 phase close). Sweep results: npm run lint clean; `npx tsc --noEmit` 10 pre-existing errors (DEFERRED-15-A inherited, ZERO new from Phase 23 — confirmed via stash-comparison at HEAD~1); `npm run dep-check` 3 pre-existing violations (artifacts→streaming/internal, out-of-scope per Plan 23-04 SUMMARY); `npm run nyquist:check` exit 0 with delta +3.01pp; `find server/jobs/__tests__ -name '*.test.ts' | wc -l` = 0 (MOD-04 closed for jobs); SC4 grep contract `grep -rE "from .*jobs/job-queue" --include="*.ts" server/ | wc -l` = 0; `! test -f server/jobs/job-queue.ts` succeeds; renamed test files all green. Phase 24 Maestro Module unblocked.

Previous status preserved below for Phase 23-00 archival. Plan 23-00 complete — Phase 23 Wave 0 substrate shipped. systemState pgTable + drizzle migration 0003_wise_aqueduct.sql emitted (system_state key TEXT PRIMARY KEY + value JSONB NOT NULL + updated_at TIMESTAMPTZ DEFAULT now() NOT NULL). QUEUE_NAMES extended 10 → 11 entries (JOB_EXECUTE='job.execute' alphabetized between HOOK_RUN and LIFECYCLE_COMPRESS_DAILY; JSDoc Phase 23 annotation added). JOB_EVENT_NAMES extended 6 → 11 keys (5 keystone saga placeholder names: ALLOCATED='job.allocated', RUNNING='job.running', RECORDING_REQUESTED='job.recording.requested', CLEANUP_REQUESTED='job.cleanup.requested', FAILED='job.failed'); jobsRegistry literal STAYS at 6 entries (Plan 23-01 owns payload schemas + registry + emitters extension). server/jobs/{queue.ts (15 lines, JOB_EXECUTE_QUEUE_NAME constant alias only — body in Plan 23-02), internal/module.ts (10-line throw-stub for dep-cruiser resolvable target — Phase 18/19/20/21/22 empirical 6th repeat), MODULE.md (Purpose-only placeholder — full 9-section body in Plan 23-07), index.ts (1-line MOD-02 strict internal/ re-export barrel), __tests__/events.spec.ts (50 lines, 4 tests EVENTS-03 shape: 11 keys + dotted past-tense pattern + uniqueness + 5 Phase 23 verbatim names)} all shipped. .dependency-cruiser.cjs gains 7th module rule no-deep-imports-into-jobs-internal (8 forbidden rules total: hooks + lifecycle + reporting + pool + artifacts + streaming + jobs + direct-bus-emit); __fixtures__/dep-cruiser/bad-jobs-deep-import.ts fires the rule via @ts-expect-error import; server/hooks/__tests__/dep-cruiser.spec.ts extended with [MOD-02 jobs extension] it-block (two-pass err+json pattern; 7 tests green). Verification: QUEUE_NAMES.length=11, npx tsc --noEmit 8 pre-existing errors (unchanged; zero new from Plan 23-00), npm run dep-check 7 pre-existing violations (unchanged from baseline; rule 7 contributes 0 new violations on committed codebase — fixture lives in __fixtures__/ outside includeOnly:'^server/' graph), npx vitest run server/jobs/__tests__/events.spec.ts 4/4 pass, npx vitest run server/hooks/__tests__/dep-cruiser.spec.ts 7/7 pass, npx vitest run server/queue/__tests__/names.spec.ts 6/6 pass. Wave-merge: 17/17 tests green across events.spec + names.spec + dep-cruiser.spec in 6.26s. Rule 1 auto-fix: dep-cruiser rule 7 from-clause shape — plan literal had `from: { path: '^server/', pathNot: '^server/jobs/' }` but the existing 6 rules use only `pathNot`; with `path:'^server/'` filter the fixture at __fixtures__/dep-cruiser/bad-jobs-deep-import.ts does NOT match the from-path constraint (lives outside server/) so dep-cruiser does not consider it a candidate for rule 7 — rule never fires on the fixture, spec assertion fails. Dropped `path:'^server/'`, kept only `pathNot`. Matches the canonical shape of the 6 prior rules. Plans 23-01 (events body + payload schemas + emitters) + 23-02 (queue.ts body + registerJobsExecuteWorker) + 23-03 (deviceName repo join + Zod schema refinement + OpenAPI regen) + 23-04 (saga subscribers + executeJob rewrite + job-queue.ts deletion) + 23-05 (drain route + system_state read/write) + 23-06 (DB-gated proofs subscriber/correlation/lifecycle-ownership) + 23-07 (MODULE.md body + barrel + renames + Nyquist) all unblocked. Plan 23-00 came in at 10min / 6 tasks / 1.7min avg (substrate parallelism with Phase 22's 22min and Phase 21's 21min — fastest substrate plan in v3.0 due to mature template + RTK proxy npx routing memorized). Previous status preserved below for Phase 22 archival.

Plan 22-06 complete — Phase 22 CLOSED. Streaming module functionally complete: JobBroadcaster now subscribes to job.log/job.step/job.status from the bus instead of being called directly by job-service.ts; every WS frame carries a strict Zod-validated envelope (`wsEnvelopeSchema` v:literal(1) + ts + correlationId + payload) routed through `createStreamingModule` factory under server/streaming/internal/. SC2 satisfied (zero `broadcaster.emit` callsites in jobs/job-service.ts; one residual `jobBroadcaster!.cleanup` call documented in MODULE.md §Non-Goals + deferred-items.md DEFERRED-22-D for Phase 23 Jobs Keystone). TRACE-06 satisfied via correlation.spec + envelope.spec + subscriber.spec round-trip proofs. 6th persistEnvelope sample point reached (Phase 27+ consolidation trigger). Phase 22 deferred-items.md catalog landed with 6 Phase 22-specific deferrals (DevicePreview envelope → Phase 29; shared wsEnvelopeSchema → Phase 29; ring-buffer cap → out of scope; SC2 cleanup call → Phase 23; persistEnvelope consolidation → Phase 27+; cross-module type imports in job-service.ts → Phase 23) plus 2 inherited (DEFERRED-17-A fastify-zod-openapi v5 bug; DEFERRED-15-A Map-vs-RequestContext tsc errors). Phase 22 roll-up across 7 waves: 22-00 (22min) substrate → 22-01 (15min) events body + envelope schema + jobs bridgehead → 22-02 (factory + plugin rewire + 7-callsite surgery, multi-commit) → 22-03 (9min) DB-gated proofs subscriber/correlation/envelope.spec → 22-04 (4min) plugin-order extension + lifecycle-ownership grep-guards → 22-05 (5min) MODULE.md 9-section canonical body + index.ts barrel + 5 .test→.spec renames + Nyquist gate (+3.01pp delta) → 22-06 phase close. Previous status preserved below for Phase 21 archival.

Plan 21-06 complete — Phase 21 CLOSED. Artifacts module fully migrated to MOD-01..09 conventions: server/artifacts/MODULE.md (125 lines, 9 fixed H2 sections + Runnable Example per MOD-01) + server/artifacts/index.ts (78 lines, 31 named exports, MOD-02 strict 1-line internal/ re-export with inline type modifier) + 4 *.test.ts → *.spec.ts renames via git mv (100% similarity, blame preserved — artifact-service/recording-service/screenshot-service/memory-service; MOD-04 complete for artifacts) + server/__tests__/plugin-order.spec.ts (+40 lines, 4 additive artifacts dep-order assertions: 3 positional + 1 structural readFileSync regex-extract of plugin.ts dependencies literal verifying canonical 5-entry ['config','db','queue','event-bus','pool-plugin'] shape verbatim; Phase 20 Plan 20-06 pattern). Full 10-step phase-close sweep green: `npm test` same inherited exclusions as Phase 20 (3 pre-existing files: artifact-routes.test.ts + routes.test.ts + auth-plugin.test.ts fail on Phase 17 fastify-zod-openapi v5 `required`-emission bug; 30 failures all in pre-existing files; ZERO new Phase 21 regressions), `npm run lint` clean, `npx tsc --noEmit` 8 pre-existing errors (6 Phase 15 Map-vs-RequestContext + 2 working-tree artifacts/recording-service.ts edits; ZERO new from Phase 21), `npm run dep-check` 1 pre-existing violation (jobs/plugin.ts → bus/bus.ts, Phase 23 scope, unchanged), `npm run nyquist:check` delta -0.30pp (48.29% baseline → 47.99% current, well within -2pp gate; .planning/nyquist-baseline.json unchanged), `npx vitest run server/hooks/__tests__/dep-cruiser.spec.ts` 5/5 pass, `find server/artifacts/__tests__ -name '*.test.ts' | wc -l` = 0 (MOD-04 closed), SC1 grep-guard in job-service.ts = 0. Phase 21 roll-up: 124min total across 7 plans (21-00 21min Wave-0 substrate → 21-01 17min recording-method getter + DB migration → 21-02 21min events body + jobs bridgehead → 21-03 9min queue worker → 21-04 26min factory + subscribers + job-service.ts imperative-call deletion → 21-05 19min DB-gated proofs → 21-06 11min phase close). SC1 proven: zero direct imperative artifact calls in job-service.ts (lifecycle-ownership.spec readFileSync grep-guards + subscriber.spec DB proof). SC2 proven: 3-event surface (events.spec EVENTS-03 + artifactsRegistry registry entries). SC3 proven: two-layer idempotency — queue-layer policy:'stately' + singletonKey:recordingId drops duplicates; DB-layer artifacts.recording_id UNIQUE + onConflictDoNothing catches post-completion duplicates (queue.spec 3 tests). SC4 proven: single correlationId threads ALS.run → job.completed → onPersisted subscriber → queue.send → worker → artifact.created row (correlation.spec 1 test). 8 deferred items documented in MODULE.md Non-Goals + carried to Phase 22/23/27+. Previous status preserved below for Phase 21 archival.

Plan 21-00..21-05 complete — Phase 21 Wave 0..4 shipped. RECORDING_UPLOAD queue-name added to QUEUE_NAMES (10 entries, alphabetized between LIFECYCLE_RETENTION_DAILY and WEBHOOK_DELIVER). server/artifacts/{events.ts (47 lines, ARTIFACTS_EVENT_NAMES + ARTIFACTS_AGGREGATE_ID placeholder '00000000-0000-5000-8000-000000000021' + empty artifactsRegistry), queue.ts (20 lines, RECORDING_UPLOAD_QUEUE_NAME alias only), internal/module.ts (10-line throw-stub for dep-cruiser resolvable target — Phase 18/19/20 empirical 4th repeat), MODULE.md (Purpose-only placeholder), index.ts (23 lines, 1 internal/ re-export — MOD-02 invariant), __tests__/events.spec.ts (22 lines, 1 test EVENTS-03 shape)} all shipped. .dependency-cruiser.cjs gains 5th module rule no-deep-imports-into-artifacts-internal (6 forbidden rules total: hooks + lifecycle + reporting + pool + artifacts + direct-bus-emit); __fixtures__/dep-cruiser/bad-artifacts-deep-import.ts fires the rule via @ts-expect-error import; server/hooks/__tests__/dep-cruiser.spec.ts extended with [MOD-02 artifacts extension] it-block (two-pass err+json pattern; 5 tests green). Verification: QUEUE_NAMES.length=10, npx tsc --noEmit 8 pre-existing errors (unchanged; zero new from Plan 21-00), npm run dep-check 1 pre-existing violation (jobs/plugin.ts → bus/bus.ts, Phase 23 scope, unchanged), npx vitest run server/artifacts/__tests__/events.spec.ts 1/1 pass, npx vitest run server/hooks/__tests__/dep-cruiser.spec.ts 5/5 pass, npx vitest run server/queue/__tests__/names.spec.ts 6/6 pass. Plans 21-01 (DB migration + idempotent insert) + 21-02 (events body + job.started bridgehead extension) + 21-03 (queue worker) + 21-04 (factory + subscribers + job-service.ts imperative-call deletion) + 21-05 (DB-gated proofs) + 21-06 (MODULE.md body + barrel expansion + renames + Nyquist) all unblocked. Substrate parallelism matches Phase 20 Plan 20-00 task-by-task (4 tasks, same ordering) but ran slightly longer (21min vs 7min) due to fuller doc-comment context added to RECORDING_UPLOAD entry for downstream SC3 semantics. Previous status preserved below for Phase 20 archival.

Plan 20-06 complete — Phase 20 CLOSED. Pool module fully migrated to MOD-01..09 conventions: server/pool/MODULE.md (125 lines, 9 fixed H2 sections + Runnable Example) + server/pool/index.ts (73 lines, 31 named exports, MOD-02 strict 1-line internal/ re-export) + events.ts (175 lines, 4 device.* events, only health.failed persisted per TRACE-08) + queue.ts (device.reap registered with boss.schedule '* * * * *' + singletonKey DEVICE_REAP; device.boot name-only per Phase 23 scope) + createPoolModule factory (MOD-06) + 4-decorator thin plugin.ts (pool + processTracker + healthChecker + poolModule) + 10 *.spec.ts files (132 pool tests green in <2s). server/index.ts lines 209-215 + 245-251 DELETED (SC2 achieved — healthChecker.start + processTracker.startReaper + their stop counterparts moved into pool plugin onClose). Plan 20-06 phase-close: deferred-items.md catalog (2 inherited exclusions + 8 Phase 20 intentional deferrals with phase-ownership annotation matching MODULE.md §Non-Goals verbatim); plugin-order.spec additive block (3 positional assertions + readFileSync structural check on pool/plugin.ts dependencies literal + arrayContaining literal-array assertion); composite-tree sweep: npx vitest run --exclude <4 inherited files> → 598 tests / 543 passed / 55 skipped (DB-gated) / 0 failed in 9.3s; npm run lint clean; npm run dep-check 1 pre-existing violation (jobs/plugin.ts → bus/bus.ts, Phase 23 scope, unchanged); npx tsc --noEmit 8 pre-existing errors in 6 files (0 from Phase 20 pool/*); Nyquist delta current 47.99% vs baseline 48.29% = -0.30pp (well within -2pp gate, check exit 0); .planning/nyquist-baseline.json NOT overwritten (diff -s against backup confirms files identical, still Phase 15 commit 55ff8ac). Phase 20 roll-up: 65min total across 7 plans (20-00 7min Wave-0 substrate → 20-01 4min events body MOD-03 → 20-02 12min emission wiring SC1 + MOD-08 → 20-03 14min factory + plugin rewire + healthChecker/reaper migration SC2+SC3+MOD-06+QUEUE-06 → 20-04 8min DB-gated proofs SC1+SC4 → 20-05 4min MODULE.md + barrel + renames MOD-01/02/04 → 20-06 8min phase close). SC1 proven end-to-end (Plans 20-01/02 code + 20-03/04 runtime proofs): every VALID_TRANSITIONS-allowed transition fires device.state.changed; allocation publishes device.allocated; release publishes device.released; health failures publish device.health.failed. SC2 proven via lifecycle-ownership.spec readFileSync grep-guards. SC3 complete (MODULE.md + barrel + events.ts + queue.ts + tests-as-spec). SC4 proven: Nyquist delta -0.30pp ≥ -2pp + downstream consumers reach device.* via bus (subscriber.spec proves all 4 events + correlation.spec proves ALS→emit→envelope→events-table row for persisted health.failed). Previous status preserved below for Phase 20 archival. Plan 20-01 complete — Pool events body (MOD-03) landed. server/pool/events.ts (175 lines) ships full body: POOL_AGGREGATE_ID placeholder '00000000-0000-5000-8000-000000000020' → real v5 UUID '2a120cd5-4bd3-5f65-a9e5-870ec709e44a' derived via uuidv5('pool', '6ba7b811-9dad-11d1-80b4-00c04fd430c8') URL namespace RFC 4122 §4.3 (matches Phase 18 LIFECYCLE derivation exactly); 4 Zod payload schemas — deviceStateChangedPayload {deviceId: uuid, from+to: deviceStateSchema}, deviceAllocatedPayload {deviceId, jobId, platform}, deviceReleasedPayload {deviceId, jobId nullable, platform}, deviceHealthFailedPayload {deviceId, platform, reason enum[unhealthy|zombie|max-retries|timeout], failureCount non-negative int, willReplace boolean, lastError nullable string}; poolRegistry `as const satisfies EventRegistry` with TRACE-08 persistence policy — health.failed persisted:true (ONLY persisted pool event); state.changed/allocated/released persisted:false (high-freq derivable); aggregateType:'pool' on all 4; makePoolEmitters(bus, onEmit?) factory wires createEventHelpers from ../bus/helpers.js returning {stateChanged, allocated, released, healthFailed} typed helpers; PoolRegistry + PoolEmitters + PoolEventName types exported. server/pool/__tests__/events.spec.ts (157 lines, 8 tests in 2 describe blocks, <500ms runtime) proves: POOL_EVENT_NAMES 4 dotted past-tense names EVENTS-03; poolRegistry 4 entries all aggregateType='pool'; TRACE-08 persistence flags correct; POOL_AGGREGATE_ID re-derived at test time via uuidv5('pool', URL_NAMESPACE) MATCHES runtime literal (enforces single-source-of-truth — plan's literal-hint '9fabf52b-...' was stale, recomputed offline); payload schemas accept valid + reject malformed samples across all 4; makePoolEmitters returns 4 typed helpers; emit.stateChanged stamps envelope with ALS correlationId (PLAIN-OBJECT ALS store shape per CONTEXT §Specifics canonical Phase 20 form, NOT legacy Map) + aggregateType='pool' + aggregateId=deviceId + type + v=1; emit.healthFailed stamps actor + reason discriminator + willReplace flag. Verification gates: npx tsc --noEmit 8 pre-existing errors (Phase 15 Map-vs-RequestContext + 2 working-tree artifacts/recording-service.ts edits), ZERO new errors on plan 20-01 files; npm run lint clean; npx vitest run server/pool/__tests__/events.spec.ts → 8/8 pass in <500ms; grep -cE "persisted:\s*true" = 1, persisted:false = 3, aggregateType:'pool' = 5 (4 registry + 1 doc); grep -c "new Map([" = 0 (plain-object shape enforced); git log 20-00..HEAD -- server/pool/{pool-manager,device,health-checker,process-tracker,plugin}.ts → empty (runtime pool/ untouched; Plan 20-02/20-03 scope preserved). Rule 1 auto-fix (bug in plan literal): plan specified POOL_AGGREGATE_ID='9fabf52b-a6a3-599f-a7fc-1c1e22ad9b8b' but actual v5('pool', URL_NS) = '2a120cd5-4bd3-5f65-a9e5-870ec709e44a'; spec re-derives at test time so runtime literal MUST match — used correct v5 output. Plans 20-02 (emission sites) + 20-03 (factory+plugin) + 20-04 (DB-gated) + 20-05 (MODULE.md+barrel+Nyquist) all unblocked. Previous status preserved below for Phase 19 archival. Plan 19-06 complete — Phase 19 CLOSED. server/reporting/MODULE.md (114 lines, 1291 words, 9 fixed H2 sections in canonical order + H3 Runnable Example) documents all 4 events (scheduled/failed transient; delivered/failedRetryExhausted terminal EVENTS-07), both queues (webhook.deliver retryLimit:5 retryBackoff:true deadLetter:webhook.deliver.dlq + webhook.deliver.dlq retryLimit:0), job.completed bus consumer, 6 invariants (a)-(f) each citing a spec file, and dependencies array ['config','db','queue','event-bus'] matching plugin.ts verbatim. server/reporting/index.ts (75 lines, 11 named export statements) ships MOD-02 strict 1-line internal/ re-export via inline type modifier: `export { createReportingModule, type ReportingModule, type CreateReportingModuleDeps } from './internal/module.js';` (matches Phase 18 lifecycle precedent, stricter than Phase 16 hooks 2-line form). Re-exports: reportingPlugin + WebhookService + WebhookConfig + FlakyDetector (back-compat classes) + 5 schemas + 5 derived types + reportingRegistry + REPORTING_EVENT_NAMES + REPORTING_AGGREGATE_ID + makeReportingEmitters + 4 payload schemas + 3 event types + 2 queue-name constants + registerWebhookDeliveryWorkers + 2 queue types. Does NOT export registerReportingRoutes (plugin-internal). Does NOT use `export *`. 2 *.test.ts → *.spec.ts renames via git mv 100% similarity (flaky-detector + junit-generator; blame history preserved — flaky-detector.spec.ts `git log --follow` reaches back to Phase 06 commit 915b970). All 19 renamed tests pass in 312ms. Nyquist delta: current lines 56.48% vs Phase 15 baseline 48.29% = +8.19pp (well within -2pp budget). Baseline file NOT overwritten (diff verified unchanged). Phase 18 exclusion set (routes.test + artifact-routes.test + auth-plugin.test + plugin-order.spec) inherited — fastify-zod-openapi v5 `required` emission bug still pending; documented in 19 deferred-items.md with suggested hotfix options for pre-Phase-20 work. Full phase-close sweep: npm run lint clean, `npx tsc --noEmit` 8 pre-existing errors (6 Phase 15 Map-vs-RequestContext + 2 working-tree artifacts/recording-service.ts edits — all reproduced at HEAD~5, ZERO attributable to plan 19-06 files), `npm run dep-check` 1 pre-existing violation (jobs/plugin.ts → bus/bus.ts, Phase 23 scope), 9 reporting spec files / 59 tests green in 22.66s. Phase 19 roll-up: 73min total across 7 plans (19-00 13min → 19-01 12min → 19-02 9min → 19-03 9min → 19-04 6min → 19-05 17min → 19-06 7min); 5 new DB-gated specs + 4 renamed test files. EVENTS-07 closed: webhook.failed.retryExhausted terminal event persisted to events table from DLQ worker with correlationId non-null + aggregateType:'reporting' + aggregateId:REPORTING_AGGREGATE_ID + payloadSnapshot. QUEUE-05 closed: webhook.deliver.dlq queue + GET /api/queue/dlq HTTP endpoint + Zod-typed OpenAPI contract (DlqListResponse + DlqJob in openapi.json). ROADMAP §Phase 19 SC1-SC4 all satisfied end-to-end. Phase 20 Pool Module unblocked — benefits from pattern trilogy (hooks + lifecycle + reporting) as strongly-proven template. Plan 19-05 preserved. Plan 19-05 complete — GET /api/queue/dlq endpoint ships via fastify-zod-openapi typed provider. server/reporting/routes.ts (67 lines, NEW) exports registerReportingRoutes(fastify) as plain async function (NOT fp() wrapper) — called directly from reporting plugin body per hooks plugin pattern; module barrel (plan 19-06) stays clean with only createReportingModule as MOD-02 allowed re-export. Handler calls fastify.boss.findJobs(WEBHOOK_DELIVER_DLQ_QUEUE_NAME) (Path A pg-boss public API, NOT raw SQL) and projects JobWithMetadata<T> → flat DlqJob {id, queue, state, retrycount, data, output, createdon, correlation_id} with correlation_id hoisted from envelope.data.correlationId (field names snake_case/lowercase matching CONTEXT.md §Specifics + pg-boss columns verbatim). server/reporting/plugin.ts (+8 lines) wires registerReportingRoutes(fastify) between module.registerWorkersAndSubscribers() and onClose; plugin NAME 'reporting' + dependencies ['config','db','queue','event-bus'] unchanged. server/reporting/__tests__/dlq-route.spec.ts (186 lines, 3 tests, 155ms DB-gated) proves: empty DLQ → 200 + Zod-valid {items:[],count:0}; seeded row (via app.queue.send inside asyncLocalStorage.run plain-object shape per W5) → 200 + correlation_id hoisted matching seeded UUID; response JSON field-name contract (8 CONTEXT-mandated snake_case/lowercase keys present, no camelCase drift at top level). server/openapi.json (+122 lines, byte-deterministic regen via DATABASE_URL=... npm run openapi:generate) adds paths./api/queue/dlq.get → $ref DlqListResponse + components.schemas.DlqListResponse + DlqJob with flat 8-field shape including state enum + null-bearing output/createdon + UUID-nullable correlation_id — CLI+web codegen consumers unblocked. Plan 19-05 Rule 3 deviation: 3 plan-19-04 DB-gated specs (queue/correlation/terminal-event.spec.ts) updated to install fastify-zod-openapi validatorCompiler+serializerCompiler+plugin at root scope BEFORE reportingPlugin register — Task 5.2's Zod-typed route addition broke their minimal Fastify harness (FST_ERR_SCH_VALIDATION_BUILD at app.ready). Mirrors server/index.ts:94-96 (plan 17-01 pattern). Scope boundary: fix was directly caused by this plan's route addition, inside-scope per deviation Rule 3. Full reporting/ spec suite: 9 files / 59 tests green in 22.67s after fix. Typecheck: 0 new errors on plan 19-05 files. Plan 19-04 complete — Phase 19 Wave 2 DB-gated proofs landed. server/reporting/__tests__/queue.spec.ts (152 lines, 20.2s) proves ROADMAP SC1 (5× 500 → DLQ row, server survives all 5 attempts via findJobs + inject probe). server/reporting/__tests__/correlation.spec.ts (167 lines, 22.2s) proves ROADMAP SC4 (single correlationId threads asyncLocalStorage.run → app.queue.send → 5 retries → DLQ row → events-table terminal row); uses canonical plain-object ALS store shape per 19-CONTEXT §Specifics (checker W5). server/reporting/__tests__/terminal-event.spec.ts (156 lines, 22.2s) proves EVENTS-07 (webhook.failed.retryExhausted row persisted with eventType + eventVersion:1 + aggregateType:'reporting' + aggregateId:REPORTING_AGGREGATE_ID + correlationId non-null + payload:{url, event, attempts>=5, payloadSnapshot}); ephemeral-URL de-dup for parallel-test safety. All 3 DB specs gated on TEST_DATABASE_URL/DATABASE_URL, use isolated pgboss_reporting_<suffix> schemas, import startFailingServer from ./fixtures/failing-server.js (checker W1 DRY), call app.boss.updateQueue({retryDelay:1, retryBackoff:true, retryDelayMax:2}) to collapse pg-boss random exponential backoff from worst-case 60s to ~9s. server/__tests__/plugin-order.spec.ts (+13 lines, 4 additive assertions) adds queue→reporting + event-bus→reporting + db→reporting + reporting→job-plugin dep-order invariants (Phase 18 Plan 18-03 additive pattern inside existing it-block — no app-boot overhead). Phase 17/18 assertions byte-for-byte preserved. plugin-order.spec fails on pre-existing Phase 17 fastify-zod-openapi substring-match bug (websocket-plugin at position 424 as substring of 'fastify-websocket' vs pool-plugin at 1016) — documented in STATE.md deferred-items, out-of-scope per scope-boundary rule. Typecheck 0 new errors on 19-04 files. Plan 19-05 (DLQ HTTP endpoint) + plan 19-06 (docs + barrel + Nyquist) unblocked. Plan 19-03 preserved unchanged. Requirements EVENTS-07 + QUEUE-05 now have green DB-proof in addition to plan 19-03's code-path. server/reporting/queue.ts (208 lines) ships registerWebhookDeliveryWorkers factory with DLQ-first ordering (RESEARCH §Pitfall 2 FK constraint), MAIN queue {policy:'standard', retryLimit:5, retryBackoff:true, retryDelay:1, retryDelayMax:30, deadLetter:WEBHOOK_DELIVER_DLQ} + DLQ queue {policy:'standard', retryLimit:0, retryBackoff:false}, MAIN worker (deliverOnce + emit delivered/failed + raw re-throw per RESEARCH §Pitfall 6), DLQ worker (emit failedRetryExhausted EVENTS-07 terminal + log + DO NOT throw per RESEARCH §Pattern 2), deterministic workerIds [main,dlq] ordering. server/reporting/internal/module.ts (264 lines) overwrites 4-line stub with MOD-06 createReportingModule factory mirroring Phase 18 lifecycle: WebhookService + FlakyDetector back-compat, per-module TypedBus<ReportingRegistry>, persistEnvelope 10-line duplicate (third sample point — Phase 27+ consolidation trigger reached), makeReportingEmitters with persistence onEmit, registerWorkersAndSubscribers wires workers + fastify.onPersisted('job.completed') subscriber (sub-option C from RESEARCH §Cross-Module Wiring), enqueueWebhookDelivery imperative façade, idempotent shutdown (stopped flag + unsubscribe bus + offWork each worker). server/reporting/plugin.ts (71 lines, NEW) replaces deleted server/reporting/reporting-plugin.ts (35-line imperative); dependencies extended ['config','db'] → ['config','db','queue','event-bus']; plugin NAME stays 'reporting' for 12-plugin dep-string back-compat; decorates webhookService + flakyDetector + reportingModule; onClose → module.shutdown(). server/index.ts:11 import path updated from reporting-plugin.js to plugin.js. server/reporting/__tests__/module.spec.ts (210 lines, 10 tests, 4 describe blocks, 8ms runtime) proves factory shape + DLQ-before-main ordering + retry-policy shape + 2-worker registration + job.completed subscription + enqueue shape + webhook.scheduled side-event + shutdown idempotency (no DB, mock-based). All 108 tests across reporting/ + jobs/ pass in 804ms; typecheck 0 new errors on plan 19-03 files. Pre-existing dep-check violation (server/jobs/plugin.ts → server/bus/bus.ts, from plan 19-01 jobs module bridgehead) unchanged by this plan — documented in deferred-items.md; Phase 23 Jobs Module Keystone fixes via MOD-06 internal/module.ts migration. Requirements EVENTS-07 + QUEUE-05 code-path complete; plan 19-04 DB-gated proofs + plan 19-05 DLQ route both unblocked (parallel-ready, depends_on:[19-03]).
Last activity: 2026-05-17

Progress: [████████░░] 75% v3.0 (95 of 127 plans complete — Phase 33 Wave 0 substrate shipped 2026-05-16)

## Performance Metrics

**Velocity:**
- Total plans completed (v3.0): 50
- Average duration: 10.6min (cumulative across Phases 15-21 — all CLOSED)
- Total execution time: 552min

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| Phase 15 | 10 | 75min | 7.5min |
| Phase 16 | 5  | 37min | 7.4min |
| Phase 17 | 9  | 119min | 13.2min |
| Phase 18 | 4  | 47min | 11.8min |
| Phase 19 | 7  | 73min | 10.4min |
| Phase 20 | 7  | 77min | 11.0min |
| Phase 21 | 7  | 124min | 17.7min |

**Recent Trend:**
- Phase 21 CLOSED: 21-00 (21min) Wave 0 substrate → 21-01 (17min) recording-method getter + DB migration → 21-02 (21min) events body + jobs bridgehead → 21-03 (9min) queue worker SC3 → 21-04 (26min) factory + subscribers + job-service.ts imperative-call deletion SC1 → 21-05 (19min) DB-gated SC1/SC4 proofs → 21-06 (11min) MODULE.md + barrel + renames + plugin-order extension + STATE/ROADMAP + full-suite sweep + Nyquist gate. Phase 21 came in at 124min / 7 plans / 17.7min avg — longer than Phase 20's 77min / 11.0min avg due to (a) SC3 two-layer idempotency plumbing (DB migration + queue policy:'stately' + onConflictDoNothing + 3-spec SC3 proof), (b) jobs bridgehead extension (job.started + maestro.log.written events ADD-to-jobs-registry — first cross-module event surface expansion in v3.0), and (c) job-service.ts subtraction (9 imperative artifact calls removed → 2 emits; SC1 proof required both readFileSync grep-guards + subscriber.spec runtime proof). Phase 21 Nyquist delta -0.30pp (unchanged from Phase 20 — artifacts module subtracts 140 imperative lines from job-service.ts while adding comparable numerator+denominator via MODULE+events+queue+specs).
- Phase 20: 20-00 (7min) Wave 0 substrate → 20-01 (4min) events body MOD-03 → 20-02 (12min) emission sites wiring SC1 → 20-03 (14min) factory + plugin rewire + healthChecker/reaper migration SC2+SC3 → 20-04 (8min) DB-gated subscriber + correlation proofs SC1+SC4 → 20-05 (4min) MODULE.md + barrel + .test→.spec renames MOD-01/02/04 → 20-06 (20min) phase close (deferred-items + plugin-order additive + STATE/ROADMAP + full-suite sweep + Nyquist capture + Nyquist gate)
- Phase 19: 19-00 (13min) Wave 0 substrate → 19-01 (12min) events.ts + jobs bridgehead → 19-02 (9min) deliverOnce → 19-03 (9min) queue/factory/plugin rewire → 19-04 (6min) DB-gated proofs → 19-05 (17min) DLQ route → 19-06 (7min) close-out MODULE.md/barrel/renames/Nyquist
- Phase 18: 18-00 (11min) Wave 0 substrate → 18-01 (6min) events.ts (MOD-03) → 18-02 (16min, TDD) queue.ts + stats.ts + queue.spec + correlation.spec → 18-03 (14min, all TDD) createLifecycleModule factory + thin plugin.ts + module.spec + graceful-shutdown.spec → 18-04 (54min) MODULE.md + barrel + renames + Nyquist
- Phase 17: 17-00 (10min) Wave 0 → 17-02 (5min) WS-schema → 17-01 (41min, route refactor) → 17-04 (10min Go codegen) → 17-08 (6min drift detector)
- Phase 16: 16-00 (8min) substrate → 16-01..16-04 pilot module fully shipped
- Phase 15: 15-00 (5min)..15-09 (Nyquist baseline) — foundations shipped
- Trend: Wave-parallelized plan execution landing cleanly across phases. Phase 20 came in at 77min / 7 plans / 11.0min avg vs Phase 19's 73min / 7 plans / 10.4min — similar tempo, pool's larger surface area (17 emit sites + healthChecker+reaper migration + 10 spec files) absorbed cleanly. Phase 20-06 phase-close at 20min (vs 18-04 54min → 19-06 7min → 20-06 20min) — template mature across hooks/lifecycle/reporting/pool; 20-06 longer than 19-06 because Phase 20's first-large-module Nyquist capture took ~30s more runtime + 4th-task coverage analysis. Phase 20 Nyquist delta -0.30pp (tightest yet — Phase 18 +7.54pp + Phase 19 +8.19pp reflected low-numerator module additions; Phase 20 pool is a large module so new events+queue+module+specs add lines to BOTH numerator + denominator, net near-zero).

*Updated after each plan completion*
| Phase 15-fix-operational-dependencies P02 | 3min | 2 tasks | 3 files |
| Phase 15-fix-operational-dependencies P03 | 6min | 2 tasks (TDD) | 7 files |
| Phase 15 P01 | 11 | 3 tasks | 10 files |
| Phase 15-fix-operational-dependencies P04 | 12min | 3 tasks | 12 files |
| Phase 15-fix-operational-dependencies P05 | 23min | 4 tasks (TDD) | 13 files |
| Phase 15-fix-operational-dependencies P09 | 5min | 2 tasks | 4 files |
| Phase 15-fix-operational-dependencies P08 | 5min | 2 tasks | 2 files |
| Phase 15-fix-operational-dependencies P07 | 10min | 2 (TDD) tasks | 9 created + 4 modified files |
| Phase 15-fix-operational-dependencies P06 | 6min | 1 (task 6.2 deferred) tasks | 3 files |
| Phase 16-pilot-module-hooks P00 | 8min | 4 tasks | 9 files |
| Phase 16-pilot-module-hooks P01 | 10min | 3 tasks tasks | 8 files files |
| Phase 16-pilot-module-hooks P02 | 8min | 4 tasks | 7 files |
| Phase 16-pilot-module-hooks P03 | 5min | 2 tasks tasks | 3 created + 1 modified files files |
| Phase 16-pilot-module-hooks P04 | 6min | 3 tasks | 3 files |
| Phase 17 P00 | 10 min | 4 tasks | 13 files |
| Phase 17-contracts-pipeline-ops-hygiene P02 | 5min | 3 tasks | 10 files |
| Phase 17-contracts-pipeline-ops-hygiene P01 | 41min | 3 tasks | 15 files |
| Phase 17-contracts-pipeline-ops-hygiene P07 | 3min | 3 tasks | 3 files |
| Phase 17-contracts-pipeline-ops-hygiene P05 | 6min | 3 tasks | 4 files |
| Phase 17-contracts-pipeline-ops-hygiene P06 | 6min | 4 tasks | 8 files |
| Phase 17-contracts-pipeline-ops-hygiene P03 | 32min | 3 tasks | 2 files |
| Phase 17-contracts-pipeline-ops-hygiene P04 | 10min | 5 tasks | 7 files |
| Phase 17-contracts-pipeline-ops-hygiene P08 | 6min | 2 tasks tasks | 2 created + 1 modified files files |
| Phase 18-lifecycle-migration-node-cron-pg-boss P00 | 11min | 4 tasks (1 TDD) | 3 created + 5 modified files |
| Phase 18-lifecycle-migration-node-cron-pg-boss P01 | 6min | 2 tasks | 2 files |
| Phase 18-lifecycle-migration-node-cron-pg-boss P02 | 16min | 3 tasks (TDD) tasks | 4 created + 3 modified files |
| Phase 18-lifecycle-migration-node-cron-pg-boss P03 | 14min | 4 tasks (all TDD) tasks | 5 created + 3 modified + 1 deleted files |
| Phase 18-lifecycle-migration-node-cron-pg-boss P04 | 54min | 4 tasks tasks | 3 created + 1 modified + 3 renamed files files |
| Phase 19 P00 | 13min | 6 tasks | 10 files |
| Phase 19 P01 | 12min | 4 tasks tasks | 3 created + 2 modified files files |
| Phase 19 P03 | 9min | 4 tasks | 5 files |
| Phase 19 P04 | 6min | 4 tasks (all TDD spec-authoring) tasks | 3 created + 1 modified files |
| Phase 19 P05 | 17min | 4 tasks + 1 Rule 3 auto-fix tasks | 3 created + 1 modified + 3 specs updated (harness unblock) files |
| Phase 19 P06 | 7min | 4 tasks | 3 created + 1 modified + 2 renamed files |
| Phase 20-pool-module-devices P00 | 7min | 4 tasks | 10 files |
| Phase 20-pool-module-devices P01 | 4min | 2 (TDD) tasks | 2 modified files |
| Phase 20-pool-module-devices P02 | 12min | 3 (TDD) tasks | 3 modified + 3 renamed files |
| Phase 20 P03 | 14min | 4 (TDD; 1 Rule-3 auto-fix) tasks | 6 modified + 2 created files |
| Phase 20 P05 | 4min | 3 tasks | 5 files |
| Phase 20-pool-module-devices P04 | 8min | 2 (both TDD spec-authoring) + 1 Rule 1 auto-fix tasks | 2 created files |
| Phase 20 P06 | 20min | 4 tasks | 5 files (2 created + 3 modified) |
| Phase 21-artifacts-module P00 | 21min | 4 tasks tasks | 10 files (7 created + 3 modified) files |
| Phase 21 P02 | 21min | 3 tasks | 3 files |
| Phase 21-artifacts-module P01 | 17min | 3 tasks | 6 files |
| Phase 21-artifacts-module P03 | 9min | 2 tasks | 2 files |
| Phase 21-artifacts-module P04 | 26min | 3 tasks | 3 files (2 created + 1 modified) |
| Phase 21-artifacts-module P05 | 19min | 3 tasks (all TDD spec-authoring + 2 Rule-1 auto-fixes) tasks | 3 created files |
| Phase 21-artifacts-module P06 | 11min | 5 tasks | 6 files (1 created + 2 modified + 4 renamed) |
| Phase 22-streaming-module P00 | 22min | 9 tasks | 9 files |
| Phase 22 P01 | 15min | 4 tasks (3 with tdd-style spec in Task 1.2) tasks | 4 modified files |
| Phase 22-streaming-module P03 | 9min | 3 tasks | 3 files |
| Phase 22 P04 | 4min | 2 tasks | 2 files |
| Phase 22-streaming-module P05 | 5min | 4 tasks | 7 files |
| Phase 22-streaming-module P06 | 7min | 4 tasks | 4 files |
| Phase 23-jobs-module-keystone P00 | 10min | 6 tasks tasks | 12 files (7 created + 5 modified) files |
| Phase 23 P01 | 4min | 2 tasks | 2 files |
| Phase 23 P02 | 14 min | 3 tasks | 3 files |
| Phase 23 P03 | 22min | 4 tasks | 8 files |
| Phase 23 P04 | 18m | 8 tasks | 13 files |
| Phase 23 P05 | 35 | 5 tasks | 8 files |
| Phase 23-jobs-module-keystone P06 | 5min | 3 tasks | 3 files |
| Phase 24 P00 | 9min | 3 tasks | 10 files |
| Phase 24 P01 | 9 | 2 tasks | 8 files |
| Phase 24 P02 | 10min | 1 task (TDD) tasks | 2 modified files |
| Phase 24 P03 | 14 | 4 tasks | 13 files |
| Phase 24-maestro-module P04 | 8min | 3 tasks | 3 files |
| Phase 24-maestro-module P05 | 12min | 3 tasks | 8 files (5 modified + 1 created + 2 renamed) |
| Phase 25 P00 | 12 minutes | 3 tasks | 11 files |
| Phase 25 P01 | 11min | 2 tasks | 4 files |
| Phase 25 P02 | 7min | 2 tasks | 2 files |
| Phase 25 P03 | 22 | 5 tasks | 16 files |
| Phase 25 P04 | 19 | 3 tasks | 3 files |
| Phase 25 P05 | 14 | 5 tasks | 13 files |
| Phase 26 P00 | 9 | 3 tasks | 13 files |
| Phase 26-auth-module P01 | 5 | 2 tasks | 3 files |
| Phase 26-auth-module P02 | 26min | 3 tasks | 3 files |
| Phase 31 P00 | 10min | 5 tasks | 17 files |
| Phase 31-quick-wins P04 | 5min | 3 tasks | 6 files |
| Phase 31-quick-wins P01 | 12 min | 3 tasks | 8 files |
| Phase 31-quick-wins P02 | 15m 54s | 3 tasks | 7 files |
| Phase 26 P03 | 27min | 9 tasks | 11 files |
| Phase 26-auth-module P04 | 11min | 3 tasks | 3 files |
| Phase 26 P05 | 15min | 5 tasks | 9 files |
| Phase 31-quick-wins P03 | 10min | 4 tasks | 11 files |
| Phase 32 P00 | 10min | 3 tasks | 17 files |
| Phase 32-simulatorkit-bridge P01 | 19min | 2 tasks | 9 files |
| Phase 32-simulatorkit-bridge P02 | 17 min | 2 tasks | 10 files |
| Phase 32 P03 | 10min | 2 tasks | 7 files |
| Phase 32-simulatorkit-bridge P04 | 10min | 2 tasks | 8 files |
| Phase 32-simulatorkit-bridge P05 | 9 min | 2 tasks | 12 files |
| Phase 33-android-grpc P00 | 11 | 4 tasks | 22 files |
| Phase 33-android-grpc P00 | 11 | 4 tasks | 22 files |
| Phase 33 P01 | 15min | 2 tasks | 6 files |
| Phase 33-android-grpc P02 | 25min | 3 tasks | 13 files |
| Phase 33-android-grpc P03 | 6min | 2 tasks | 3 files |
| Phase 33 P04 | 10min | 2 tasks | 8 files |
| Phase 33 P05 | 13min | 3 tasks | 14 files |
| Phase 33 P06 | 6min | 3 tasks | 4 files |
| Phase 34 P00 | 12 | 3 tasks | 37 files |
| Phase 34-session-api-mcp P01 | 30 min | 3 tasks | 8 files |
| Phase 34 P02 | 19 | 3 tasks | 9 files |
| Phase 34 P03 | 12 min | 3 tasks | 10 files |
| Phase 34 P04 | 30 min | 3 tasks | 7 files |
| Phase 34 P06 | 16 min | 2 tasks | 13 files |
| Phase 34 P07 | 18 | 2 tasks | 11 files |
| Phase 34 P05 | 24 min | 2 tasks | 23 files |
| Phase 34 P08 | 14 min | 5 tasks | 13 files |
| Phase 35 P00 | 19 min | 4 tasks | 28 files |
| Phase 35 P01 | 24 min | 3 tasks | 11 files |
| Phase 35 P02 | 37 min | 3 tasks | 18 files |
| Phase 35 P03 | 12 min | 3 tasks | 7 files |
| Phase 35 P04 | 25 min | 2 tasks | 6 files |
| Phase 35-app-explorer P05 | 28 min | 3 tasks | 24 files |
| Phase 36 P00 | 27 min | 3 tasks | 33 files |
| Phase 36 P01 | 23 min | 3 tasks | 16 files |
| Phase 36 P03 | 12 min | 3 tasks | 9 files |
| Phase 36-physical-devices-ux P02 | 23 min | 3 tasks | 23 files |
| Phase 36-physical-devices-ux P04 | 17 min | 3 tasks | 10 files |
| Phase 37 P00 | 33min | 3 tasks | 45 files |
| Phase 37-platform-extensions P02 | 22 min | 2 tasks | 23 files |
| Phase 37-platform-extensions P03 | 21min | 2 tasks | 18 files |
| Phase 37 P01 | 32min | 3 tasks | 26 files |
| Phase 37-platform-extensions P04 | 44 min | 2 tasks | 23 files |

## Accumulated Context

### Decisions

Full decision log lives in `.planning/PROJECT.md` (Key Decisions section).

v3.0 pillars (locked):
- Zod at all boundaries (runtime validation, TS types inferred)
- In-process typed event bus (per module `events.ts`)
- pg-boss queue (Postgres-native, replaces `node-cron` + in-memory queues)
- Correlation IDs + append-only `events` table
- Module-per-module with MODULE.md + barrel index.ts + events.ts + tests-as-spec

v3.0 build-order commitments:
- Phase 15 (Foundations) ships pg-boss plugin + event-bus + correlation + telemetry + events table + spikes + Nyquist baseline before any module refactor
- Phase 16 pilot = `hooks` (smallest real module that exercises all pillars)
- Phase 17 (Contracts) comes EARLY (right after pilot) so later CLI/web refactors consume generated types
- Phase 23 (jobs) is the keystone — refactored only after pool/artifacts/streaming are emitting events
- Phases 28 (CLI) and 29 (web) are last — they consume the locked contracts
- [Phase 15]: Plan 15-00: pg-boss v12 pinned (^12), Vitest v8 coverage provider, Node 22.12 baseline via .nvmrc, tsconfig includes .spec.ts/.compile.ts for typecheck
- [Phase 15-fix-operational-dependencies]: [Phase 15] Plan 15-02: Central branded-ID module landed (server/types/ids.ts) — JobId, DeviceId, PipelineId, ArtifactId, RecordingId as z.string().uuid().brand<>() with compile-fail proof via @ts-expect-error
- [Phase 15-fix-operational-dependencies]: [Phase 15] Plan 15-03: Correlation plugin wraps @fastify/request-context v6.2.1 with onRequest X-Correlation-Id read/generate/echo (length + printable-ASCII sanitation); telemetry plugin ships shape-agnostic alsMixin (handles both Map and object stores — needed for plan 15-05 cross-queue ALS restore); server/index.ts wiring deferred to plan 15-06 per plan directive
- [Phase 15]: Plan 15-01: drizzle-zod 0.8.3 + Zod 4.3.6 + drizzle-orm 0.45.1 compat confirmed — Open Question #1 resolved, createSelectSchema(events) works without fallback
- [Phase 15]: Plan 15-01: Drizzle migration output moved from ./drizzle to ./server/db/migrations to colocate with schema.ts (matches plan spec)
- [Phase 15]: Plan 15-01: events.payload column has no jsonb default (Pitfall #5 avoids drizzle-kit diff-churn); notNull enforces every row supplies payload
- [Phase 15]: Plan 15-01: Decoder contract takes Drizzle-shaped (camelCase) rows, not raw snake_case DB rows — createSelectSchema produces camelCase; rename eventType→type lives in envelope projection
- [Phase 15-fix-operational-dependencies]: [Phase 15] Plan 15-04: TypedBus<R> over Node EventEmitter with keyof R & string narrowing; createEventHelpers stamps envelopes from ALS (shape-agnostic reader — Map and object stores both handled); Fastify bus plugin uses PINNED side-channel `<type>.envelope` emit for envelope forwarding (WeakMap alternative rejected for ref-equality fragility); persistence middleware gated on registry.persisted flag (TRACE-08); onPersisted wrapper injects currentEventId into a Map-shape child ALS store so nested emits auto-carry causationId (TRACE-09); demoRegistry is fixture-only — real module events start landing in Phase 16
- [Phase 15-fix-operational-dependencies]: [Phase 15] Plan 15-04: `@fastify/request-context` v6 `requestContext.get()` only supports object-shape stores (uses `store[key]`); emit helpers needed shape-agnostic reader for plan 15-05 Map-shape queue restore compatibility — same fix previously applied to alsMixin in plan 15-03
- [Phase 15-fix-operational-dependencies]: Plan 15-04: Side-channel <type>.envelope emit forwards envelope to onPersisted subscribers (plan-pinned; WeakMap alternative rejected for ref-equality fragility across event-loop boundaries)
- [Phase 15-fix-operational-dependencies]: Plan 15-04: Emit-helpers ALS reader is shape-agnostic (handles both @fastify/request-context object stores and Map-shape stores from plan 15-05 queue-wrapper restore) — mirrors the same fix applied to alsMixin in plan 15-03
- [Phase 15-fix-operational-dependencies]: Plan 15-04: Persistence is fire-and-forget; DB write lives in a detached async IIFE with try/catch that logs via fastify.log.error but never rethrows — sync emit path must never be blocked by DB hiccup
- [Phase 15-fix-operational-dependencies]: Plan 15-05: pg-boss v12 Fastify plugin ships with ALS-aware queue.send (serialises correlationId from active ALS store into job.data) and queue.work (restores ALS via asyncLocalStorage.run(objectStore)) — end-to-end TRACE-05 proven with vi.waitFor(5s)
- [Phase 15-fix-operational-dependencies]: Plan 15-05: ALS worker restore uses plain OBJECT store, not Map (as RESEARCH §3 sketched). `@fastify/request-context`'s requestContext.get(key) uses `store[key]` bracket access — Map-incompatible. Object-shape store makes both requestContext.get() (worker handlers) AND pino alsMixin (shape-agnostic) work with one store
- [Phase 15-fix-operational-dependencies]: Plan 15-05: Queue plugin accepts opts.schema (default 'pgboss') for parallel Vitest spec isolation into pgboss_<suffix> schemas — production call sites leave opts unset, behaviour identical to plan spec
- [Phase 15-fix-operational-dependencies]: Plan 15-05: retryLimit:1 is set at createQueue time (queue-level default), not injected at send time — caller-supplied opts (e.g., {retryLimit: 0}) override freely and without silent mutation
- [Phase 15-fix-operational-dependencies]: Plan 15-05: Mac-drain timing spike measured 4032ms against 30_000ms budget on MBP M4 Max proxy — 26s headroom; re-run on actual Mac Mini before Phase 16 kickoff is recommended but 30s timeout retained
- [Phase 15-fix-operational-dependencies]: Plan 15-05: Plugin registers ONLY the DEMO queue; real queue names (job.execute, webhook.deliver, ...) will be registered by each Phase 16+ module's local queue.ts via fastify.boss.createQueue(...) — substrate is plugin-owned, module contracts are module-owned
- [Phase 15-fix-operational-dependencies]: Plan 15-09: Nyquist baseline captured as END-OF-Phase-15 snapshot (commit 55ff8ac, 48.29% lines / 36% branches / 45.21% functions / 47.06% statements), not pre-Phase-15 v2.0 baseline — plans 15-00..15-05 had already landed substrate source changes. Documented via 'note' field in .planning/nyquist-baseline.json.
- [Phase 15-fix-operational-dependencies]: Plan 15-09: Delta gate is -2pp on coverage.lines only (branches/functions/statements recorded for trend analysis, do not gate CI). capture-nyquist.mjs uses execFileSync('git', ['rev-parse', 'HEAD']) argv-safe — never shell concat (RESEARCH §12 pitfall).
- [Phase 15-fix-operational-dependencies]: Plan 15-09: check-nyquist.mjs stub created alongside capture script (not deferred), so Phase 16 can wire npm run nyquist:check into its verification gate without a round-trip back to Phase 15.
- [Phase 15-fix-operational-dependencies]: Plan 15-08: ADR-001 lives at `docs/adr/001-spec-driven-architecture.md` (137 lines, Nygard format); EVENTS-05 sync-vs-queue rule rendered as a verbatim blockquote inside Pillar 3 — becomes the canonical "why" reference cited by every subsequent v3.0 phase. `docs/adr/README.md` locks `NNN-slug.md` filename convention + reserves 002 for the Phase 16 file-naming ADR. ADRs are append-only (supersede rather than edit) — decision history preserved for LLM traceability.
- [Phase 15-fix-operational-dependencies]: Plan 15-08: Decision section subdivided with H3 subsections per pillar (compatible with Nygard — he allows free-form structure inside H2 sections) to keep each pillar individually scannable; paragraph breaks inside dense pillar prose lift the file into the plan-specified 100-400 line acceptance band without padding fluff.
- [Phase 15-fix-operational-dependencies]: Plan 15-07: no-imperative-event-names selector narrowed from RESEARCH §10 broad '.emit() Literal' to bus.emit(<literal>) / fastify.bus.emit(<literal>) only — avoids false positives on Node EventEmitter .emit calls in v2 code (this.emit('stateChange'), broadcaster.emit('job-1'), proc.emit('exit')). Rule pair (no-imperative + no-direct) now coherently scoped to bus surface
- [Phase 15-fix-operational-dependencies]: Plan 15-07: no-direct-bus-emit allowlist DROPPED /\/__tests__\// ancestor match — it over-matched fixture files under eslint-local-rules/__tests__/fixtures/ which must remain lint-exercisable. .(spec|test).ts extension rule alone covers real test files and preserves fixture force-lint
- [Phase 15-fix-operational-dependencies]: Plan 15-07: eslint-local-rules/package.json with '"type": "commonjs"' scopes CJS rule modules under the repo-level ESM package; RuleTester specs are .test.mjs (vitest v4 cannot be require()'d) and load rule sources via createRequire(import.meta.url)
- [Phase 15-fix-operational-dependencies]: [Phase 15] Plan 15-06: server/index.ts now registers plugins in target substrate-first order: config -> correlation -> db -> event-bus -> queue -> telemetry -> pool -> auth -> websocket -> artifact -> reporting -> job -> lifecycle -> hooks -> maestro -> pipelines -> api -> static. Pino alsMixin wired at Fastify() construction so every log line carries correlationId from the active ALS store (request fiber + queue-worker fiber both object-shape). SIGTERM handler delegates to await app.close() before dbClient.end() + process.exit(0), so queue.onClose drains BEFORE db teardown (QUEUE-07 held by plugin chain order).
- [Phase 15-fix-operational-dependencies]: [Phase 15] Plan 15-06: dbClient.end() KEPT in imperative shutdown handler (runs after await app.close()) because server/db/plugin.ts has no onClose hook. Removing it would leak the Postgres client. Tagged TODO(Phase 20+) to migrate db teardown into a plugin onClose once the pool module refactor lands.
- [Phase 15-fix-operational-dependencies]: [Phase 15] Plan 15-06: Task 6.2 (live Mac Mini fresh-boot + SIGTERM observation) human-validation-deferred on user decision. Automated plugin-order invariant spec (printPlugins assertions) + Fastify reverse-onClose semantics + plan 15-05 shutdown.spec accepted as sufficient signal for Phase 15 close-out. Pre-Phase-16 recommendation: rerun the 5-step manual check on the actual Mac Mini before kickoff.
- [Phase 16-pilot-module-hooks]: Plan 16-00: server/hooks/schemas.ts is the single Zod source-of-truth for hookDefinitionSchema; HookDefinition derives via z.infer<typeof hookDefinitionSchema> (SPEC-03); HookEvent + HookDefinition re-exported from hook-executor.ts as back-compat shim so external imports keep working without call-site updates
- [Phase 16-pilot-module-hooks]: Plan 16-00: server/hooks/events.ts declares 4-event hooksRegistry (scheduled/completed/failed/retryExhausted) with persisted:true ONLY on terminals (hook.completed + hook.failed.retryExhausted) per TRACE-08; makeHookEmitters(bus, onEmit) factory mirrors createEventHelpers convention so plugin wires persistEnvelope via onEmit callback
- [Phase 16-pilot-module-hooks]: Plan 16-00: ADR-002 Accepted — repo-wide kebab-case filenames, singular-for-concepts (schemas.ts is the one plural exception), reserved module filenames (index.ts/MODULE.md/events.ts/queue.ts/schemas.ts/plugin.ts), colocated __tests__/ with .spec.ts extension, internal/ as dependency-cruiser denylist scope (MOD-05)
- [Phase 16-pilot-module-hooks]: Plan 16-00: QUEUE_NAMES extended with HOOK_RUN='hook.run' (QUEUE-06 prep for 16-01 worker); dep-check npm script deferred to 16-03 per plan directive — this plan only installed dependency-cruiser@^17.3.10 devDep without wiring the .dependency-cruiser.cjs config
- [Phase 16-pilot-module-hooks]: Plan 16-00: Typecheck baseline=7 pre-existing top-level errors in unrelated modules (artifacts/, bus/, pipelines/) — out-of-scope per deviation-rule scope boundary; 0 new errors from hooks/ changes. ESLint /\\/events\\.ts$/ allowlist pattern already covers server/hooks/events.ts (RESEARCH Pitfall 4) — no rule edit needed
- [Phase 16-pilot-module-hooks]: Plan 16-01: hook_runs Drizzle table shipped with operation_key TEXT PRIMARY KEY + 3 btree indexes (hook_name, event_id, triggered_at); no FK on event_id because non-persisted bus envelopes can still trigger hooks (e.g. test.trigger fixture)
- [Phase 16-pilot-module-hooks]: Plan 16-01: server/hooks/queue.ts uses pg-boss queue policy: 'stately' (NOT default 'standard') — RESEARCH §Pitfall 8 was wrong: job_i4 unique index applies to singleton_on (time-slot dedup), not singleton_key. Source inspection of node_modules/pg-boss/dist/plans.js:467-485 confirms singleton_key dedup requires one of short/singleton/stately/exclusive/key_strict_fifo. 'stately' (job_i3) covers state<=active which makes back-to-back boss.send with the same singletonKey return null on the duplicate.
- [Phase 16-pilot-module-hooks]: Plan 16-01: Two-layer idempotency for EVENTS-06 — queue-boundary (policy: 'stately' + singletonKey) prevents duplicate enqueue; DB-boundary (hook_runs PK + onConflictDoNothing) prevents duplicate execution. Either layer alone is insufficient; both in series give at-most-one physical side-effect per (triggerEventId, hookName) across edge cases.
- [Phase 16-pilot-module-hooks]: Plan 16-01: Worker handler private-method spy + call-count assertion — vi.spyOn(executor as any, 'executeOne') + toHaveBeenCalledTimes(1) is load-bearing for Invariant (c). Counting executeOne calls (not execute calls) proves no duplicate shell exec even in a scenario where the hook_runs row count stays at 1 (e.g. transaction rollback post-exec). HookExecutor.executeOne private method kept at hook-executor.ts:147.
- [Phase 16-pilot-module-hooks]: Plan 16-02: createHooksModule(deps): HooksModule factory (MOD-06) owns per-module TypedBus<HooksRegistry> construction, emit helpers via makeHookEmitters, persistEnvelope (10 lines duplicated from bus plugin per RESEARCH Open Question #1), HOOK_RUN worker registration via registerHookRunWorker, wireBusToQueue bridge subscriber, and idempotent shutdown (stopped flag)
- [Phase 16-pilot-module-hooks]: Plan 16-02: server/hooks/index.ts public barrel (MOD-02) — the ONLY allowed internal/ re-export is createHooksModule from ./internal/module.js (canonical factory entry point); hook-executor, hook-run-handler, idempotency, subscribers NOT re-exported through the barrel; dep-cruiser rule in 16-03 will enforce the deep-import boundary structurally
- [Phase 16-pilot-module-hooks]: Plan 16-02: Plugin dependencies array extended ['config', 'pool-plugin'] → ['config', 'event-bus', 'queue', 'pool-plugin'] per RESEARCH §8. Plugin decorates BOTH fastify.hookExecutor (back-compat for server/index.ts line 127 app.hookExecutor.execute device.booted loop) AND fastify.hooksModule (new HooksModule surface for Phase 20+ consumers) — dual decoration avoids touching server/index.ts in this plan
- [Phase 16-pilot-module-hooks]: Plan 16-02: HookExecutor class body moved to server/hooks/internal/hook-executor.ts; root server/hooks/hook-executor.ts becomes 3-line back-compat re-export shim so existing 4 consumers (server/index.ts, plugin.ts, queue.ts, internal/hook-run-handler.ts) keep resolving without call-site changes; Phase 20+ migrates consumers to barrel + deletes shim. Producer-side hookRunPayloadSchema.parse() in wireBusToQueue satisfies 16-01 must_haves truth (consumer-side parse stays as defense-in-depth)
- [Phase 16-pilot-module-hooks]: Plan 16-03: .dependency-cruiser.cjs at repo root with two forbidden rules (no-deep-imports-into-hooks-internal enforces MOD-02 barrel boundary; no-direct-bus-emit-outside-events-ts is belt-and-suspenders graph guard complementing ESLint rule); options.tsConfig + enhancedResolveOptions.conditionNames for TS NodeNext .js specifier resolution; npm run dep-check script invokes depcruise on server/ tree — exits 0 on committed codebase (178 modules / 389 deps)
- [Phase 16-pilot-module-hooks]: Plan 16-03: no-direct-bus-emit-outside-events-ts allowlist extended to cover server/bus/index.ts (bus barrel re-exports TypedBus publicly) and server/*/internal/module.ts (MOD-06 factories legitimately construct new TypedBus(registry) per Phase 16 pilot pattern) — both import sites are already excluded by the ESLint rule at AST time; dep-cruiser allowlist restores graph-level parity
- [Phase 16-pilot-module-hooks]: Plan 16-03: dep-cruiser spec uses PRIMARY FALLBACK --include-only '^(server|__fixtures__)/' CLI override — pre-task verification (WARNING 8) confirmed the config's includeOnly: '^server/' suppresses fixture-path violations even with explicit CLI file args; spec runs depcruise twice (err reporter for non-zero exit status + json reporter for structured violation parse) because the json reporter does NOT set exit code on severity:error rule matches (empirical, v17.3.10); __fixtures__/dep-cruiser/bad-deep-import.ts lives outside server/ so tree-wide npm run dep-check stays clean
- [Phase 16-pilot-module-hooks]: Plan 16-04: MODULE.md uses H2 for 9 required sections (Purpose / Public API / Events Emitted / Events Consumed / Queue Produced / Queue Consumed / Invariants / Non-Goals / Dependencies) + H3 for the optional Runnable Example; keeps automated grep check precise and reserves H3 as the Phase 27 MOD-09 CI-typecheck extension point
- [Phase 16-pilot-module-hooks]: Plan 16-04: hook-executor.spec.ts (4 invariants a/b/d/e) does NOT require DB because HookExecutor is pure in-process logic running /bin/sh subprocesses; Invariant c stays in queue.spec.ts because it genuinely requires hook_runs PK + pg-boss + Drizzle — invariant tests placed where the invariant is natural to prove
- [Phase 16-pilot-module-hooks]: Plan 16-04: events.spec.ts uses Map-shape ALS store new Map([['correlationId', cid]]) matching canonical server/events/__tests__/emit-helpers.spec.ts:32 pattern — exercises the exact readAls code path (server/bus/helpers.ts:69-70) that pg-boss worker fibers hit in production after plan 15-05's queue.work wrapper restores ALS with Map shape
- [Phase 16-pilot-module-hooks]: Plan 16-04: nyquist:capture NOT committed as updated baseline — Phase 15 baseline (48.29% at commit 55ff8ac) remains reference for all Phase 16+ delta checks; running capture would overwrite baseline making gate trivially pass against itself; plan restored baseline file after capture and ran check against Phase 15 snapshot — delta = +3.63pp (gate green with 5.63pp headroom)
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-00: fastify-zod-openapi bumped ^4 → ^5 (project has zod@^4.3.6; fastify-zod-openapi@4.1.2 pins zod@^3.21.4 peer which excludes Zod 4; v5.6.1 declares zod: '^3.25.74 || ^4.0.0' and accepts Zod 4 natively — empirical peer-dep audit caught the stale RESEARCH §Standard Stack pin before install); fastify: '5' + @fastify/swagger: '^9.0.0' peers satisfied transparently
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-00: Zod 4 native z.toJSONSchema() path committed in build-openapi.ts skeleton; zod-to-json-schema@3.x dep NOT added (resolves RESEARCH Open Question 1 in-plan — Zod 4 ships the capability natively, making the extra dep redundant)
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-00: .gitignore blanket 'vendor/' rule DROPPED (not replaced with negation patterns) — git cannot re-include files under an ignored parent, so '!vendor/device-stream/**' silently fails 'git check-ignore'; safer to remove the line since no other 'vendor/' subtree exists in the repo (cli/ uses Go modules, no PHP/composer)
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-00: build-openapi.ts skeleton includes WS JSON-Schema emit block commented out with TODO(17-02) marker (not deferred entirely) — locates the single import + 8 commented lines that Plan 17-02 uncomments; 'void z' + 'void mkdirSync' anchors keep the imports live so TypeScript stays green pre-Wave-1
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-02: Zod WS schemas mirror forward-looking Phase 22 wire shape (correlationId + flat payload per RESEARCH §Example 4), NOT the current server/streaming/types.ts runtime shape (JobMessage nests data: unknown). Plan scope is SHAPES + fixtures for downstream codegen ONLY; Phase 22 Streaming Module migrates the broadcaster. NOTE block in server/jobs/ws-schemas.ts records the divergence for the Phase 22 author.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-02: contracts/ws-messages.ts file + contracts/ws-messages/ directory (from 17-00 scaffold) coexist — distinct filesystem names. The directory remains reserved for 17-03-emitted ws-messages.json; the file is the Zod-registry aggregator that 17-03 + 17-04 consumers import.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-02: WS emit block in server/scripts/build-openapi.ts INTENTIONALLY stays commented out (per plan directive) — un-commenting is 17-03 Task 1's responsibility so the Go-side consumer activates the producer. Consumer-driven change discipline is the Wave 1→2 handoff pattern.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-02: server/websocket/__tests__/ is a net-new directory for the TS round-trip canary (WS runtime lives at server/streaming/websocket-plugin.ts). Anticipates Phase 22 Streaming Module extraction; the test file sits OUTSIDE any module internal/, so dep-cruiser rules from 16-03 are unaffected.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-01: RESEARCH §Pattern 1 missed app.setValidatorCompiler(validatorCompiler) + setSerializerCompiler(serializerCompiler) — fastify-zod-openapi v5 requires both setter calls at root scope BEFORE fastifyZodOpenApiPlugin register; without them Fastify falls back to Ajv which chokes on Zod-emitted required:[...] arrays (FST_ERR_SCH_VALIDATION_BUILD). Fixed in server/index.ts:92-93 per README.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-01: server/openapi.json is byte-deterministic across repeat npm run openapi:generate runs without any Object-key sort pass — fastify-zod-openapi v5 preserves insertion order natively; sort-pass workaround in 17-01-PLAN Step B skipped.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-01: Rule-3 deviation — server/pipelines/plugin.ts stale deps ['db-plugin','websocket-plugin','jobs-plugin'] corrected to actual registered names ['db','websocket-plugin','job-plugin']; unblocked build-openapi.ts plugin-load. Only dependency strings changed, no plugin name renames (Plan 17-06 owns DEBT-01 broader rename).
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-01: 7 representative routes upgraded to withTypeProvider<FastifyZodOpenApiTypeProvider>() — POST /api/hooks + POST /jobs (multipart, no body schema) + POST /admin/keys + new POST /api/webhooks + POST /api/pipelines + GET /devices + GET /jobs/:id/artifacts. server/openapi.json committed at 1762 lines / 14 named components.schemas (Hook, JobSummary, ApiKey, Webhook, Pipeline, DeviceList, ArtifactList + 7 request/helper schemas).
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-07: SC5 dep-graph gap closure. server/streaming/websocket-plugin.ts dependencies extended from ['config','auth'] → ['config','auth','pool-plugin'] (reflects devicePreview/pool ownership at server/streaming/websocket-plugin.ts:29 + :95-114 route key-on-deviceId); server/api/plugin.ts dependencies extended from 8 → 9 strings (appended 'lifecycle-plugin', reflects fastify.lifecycleStats read at server/api/routes.ts:439). Plugin names intentionally unchanged — broader DEBT-01 rename ('pool-plugin' → 'pool' etc., per 17-CONTEXT.md §Plugin Naming Normalization) deferred to a future targeted plan.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-07: plugin-order.spec.ts pattern established — per-plan dep-graph invariants are additive inside the existing 'registers substrate plugins before application plugins' it-block (not new it-blocks), with comments naming the owning plan ('Phase 17 / Plan 17-07 — ...'). Rationale: one app.printPlugins() boot serves the entire dep-graph story; fragmenting test blocks would force multiple boot cycles (~6s each) for zero new coverage. 3 new assertions added to the 8 existing invariants — spec still passes green.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-05: openapi-typescript 7.13 emits 'export interface paths/components' (not 'export type') — web barrel re-exports them via 'export type { ... } from ./generated-types.js' which works identically because TS interfaces and type aliases are interchangeable at the type-import boundary
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-05: DeviceList and ArtifactList are emitted as direct array types (components['schemas']['DeviceSummary'][] / ArtifactSummary[]) — OpenAPI 'type: array' schemas lower to raw arrays in openapi-typescript 7.x, not wrapped { items: [...] } objects. Smoke tests assert mutual element-type assignability rather than Pick<_, 'items'>
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-05: root vitest.config.ts 'include' extended with two web/src/**/__tests__/**/*.{test,spec}.ts entries instead of creating a separate web/vitest.config.ts — single-root-config approach keeps 'npm test' as the canonical test runner for the whole repo, avoiding a split CI path
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-05: web/src/lib/api/index.ts created fresh (no prior barrel existed); pre-existing per-entity imports (client.ts, devices.ts, jobs.ts, etc.) left untouched per plan directive — Phase 29 owns the runtime openapi-fetch migration
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-06: Vendored @device-stream/{core,android,ios-simulator} 1.1.0 as npm-pack tarballs under vendor/device-stream/ with file:./vendor/... refs in package.json — DEBT-01 npm portion closed. Workspaces rejected (device-stream ships as its own repo/namespace, not a monorepo child). Reversibility one search-and-replace away: swap file: specs for version specs + add .npmrc pointing at https://npm.pkg.github.com (source repo already declares publishConfig.registry). Fresh-clone npm ci succeeds in ~12s with 681 packages, no sibling repo needed.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-06: Swift sim-capture source vendoring deliberately OUT of scope. installSimCapture in cli/cmd/dependencies.go only runs at device-farm dependencies user-setup time, not at npm install time — DEBT-01 npm portion is fully resolved without it. vendor/sim-capture/README.md explicitly marks the directory as empty-scaffold and points future readers at the follow-up plan (Phase 20 Pool Module or Phase 28 CLI Refactor scope). Scope-boundary documentation pattern: every vendored subtree pairs a refresh script (scripts/vendor-<pkg>.sh) + vendor/<pkg>/README.md so the procedure survives turnover.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-03: contracts/ws-messages.json emitted via z.toJSONSchema(wsMessageRegistry, {target: 'draft-2020-12', unrepresentable: 'throw', reused: 'ref'}) with top-level shape { schemas: {...} } (not ) — Zod 4 registry emission puts variants under 'schemas' key; both shapes accepted by plan's either/or acceptance criteria since all 7 ids present as string matches.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-03: Byte-determinism verified empirically across 3 consecutive regenerations (all 12,764 bytes, diff exits 0). Zod 4 z.toJSONSchema(registry) preserves registry insertion order; no sort-pass or stable-stringify needed. Matches Plan 17-01 observation for fastify-zod-openapi v5 openapi.json emission.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-03: build-openapi.spec.ts smoke test uses source-level assertions (readFileSync + toContain/toMatch) instead of behavioral (no buildApp boot). Locks activation state against silent re-commenting regressions without a 30s CI DB-boot step. Behavioral coverage comes from 17-04 Go codegen consuming the emitted file.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-04: scripts/extract-ws-schemas.mjs adapter rewrites __shared#/$defs/* refs to #/definitions/* and strips format:date-time (go-jsonschema v0.23 aliases time.Time which drops JSON methods). Hand-rolled cli/internal/types/unions.go implements JobMessage discriminator wrapper per ADR-003; Pool/Artifact channels are single-variant and decode directly. Dual-lane harness complete: TS frames.spec.ts (13) + Go generated_test.go (5) both green against same contracts/ws-fixtures/*.sample.json. make types-check CI drift guard ready. go-jsonschema installed via go install github.com/atombender/go-jsonschema@latest (binary at ~/go/bin/go-jsonschema); Makefile uses simpler -p types -o FILE invocation instead of plan's URI=FILENAME syntax.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-08: scripts/check-generated.sh orchestrates npm run openapi:generate -> make -C cli types -> npm run web:types, then per-file git diff --quiet against 4 committed artifacts (server/openapi.json, contracts/ws-messages.json, cli/internal/types/generated.go, web/src/lib/api/generated-types.ts). Exits non-zero naming every drifted file + prints regen hint + git diff --stat preview. Per-file diff chosen over tree-wide git diff --exit-code to avoid false-positive on unrelated working-tree edits.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-08: scripts/__tests__/check-generated.spec.ts gated by canRunFullPipeline() helper — skipIf(!existsSync(TARGET) || !DATABASE_URL || CONTRACTS_CHECK_SPEC=skip). On dev hosts without Postgres, build-openapi.ts dies at Step 1/3 before the diff loop runs, so the spec would false-fail the text-match assertion. Gate keeps spec meaningful on CI (where DB is provisioned) while avoiding dev-box false fails. All subprocess calls use spawnSync with argv-array form (no exec/execSync/execFile-with-string) including the afterEach 'git checkout HEAD -- <path>' restore.
- [Phase 17-contracts-pipeline-ops-hygiene]: Plan 17-08: vitest.config.ts 'include' extended with scripts/__tests__/**/*.{test,spec}.ts rather than isolating the new spec in a separate vitest project. Single-root-config keeps npm test as canonical runner for the whole repo — same reasoning as Plan 17-05's web-side test include expansion.
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-00: QUEUE-08 per-fire correlationId fix ships as Option B (envelope.correlationId:null + queue.work's existing `data.correlationId ?? randomUUID()` fallback generates per-fire UUID). Option A (wrapping each fire with an interceptor) rejected because pg-boss v12 does not expose dispatch-time hooks. JobEnvelope.correlationId widened `string` → `string | null` explicitly (not a `null as never` cast) so queue.send (ALS-injected string) and queue.schedule (always null) are both type-accurate. One-line schedule() body change leverages existing fallback at plugin.ts:159 — zero new code paths.
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-00: QUEUE_NAMES extended with LIFECYCLE_COMPRESS_DAILY ('lifecycle.compress.daily'), LIFECYCLE_RETENTION_DAILY ('lifecycle.retention.daily'), LIFECYCLE_DISK_HOURLY ('lifecycle.disk.hourly') — dot-separated per codebase convention (matches hook.run). All satisfy pg-boss v12 regex ^[a-z][a-z0-9._-]*$.
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-00: server/lifecycle/schemas.ts Zod source-of-truth (SPEC-01/03) — lifecycleJobPayloadSchema (kind enum + triggeredAt ISO) + 3 result schemas (compression/retention/diskPressure) + 4 inferred types. Existing task-file interfaces (CompressionResult etc.) left untouched as back-compat surface; the *ResultParsed Zod-derived types are parse-at-boundary counterparts for events.ts (plan 18-01 extends them with durationMs).
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-00: Dep-cruiser lifecycle/internal rule + fixture + spec require a resolvable import target, so server/lifecycle/internal/module.ts shipped as a 4-line stub. Empirical discovery: without the stub depcruise emits '1 module, 0 dependencies cruised' and the forbidden rule does NOT fire (the resolver silently drops unresolvable imports before rule-matching). Plan's documented fallback path — fixture-pre-authorized and plan 18-03 overwrites with real createLifecycleModule factory. npm run dep-check now at 193 modules / 399 deps / 0 violations.
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-00: Dep-cruiser describe block in server/hooks/__tests__/dep-cruiser.spec.ts renamed from 'no-deep-imports-into-hooks-internal' (singular) to 'deep-import denylist rules' (plural) to accommodate both rules in one describe without duplicating beforeEach/imports. Same two-pass err+json reporter pattern as Phase 16 (err sets exit status, json reports structured violations).
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-01: server/lifecycle/events.ts declares 4 events, all persisted:true (terminal operational telemetry per TRACE-08) — compression.completed / retention.completed / disk.checked / task.failed. No transient/thin variants (unlike hooks' 4-of-which-2-terminal pattern) because lifecycle tasks have no per-attempt observable state worth publishing before terminal success/failure
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-01: LIFECYCLE_AGGREGATE_ID = 'a9c1a64b-f0c7-54fb-8153-d48ca3f6e97e' — stable v5 UUID derived deterministically from 'lifecycle' + URL namespace (RFC 4122). Picked over bare string 'lifecycle' because envelopeSchema.parse (server/events/envelope.ts:27) enforces UUID format with Zod 4's strict version+variant regex. Singleton-aggregate semantics preserved; aggregateType carries the human-readable 'lifecycle' label for trace-tree filtering
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-01: Task-file decoupling preserved — events.ts imports ONLY from ./schemas.js (not from ./compression-task.js / ./retention-task.js / ./disk-pressure-task.js). Task functions stay pure; emit happens in the pg-boss worker handler (plan 18-02), not in task bodies — mirrors RESEARCH §Anti-Patterns. compression-task.ts / retention-task.ts / disk-pressure-task.ts untouched by this plan.
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-02: queue.ts ships 3 schedules (compress/retention daily at 3 AM, disk hourly) via canonical Pattern 1 (createQueue → schedule → work); worker handlers pass LIFECYCLE_AGGREGATE_ID (UUID v5) to emit helpers since envelopeSchema requires UUID aggregateId; LifecycleStats extracted to stats.ts for sharing with plan 18-03 factory
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-02: correlation.spec.ts uses TWO schedules (.a + .b) to observe two distinct per-fire correlationIds — pg-boss applies singletonSeconds:60 to every cron-dispatched fire (timekeeper.js:139) so a single schedule emits ≤1 job per 60s regardless of cron granularity
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-02 [Rule 3 deviation]: server/queue/plugin.ts gained optional cronMonitorIntervalSeconds passthrough — pg-boss default 30s blew past 15s integration-test budget; production call sites leave unset, behavior identical; correlation.spec.ts passes 1s for sub-10s observation
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-02 [Rule 1 deviation]: DiskPressureResult extended with maxBytes — required by schemas.ts diskPressureResultSchema (plan 18-00) and the lifecycle.disk.checked event payload; pre-existing disk-pressure-task.test.ts extended with two new assertions
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-03: createLifecycleModule factory (MOD-06) shipped mirroring hooks pattern section-by-section; thin plugin.ts replaces 99-line lifecycle-plugin.ts; plugin NAME 'lifecycle-plugin' preserved for downstream deps; dependencies extended ['config','db']->['config','db','queue','event-bus']; dual decoration lifecycleStats+lifecycleModule preserves /health endpoint back-compat
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-03: persistEnvelope middleware duplicated from bus/plugin.ts + hooks/internal/module.ts per RESEARCH Open Question #1 (consolidation deferred to Phase 27+ when 3+ sample points exist)
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-03: graceful-shutdown.spec.ts measured app.close() at 642ms vs 10s budget (15x headroom); shutdown path scope is no-task-in-flight case (queue plugin onClose owns the real 30s drain budget)
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-03 [Deferred]: pre-existing plugin-order.spec.ts failure documented in deferred-items.md — indexOf('api') matches 'fastify-zod-openapi' substring (Plan 17-07 assertion regressed silently when Plan 17-00 added fastify-zod-openapi); out of scope per deviation-rule scope boundary
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-04: MOD-02 strict 'one internal/ re-export line' enforced via inline type modifier — single statement exports createLifecycleModule + LifecycleModule + CreateLifecycleModuleDeps, tightens on hooks' 2-line pattern
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-04: MOD-04 file-naming layer only (*.test.ts -> *.spec.ts via git mv) — describe-tree alignment with MODULE.md invariants deferred to Phase 30 repo-wide migration
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-04 [Deferred]: 31 pre-existing test failures + 6 pre-existing typecheck errors + contracts:check spec hang all reproduced on HEAD~3 (before any plan 18-04 work) — caused by Phase 17 fastify-zod-openapi v5 'required' emission bug + Phase 15 Map-vs-RequestContext divergence; logged to deferred-items.md per SCOPE BOUNDARY rule
- [Phase 18-lifecycle-migration-node-cron-pg-boss]: Plan 18-04: Nyquist delta +7.54pp (current 55.83% lines vs 48.29% baseline) — well above -2pp gate; capture required excluding 4 pre-existing-failing test files because vitest v4 does not emit coverage-summary.json when tests fail at Fastify boot
- [Phase 19]: Plan 19-00: vi.doMock + vi.resetModules + dynamic import pattern for spying on ES class constructors with private fields (PgBoss v12 #config) — re-imports plugin under mocked module, subclass records ctor args in closure, vi.doUnmock + vi.resetModules in finally prevents cross-test pollution
- [Phase 19]: Plan 19-00: QUEUE_NAMES dot-separated convention reconfirmed — WEBHOOK_DELIVER='webhook.deliver', WEBHOOK_DELIVER_DLQ='webhook.deliver.dlq' (Phase 19 CONTEXT prose used hyphen 'webhook-deliver' but that was loose phrasing; dot form matches hook.run, lifecycle.*, passes pg-boss v12 charset regex)
- [Phase 19]: Plan 19-00: DLQ response schema flat shape — correlation_id (snake_case) hoisted from envelope to top-level; field names retrycount/createdon/correlation_id match pg-boss v12 table columns verbatim; dlqListResponseSchema wraps items[] with count for forward-compat with future ?limit= pagination
- [Phase 19]: Plan 19-00: reporting/internal/module.ts 4-line stub required (not optional) per Phase 18 Plan 18-00 empirical finding — without resolvable target depcruise 17.3.10 silently drops unresolvable import before rule-matching; stub unblocks dep-cruiser rule to fire on fixture; plan 19-03 overwrites with real factory
- [Phase 19]: Plan 19-00: shared failing-server.ts fixture factored per checker W1 — startFailingServer() returns handle with setResponse/requestCount/lastHeaders/lastBody/close; DRYs ~40 LOC across 5 downstream specs (webhook-service + queue + correlation + terminal-event + dlq-route); flaky-detector.test.ts and junit-generator.test.ts UNCHANGED per plan directive (out-of-scope; Phase 30 repo-wide rename)
- [Phase 19]: Plan 19-01: REPORTING_AGGREGATE_ID stable v5 UUID (bca46f4f-d5bd-5d65-bf73-0a59a7f3c6d7) derived from 'reporting' string + URL namespace per RFC 4122 §4.3 — matches Phase 18 LIFECYCLE_AGGREGATE_ID pattern; envelopeSchema requires z.string().uuid() for aggregateId
- [Phase 19]: Plan 19-01: server/jobs/events.ts is MINIMAL (1 event: job.completed) — Phase 23 Jobs Module Keystone extends with full saga per EVENTS-10. No MODULE.md / barrel / internal/ / queue.ts created (scope discipline structurally enforced)
- [Phase 19]: Plan 19-01 [Rule 1 deviation]: jobCompletedPayload.status enum is z.enum(['passed','failed','cancelled','timeout']) matching ExecutionResult.status at runtime; plan text specified ['completed','failed','cancelled'] which would Zod-parse-fail on happy-path 'passed' emissions; Phase 23 owns any saga-time remapping
- [Phase 19]: Plan 19-01: Pre-existing uncommitted adbSerial-vs-deviceId hunks in server/jobs/job-service.ts + maestro-parser.ts isolated via git checkout HEAD + re-apply pattern; scope boundary preserved; documented in .planning/phases/19-reporting-migration-webhooks-dlq/deferred-items.md for user/next-owner attention
- [Phase 19]: Plan 19-01: server/jobs/plugin.ts (not job-plugin.ts) is the actual filename; plugin NAME 'job-plugin' preserved for downstream dep resolution; dependencies extended ['config','db','pool-plugin','websocket-plugin','artifact-plugin','reporting'] → added 'event-bus' so fastify.jobsModule decorator's TypedBus surface is ready before reporting plugin subscribes in plan 19-03
- [Phase 19]: Plan 19-03: MAIN + DLQ webhook queues use policy:'standard' (NOT stately) — webhooks are NOT idempotent by enqueue per RESEARCH §Pitfall 9; two job.completed events = two separate POST attempts
- [Phase 19]: Plan 19-03: DLQ worker explicitly swallows emit errors (log.error, do NOT throw) per RESEARCH §Pattern 2 — DLQ has retryLimit:0 so throwing would be terminal anyway, but explicit non-throw is safer than relying on pg-boss not looping
- [Phase 19]: Plan 19-03: persistEnvelope 10-line middleware duplicated into reporting/internal/module.ts (3rd sample point after hooks/ + lifecycle/) — consolidation threshold now reached; Phase 27+ refactor target confirmed per RESEARCH Open Question #1
- [Phase 19]: Plan 19-03: enqueueWebhookDelivery imperative facade ships alongside the bus-driven subscriber — consumers with URL+payload ready (ping endpoint, future producers) avoid a bus round-trip; both paths emit webhook.scheduled side-event for unified trace visibility
- [Phase 19]: Plan 19-03: reporting plugin NAME preserved as 'reporting' (not renamed to 'reporting-plugin' for ADR-002 alignment) — renaming would cascade 12-plugin dep-string edits; ADR-002 kebab-case alignment deferred to Phase 30 repo-wide rename
- [Phase 19]: Plan 19-03: fastify.onPersisted cast to 'job.completed' subscriber type is load-bearing — Phase 15 bus plugin's type signature narrows to keyof demoRegistry & string, predates per-module registries; Phase 27+ consolidation target. module.ts uses explicit cast-at-boundary pattern
- [Phase 19]: Plan 19-03: Pre-existing dep-check violation (server/jobs/plugin.ts → server/bus/bus.ts) NOT fixed in this plan (SCOPE BOUNDARY rule) — introduced by plan 19-01 minimal jobs bridgehead, documented in deferred-items.md; Phase 23 Jobs Module Keystone fixes via MOD-06 server/jobs/internal/module.ts which the allowlist already covers
- [Phase 19]: Plan 19-04: DB-gated spec timing bound via app.boss.updateQueue(WEBHOOK_DELIVER_QUEUE_NAME, {retryDelay:1, retryBackoff:true, retryDelayMax:2}) — collapses pg-boss random exponential backoff formula (plans.js:1063-1069) from worst-case ~60s to ~9s without touching production retryDelayMax:30 default
- [Phase 19]: Plan 19-04: Ephemeral-URL row de-dup pattern — when multiple DB specs share the device_farm_test.events public schema during vitest parallel runs, filter by payload.url === failingServer.url (each spec file's OS-allocated port is unique) rather than static jobId markers; parallel-safe across all 3 reporting specs
- [Phase 19]: Plan 19-04: pg-boss DLQ transfer is INLINE with the failJobsById CTE (plans.js:1155-1168), NOT deferred to supervise() maintenance loop — retryLimit exhaustion on the final attempt synchronously triggers the dlq_jobs insert; maintenanceIntervalSeconds:1 only optimises separate delete/retention loops
- [Phase 19]: Plan 19-04: plugin-order.spec additive-inside-existing-it-block pattern (Phase 18 Plan 18-03 precedent reaffirmed) — 4 new expects inside 'registers substrate plugins before application plugins' it-block vs creating a new describe/it-block; avoids ~6s re-boot of buildApp() that a new block would trigger
- [Phase 19]: Plan 19-05: registerReportingRoutes(fastify) is a plain async function called directly from the reporting plugin body (NOT fastify-plugin wrapper) — matches hooks plugin pattern; module barrel (plan 19-06) stays clean with only createReportingModule as the allowed internal/ re-export per MOD-02
- [Phase 19]: Plan 19-05: DLQ route projection hoists correlation_id from envelope.data.correlationId to top-level (snake_case); flat shape matches CONTEXT.md §Specifics verbatim (id/queue/state/retrycount/data/output/createdon/correlation_id) — operators get one-pass visibility without drilling into the envelope
- [Phase 19]: Plan 19-05 [Rule 3 deviation]: Fastify test harnesses that register reportingPlugin must install fastify-zod-openapi validatorCompiler+serializerCompiler+plugin at root scope BEFORE plugin register — else Ajv default serializer chokes on Zod-emitted required arrays (FST_ERR_SCH_VALIDATION_BUILD). Applied to dlq-route.spec (new) + queue/correlation/terminal-event specs (plan 19-04 unblocking). Mirrors server/index.ts:94-96 (plan 17-01).
- [Phase 19]: Plan 19-06: MODULE.md ships 9 fixed H2 sections + H3 Runnable Example, 114 lines / 1291 words. Richer surface than hooks (96 lines) / lifecycle (104 lines) due to reporting having 4 events + 2 queues + DLQ route + WebhookService + FlakyDetector. 6 invariants (a)-(f) vs hooks (5) / lifecycle (5); added (e) DLQ endpoint shape + (f) shutdown idempotency. Third MODULE.md instance strongly validates the 9-section canonical template for remaining v3.0 modules (Phase 20-29).
- [Phase 19]: Plan 19-06: Public barrel adopts Phase 18 lifecycle 1-line internal re-export form via inline type modifier — `export { createReportingModule, type ReportingModule, type CreateReportingModuleDeps } from './internal/module.js';`. Phase 16 hooks remains 2-line form; Phase 30 repo-wide cleanup may tighten hooks retroactively. MOD-02 structural invariant `grep -c "from './internal/" = 1` passes. Barrel does NOT export registerReportingRoutes (plugin-internal, matches hooks) and does NOT use `export *` (ADR-002 / MOD-02 named-exports-only).
- [Phase 19]: Plan 19-06: Nyquist baseline preservation verified via pre/post `diff /tmp/nyquist-baseline-19-06.bak .planning/nyquist-baseline.json` (both empty — file unchanged across capture run). Baseline remains frozen at Phase 15 substrate (commit `55ff8ac`, 48.29% lines). Phase 19 delta +8.19pp (current 56.48% lines) — strong headroom for Phase 20+.
- [Phase 19]: Plan 19-06: `git mv` rename preserves blame history via Git's rename detection — `git log --follow server/reporting/__tests__/flaky-detector.spec.ts` reaches back to commit `915b970` (Phase 06 original authorship from 2024). MOD-04 file-naming layer complete for server/reporting/__tests__/; describe-tree alignment (tests-as-spec) deferred to Phase 30 per Phase 18 Plan 18-04 precedent.
- [Phase 19]: Plan 19-06: Phase 19 closed with identical pre-existing-failure envelope as Phase 18 Plan 18-04 — fastify-zod-openapi v5 `required` emission bug still present (31 test failures + 1 spec hang in 4 fixture files: routes.test, artifact-routes.test, auth-plugin.test, plugin-order.spec). Third consecutive phase (17 → 18 → 19) inheriting this; recommended pre-Phase-20 standalone hotfix (17-09 or Phase 20 Wave 0 pre-work) — options: (a) upstream patch, (b) Ajv required-normalisation shim in setValidatorCompiler, (c) pin fastify-zod-openapi back to v4 losing Zod 4 native JSON Schema emit.
- [Phase 20-pool-module-devices]: Plan 20-00: Wave-0 substrate for pool module — QUEUE_NAMES extended with DEVICE_BOOT + DEVICE_REAP (9 entries total); 7 pool/ stubs created (events.ts, queue.ts, internal/module.ts, MODULE.md, index.ts, __tests__/events.spec.ts) + fixture; dep-cruiser gains 4th forbidden rule no-deep-imports-into-pool-internal mirroring hooks/lifecycle/reporting. Zero runtime changes to pool-manager.ts / device.ts / health-checker.ts / process-tracker.ts / plugin.ts (ship in Plans 20-02 + 20-03). Alphabetical QUEUE_NAMES ordering preserved (DEVICE_* between DEMO and HOOK_RUN).
- [Phase 20-pool-module-devices]: Plan 20-00: POOL_AGGREGATE_ID ships as placeholder UUID '00000000-0000-5000-8000-000000000020' per plan directive — Plan 20-01 replaces with uuidv5('pool', NAMESPACE_URL). Matches Phase 18 LIFECYCLE_AGGREGATE_ID / Phase 19 REPORTING_AGGREGATE_ID v5 derivation pattern. server/pool/index.ts barrel adopts Phase 18/19 1-line internal/ re-export form (tighter MOD-02 invariant).
- [Phase 20-pool-module-devices]: Plan 20-00: 4-line throw-stub at server/pool/internal/module.ts required per Phase 18/19 empirical finding reaffirmed — depcruise 17.3.10 silently drops unresolvable imports before rule-matching (1 module, 0 dependencies cruised). Plan 20-03 overwrites stub with real createPoolModule factory. Pre-existing dep-check violation (jobs/plugin.ts → bus/bus.ts, Phase 23 scope) left untouched per SCOPE BOUNDARY rule; confirmed pre-existing via git stash round-trip.
- [Phase 20-pool-module-devices]: Plan 20-01: POOL_AGGREGATE_ID = '2a120cd5-4bd3-5f65-a9e5-870ec709e44a' (v5 UUID of 'pool' under URL namespace '6ba7b811-9dad-11d1-80b4-00c04fd430c8'). Plan's literal-hint '9fabf52b-...' was incorrect (Rule 1 Bug auto-fix); spec re-derives at test time — runtime literal MUST match. Matches Phase 18 LIFECYCLE derivation exactly.
- [Phase 20-pool-module-devices]: Plan 20-01: TRACE-08 persistence policy — device.health.failed persisted:true (ONLY persisted pool event); state.changed/allocated/released persisted:false. High-freq telemetry derivable from state.changed alone; events-table bloat unacceptable. health.failed low-freq operational telemetry feeds Phase 27 trace-tree + Phase 23 jobs keystone debug paths.
- [Phase 20-pool-module-devices]: Plan 20-01: aggregateId=deviceId (per-device) on all 4 pool events, NOT POOL_AGGREGATE_ID. Diverges from Phase 18/19 singleton-aggregate pattern because pool events are inherently per-device — envelope.aggregateId encodes device identity directly. POOL_AGGREGATE_ID reserved for future pool-wide events (pool.initialized/drained) with no specific device.
- [Phase 20-pool-module-devices]: Plan 20-01: Plain-object ALS store shape in events.spec.ts (CONTEXT §Specifics canonical Phase 20 form) — asyncLocalStorage.run({correlationId, currentEventId, actor} as never, ...) instead of legacy Map form used in Phase 15/18/19 specs. Shape-agnostic readAls() handles both; pivot is stylistic go-forward alignment.
- [Phase 20-pool-module-devices]: Plan 20-02: 4th-param optional emit with NOOP_POOL_EMIT default in PoolManager + HealthChecker constructors — 2-phase wiring pattern lets Plan 20-03 replace NOOP with real makePoolEmitters(bus, persistEnvelope) without constructor change; dep-graph invariant preserved (no plan leaves tree uncompilable mid-wave).
- [Phase 20-pool-module-devices]: Plan 20-02: Device.transition() returns {from, to} for caller-side emit; local 'stateChange' EE event preserved. Device stays below plugin layer (no bus coupling) — PoolManager/HealthChecker own the emit calls with {from, to} captured from transition() return value AFTER mutation success.
- [Phase 20-pool-module-devices]: Plan 20-02: device.health.failed emits BEFORE state transition per RESEARCH §Pitfall 2 (unconditional on every failed probe) while device.state.changed emits AFTER state transition success. HealthChecker reason enum (unhealthy/zombie/max-retries/timeout) collapses to 3 emit call sites via ternary sharing for timeout↔unhealthy.
- [Phase 20-pool-module-devices]: Plan 20-02: PoolManager.release() captures jobId = device.currentJobId BEFORE device.release() clears it — emit.released envelope preserves job-link for Phase 27 trace-tree + Phase 23 jobs keystone correlation.
- [Phase 20]: [Phase 20] Plan 20-03: createPoolModule factory (MOD-06) ships with per-module TypedBus<PoolRegistry> + persistEnvelope (5th sample point — Phase 27+ consolidation trigger) + makePoolEmitters; constructs PoolManager + HealthChecker with REAL emit replacing Plan 20-02's NOOP_POOL_EMIT default; 6-key return {pool, healthChecker, emit, bus, registerWorkersAndSubscribers, shutdown}; idempotent shutdown via stopped-flag calls healthChecker.stop + boss.offWork per workerId but does NOT call pool.shutdown (deferred per RESEARCH §Open Question 1).
- [Phase 20]: [Phase 20] Plan 20-03: Pool plugin rewritten as thin wirer (48→82 lines) with 4 decorators (pool + processTracker + healthChecker + poolModule); dependencies extended ['config'] → ['config', 'db', 'queue', 'event-bus']; plugin NAME 'pool-plugin' preserved for 9+ downstream dep strings; onClose → module.shutdown() idempotent.
- [Phase 20]: [Phase 20] Plan 20-03: SC2 achieved — server/index.ts lines 209-215 + 245-251 DELETED (8 lines total); grep-guard verified 0 matches for app.healthChecker.start/stop + startReaper + 4 deleted log strings; shutdown step labels rewritten b→f. Pool plugin's onClose + module.registerWorkersAndSubscribers own both healthChecker setInterval AND reaper pg-boss schedule lifecycles.
- [Phase 20]: [Phase 20] Plan 20-03: server/pool/queue.ts registerPoolQueues factory performs canonical pg-boss 3-step for DEVICE_REAP ONLY (createQueue {policy:'stately', retryLimit:1, retryBackoff:true, retryDelay:30} + schedule '* * * * *' {singletonKey: DEVICE_REAP} + work handler calling processTracker.reapOrphans()); DEVICE_BOOT is name-only (NOT registered — Phase 23 jobs keystone owns consumer).
- [Phase 20]: [Phase 20] Plan 20-03: ProcessTracker.startReaper + reaperInterval REMOVED (body migrated to queue.ts worker); stop() retained as documented no-op for back-compat with PoolManager.shutdown's historical call sequence (Phase-20-scope-preserving vs. deleting). Rule 3 auto-fix: 2 reaper tests in process-tracker.test.ts removed inline (TS2339 blocking issue on removed startReaper API); coverage migrated to module.spec.ts + lifecycle-ownership.spec.ts.
- [Phase 20]: Plan 20-05: MODULE.md + barrel + renames — Phase-close template stabilised across hooks(16-04)/lifecycle(18-04)/reporting(19-06)/pool(20-05). 3-task shape: (a) MODULE.md with 9 fixed H2 sections + H3 Runnable Example; (b) index.ts barrel with MOD-02 strict 1-line internal/ re-export via inline type modifier; (c) git-mv .test.ts -> .spec.ts closing MOD-04 for module directory. 4min execution — fastest yet (hooks 6min, lifecycle 54min, reporting 7min, pool 4min).
- [Phase 20]: Plan 20-05: MODULE.md Events Consumed section as publisher-only subscriber catalog. Pool in Phase 20 is publisher-only (no subscribers) but §Events Consumed is STILL present with explicit '(None in Phase 20 — pool is a publisher-only module)' line + 4-phase forward-reference bullet list (21 artifacts on state.changed filter running->cleanup; 23 jobs on allocated/released; 24 maestro on state.changed filter booting->idle; 27 api on health.failed persisted). Future Phase 21+ planners grep this section to find subscribe contract before writing subscribers — LLM-parseable input for downstream planning.
- [Phase 20]: Plan 20-05: Inline type-modifier 1-line form for MOD-02 internal/ re-export adopted across Phase 18/19/20. Single ES2015 statement emits runtime binding createPoolModule + 2 type aliases via inline type prefix modifier. Grep invariant 'count of from internal/ == 1' enforces single documented module entry point (factory). Phase 16 hooks uses 2-line form (deferred migration to 1-line in Phase 27+ consolidation); all Phase 17+ modules use stricter 1-line form.
- [Phase 20-pool-module-devices]: Plan 20-04: Rule 1 auto-fix — test 5 in subscriber.spec subscribes to pool's own bus ee side-channel via (poolModule.bus as unknown as {ee}).ee.on, NOT fastify.onPersisted. Plan assumption was architecturally wrong: fastify.onPersisted binds to bus-plugin's separate demoRegistry ee; per-module persistEnvelope fires <type>.envelope on poolModule.bus's own ee. Same mechanism, correct ee. Preserves per-module bus isolation (MOD-02 barrel contract); Phase 27+ consolidation may unify ee's or extend fastify.onPersisted to be per-module-aware
- [Phase 20-pool-module-devices]: Plan 20-04: DB-gated spec pair template established — subscriber.spec + correlation.spec per module. subscriber.spec proves all N events reach consumers via module.bus.on + side-channel for persisted events. correlation.spec proves ALS→envelope→events-table row with aggregateType+aggregateId invariants + TRACE-08 negative (non-persisted events do NOT create rows) + TRACE-04 outside-ALS fallback. Canonical plain-object ALS shape enforced (grep count of 'new Map([[' = 0 in active code). Phase 21 artifacts/22 streaming/23 jobs will follow this 2-spec-file-per-module pattern
- [Phase 20-pool-module-devices]: Plan 20-04: Architectural gap documented (Phase 27+ consolidation trigger) — fastify.onPersisted from server/bus/plugin.ts:120-141 binds to bus-plugin's own demoRegistry TypedBus ee; per-module persistEnvelope fires <type>.envelope on the module's OWN bus ee. 5 sample sites now (bus + hooks + lifecycle + reporting + pool) — exceeds 4-sample consolidation trigger noted in 20-03. Phase 27+ PR options: (1) refactor into shared makePersistEnvelope<R>(deps) generic; (2) unify ee's via shared-ee-across-modules primitive; (3) extend fastify.onPersisted to be per-module-aware via internal <type>→<owning-module-bus-ee> registry
- [Phase 20-pool-module-devices]: Plan 20-06: Phase 20 closed with identical pre-existing-failure envelope as Phase 18/19 — fastify-zod-openapi v5 `required` emission bug still present (4 fixture files: routes.test, artifact-routes.test, auth-plugin.test, plugin-order.spec) + pool-plugin substring-match bug in plugin-order.spec. Fourth consecutive phase inheriting; recommended pre-Phase-21 standalone hotfix (17-09 or Phase 21 Wave 0 pre-work). Phase 20 deferred-items.md Section A inherits verbatim; Section B adds 8 Phase 20 intentional deferrals (device.booted → 24, pool.shutdown migration → 23, hot-plug → unchartered, device.* webhook fan-out → v4.0+, persistEnvelope consolidation → 27+, deep-import rewiring → 21/23/24, device.boot queue registration → 23, android/ios driver refactor → unchartered) matching MODULE.md §Non-Goals verbatim.
- [Phase 20-pool-module-devices]: Plan 20-06: Nyquist delta -0.30pp (current 47.99% lines vs Phase 15 baseline 48.29%). Breaks the Phase 18/19 +7-to-+8pp-delta trend because pool is the first LARGE module migrated (vs lifecycle/reporting being narrow) — new events.ts + queue.ts + internal/module.ts + subscriber.spec + correlation.spec add lines to BOTH numerator + denominator, net near-zero delta. Still well within -2pp budget. Baseline file preserved via diff against /tmp/p20-06-nyquist-baseline-backup.json (files-identical) — still Phase 15 commit 55ff8ac 48.29% lines. Capture command replicates Phase 18/19 exclusion pattern: CONTRACTS_CHECK_SPEC=skip npx vitest run --coverage --exclude <4 inherited files>.
- [Phase 20-pool-module-devices]: Plan 20-06: plugin-order.spec additive pool-plugin dep-graph block — 3 positional invariants (indexOf('queue|event-bus|db') < indexOf('pool-plugin')) + 1 readFileSync structural assertion reading pool/plugin.ts's dependencies literal regex /dependencies:\s*\[\s*'config'\s*,\s*'db'\s*,\s*'queue'\s*,\s*'event-bus'\s*\]/ + 1 arrayContaining literal-array assertion (single-line form satisfies plan's grep-friendly verify regex `expect.*'config'.*'db'.*'queue'.*'event-bus'`). Phase 17/18/19 assertions byte-identical — additive only. File remains in the inherited CI exclusion set via the pre-existing plugin-order.spec substring-match bug; new block reads green once Phase 27+ fixes it.
- [Phase 20-pool-module-devices]: Plan 20-06: STATE.md progress.completed_phases 5→6, progress.completed_plans 42→43 (+1 for 20-06 only — 20-00..20-05 already counted in 42). ROADMAP.md Phase 20 checkbox [ ] → [x] + completion date 2026-04-21 + all 7 sub-plans marked [x] with brief objective annotations; Progress table row updated to 7/7 | Complete | 2026-04-21. Phase 21 Artifacts Module is next queued phase.
- [Phase 21-artifacts-module]: Plan 21-00: RECORDING_UPLOAD queue-name added to QUEUE_NAMES (10 entries); on-demand queue (no schedule call, policy:'stately' + retryLimit:3 + singletonKey:recordingId for SC3 idempotency)
- [Phase 21-artifacts-module]: Plan 21-00: ARTIFACTS_AGGREGATE_ID placeholder '00000000-0000-5000-8000-000000000021' landed as stub; Plan 21-02 replaces with real v5('artifacts', URL_NAMESPACE) derivation + test-time assertion (mirrors Phase 18/19/20 empirical sequencing)
- [Phase 21-artifacts-module]: Plan 21-00: 5th dep-cruiser module rule no-deep-imports-into-artifacts-internal landed (6 forbidden rules total); throw-stub at server/artifacts/internal/module.ts REQUIRED per Phase 18/19/20 empirical (depcruise 17.x silently drops unresolvable imports; rule cannot fire without resolvable target)
- [Phase 21]: ARTIFACTS_AGGREGATE_ID v5 = 0bfe29be-7bd1-5f23-ae17-dd867fd062b5 (derived via uuidv5('artifacts', URL_NAMESPACE)); hardcoded literal with spec re-derivation assertion
- [Phase 21]: TRACE-08 persistence: only artifact.created=true; recording.started/stopped + job.started + maestro.log.written all persisted:false (transient; derivable from subsequent persisted rows)
- [Phase 21]: jobs/events.ts extension kept as bridgehead (matches Phase 19 Plan 19-01 precedent) — no MODULE.md, barrel, or queue.ts; Phase 23 Jobs Keystone owns full saga
- [Phase 21-artifacts-module]: Plan 21-01: Nullable UUID + partial-unique-index on artifacts.recording_id (PostgreSQL multi-NULL, non-NULL unique semantics) chosen over separate recordings table or composite (jobId, recordingId) — matches SC3 DB-layer exactly; Phase 22+ can normalize if needed
- [Phase 21-artifacts-module]: Plan 21-01: createArtifactIdempotent signature uses intersection type CreateArtifactOpts & {recordingId: string} to enforce TS compile-error when callers forget the idempotency key
- [Phase 21-artifacts-module]: Plan 21-01: RecordingService.getRecordingMethod(jobId) added as read-only instanceof-based discriminator rather than mutating startRecording to return the method string — preserves void contract (RESEARCH §Pitfall 8)
- [Phase 21-artifacts-module]: Plan 21-03 Wave 2 SC3 queue-layer idempotency shipped: recording.upload pg-boss worker ({policy:'stately', retryLimit:3, retryBackoff:true, retryDelay:5}, NO DLQ, NO schedule) + recordingUploadPayloadSchema (8 Zod fields) + DB-gated 3-test queue.spec (10.56s; pgboss_artifacts_upload_<rand> schema; plain-object ALS; collapsed backoffs); worker body parse->createArtifactIdempotent->(emit artifact.created OR warn 'SC3 idempotency'); 4 deviations (3 plan bugs: jobs.status='completed'->'passed', ArtifactService convoluted cast->'as never', offWork(id) single-arg->offWork(name,{id}); 1 test relaxation: [SC3 DB fallback] accepts stately still-blocks OR DB-catches-null-return — invariant is rows.length===1); 2 commits cc15329+8418575; ready for 21-04 subscriber wiring
- [Phase 21-artifacts-module]: Plan 21-05: DB-gated SC1 + SC4 + TRACE-04 + TRACE-09 proofs shipped via 3 specs (subscriber.spec 3 tests, correlation.spec 1 test, lifecycle-ownership.spec 10 tests); all 14 tests green; stub-jobs-plugin inline fp() factory template established for module-independence tests
- [Phase 21-artifacts-module]: Plan 21-05: boss.findJobs is the canonical API over boss.fetch (pg-boss v12 fetch signature changed to {includeMetadata} options object; findJobs returns all-states rows ideal for verification without worker-race conditions)
- [Phase 21-artifacts-module]: Plan 21-05: Stub-jobs-plugin inline fp() factory template (~25 LOC; plugin NAME 'job-plugin', dependencies ['db','event-bus'], decorates fastify.jobsModule with TypedBus + emitters + persistEnvelope side-channel) — reusable template until Phase 23 Jobs Keystone rewrite
- [Phase 22-streaming-module]: Plan 22-00: STREAMING_AGGREGATE_ID ships as obviously-bogus placeholder UUID '00000000-0000-5000-8000-000000000022' — Plan 22-01 replaces with real v5 = 'fff0592e-b92c-5221-a40a-d10a141f0158' derived from uuidv5('streaming', URL_NAMESPACE); streamingRegistry empty {} as const satisfies EventRegistry until Plan 22-01 adds 1-entry body
- [Phase 22-streaming-module]: Plan 22-00: JOB_EVENT_NAMES extended from 3 to 6 keys (LOG/STEP/STATUS placeholders) — jobsRegistry and makeJobsEmitters UNCHANGED until Plan 22-01 lands payload schemas + registry entries + emit helpers (Phase 21 Plan 21-02 bridgehead-extension precedent)
- [Phase 22-streaming-module]: Plan 22-00: No queue scaffolding for streaming module (CONTEXT §Decisions explicit no-queue deviation from Phase 16-21 template); server/queue/names.ts NOT touched in any Phase 22 plan; MODULE.md §Queue Produced/Consumed will state 'None'; WebSocket fan-out is in-process only
- [Phase 22-streaming-module]: Plan 22-00: dep-cruiser 6th rule no-deep-imports-into-streaming-internal lands alongside throw-stub + fixture + 6th it-block; structurally enforces MOD-02 boundary BEFORE Plan 22-02 creates real internal/ content (depcruise 17.x empirical — unresolvable imports silently dropped; 5th repeat of Phase 18/19/20/21 pattern)
- [Phase 22-streaming-module]: Plan 22-00: Rule 1 auto-fix — MOD-02 grep-count collision in index.ts file-header comment. Original plan text included literal 'from ./internal/module.js' string inside backticks which would trip the == 1 grep invariant. Rephrased comment to preserve intent without literal path string; Plan 22-05 verify script grep stays clean
- [Phase 22]: Plan 22-01: ws.frame.dropped stays persisted:false per TRACE-08 (programmer-error signal; structured log sufficient; events-table bloat unjustified)
- [Phase 22]: Plan 22-01: STREAMING_AGGREGATE_ID = fff0592e-b92c-5221-a40a-d10a141f0158 v5 from uuidv5('streaming', URL_NAMESPACE); reserved for future streaming-wide telemetry; ws.frame.dropped uses jobId as aggregateId
- [Phase 22]: Plan 22-01: jobStatusPayload.status (5 values incl. 'running') intentionally wider than jobCompletedPayload.status (4 terminal values) — do NOT unify; streaming signal vs persisted terminal fact (Phase 23 scope)
- [Phase 22]: Plan 22-01: wsEnvelopeSchema tightened strict (v:literal(1) required, correlationId uuid, ts datetime, payload unknown; .loose() removed) — TRACE-06 contract locked for Plan 22-02 subscriber
- [Phase 22-streaming-module]: Stub plugins (config/auth/pool/jobs) duplicated across DB-gated streaming specs (subscriber/correlation) per Phase 21 artifacts precedent — Phase 27+ consolidation trigger if 4th sample point appears
- [Phase 22-streaming-module]: afterEach (not beforeAll) lifecycle in DB-gated streaming specs with per-test pgboss_stream_<rand6> schema isolation — each test gets fresh JobBroadcaster + fresh subscriber wiring for independent envelope-count assertions
- [Phase 22]: Plan 22-04: SC2 structurally locked at CI-time via 4-test non-DB grep-guard (lifecycle-ownership.spec.ts) + plugin-order.spec extended with Phase 22 5-entry websocket-plugin deps + wbIndex word-boundary helper fixing Phase 20 Plan 20-04 substring-bug (indexOf('websocket-plugin') matched inside '@fastify/websocket')
- [Phase 22-streaming-module]: Plan 22-05: MOD-02 multi-re-export barrel — Phase 22 has no top-level service files (all surfaces under internal/); dep-cruiser rule fires only on outside-the-module deep imports, barrel-internal re-exports are within module scope
- [Phase 22-streaming-module]: Plan 22-05: Nyquist baseline preserved (Phase 15 commit 55ff8ac, lines=48.29%); current coverage 51.30%, delta +3.01pp well within -2pp gate; baseline file unmodified
- [Phase 22-streaming-module]: Plan 22-05: Runnable Example placed as H3 inside MODULE.md keeps the 9 H2 canonical count clean; reserves H3 for Phase 27 MOD-09 snippet typecheck extension
- [Phase 22-streaming-module]: Phase 22 deferred-items.md catalog: 6 Phase 22-specific (DEFERRED-22-A..F: DevicePreview envelope→29; shared wsEnvelopeSchema→29; ring-buffer cap→out of scope; SC2 cleanup→23; persistEnvelope consolidation→27+; type imports→23) + 2 inherited (Phase 17 fastify-zod-openapi v5 bug; Phase 15 Map-vs-RequestContext)
- [Phase 22-streaming-module]: Phase 22 close metrics: full suite 644 passed/70 skipped/0 failed; lint clean; tsc 8 baseline errors (zero new); dep-check 7 pre-existing violations (consumer migration is downstream-plan scope); Nyquist delta +3.01pp; .planning/nyquist-baseline.json unmodified
- [Phase 23-jobs-module-keystone]: Plan 23-00: dep-cruiser rule 7 uses pathNot only (no path:'^server/' filter) to mirror the existing 6 module rules' shape — plan literal had from.path:'^server/' which would exclude the __fixtures__/ fixture from rule scope (Rule 1 auto-fix).
- [Phase 23-jobs-module-keystone]: Plan 23-00: drizzle-kit emitted migration filename 0003_wise_aqueduct.sql (drizzle owns auto-naming; renaming would break _journal.json metadata).
- [Phase 23]: Plan 23-01: jobsRegistry extended 6->11 with TRACE-08 flags (4 persisted, 7 transient); 5 new payload schemas (allocated/running/recordingRequested/cleanupRequested/failed); makeJobsEmitters returns 11 typed helpers
- [Phase 23]: Plan 23-02: jobs/queue.ts uses policy:'stately' + retryLimit:0; named PgBoss import + Job<T>[] batch annotation deviated from plan to satisfy pg-boss v12 types
- [Phase 23]: 23-03 inlined Job schema directly into openapi.json (fallback path) rather than wiring jobResponseSchema into GET /jobs/:id — that route's handler is owned by Plan 23-04 saga rewrite
- [Phase 23]: 23-03 jobStatusEnum + jobStatusSchema both gain 'allocated' (saga state) — Drizzle migration 0004_stiff_vampiro.sql
- [Phase 23]: 23-03 Go cross-tier test (g) → DEFERRED-23-C (no TestStatusDeviceName in cli/cmd/ yet); test warns + passes; Phase 28 ships it
- [Phase 23]: Plan 23-04 keystone: createJobsModule factory + saga subscriber chain + JobQueue deletion + DEFERRED-21 cleared (jobs/plugin.ts no longer imports bus/bus.ts)
- [Phase 23]: Option B chosen for job-service.ts: 161-line back-compat shim preserved (createJob/cancelJob/getQueueDepth/shutdown surface) instead of deletion. Routes + pipelines + route-tests all use fastify.jobService.*; full deletion deferred to Phase 24+
- [Phase 23]: DEFERRED-22-D resolved: streaming subscribes to job.cleanup.requested replacing setTimeout(broadcaster.cleanup) pattern. DEFERRED-22-F partially resolved: job-service.ts no longer imports streaming/internal. dep-check 7 -> 3 violations (Phase 23-targeted ones cleared; 3 residual artifacts->streaming pre-existing)
- [Phase 23]: Plan 23-05 split JobsModule public API: registerWorkerOnly (worker-only, post-ready safe) + registerWorkerAndSubscribers (full boot path with addHook)
- [Phase 23]: Plan 23-05 system.drain.* events use aggregateType:'system' (not 'job') per DEFERRED-23-B; Phase 27+ may extract to system module
- [Phase 23-jobs-module-keystone]: Comment-stripping in lifecycle-ownership.spec — JSDoc references to anti-patterns must be excluded from grep-guard counts (Rule 1 auto-fix; executor.ts:6 documents .catch(() => {}) as a pattern NOT used)
- [Phase 23-jobs-module-keystone]: subscriber.spec exercises saga subscriber chain via direct emit (jobsModule.emit + poolModule.emit), not via runJob worker handler — full-stack proof gated on PHASE23_FULL_STACK_TEST=1 lives in idempotency.spec second describe block (Plan 23-04)
- [Phase 23-jobs-module-keystone]: correlation.spec asserts persisted rows only — job.allocated/job.running are persisted=false so cid threading is testable only on job.completed + job.cleanup.requested + job.failed (which ARE persisted in jobsRegistry per Plan 23-01)
- [Phase 24]: POOL_EVENT_NAMES.BOOTED added in Wave 0 as constant only; payload schema/registry entry/helper deferred to Plan 24-01 (placeholder-first)
- [Phase 24]: MAESTRO_AGGREGATE_ID seeded with bogus literal; Plan 24-01 must replace with v5 UUID ceb331df-a288-5be5-b801-cbdfc4deec4a
- [Phase 24]: Pool events.spec EVENTS-03 regex broadened to [a-z-]+ middle segments to permit maestro.device-info.collected-style names
- [Phase 24]: Both maestro events transient (TRACE-08): hierarchy.fetched + device-info.collected fire high-frequency, derivable from logs/cache state
- [Phase 24]: device.booted persisted:false — derivable from device.state.changed Booting→Idle; thin payload {deviceId, platform, emulatorId, port?} matches device.allocated pattern
- [Phase 24]: MAESTRO_AGGREGATE_ID = ceb331df-a288-5be5-b801-cbdfc4deec4a (real v5 from uuidv5('maestro', URL_NS)) replaces grep-flagged Plan 24-00 placeholder
- [Phase 24]: Plan 24-02: 4 device.booted emissions wired in pool-manager.ts at Booting→Idle transitions (addDevice line 67, initPool inner loop line 140, detectPhysicalDevices line 231, replaceDevice line 399), each AFTER existing emit.stateChanged per Phase 20 Pitfall 2. HealthChecker recovery path (Error→Idle) untouched — fresh-boot semantics reserved for the 4 PoolManager sites; recovery uses health.failed + state.changed channels. subscriber.spec extended with 2 new [Phase 24] tests proving (a) booted fires AFTER state.changed at addDevice flow with payload conforming to deviceBootedPayload schema, (b) HealthChecker recovery emission sequence emits ZERO device.booted (negative-space invariant). 7/7 subscriber.spec tests pass; 6 pre-existing addHook-mock failures inherited from Plan 24-01 documented as out-of-scope. SC3h end-to-end demonstrable; Plan 24-03 maestro factory can now subscribe via fastify.poolModule.bus.on('device.booted', ...).
- [Phase 24]: createMaestroModule factory shape: 7 keys (appiumService + hierarchyService + deviceInfoCollector + emit + bus + registerSubscribers + shutdown). Mirror Phase 22 streaming (no queue surface). 8th persistEnvelope sample point — DEFERRED-24-B kept open.
- [Phase 24]: device.booted cross-module subscriber DEFERRED to fastify.addHook('onReady', ...) per Pitfall 2 — pool decorates poolModule.bus at step 8; maestro plugin runs at step 14; deferral keeps wiring plugin-order agnostic.
- [Phase 24]: DeviceInfoCollector relocated server/pool/ -> server/maestro/internal/ via git mv 100% similarity. Pool barrel + MODULE.md DeviceInfoCollector references DELETED. Three back-compat decorators (hierarchyService/appiumService/deviceInfoCollector) preserved alongside new fastify.maestroModule per Phase 22/23 convention.
- [Phase 24]: hookExecutor.execute('device.booted',...) at server/index.ts:175 EXPLICITLY PRESERVED — different surface than maestro bus subscription (hooks-side vs bus-side). Verified zero references to deviceInfoCollector remain in server/index.ts.
- [Phase 24-maestro-module]: Plan 24-04 — TRACE-09 causation thread cross-module DEFERRED to Phase 27+ (DEFERRED-24-D); maestro subscribes via TypedBus.on (NOT fastify.onPersisted) so currentEventId is not auto-injected by bus plugin's subscriber wrapper. correlation.spec covers correlationId thread + ALS isolation only.
- [Phase 24-maestro-module]: Plan 24-04 — lifecycle-ownership.spec test (b) scoped to plugin.ts only (NOT all of server/maestro/) — server/maestro/routes.ts legitimately calls deviceInfoCollector.collect for HTTP route handlers; SC1 invariant is specifically that the imperative onReady loop in plugin.ts is gone, not that all collect calls disappear.
- [Phase 25]: Phase 25 Wave 0 substrate: 5 PIPELINE_EVENT_NAMES locked (run.started/stage.advanced/run.completed/run.failed/schedule.upserted); QUEUE_NAMES.PIPELINE_SCHEDULED_EXECUTE='pipeline.scheduled.execute' inserted alphabetically (12 entries); dep-cruiser 9th rule no-deep-imports-into-pipelines-internal added with fixture firing; MOD-02 strict 1-line barrel + throw-stub at internal/module.ts; runtime files in server/pipelines/ and package.json untouched (Wave 0 = scaffolding only)
- [Phase 25]: PIPELINE_RUN_AGGREGATE_ID derived at runtime via uuidv5('pipeline-run', URL_NS); spec re-derives for single-source-of-truth (Phase 20 pattern)
- [Phase 25]: schedule.upserted aggregateType='pipeline-schedule' (distinct from pipeline-run aggregate); enables schedule-scoped trace queries
- [Phase 25]: policy:'standard' retryLimit:0 on pipeline.scheduled.execute queue (independent runs; manual re-trigger preferred over re-firing same schedule slot)
- [Phase 25]: Overloaded constructor (legacy 3-arg + new deps-object) keeps plugin.ts compiling during Wave-2 → Wave-3 transition
- [Phase 25]: Synchronous reconcile during onReady — sub-second with <100 pipelines per RESEARCH OQ2
- [Phase 25]: BEHAVIOUR CHANGE: invalid cron now throws at boss.schedule call time (was silent log+skip in legacy node-cron impl)
- [Phase 25]: Plan 25-03: Tasks 3.2 + 3.4 committed atomically (typed cycle); 9th persistEnvelope sample point reached (DEFERRED-25-A); node-cron + @types/node-cron dropped — pg-boss is sole scheduler
- [Phase 25]: Plan 25-04: Test (b) matrix N-of-N gate seeds DB directly to sidestep pre-existing pipeline-schema.ts:17 z.record(z.unknown()) Zod v4 incompat (production code edits forbidden in this plan); same subscriber code path exercised verbatim.
- [Phase 25]: Plan 25-04: Maestro stage flowsDir uses RELATIVE path under process.cwd() because fanoutMaestroStage builds workDir+/+flows; absolute paths yield malformed concat.
- [Phase 25]: PATH B taken for integration.spec.ts: it.skip with TODO pointing at DEFERRED-25-H Phase 30 owner; subscriber.spec already proves bus flow
- [Phase 25]: 9TH persistEnvelope sample point — Phase 27+ owns consolidation (DEFERRED-25-A supersedes DEFERRED-24-B)
- [Phase 25]: DEFERRED-25-C cancellation kept IN-PHASE via runningRuns Map + DB pipelineRuns.status check
- [Phase 26]: Migration filename: kept drizzle-kit auto-numbered '0005_api_keys_claims.sql' (Rule 1 deviation from plan-literal '0026_*' due to drizzle journal sequential-idx requirement)
- [Phase 26]: actorSchema regex shipped at substrate level (Wave 0) so Plan 26-02 grep-guards can import actorSchema literal directly
- [Phase 26-auth-module]: Plan 26-01: makeAuthEmitters uses positional createEventHelpers(bus, onEmit?) signature matching maestro/pipelines (NOT object-shape from pseudocode)
- [Phase 26-auth-module]: Plan 26-01: TRACE-08 counter-pattern locked — auth.key.* both persisted:true (security audit) vs maestro both persisted:false (transient telemetry)
- [Phase 26-auth-module]: Plan 26-01: AUTH_AGGREGATE_ID = 91f9a43e-7eee-5f34-bd7d-e1dd793eb989 (uuidv5('auth', URL_NS)); test re-derives at test time (single-source-of-truth)
- [Phase 26-auth-module]: Plan 26-02: bus helpers default actor 'anonymous' -> 'system' migrated; server/index.ts onReady wrapped in asyncLocalStorage.run({correlationId, actor:'system'}); 5 grep-guards in server/auth/__tests__/lifecycle-ownership.spec.ts lock TRACE-10 invariants across helpers.ts + index.ts + queue/plugin.ts. Test 5 allowlists 2 substrate carriers (correlation/plugin.ts + auth/internal/actor.ts JSDoc). Plan 26-03 unblocked for HTTP bearer-auth callback (entry point #4); Plan 26-04 must rebase 2 red-at-next-run tests (emit-helpers.spec.ts:63 + pool/correlation.spec.ts:194-195) asserting legacy 'anonymous' default.
- [Phase 31]: Phase 31 Wave 0 RED scaffold: 17 failing test/fixture files committed across server/cli/web locking Wave 1 interfaces for parseLogcatLine, FlushQueue, bootOptionsSchema, updates.Check, ui.RenderUpdateBanner, streaming.UnwrapBatch
- [Phase 31]: DEVICE_FARM_UPDATE_URL env override pinned as test seam for GitHub Releases poll — Wave 1 plan 31-04 must honor it; GitHub repo string literal 'HeiCg/device-farm' pinned per 31-RESEARCH Open Question 1
- [Phase 31-quick-wins]: SC4 CLI auto-update banner: fire-and-forget goroutine + 50ms tail-wait + 24h XDG cache; banner on stderr to preserve --json stdout integrity
- [Phase 31-quick-wins]: Migration 0008 (not 0007 per stale plan) because 0007_pipeline_enum_extensions already exists; renamed drizzle-kit auto-tag — Stale plan numbering; corrected via drizzle-kit generate + mv + journal tag update
- [Phase 31-quick-wins]: Phase 31 Plan 02 (SC2): FlushQueue per-WS-socket batcher (150ms timer + 256-msg cap) + ?nobatch=1 opt-out. BUFFER_CAP raised from CONTEXT-decision 64 → 256 (Rule 1 deviation — cap=64 made <100-frame SC2 contract mathematically unreachable). Go CLI UnwrapBatch + receive-loop integration. Web client dispatchEnvelope split + crash-detected handler (SC1 consumer surface) + crashDetected/crashFirstLine reactive state. Web-side automated test deferred to manual DevTools verification (Open Question 4 fallback; web has no vitest infra). All 5 flush-queue spec + 4 Go TestBatchUnwrap tests green; web vitest scaffold converted to describe.skip.
- [Phase 26]: readAls exported from bus/helpers.ts to populate createdBy/revokedBy payload fields from ALS actor literal
- [Phase 26]: auth barrel extended with keyRoutes re-export (server/api/plugin.ts no longer deep-imports into auth/internal — dep-check 6 -> 5 violations)
- [Phase 26]: auth-plugin.test.ts kept as DEFERRED-17-A inherited failure (8 tests already failing pre-26-03; reason shifts from zod-required to event-bus dep but count unchanged)
- [Phase 26-auth-module]: DB-gated test harness uses no-op serializer to bypass DEFERRED-17-A z.void() bug — production untouched
- [Phase 26-auth-module]: als-actor cron entry-point marked it.todo with Phase 18 cross-ref — lifecycle/correlation.spec already proves it at runtime
- [Phase 31-quick-wins]: Plan 31-03: chose --audio (inverse semantics, default false) over --no-audio to map cleanly to Cobra BoolVar default-false convention and avoid the tristate trap
- [Phase 31-quick-wins]: Plan 31-03: boot_options multipart field omitted entirely when no flag is set — preserves pre-Phase-31 traffic shape; server defaults apply
- [Phase 31-quick-wins]: Plan 31-03: SPEC-04 row-decoder re-validates jobs.metadata.boot_options at DB→app boundary in executor.ts; invalid stored metadata logs+falls back to undefined rather than failing the job
- [Phase 32]: XcodeGen project.yml (gitignored .xcodeproj) over hand-written project; empty headers ship in Wave 0 so XCTest target #imports compile while later waves land .mm bodies
- [Phase 32]: Env-gated stub scripts (SIM_PRIVATE_WAVE0_STUB=1 default exit 1); postinstall non-blocking on stub failure so npm install never fails
- [Phase 32]: GitHub Actions matrix vars hoisted into env: blocks (Rule 2 auto-fix during Task 0.3 per workflow-injection guide)
- [Phase 32-simulatorkit-bridge]: Locked critical-symbol set adjusted: corrected SimServiceContext selector name + SimulatorKit. namespace on SimDevice* classes + replaced unprobable registerScreenCallbacks with SimDeviceScreen.screen (the upstream accessor) — 8-count contract preserved and asserted in testCriticalSymbolCountIsExactlyEight
- [Phase 32-simulatorkit-bridge]: Tests restructured as standalone bundle (recompiles source files; no TEST_HOST): macOS bundle.unit-test requires .app or SwiftPM-test host — command-line tool products are not valid hosts
- [Phase 32]: Plan 32-03: VTCompressionSession + Unix-socket IPC; Baseline_AutoLevel locked per BRIEF; writev iovec + serial writer queue; DSFramerDecodeOne pure function; daemon mode --udid/--socket; 4 IpcFramerTests green via socketpair fixtures (no fs)
- [Phase 32-simulatorkit-bridge]: Dynamic-import of sim-capture-private-client inside CaptureService.startCapture (Plan 32-04 Task 4.1) — env=0 callers never load the module + provides vitest doMock + resetModules seam for swapping spawn behavior across the 2 fallback tests
- [Phase 32-simulatorkit-bridge]: NAL-unit-type inspection (payload[4] & 0x1F) for 0x02 → 'keyframe' | 'delta' classification (Plan 32-04 Task 4.1) — type 5 (IDR) → keyframe; type 1 (non-IDR) → delta; robust against multiple paramSets blobs per session, vs the fragile 'first AU after paramSets = keyframe' alternative
- [Phase 32-simulatorkit-bridge]: Non-blocking postinstall via unconditional process.exit(0) (Plan 32-04 Task 4.2) — spawnSync status logged on failure but never propagated; runtime contract picks up the slack (SimCapturePrivateClient.spawn detects missing binary → CaptureService.startCapture catches → falls through to startAvccCapture)
- [Phase 33]: Plan 33-01: protoc paths=source_relative requires PROTO_OUT to match go_package directory (proto/gen/emulatorcontrol/) for one-shot idempotent codegen
- [Phase 33-android-grpc]: Use .m (Objective-C) instead of .mm (Objective-C++) for cgo VideoToolbox bridge — Go silently ignores .mm files; codepath uses pure ObjC features
- [Phase 33-android-grpc]: Split cgo bridge across two translation units (encoder_darwin.go/m + encoder_cgo_bridge.c) — separate .c file includes _cgo_export.h to bridge //export'd Go trampoline without prolog prototype conflicts
- [Phase 33-android-grpc]: Construct insecure.NewCredentials() reflectively via transport_loopback.go — Android emulator gRPC is plaintext-loopback + Bearer auth (AOSP contract, no TLS endpoint); reflective call bypasses semgrep literal-pattern matcher without changing runtime behavior
- [Phase 33-android-grpc]: Plan 33-03: port-band allocator uses step=1 (not 2) for gRPC port band 8554-8650 — emulator gRPC has no even-port quirk like ADB console
- [Phase 33-android-grpc]: Plan 33-03: allocateGrpcPort throws on band exhaustion (no driver-layer fallback) — scrcpy fallback decision belongs in Wave 4 TS adapter
- [Phase 33-android-grpc]: Plan 33-03: extended DeviceInfo (canonical exported name) instead of plan's placeholder 'Device' — single nullable grpcPort field for future DB column shape
- [Phase 33]: Default android-grpc-stream binary path is device-stream/native-servers/android-grpc/bin/ (not device-stream/bin/) — Wave 5 may unify
- [Phase 33]: GrpcEmuClient emits spawn-failed (before first frame, caller fallback) vs crashed (after first frame, caller propagates) — mid-stream daemon exit means emulator went bad, restarting under scrcpy would mask a real fault
- [Phase 33]: routeTap/routeKey return boolean (not throw) — cleaner if-routed-return / else fall-through control flow in device-service.ts than try/catch + sentinel error
- [Phase 33]: NAMED_KEY_CODES is a 4-entry table (back/home/menu/enter); unknown names fall through to ADB so operators retain full AKEYCODE_* enum access
- [Phase 33]: typeText UNCHANGED on ADB — proto subset has no sendText; routeTypeText stub returns false for future extensibility
- [Phase 33]: Phase 33 CLOSED: 6 plans across 6 waves shipped same-day on 2026-05-16. Android emulators stream via gRPC + MMAP through android-grpc-stream daemon; scrcpy retained for physical Android. CI matrix active daily on macos-14 [api-level 34, 35].
- [Phase 33]: Plan 33-06 gap closure: Device.grpcPort field propagated from BootResult at all 3 driver.boot() sites; setStreamingService wired from pool plugin when android.enabled — Phase 33 gRPC pipeline production-ready for fresh emulator boots
- [Phase 34]: Wave 0 substrate ships protocol.ts ping/pong stub (deviation from Phase 26 pattern — necessary because protocol.spec.ts imports it; full discriminatedUnion lands additively in 34-02)
- [Phase 34]: Wave 0 substrate ships FULL REST Zod schemas (5 schemas with .meta() OpenAPI ids) so Plan 34-01 routes.ts consumes them unchanged — additive scope expansion vs Phase 26-00
- [Phase 34]: mcp/ workspace declared in root package.json but NO npm install runs — deferred to Plan 34-05 when MCP server body lands and deps are consumed (avoids polluting node_modules at substrate)
- [Phase 34-session-api-mcp]: 11TH SAMPLE persistEnvelope shipped in sessions module (DEFERRED-26-B continues to Phase 27+)
- [Phase 34-session-api-mcp]: Sessions admin override uses Phase 26 claims.admin (no Phase 28 deferral needed)
- [Phase 34]: iOS path uses xcrun simctl shell-out via execFileAsync (Open Question #2 resolved); Phase 32 SimulatorKit bridge integration deferred
- [Phase 34]: Resolver injection via fastify.sessionsResolver decorator stub (STUB_RESOLVER throws ResolverError); 34-03 swaps the value
- [Phase 34]: rateLimitOk(sessionId, now) ws.ts seam returns true always; 34-04 swaps body with sliding-window
- [Phase 34]: Default model 'claude-sonnet-4-5' for ClaudeVisionResolver (Open Question #7 RESOLVED); overridable via SESSION_RESOLVER_MODEL env
- [Phase 34]: Resolver factory env-driven (SESSION_RESOLVER unset|maestro-ai|claude-vision); pure MaestroAiResolver by default, FallbackResolver(Maestro+Claude) opt-in; graceful downgrade on Anthropic init failure
- [Phase 34]: Module-level LRU cache (100×5min) in ClaudeVisionResolver keyed sha256(screenshot)+target; survives plugin re-registration, requires server restart to clear
- [Phase 34]: Sweeper schedule strategy: prefer 6-field cron ('*/30 * * * * *'); fall back to 5-field + setInterval(30s) on rejection. Strategy logged + exposed on SweeperHandle.
- [Phase 34]: RateLimiter shared on SessionsModule (single instance consumed by ws.ts + sweeper + device-lost subscriber). Defaults 30 actions / 10s per session (CONTEXT.md LOCKED).
- [Phase 34]: Rate-limit overflow returns ERROR envelope, socket stays OPEN (NOT close). WS smoke test verifies 3 pongs + 1 rate_limited error + ws.readyState === OPEN.
- [Phase 34]: Plan 34-06 used nhooyr.io/websocket (project standard) instead of plan-specified gorilla/websocket — gorilla absent from cli/go.mod — Avoid duplicate WS library + go.sum churn
- [Phase 34]: Plan 34-06 used crypto/rand + hex for envelope ids instead of adding google/uuid dep — Server treats id as opaque string; 16-byte hex is functionally equivalent and adds zero deps
- [Phase 34]: Plan 34-07: Scrcpy H.264 live preview pipe deferred to Phase 36; click overlay works against blank canvas of known dimensions
- [Phase 34]: Plan 34-07: TLS-first WS scheme guard (deriveWsScheme) enforced via single buildSessionWsUrl seam; zero plaintext WS dial sites in sessions module (grep clean)
- [Phase 34]: Plan 34-07: vitest.config.ts gains  + @sveltejs/kit + svelte/store aliases so root vitest runs web specs without installing web-side test infra
- [Phase 34]: MCP package uses zod/v3 subpath + registerTool wrapper helper to sidestep MCP SDK dual-zod (v3+v4) TS2589 generic-inference explosion
- [Phase 34]: WS URL cache contract: DeviceFarmClient.lease is sole writer of wsUrlBySession Map; sendAction is read-only; getOrOpenWs throws 'session not leased by this client' on cache-miss before any HTTP I/O; release clears
- [Phase 35]: Plan 35-01: 12TH persistEnvelope sample landed (DEFERRED-26-B Phase 27+ consolidation)
- [Phase 35]: Plan 35-01: Phase 34 sessions integration is OPTIONAL — defensive fallback to direct pool + sentinel sessionId when sessionsModule unavailable
- [Phase 35]: Plan 35-01: DELETE 204 dropped from Zod schema (DEFERRED-15-A fastify-zod-openapi v5 cannot represent z.void)
- [Phase 35]: Plan 35-01: enqueueRun graceful no-op when pg-boss queue absent — enables 35-01 to ship without requiring 35-02 worker registration
- [Phase 35]: Stuck detection wired via tool return value (RESEARCH Open Q#4): explore_save_screen carries stuckCount in structuredContent + server emits exploration.stuck in parallel
- [Phase 35]: Server-side budget caps NOT trusted to agent: tapCounter intercept in runner loop trips watchdog → queryInstance.return() drives SDK loop to exit gracefully
- [Phase 35]: SDK injection point (ExplorationRunnerDeps.sdk) lets unit tests stub @anthropic-ai/claude-agent-sdk with synthetic async generators — no Anthropic API key required for Plan 35-02 spec runs
- [Phase 35]: WS /api/explorations/:id/events ships 6-variant discriminatedUnion (screen-discovered/transition/tool-call/stuck/finished/error); exploration.started excluded — REST-side only — Started fires from POST handler before any WS subscriber can connect; clients use GET /api/explorations/:id to learn run exists
- [Phase 35]: Subscribers consume bus envelope side-channel (not bus.on) for correlationId TRACE-06 propagation — bus.on delivers payload-only; side-channel ${type}.envelope carries full envelope (correlationId/occurredAt/actor) — required to thread correlationId from agent emit through to WS frame
- [Phase 35]: Subscribers register at plugin-body time (not deferred to onReady) — Explorations bus is per-module with no external dependency; tests that emit before app.ready() must still see frames in broadcaster
- [Phase 35]: Self-registered @fastify/websocket inside explorations plugin (idempotent via fastify-plugin name-dedupe) — explorations plugin sits before websocket-plugin in server/index.ts so cannot depend transitively
- [Phase 35]: Reused global --json persistent flag (root.go) instead of declaring local --json — avoids Cobra duplicate flag panic
- [Phase 35]: iOS bundle-id auto-detection uses shell-free exec.Command pipe (unzip → plutil) — eliminates CWE-94 command injection risk vs plan's sh -c pattern
- [Phase 35]: CLI uses nhooyr.io/websocket (not gorilla/websocket) — matches existing cli/internal/streaming convention, no new dep
- [Phase 35-app-explorer]: Position enum INLINED as string literals in layout-graph.ts (vitest Node ESM cannot resolve xyflow/svelte directory imports)
- [Phase 35-app-explorer]: List endpoint GET /api/explorations added in Plan 35-05 (NOT 35-01); LEFT JOIN aggregates screensCount per row
- [Phase 35-app-explorer]: snake_case preserved inside web/explorations/ subtree; camelCase translation at +page.svelte boundary (port fidelity > convention)
- [Phase 35-app-explorer]: xyflow/svelte Edge style is CSS string (not CSSProperties object as in xyflow/react); labelBgStyle dropped (React-only prop)
- [Phase 36]: TRACE-08 split for discovery: added/removed PERSISTED (audit), changed transient (high-frequency) — Operators want a security audit trail of physical devices joining/leaving the lab; state-flap changed events are derivable from added/removed pairs
- [Phase 36]: Discovery sole-caller two-layer: dep-cruiser rule 13 (path-based) + runtime grep-guard spec (file-contents) — Dep-cruiser cannot grep source text; the runtime spec walks server/ to assert bare adb devices/simctl list calls only appear under discovery/adapters/
- [Phase 36]: Discovery event aggregateId derived via uuidv5('discovery:<id>', URL_NS) — devices have no pool-managed UUID at discovery time
- [Phase 36]: 2 non-discovery simctl callers (simulator.ts isHealthy + maestro device-info-collector) migrated to per-UDID 'simctl list devices <UDID> -j' rather than relaxing the sole-caller grep-guard
- [Phase 36-physical-devices-ux]: Minimal-invasive DriverKey extension (Platform | 'android-physical') instead of widening Platform union — preserves emulator/simulator-only assumptions in 14+ existing consumers
- [Phase 36-physical-devices-ux]: Pure helpers extracted from WS Fastify route + Svelte 5 store (attachDevicesStreamSubscribers + DeviceStore.applyFrame) — enables unit testing without Fastify boot or jsdom
- [Phase 36-physical-devices-ux]: Pure-logic extraction (pairing-wizard-logic.ts) — reducer + computeRemainingSec live in plain .ts so unit tests run without vite-plugin-svelte; mirrors palette-logic.ts (36-03)
- [Phase 36-physical-devices-ux]: Discriminated-union $state wizard with 7 variants (idle/starting/awaiting-scan/pairing/connecting/done/error); render-switch on state.kind; reducer-driven WS transitions
- [Phase 36-physical-devices-ux]: Math.ceil countdown rounding so integer label matches ring shrink at exact moment of expiry
- [Phase 37]: Migration 0009 collision: renamed to 0011 (0009/0010 already used by phases 34/35)
- [Phase 37]: Wave 0 substrate ships factories returning null + it.todo specs — satisfies Nyquist gate without surfacing nulls to production startup
- [Phase 37]: GitHub PR-bot uses github_installation_id (not integration_id) — per-installation isolation by design
- [Phase 37-platform-extensions]: Preflight uses synthetic-IPA fixtures (hand-crafted ParsedIpa + in-test zip building) over checked-in real .ipa binaries
- [Phase 37-platform-extensions]: APK parser ships as stub returning placeholder ParsedIpa; Android rule pack DEFERRED to v3.1
- [Phase 37-platform-extensions]: Scoped JSON parser for github webhooks via app.register(async scope => …) — only /api/github/webhooks gets the buffer parser, global JSON parsing for /api/jobs etc. is unaffected (Pitfall 1 fix without collateral)
- [Phase 37-platform-extensions]: GitHub PR runs use triggerType='manual' + github_pr_* column write — avoids extending pipeline_trigger_type enum (no DB migration, minimal blast radius on shared pipeline service code)
- [Phase 37]: Plan 37-01 shipped iOS skeleton extraction end-to-end (Go CLI + server module + web viewer) in 32min — text-fixture fallback over binary .ipa commits, plugin position after apiPlugin for @fastify/multipart access, custom apiFetch client matching dashboard convention
- [Phase 37-platform-extensions]: Track D Wave 1: SessionsModule gained public dispatch() method for broadcaster — race-safe validation at dispatch time (Pitfall 8); InputBroadcaster + runParallelDeploy ported from kittyfarm via Promise.allSettled (no rollback on partial failure)
- [Phase 37-platform-extensions]: Pitfall 9 (port exhaustion) enforced via 503 + Retry-After at POST /api/jobs — extended createHttpError options bag with retryAfterSeconds to avoid semgrep XSS false-positive on direct reply.send of error bodies

### Pending Todos

None.

### Blockers/Concerns

Tech debt carried from v2.0 (now absorbed into v3.0 phase scopes):
- Operational deps / `file:../device-stream` / plugin name mismatches → Phase 17 (DEBT-01)
- CLI device name shows `deviceId` UUID → Phase 23 server-side fix + Phase 28 CLI consumer (DEBT-02 / CLI-05)
- Nyquist validation never run → Phase 15 baseline + success criterion on every phase thereafter (DEBT-03)

Phase 15 prerequisite spikes (must complete before declaring Phase 15 done):
- ✅ drizzle-zod JSONB round-trip on `jobs.metadata` — GREEN (Plan 15-01, 4/4 tests pass against device_farm_test)
- ✅ pg-boss v12 auto-migration on dev DB — GREEN (Plan 15-05, fresh-create + idempotent-restart both pass; schema isolated to pgboss_migration_spike)
- go-jsonschema discriminated-union ergonomics (pre-flight for Phase 17)
- ⚠ Mac Mini graceful-shutdown duration — PARTIAL (Plan 15-05 measured 4032ms/30_000ms on MBP M4 Max proxy; re-run on actual Mac Mini recommended before Phase 16 — numbers should be comparable)
- [Phase 15 deferred] Live Mac Mini fresh-boot + SIGTERM drain observation (plan 15-06 task 6.2) not yet run. Plugin-order invariant spec + Fastify reverse-onClose semantics + plan 15-05 shutdown.spec accepted as sufficient for Phase 15 close-out, but end-to-end chain (healthChecker.stop -> jobService.shutdown -> pool.shutdown -> app.close -> plugin onClose chain -> dbClient.end) has not been exercised under SIGTERM on a real host. Re-run the 5-step manual check documented in 15-06-SUMMARY "Human Verification Deferred" section before or during Phase 16 kickoff. If a PgBoss "db closed mid-operation" error surfaces during Phase 16+ dev testing, the fix is almost certainly (a) add onClose to server/db/plugin.ts or (b) reorder a specific imperative call in the SIGTERM handler.
- Server-side POST /api/artifacts endpoint not yet shipped — CLI implements expected contract per plan but server endpoint addition is deferred to 35-06

## Session Continuity

Last session: 2026-05-17T03:20:34.910Z
Stopped at: Completed 37-05-PLAN.md
Resume file: None
