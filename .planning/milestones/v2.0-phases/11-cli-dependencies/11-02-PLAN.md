---
phase: 11-cli-dependencies
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - cli/cmd/dependencies.go
autonomous: false
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - DEP-04
  - DEP-05
  - DEP-06
  - DEP-07
  - DEP-08
  - DEP-09
  - DEP-10

must_haves:
  truths:
    - "Running `device-farm dependencies` on the user's machine installs missing tools or reports all present"
    - "Progress output shows per-item status during installation"
    - "After dependencies completes, `device-farm doctor` shows all checks passing"
  artifacts:
    - path: "cli/cmd/dependencies.go"
      provides: "Fully working dependencies command"
      contains: "runDependencies"
  key_links:
    - from: "cli/cmd/dependencies.go"
      to: "cli/cmd/doctor.go"
      via: "Re-runs doctor checks after install to verify success"
      pattern: "check(Binary|AndroidSDK|Xcode|Postgres)"
---

<objective>
Verify the `device-farm dependencies` command works end-to-end on the real machine.

Purpose: Unit tests cannot verify actual package installation. This plan builds the CLI and runs the command to confirm real-world behavior.
Output: Confirmed working `dependencies` command on the user's machine.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-cli-dependencies/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CLI and run dependencies command</name>
  <files>cli/cmd/dependencies.go</files>
  <read_first>
    - cli/cmd/dependencies.go (the implementation from plan 01)
    - cli/cmd/doctor.go (check functions for reference)
  </read_first>
  <action>
1. Build the CLI binary: `cd /Users/heicg/Desktop/projects/device-farm/cli && make build`
2. Run `./bin/device-farm doctor --json` to see current dependency status
3. Run `./bin/device-farm dependencies` to attempt installing any missing dependencies
4. If any install function has bugs (wrong command, bad path, missing error handling), fix them in `cli/cmd/dependencies.go`
5. Run `./bin/device-farm doctor` after to verify doctor passes
6. Run `./bin/device-farm dependencies --json` to verify JSON output format

Fix any runtime issues discovered -- the unit tests from plan 01 cover logic, but real subprocess execution may reveal:
- Wrong brew formula names
- sdkmanager path issues after cask install
- softwareupdate parsing differences on this macOS version
- sim-capture build path issues

This is an iterative fix-and-rerun task.
  </action>
  <verify>
    <automated>cd /Users/heicg/Desktop/projects/device-farm/cli && go build -o bin/device-farm . && ./bin/device-farm dependencies --json 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'Valid JSON with {len(d)} results'); assert all('name' in r and 'status' in r for r in d)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/heicg/Desktop/projects/device-farm/cli && ./bin/device-farm dependencies --json` outputs valid JSON array
    - Each JSON object has "name" and "status" fields
    - `cd /Users/heicg/Desktop/projects/device-farm/cli && ./bin/device-farm --help | grep -c dependencies` returns 1
  </acceptance_criteria>
  <done>CLI builds and `device-farm dependencies` runs without crashes, producing valid JSON output and real install/skip behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dependencies command on real machine</name>
  <files>cli/cmd/dependencies.go</files>
  <action>
Human verification step. Claude has already built and smoke-tested the command in Task 1. The user now verifies the full install flow on their machine.
  </action>
  <what-built>The `device-farm dependencies` command that auto-installs missing dependencies detected by doctor. It checks what is missing, installs via brew/sdkmanager/scripts, shows real-time progress, and re-runs doctor to verify.</what-built>
  <how-to-verify>
    1. Run: `cd /Users/heicg/Desktop/projects/device-farm/cli && ./bin/device-farm doctor` -- note any failing checks
    2. Run: `cd /Users/heicg/Desktop/projects/device-farm/cli && ./bin/device-farm dependencies` -- observe real-time install progress
    3. Verify: each missing dependency shows "Installing X..." followed by streamed output and a result
    4. Verify: already-installed dependencies are skipped (not reinstalled)
    5. Run: `cd /Users/heicg/Desktop/projects/device-farm/cli && ./bin/device-farm doctor` again -- confirm previously failing checks now pass
    6. Run: `cd /Users/heicg/Desktop/projects/device-farm/cli && ./bin/device-farm dependencies --json` -- confirm JSON output is valid
  </how-to-verify>
  <verify>
    <automated>cd /Users/heicg/Desktop/projects/device-farm/cli && ./bin/device-farm dependencies --json 2>/dev/null | python3 -c "import sys,json; json.load(sys.stdin); print('Valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms install flow works for missing dependencies
    - User confirms already-installed deps are skipped
    - User confirms doctor passes after dependencies completes
  </acceptance_criteria>
  <done>User has approved the dependencies command behavior on their real machine.</done>
  <resume-signal>Type "approved" or describe any issues with the install behavior</resume-signal>
</task>

</tasks>

<verification>
1. `device-farm dependencies` completes without crashes
2. `device-farm dependencies --json` produces valid JSON
3. `device-farm doctor` passes after running dependencies
4. Already-installed deps are skipped on second run
</verification>

<success_criteria>
- User confirms `device-farm dependencies` installs missing tools correctly
- User confirms `device-farm doctor` shows all checks passing after install
- User confirms progress output is clear and readable
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-dependencies/11-02-SUMMARY.md`
</output>
