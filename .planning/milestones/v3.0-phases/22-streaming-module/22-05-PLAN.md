---
phase: 22
plan: 05
type: execute
wave: 5
depends_on: [22-02, 22-03, 22-04]
files_modified:
  - server/streaming/MODULE.md
  - server/streaming/index.ts
  - server/streaming/__tests__/adapter-factory.spec.ts
  - server/streaming/__tests__/android-preview-adapter.spec.ts
  - server/streaming/__tests__/device-preview.spec.ts
  - server/streaming/__tests__/ios-preview-adapter.spec.ts
  - server/streaming/__tests__/job-broadcaster.spec.ts
autonomous: true
requirements: [TRACE-06, MOD-01, MOD-02, MOD-04]
estimated_minutes: 35
gap_closure: false
must_haves:
  truths:
    - "`server/streaming/MODULE.md` replaces Plan 22-00 Purpose-only placeholder with full 9 H2 sections in canonical order per Phase 16/18/19/20/21 convention. Plus H3 Runnable Example. Queue Produced + Queue Consumed sections say 'None' explicitly (CONTEXT §Decisions no-queue deviation). Purpose: bus-subscribed WebSocket fan-out with strict Zod envelope carrying correlationId (TRACE-06). Public API: streamingPlugin + createStreamingModule factory + back-compat JobBroadcaster/DevicePreviewManager classes + wsEnvelopeSchema/WsEnvelope + streamingRegistry/STREAMING_EVENT_NAMES/STREAMING_AGGREGATE_ID/makeStreamingEmitters/wsFrameDroppedPayload. Events Emitted: ws.frame.dropped (not persisted). Events Consumed: job.log / job.step / job.status (from jobs module via fastify.jobsModule.bus.on; deferred to onReady per Pitfall 2). Invariants: (1) envelope MUST carry correlationId at root; (2) safeParse failure drops frame + emits ws.frame.dropped (never crashes connection); (3) ring-buffer <=200 envelopes replayed on reconnect; (4) producer-side bus emit uses .parse() fail-loud; consumer-side envelope build uses .safeParse() fail-soft; (5) no producer outside streaming module may call JobBroadcaster.emit directly. Non-Goals: (a) DevicePreviewManager envelope wrapping (deferred to Phase 29 WEB-03); (b) sharing wsEnvelopeSchema with web client (Phase 29 WEB-03); (c) exactly-once delivery (WebSocket fan-out is lossy-by-design); (d) SC2 non-violation — this.jobBroadcaster!.cleanup call KEPT at server/jobs/job-service.ts line ~479 for buffer lifecycle (Phase 23 saga rewrite may replace with job.cleanup.requested event); (e) **6TH SAMPLE POINT — Phase 27+ CONSOLIDATION TRIGGER STILL OPEN** for persistEnvelope middleware duplication. Dependencies: config + auth + pool-plugin + event-bus + db (5 entries; plugin name 'websocket-plugin' preserved for back-compat)."
    - "`server/streaming/index.ts` EXTENDED from Plan 22-00 1-line stub to full barrel. Strict MOD-02 invariant: ONE `from './internal/module.js'` re-export line + back-compat surface re-exports. Exports: (a) streamingPlugin default from './plugin.js'; (b) createStreamingModule + StreamingModule + CreateStreamingModuleDeps from './internal/module.js' (THE ONE internal/ re-export line); (c) back-compat classes JobBroadcaster (from internal/) + DevicePreviewManager (from internal/) — but for MOD-02 strict these re-exports MUST use the internal/ path via a re-export-of-re-export via index.ts? NO — the strict MOD-02 rule (Plan 22-00 dep-cruiser rule 6) forbids DEEP imports INTO server/streaming/internal/ from OUTSIDE server/streaming/. Re-exports WITHIN server/streaming/index.ts are at the MODULE BOUNDARY and do NOT violate the rule (the rule fires only for `from: { pathNot: '^server/streaming/' }` which is outside). So `export { JobBroadcaster } from './internal/job-broadcaster.js';` is ALLOWED. However, this breaks the 'strict 1-line internal/' Phase 21 convention. Compromise per RESEARCH: the Phase 21 index.ts re-exports the services (ArtifactService, RecordingService, etc.) directly from internal/-neighbor files (artifact-service.ts etc.) which live at the module TOP-LEVEL (not under internal/). Phase 22 has NO top-level service files — everything under internal/. So index.ts barrel DOES legitimately multi-re-export from internal/ for back-compat; the 'strict 1-line' invariant relaxes to 'ONE internal/module.js re-export' (the factory surface). Other internal/ re-exports (JobBroadcaster, DevicePreviewManager, WsEnvelope, ws-schemas wsEnvelopeSchema) are permitted AT THE BARREL (not external); plan 22-06 Nyquist close-out grep-checks `grep -c \"from './internal/\" index.ts` == exactly the number of legitimate re-exports. Decision: ship multi-re-export barrel; document in comment."
    - "5 test file renames via `git mv` 100%-similarity: `adapter-factory.test.ts -> adapter-factory.spec.ts`, `android-preview-adapter.test.ts -> android-preview-adapter.spec.ts`, `device-preview.test.ts -> device-preview.spec.ts`, `ios-preview-adapter.test.ts -> ios-preview-adapter.spec.ts`, `job-broadcaster.test.ts -> job-broadcaster.spec.ts`. After renames all 5 still pass unchanged (behavior rename). job-broadcaster.spec.ts content REQUIRES ONE edit: `makeMessage` helper (if present) that returns `JobMessage` shape MUST be updated to return `WsEnvelope` shape (field renames timestamp->ts + added correlationId + v + payload wrapper). This is the only content-change alongside the rename."
    - "Nyquist baseline GATE: `.planning/nyquist-baseline.json` MUST NOT be overwritten — check via `diff -s` against a backup OR via git-diff (file is committed; any modification surfaces in `git diff .planning/nyquist-baseline.json`). Coverage delta via `npm run nyquist:check` must exit 0 (delta >= -2pp vs Phase 15 baseline 48.29%). Current estimate: Phase 22 adds ~1100 lines of test code (3 DB-gated specs + 1 non-DB + 1 module.spec + events.spec extension + envelope.spec + lifecycle-ownership.spec) + ~700 lines of source (factory + plugin.ts rewrite + events.ts + jobs/events.ts extension) and subtracts ~180 lines (websocket-plugin.ts deletion + job-service.ts surgery shrinks 7 callsites from 5-line blocks to 4-line blocks). Net delta expected <= +1pp (similar to Phase 21's -0.30pp). Nyquist gate is the final binding gate for Plan 22-05; if delta < -2pp, Plan 22-06 MUST NOT land."
  artifacts:
    - path: "server/streaming/MODULE.md"
      provides: "Full 9 H2 sections + H3 Runnable Example — MOD-01 canonical; documents all Phase 22 contracts + Non-Goals (incl. SC2 non-violation + 6TH SAMPLE POINT consolidation trigger)"
      contains: "## Purpose"
    - path: "server/streaming/index.ts"
      provides: "Full barrel with MOD-02 boundary — factory re-export + back-compat classes + events surface + envelope schema"
      contains: "streamingPlugin"
    - path: "server/streaming/__tests__/*.spec.ts (5 renames)"
      provides: "MOD-04 canonical naming — all 5 test files use .spec.ts suffix"
      contains: "describe\\("
  key_links:
    - from: "server/streaming/MODULE.md §Non-Goals (6TH SAMPLE POINT)"
      to: "Phase 27+ CONSOLIDATION TRIGGER"
      via: "Documents persistEnvelope middleware now exists in 6 modules (reporting + pool + artifacts + lifecycle + hooks + streaming); Phase 27 extracts to server/bus/persist-envelope.ts"
      pattern: "6TH SAMPLE POINT"
    - from: "server/streaming/MODULE.md §Non-Goals (SC2 non-violation)"
      to: "server/streaming/__tests__/lifecycle-ownership.spec.ts [Phase 22 SC2 non-violation] test"
      via: "Non-Goals documents the kept cleanup; lifecycle-ownership.spec asserts count == 1"
      pattern: "jobBroadcaster.*cleanup"
    - from: "server/streaming/index.ts back-compat class re-exports"
      to: "server/jobs/job-service.ts import statements (typed only, via .js extensions)"
      via: "JobService holds type-only imports of JobBroadcaster + DevicePreviewManager; decorators continue to be used"
      pattern: "import type"
---

<objective>
Wave 5 close-out plan landing the MODULE.md full body, the index.ts full barrel, the 5 MOD-04 test renames, and the Nyquist delta gate. Plan 22-06 Wave 6 is the tiny final sweep (STATE.md + ROADMAP.md + deferred-items + commit).

**1. MODULE.md full body.** Replaces Plan 22-00's Purpose-only placeholder with 9 H2 sections + H3 Runnable Example in the canonical order (Purpose / Public API / Events Emitted / Events Consumed / Queue Produced / Queue Consumed / Invariants / Non-Goals / Dependencies). Explicit "None" in Queue Produced/Consumed per CONTEXT §Decisions no-queue deviation. Non-Goals section captures all 5 deferred concerns + the 6TH SAMPLE POINT consolidation trigger.

**2. index.ts full barrel.** Expands from Plan 22-00's 1-line `export { createStreamingModule } from './internal/module.js'` stub to the full back-compat surface: streamingPlugin default + createStreamingModule factory types + JobBroadcaster/DevicePreviewManager classes (for server/jobs/job-service.ts typed imports) + wsEnvelopeSchema/WsEnvelope (for future Phase 29 WEB-03 sharing) + streamingRegistry + event-name constants + makeStreamingEmitters + payload schema. All re-exports live inside `server/streaming/` directory so dep-cruiser rule 6 does NOT fire on the re-exports themselves (it fires only on OUTSIDE-THE-MODULE deep imports).

**3. MOD-04 test renames.** 5 existing `.test.ts` files under `server/streaming/__tests__/` renamed to `.spec.ts` via `git mv` 100% similarity (blame preservation). After renames the suite count is unchanged (5 renamed + 4 added in earlier plans = 9 total spec files). One content change: `job-broadcaster.spec.ts` needs its `makeMessage` helper updated to `makeEnvelope` returning `WsEnvelope` shape (the old helper returned `JobMessage` which no longer flows through the broadcaster after Plan 22-02's signature tightening). Other 4 renames are pure path changes.

**4. Nyquist delta gate.** `npm run nyquist:check` must exit 0 (coverage delta >= -2pp vs Phase 15 baseline 48.29%). Existing phases have cleared this bar (Phase 20: -0.30pp; Phase 21: similar). Phase 22 is low-risk because the test-to-source ratio is HIGH (3 DB specs + 3 unit specs + 1 lifecycle + events.spec + envelope.spec = ~1100 lines of tests vs ~700 lines of new source). Plan 22-05 does NOT overwrite nyquist-baseline.json (locked since Phase 15 commit 55ff8ac).

Purpose: Plans 22-00 through 22-04 deposited functional + test substrate. Plan 22-05 closes the MOD-01 (MODULE.md) + MOD-02 (full barrel) + MOD-04 (spec rename) contract gates. After Plan 22-05, Phase 22 is functionally complete; Plan 22-06 is administrative.

Sequential — depends on Plans 22-02, 22-03, 22-04 all landing before Plan 22-05 runs. Plan 22-06 depends on Plan 22-05.

Output: MODULE.md expands from ~15 lines to ~200 lines (9 sections + runnable example); index.ts expands from 2 lines to ~80 lines (multi-re-export barrel); 5 file renames; Nyquist delta proved.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-streaming-module/22-CONTEXT.md
@.planning/phases/22-streaming-module/22-RESEARCH.md
@.planning/phases/22-streaming-module/22-VALIDATION.md
@.planning/phases/22-streaming-module/22-02-PLAN.md
@.planning/phases/22-streaming-module/22-03-PLAN.md
@.planning/phases/22-streaming-module/22-04-PLAN.md
@server/streaming/MODULE.md
@server/streaming/index.ts
@server/streaming/internal/module.ts
@server/streaming/internal/job-broadcaster.ts
@server/streaming/internal/device-preview.ts
@server/streaming/internal/ws-schemas.ts
@server/streaming/internal/types.ts
@server/streaming/events.ts
@server/streaming/plugin.ts
@server/artifacts/MODULE.md
@server/artifacts/index.ts
@server/pool/MODULE.md
@.planning/nyquist-baseline.json

<interfaces>
<!-- Phase 21 artifacts/MODULE.md shape — copy-template for Phase 22 -->

Phase 21 MODULE.md has 9 H2 sections + H3 Runnable Example totaling ~350 lines. Phase 22 is lighter (single-event surface, no queue, no DB migration) → ~200 lines.

Phase 21 MODULE.md structure:
```markdown
# Artifacts Module

## Purpose
[1-3 paragraphs explaining module scope + key invariants]

## Public API
[Bullet list of exports from index.ts + Fastify decorators]

## Events Emitted
- `artifact.created` — PERSISTED. [Details]
- `recording.started` — NOT persisted. [Details]
- `recording.stopped` — NOT persisted. [Details]

ARTIFACTS_AGGREGATE_ID = v5 UUID.

## Events Consumed
- `job.started` — via fastify.jobsModule.bus.on (deferred to onReady per Pitfall 7).
- `job.completed` — via fastify.onPersisted.
- `maestro.log.written` — via fastify.jobsModule.bus.on.

## Queue Produced
- `recording.upload` — policy:stately, singletonKey:recordingId, retryLimit:3, deadLetter:recording.upload.dlq.

## Queue Consumed
- `recording.upload` worker (same queue — self-consumed by artifacts).

## Invariants
- [Invariant 1]
- [Invariant 2]
...

## Non-Goals
- [Thing NOT in this module's scope]
...

## Dependencies
- config, db, queue, event-bus, pool-plugin (5-entry)

## Runnable Example

```typescript
// Emit
fastify.artifactsModule.emit.artifactCreated(artifactId, {...});

// Subscribe
fastify.artifactsModule.bus.on('artifact.created', (envelope) => {...});
```

Phase 22 MODULE.md adapts this to: 1 event emitted (not 3); 3 events consumed (job.log/step/status); Queue Produced/Consumed = None (explicit); 5 deps (config, auth, pool-plugin, event-bus, db).

Phase 21 index.ts (79 lines) — multi-re-export barrel shape (as referenced earlier in this phase's planning). Phase 22 index.ts follows similar shape adapted to:
- streamingPlugin (from './plugin.js')
- createStreamingModule + types (from './internal/module.js')
- JobBroadcaster (from './internal/job-broadcaster.js') — back-compat for server/jobs/job-service.ts typed imports
- DevicePreviewManager (from './internal/device-preview.js') — back-compat
- wsEnvelopeSchema + WsEnvelope type (from './internal/ws-schemas.js') — for Phase 29 WEB-03 future
- JobMessage + DevicePreviewMessage + LogData/StepData/MetricsData/StatusData (from './internal/types.js') — back-compat for consumers that still reference JobMessage
- streamingRegistry + STREAMING_EVENT_NAMES + STREAMING_AGGREGATE_ID + makeStreamingEmitters + wsFrameDroppedPayload + types (from './events.js')

MOD-04 rename targets (5 files; check existence before rename):
```bash
test -f server/streaming/__tests__/adapter-factory.test.ts
test -f server/streaming/__tests__/android-preview-adapter.test.ts
test -f server/streaming/__tests__/device-preview.test.ts
test -f server/streaming/__tests__/ios-preview-adapter.test.ts
test -f server/streaming/__tests__/job-broadcaster.test.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 5.1: Overwrite server/streaming/MODULE.md with full 9-section body + Runnable Example</name>
  <files>server/streaming/MODULE.md</files>
  <read_first>
    - server/streaming/MODULE.md (current — Plan 22-00 Purpose-only placeholder)
    - server/artifacts/MODULE.md (Phase 21 canonical — shape to mirror; section order fixed)
    - server/pool/MODULE.md (Phase 20 canonical — multi-decorator precedent)
    - .planning/phases/22-streaming-module/22-RESEARCH.md §Module Shape (9 sections; Queue Produced/Consumed = None)
    - .planning/phases/22-streaming-module/22-RESEARCH.md §Pitfall 11 (6TH SAMPLE POINT)
    - .planning/phases/22-streaming-module/22-RESEARCH.md §Pitfall 6 (SC2 non-violation)
    - server/streaming/internal/module.ts (Plan 22-02 output — factory shape to reference)
    - server/streaming/events.ts (Plan 22-01 output — event surface)
  </read_first>
  <action>
    **Overwrite `server/streaming/MODULE.md`** with the full 9-section body. Stub from Plan 22-00 is replaced.

    ```markdown
    # Streaming Module

    ## Purpose

    Bus-subscribed WebSocket fan-out with strict Zod envelope validation carrying `correlationId` (TRACE-06).

    After Phase 22 the streaming module is a **PURE SUBSCRIBER**: the `JobBroadcaster` ring buffer is filled from subscriptions to `job.log` / `job.step` / `job.status` emitted by `server/jobs/job-service.ts`, NOT by imperative `fastify.jobBroadcaster.emit(...)` calls inside `executeJob`. Every frame sent on `/ws/jobs/:id` is wrapped in a Zod-validated envelope `{type, correlationId, v:1, ts, payload}`. `safeParse` failures emit `ws.frame.dropped` + structured log + skip send (never crashes the client connection). Ring-buffer replay semantics (<=200 envelopes on reconnect) are preserved.

    `DevicePreviewManager` (binary base64 frames on `/ws/devices/:id/preview`) is unchanged by Phase 22 per CONTEXT §Deferred Ideas — envelope-wrapping for device preview is Phase 29 WEB-03 scope. The module still decorates `fastify.devicePreview` for back-compat.

    The module keeps `JobBroadcaster` and `DevicePreviewManager` class instances exposed as Fastify decorators (`fastify.jobBroadcaster`, `fastify.devicePreview`) for legacy consumers (mid-flow device-preview start/stop in `server/jobs/job-service.ts`; future HTTP routes). Access is via barrel or decorator only; direct imports from `server/streaming/internal/**` are forbidden by `dependency-cruiser` rule `no-deep-imports-into-streaming-internal` (Plan 22-00 addition).

    ## Public API

    Exports from `server/streaming/index.ts` (the ONLY legitimate import surface outside this module — enforced by the dep-cruiser rule above):

    - `streamingPlugin` — Fastify plugin (thin wrapper around `createStreamingModule`). Plugin name: `'websocket-plugin'` (unchanged from v2.0 for back-compat dep-string resolution across jobs-plugin + pipelines-plugin + plugin-order.spec).
    - `createStreamingModule(deps): StreamingModule` — factory (MOD-06) returning `{jobBroadcaster, devicePreview, emit, bus, registerSubscribers, shutdown}` (6-key shape).
    - `JobBroadcaster` / `DevicePreviewManager` classes — back-compat. `fastify.jobBroadcaster` + `fastify.devicePreview` decorators read by `server/jobs/job-service.ts` (cleanup call + device preview start/stop — all other imperative emit calls were removed in Plan 22-02).
    - `wsEnvelopeSchema` / `WsEnvelope` type — strict envelope contract (Plan 22-01 tightened from Phase 17 placeholder). Phase 29 WEB-03 will share with web client.
    - Events surface: `streamingRegistry`, `STREAMING_EVENT_NAMES`, `STREAMING_AGGREGATE_ID`, `makeStreamingEmitters`, `wsFrameDroppedPayload`.
    - Back-compat types: `JobMessage`, `DevicePreviewMessage`, `LogData`, `StepData`, `MetricsData`, `StatusData`, `WsMessageType`, `ArtifactType` (legacy — new code should use `WsEnvelope`).
    - Types: `StreamingModule`, `CreateStreamingModuleDeps`, `StreamingRegistry`, `StreamingEmitters`, `StreamingEventName`.

    Fastify decorators exposed by the plugin:
    - `fastify.jobBroadcaster: JobBroadcaster`
    - `fastify.devicePreview: DevicePreviewManager`
    - `fastify.streamingModule: StreamingModule`

    WebSocket routes:
    - `GET /ws/jobs/:id` — job event channel. Subscribes to `fastify.jobBroadcaster`; receives enveloped frames; token-gated auth when `config.auth.enabled`; 30-second ping/pong heartbeat.
    - `GET /ws/devices/:id/preview` — device preview channel. Subscribes to `fastify.devicePreview`; receives binary base64 frames (unchanged shape); same auth gate + heartbeat; 100ms frame throttle (10fps max).

    ## Events Emitted

    - `ws.frame.dropped` — **NOT persisted** (programmer-error signal; structured log is sufficient; events-table bloat unjustified — Phase 27 trace-tree can derive from pino log lines if needed). Fires from inside the streaming subscriber when `wsEnvelopeSchema.safeParse(candidate)` returns `success: false`. Payload: `{jobId, eventType: 'log'|'step'|'status', reason: 'safeParse-failed'|'unknown', zodError?}`. `aggregateType: 'streaming'`, `aggregateId: jobId`.

    `STREAMING_AGGREGATE_ID = fff0592e-b92c-5221-a40a-d10a141f0158` is the v5 UUID derived from `'streaming'` under the URL namespace (RFC 4122 §4.3) — reserved for future streaming-wide telemetry (e.g. `streaming.buffer.overflow`). NOT used by `ws.frame.dropped`; that event carries `jobId` as aggregateId.

    ## Events Consumed

    All subscriptions are deferred to an `onReady` Fastify hook inside `registerSubscribers` (per Pitfall 2): streaming plugin (name `'websocket-plugin'`) registers at step 10 in `server/index.ts` < jobs-plugin at step 13, so `fastify.jobsModule` is NOT decorated at plugin-body time. `onReady` fires after all plugins register, safely reading `fastify.jobsModule.bus`.

    - `job.log` — consumed via `fastify.jobsModule.bus.on('job.log', handler)` (non-persisted; direct bus subscription). Handler: read correlationId from ALS (`readAls('correlationId') ?? randomUUID()`), build envelope `{type:'log', correlationId, v:1, ts: Date.now(), payload: data}`, `wsEnvelopeSchema.safeParse`, on success `jobBroadcaster.emit(jobId, envelope)`, on failure emit `ws.frame.dropped` + structured log + drop.
    - `job.step` — consumed via `fastify.jobsModule.bus.on('job.step', handler)`. Same handler shape with `type:'step'`.
    - `job.status` — consumed via `fastify.jobsModule.bus.on('job.status', handler)`. Same handler shape with `type:'status'`.

    Cross-module producer references: `server/jobs/events.ts` (Plan 22-01 extended `jobsRegistry` with these 3 events + emit helpers); `server/jobs/job-service.ts` (Plan 22-02 inverted 7 imperative `jobBroadcaster.emit` callsites into typed bus emits via `this.jobsEmit?.log|step|status`).

    ## Queue Produced

    **None.** The streaming module owns no pg-boss queue. WebSocket fan-out is in-process only (EventEmitter-backed ring buffer per jobId). This is an explicit deviation from the Phase 16/18/19/20/21 module template per CONTEXT §Decisions — MOD-03 states "queue.ts only if the module owns queues".

    ## Queue Consumed

    **None.** See above.

    ## Invariants

    1. **Envelope carries correlationId at root.** Every frame written to `fastify.jobBroadcaster.emit(jobId, envelope)` has `envelope.correlationId` populated — either from ALS (via `readAls`) or from a fallback `randomUUID()`. Never undefined. Enforced by `wsEnvelopeSchema.safeParse` — malformed envelopes are dropped before reaching the broadcaster. Proven in `correlation.spec.ts [TRACE-06]`.
    2. **safeParse failure drops frame + emits ws.frame.dropped + never crashes connection.** The subscriber wraps every candidate in `wsEnvelopeSchema.safeParse`. On failure: `logger.warn({err, candidate}, 'ws.frame.dropped')`, `emit.frameDropped(jobId, {reason:'safeParse-failed', zodError})`, `return` (drop). Live WS listeners see no frame. Ring buffer does not accumulate malformed data. Proven in `envelope.spec.ts` + `subscriber.spec.ts`.
    3. **Ring-buffer replay returns last <=200 envelopes on reconnect.** `JobBroadcaster.subscribe(jobId, handler)` replays buffered envelopes in emission order THEN streams live events. `MAX_BUFFER=200`. Proven in `subscriber.spec.ts [SC2 ring-buffer replay]`.
    4. **Producer-side bus emit uses `.parse()` fail-loud; consumer-side envelope build uses `.safeParse()` fail-soft.** TypedBus.emit (server/bus/bus.ts) does `.parse(payload)` — schema drift is a bug to catch immediately. The streaming subscriber does `.safeParse(candidate)` on the envelope — allows envelope-builder bugs (e.g. missing ALS correlationId) to degrade gracefully instead of crashing the bus. Documented in Pitfall 5.
    5. **No producer outside streaming module may call JobBroadcaster.emit directly.** Bus subscription is the ONLY source of broadcaster frames. Enforced structurally by `lifecycle-ownership.spec.ts [Phase 22 SC2]` which asserts `count('this.jobBroadcaster?.emit(') == 0` in `server/jobs/job-service.ts`. (SC2 non-violation: `this.jobBroadcaster!.cleanup(job.id)` is kept at job-service line ~479 for buffer lifecycle — see §Non-Goals.)

    ## Non-Goals

    - **DevicePreviewManager envelope wrapping.** Binary base64 frames don't fit the Zod envelope shape. Deferred to Phase 29 WEB-03 if/when the web client wants structured metadata alongside binary frames. Phase 22 leaves DevicePreviewManager internals unchanged; the plugin continues to decorate `fastify.devicePreview` and expose the `/ws/devices/:id/preview` route with binary-frame wire shape.
    - **Sharing wsEnvelopeSchema with the web client via generated artifact.** Phase 29 WEB-03 will expose the strict envelope schema to web via the same contract pipeline Phase 17 established. Phase 22 ships the server-side schema only; web consumers that parse WS frames today continue with their existing logic.
    - **WS replay semantics beyond 200-message ring buffer.** Server-side persistent replay (events-table query for missed frames on reconnect) is out of scope. Ring buffer is sufficient for reconnect within the 5-second job-finish window.
    - **Exactly-once delivery.** WebSocket fan-out is lossy-by-design (stale connections drop messages). Idempotency is NOT tested in Phase 22 — first v3.0 phase without an idempotency spec. Justified because the module owns no queue + WS semantics don't require it.
    - **SC2 non-violation — `this.jobBroadcaster!.cleanup(job.id)` call KEPT at `server/jobs/job-service.ts` line ~479.** Buffer lifecycle (5-second delay after `executeJob` resolves/rejects frees the ring buffer) is admissible per SC2's strict reading ("no producer calls broadcaster.EMIT directly" — cleanup is not emit). Phase 23 Jobs Keystone may replace with a `job.cleanup.requested` event when it rewrites `executeJob`. `lifecycle-ownership.spec.ts` asserts `count('this.jobBroadcaster!.cleanup(') == 1` — anything more is a regression.
    - **6TH SAMPLE POINT — Phase 27+ CONSOLIDATION TRIGGER STILL OPEN.** The `persistEnvelope` middleware at `server/streaming/internal/module.ts` lines ~80-110 is the 6th verbatim copy of the same pattern (Phase 16 hooks + Phase 18 lifecycle + Phase 19 reporting + Phase 20 pool + Phase 21 artifacts + Phase 22 streaming). Phase 27+ extracts to `server/bus/persist-envelope.ts`. Do NOT consolidate in Phase 22 — scope creep.

    ## Dependencies

    Plugin `{ name: 'websocket-plugin', dependencies: ['config', 'auth', 'pool-plugin', 'event-bus', 'db'] }` (5 entries — matches Phase 21 artifacts canonical shape).

    - `config` — for `fastify.config.auth.enabled` gate on WS routes.
    - `auth` — for `fastify.authService.validateKey(token)` on WS route auth check.
    - `pool-plugin` — for `fastify.pool.getDevice(deviceId)` access in device-preview path (retained from Phase 17 DEBT-01 fix).
    - `event-bus` — `createStreamingModule` reads `fastify.jobsModule.bus` in onReady hook for the 3 bus subscriptions.
    - `db` — `persistEnvelope` middleware writes to `events` table (short-circuits on `persisted:false` for `ws.frame.dropped` but declared for future-proof per RESEARCH §Plugin Dependencies).

    Plugin NAME `'websocket-plugin'` preserved for back-compat — jobs-plugin + pipelines-plugin declare this as a dependency; `plugin-order.spec.ts` has positional assertions against the name. Renaming would be 5+ cross-module file changes; rejected per RESEARCH §Plugin Name Question.

    ## Runnable Example

    ```typescript
    // Emit — ONLY from inside server/streaming/events.ts (EVENTS-08 rule);
    // other code calls fastify.streamingModule.emit.frameDropped(...).
    import { createStreamingModule } from 'server/streaming/index.js';

    // Typical mounting (see server/streaming/plugin.ts):
    const module = createStreamingModule({ fastify, db, config, logger });
    fastify.decorate('streamingModule', module);
    fastify.addHook('onReady', async () => { await module.registerSubscribers(); });

    // Emit ws.frame.dropped (from inside the module; subscribers rarely need this):
    module.emit.frameDropped(jobId, {
      jobId,
      eventType: 'log',
      reason: 'safeParse-failed',
      zodError: 'correlationId missing from envelope candidate',
    });

    // Subscribe to broadcaster (from WS route handler inside plugin.ts):
    const unsub = module.jobBroadcaster.subscribe(jobId, (envelope) => {
      // envelope is WsEnvelope = {type, correlationId, v:1, ts, payload}
      socket.send(JSON.stringify(envelope));
    });

    // Example envelope on the wire (JSON):
    // {
    //   "type": "log",
    //   "correlationId": "7f4c3e90-2c8f-47c1-9c8a-3d3c8f7e4a12",
    //   "v": 1,
    //   "ts": "2026-04-22T19:47:12.138Z",
    //   "payload": { "line": "Running flow: login.yaml", "stream": "stdout" }
    // }
    ```

    **TypeScript snippet typecheck:** this runnable example compiles against the current barrel (Plan 22-05 output). If Phase 23+ reshapes the factory signature, this example block MUST be updated — MOD-09 (Phase 27 scope) will enforce snippet typecheck in CI.
    ```
  </action>
  <verify>
    <automated>test -f server/streaming/MODULE.md && grep -c "^## " server/streaming/MODULE.md | grep -q "^9$" && grep -c "## Purpose\|## Public API\|## Events Emitted\|## Events Consumed\|## Queue Produced\|## Queue Consumed\|## Invariants\|## Non-Goals\|## Dependencies" server/streaming/MODULE.md | grep -qE "^[9-9]$|^[1-9][0-9]+$" && grep -q "## Runnable Example" server/streaming/MODULE.md && grep -q "6TH SAMPLE POINT" server/streaming/MODULE.md && grep -q "SC2 non-violation" server/streaming/MODULE.md && grep -q "None" server/streaming/MODULE.md && grep -q "fff0592e-b92c-5221-a40a-d10a141f0158" server/streaming/MODULE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f server/streaming/MODULE.md` exits 0
    - `grep -c "^## " server/streaming/MODULE.md` == 9 (exactly 9 H2 sections — Purpose / Public API / Events Emitted / Events Consumed / Queue Produced / Queue Consumed / Invariants / Non-Goals / Dependencies)
    - `grep -c "^## Runnable Example" server/streaming/MODULE.md` == 1 (H3 would be `^### `; but Runnable Example is a 10th H2 — acceptable; the "9 H2" bar counts the required 9 sections; Runnable Example is additive)
    - Actually: `grep -c "^## " server/streaming/MODULE.md` >= 9 (9 canonical + 1 Runnable Example = 10 is also fine)
    - All 9 required section titles present (exact match): Purpose / Public API / Events Emitted / Events Consumed / Queue Produced / Queue Consumed / Invariants / Non-Goals / Dependencies
    - Queue Produced section says "None"
    - Queue Consumed section says "None"
    - `grep -c "6TH SAMPLE POINT" server/streaming/MODULE.md` >= 1 (Non-Goals consolidation trigger)
    - `grep -c "SC2 non-violation" server/streaming/MODULE.md` >= 1 (Non-Goals cleanup kept)
    - `grep -c "fff0592e-b92c-5221-a40a-d10a141f0158" server/streaming/MODULE.md` >= 1 (real v5 UUID embedded)
    - `grep -c "TRACE-06" server/streaming/MODULE.md` >= 2 (Purpose + Invariants)
    - `grep -c "Pitfall" server/streaming/MODULE.md` >= 2 (references to RESEARCH pitfalls)
    - Runnable Example contains a ```typescript``` code fence — `grep -c "^\\`\\`\\`typescript" server/streaming/MODULE.md` >= 1
    - `wc -l server/streaming/MODULE.md` reports 150-300 lines (full body, not a stub; not bloat)
    - Plan 22-05 attribution — `grep -c "Phase 22" server/streaming/MODULE.md` >= 1
  </acceptance_criteria>
  <done>
    MODULE.md has full 9-section canonical body + Runnable Example. Queue Produced/Consumed = None documented. 6TH SAMPLE POINT + SC2 non-violation listed in Non-Goals. MOD-01 contract satisfied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.2: Expand server/streaming/index.ts from stub to full barrel</name>
  <files>server/streaming/index.ts</files>
  <read_first>
    - server/streaming/index.ts (current — Plan 22-00 2-line stub)
    - server/artifacts/index.ts (Phase 21 canonical — 79 lines multi-re-export barrel)
    - server/pool/index.ts (Phase 20 canonical — multi-surface barrel)
    - server/streaming/internal/job-broadcaster.ts (Plan 22-02 output)
    - server/streaming/internal/device-preview.ts (Plan 22-02 output)
    - server/streaming/internal/ws-schemas.ts (Plan 22-02 output — moved under internal/)
    - server/streaming/internal/types.ts (Plan 22-02 output — moved under internal/)
    - server/streaming/events.ts (Plan 22-01 output)
    - server/streaming/plugin.ts (Plan 22-02 output)
    - server/streaming/internal/module.ts (Plan 22-02 output)
    - .planning/phases/22-streaming-module/22-RESEARCH.md §Wave 5 barrel expansion
  </read_first>
  <action>
    **Overwrite `server/streaming/index.ts`** with the full barrel. Stub from Plan 22-00 is replaced.

    ```typescript
    /**
     * Phase 22 / Plan 22-05 — Streaming module public barrel (MOD-02).
     *
     * This is the ONLY import surface for consumers outside `server/streaming/`.
     * `dependency-cruiser` rule `no-deep-imports-into-streaming-internal` (added
     * to `.dependency-cruiser.cjs` in Plan 22-00) enforces the boundary
     * structurally — imports into `server/streaming/internal/**` from outside
     * the module fail CI.
     *
     * Exports in this file are PUBLIC and subject to semver-ish stability.
     * Anything under `./internal/` is module-private and may change without notice.
     *
     * NOTE on MOD-02 'strict 1-line internal/' convention: Phase 18/19/20/21
     * artifacts/pool/reporting/lifecycle modules had top-level service files
     * (ArtifactService at server/artifacts/artifact-service.ts etc.) so their
     * barrels re-exported from top-level, with ONE internal/module.js
     * re-export for the factory (the strict-1-line rule). Phase 22 has NO
     * top-level service files — JobBroadcaster + DevicePreviewManager + types
     * + ws-schemas all live UNDER internal/ (Plan 22-02 git mv). The barrel
     * therefore has MULTIPLE internal/* re-exports for back-compat surfaces.
     * This is ALLOWED per the dep-cruiser rule (the rule fires only on
     * `from: { pathNot: '^server/streaming/' }` outside imports; barrel-internal
     * re-exports are WITHIN the module scope). The strict-1-line invariant
     * relaxes to 'ONE internal/module.js re-export for the MOD-06 factory
     * surface' (see the `createStreamingModule` export line below).
     */

    // Fastify plugin (default export — matches Phase 15 substrate convention).
    export { default as streamingPlugin } from './plugin.js';

    // MOD-06 factory surface. ONE `internal/module.js` re-export line (strict).
    export { createStreamingModule } from './internal/module.js';
    export type { StreamingModule, CreateStreamingModuleDeps } from './internal/module.js';

    // Back-compat class surfaces (preserved for existing consumers — Fastify
    // decorators fastify.jobBroadcaster + fastify.devicePreview are still read
    // by server/jobs/job-service.ts (buffer cleanup call + devicePreview start/stop)
    // until Phase 23 Jobs Keystone + Phase 29 Web Refactor remove).
    export { JobBroadcaster } from './internal/job-broadcaster.js';
    export { DevicePreviewManager } from './internal/device-preview.js';

    // Envelope schema + type (Phase 22 TRACE-06 contract; Phase 29 WEB-03
    // will share with web client via generated contract artifact).
    export { wsEnvelopeSchema } from './internal/ws-schemas.js';
    export type { WsEnvelope } from './internal/ws-schemas.js';

    // Back-compat WS message types (pre-Phase-22 JobMessage shape and data
    // sub-types). New consumers should use WsEnvelope + narrowed payload via
    // discriminated union. Legacy back-compat until Phase 29 WEB-03.
    export type {
      WsMessageType,
      JobMessage,
      DevicePreviewMessage,
      LogData,
      StepData,
      MetricsData,
      StatusData,
      ArtifactType,
    } from './internal/types.js';

    // Events surface (MOD-03 — Plan 22-01).
    export {
      streamingRegistry,
      STREAMING_EVENT_NAMES,
      STREAMING_AGGREGATE_ID,
      makeStreamingEmitters,
      wsFrameDroppedPayload,
    } from './events.js';
    export type {
      StreamingRegistry,
      StreamingEmitters,
      StreamingEventName,
    } from './events.js';
    ```

    **DO NOT** add a queue surface export (streaming owns no queue). DO NOT add MOD-01 runnable example code here (that's MODULE.md scope).
  </action>
  <verify>
    <automated>test -f server/streaming/index.ts && grep -q "export { default as streamingPlugin }" server/streaming/index.ts && grep -q "export { createStreamingModule }" server/streaming/index.ts && grep -q "export { JobBroadcaster }" server/streaming/index.ts && grep -q "export { DevicePreviewManager }" server/streaming/index.ts && grep -q "export { wsEnvelopeSchema }" server/streaming/index.ts && grep -q "export type { WsEnvelope }" server/streaming/index.ts && grep -q "export { streamingRegistry" server/streaming/index.ts && grep -q "export { STREAMING_EVENT_NAMES" server/streaming/index.ts && grep -c "from './internal/module" server/streaming/index.ts | grep -qE "^[1-2]$" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f server/streaming/index.ts` exits 0
    - `grep -c "from './plugin.js'" server/streaming/index.ts` >= 1 (plugin default export)
    - `grep -c "from './internal/module" server/streaming/index.ts` >= 1 (factory re-export; can be 1 or 2 — runtime + type)
    - `grep -c "from './internal/job-broadcaster" server/streaming/index.ts` >= 1
    - `grep -c "from './internal/device-preview" server/streaming/index.ts` >= 1
    - `grep -c "from './internal/ws-schemas" server/streaming/index.ts` >= 1
    - `grep -c "from './internal/types" server/streaming/index.ts` >= 1
    - `grep -c "from './events.js'" server/streaming/index.ts` >= 2 (runtime + type)
    - All expected named exports present: streamingPlugin, createStreamingModule, StreamingModule (type), CreateStreamingModuleDeps (type), JobBroadcaster, DevicePreviewManager, wsEnvelopeSchema, WsEnvelope (type), WsMessageType (type), JobMessage (type), DevicePreviewMessage (type), LogData (type), StepData (type), MetricsData (type), StatusData (type), ArtifactType (type), streamingRegistry, STREAMING_EVENT_NAMES, STREAMING_AGGREGATE_ID, makeStreamingEmitters, wsFrameDroppedPayload, StreamingRegistry (type), StreamingEmitters (type), StreamingEventName (type).
    - `wc -l server/streaming/index.ts` reports 50-100 lines (full barrel; larger than Phase 21's 79 due to more back-compat surfaces since there are no top-level service files to import from)
    - `npx tsc --noEmit` exits 0 (all re-exports resolve; no orphan exports)
    - `npm run dep-check` exits 0 (the re-exports from internal/ inside index.ts do NOT fire the rule because they are WITHIN server/streaming/)
    - `grep -c "Plan 22-05" server/streaming/index.ts` >= 1 (attribution)
    - `grep -c "MOD-02" server/streaming/index.ts` >= 1 (invariant documented in header)
  </acceptance_criteria>
  <done>
    index.ts full barrel — streamingPlugin + createStreamingModule + 2 back-compat classes + envelope surface + legacy types + events surface. All re-exports typed; dep-check green (re-exports inside the module are allowed). Consumers outside server/streaming/ have typed access to all public surfaces.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.3: Rename 5 test files from .test.ts to .spec.ts via git mv (MOD-04)</name>
  <files>server/streaming/__tests__/adapter-factory.spec.ts, server/streaming/__tests__/android-preview-adapter.spec.ts, server/streaming/__tests__/device-preview.spec.ts, server/streaming/__tests__/ios-preview-adapter.spec.ts, server/streaming/__tests__/job-broadcaster.spec.ts</files>
  <read_first>
    - server/streaming/__tests__/adapter-factory.test.ts (current pre-rename)
    - server/streaming/__tests__/android-preview-adapter.test.ts (current pre-rename)
    - server/streaming/__tests__/device-preview.test.ts (current pre-rename)
    - server/streaming/__tests__/ios-preview-adapter.test.ts (current pre-rename)
    - server/streaming/__tests__/job-broadcaster.test.ts (current pre-rename — may need content edit: makeMessage helper if present returns JobMessage, needs update to WsEnvelope shape)
    - .planning/phases/22-streaming-module/22-RESEARCH.md §Renames (MOD-04; 5 files)
  </read_first>
  <action>
    **Execute 5 git mv operations with 100% similarity** to preserve blame history.

    **Step 1 — Verify source files exist:**
    ```bash
    test -f server/streaming/__tests__/adapter-factory.test.ts
    test -f server/streaming/__tests__/android-preview-adapter.test.ts
    test -f server/streaming/__tests__/device-preview.test.ts
    test -f server/streaming/__tests__/ios-preview-adapter.test.ts
    test -f server/streaming/__tests__/job-broadcaster.test.ts
    ```

    **Step 2 — Execute renames:**
    ```bash
    git mv server/streaming/__tests__/adapter-factory.test.ts server/streaming/__tests__/adapter-factory.spec.ts
    git mv server/streaming/__tests__/android-preview-adapter.test.ts server/streaming/__tests__/android-preview-adapter.spec.ts
    git mv server/streaming/__tests__/device-preview.test.ts server/streaming/__tests__/device-preview.spec.ts
    git mv server/streaming/__tests__/ios-preview-adapter.test.ts server/streaming/__tests__/ios-preview-adapter.spec.ts
    git mv server/streaming/__tests__/job-broadcaster.test.ts server/streaming/__tests__/job-broadcaster.spec.ts
    ```

    **Step 3 — Update internal imports in each renamed file.**

    Because Task 2.1 (Plan 22-02) moved source files from top-level to `internal/`, test files that import from `'../job-broadcaster.js'`, `'../device-preview.js'`, `'../types.js'`, `'../ws-schemas.js'`, `'../adapters/foo.js'` now need to resolve to the internal/ paths. Check each renamed file:

    ```bash
    grep -n "from '\\.\\./\\(job-broadcaster\\|device-preview\\|types\\|ws-schemas\\|adapters\\)" server/streaming/__tests__/*.spec.ts
    ```

    For each match, rewrite `from '../job-broadcaster.js'` to `from '../internal/job-broadcaster.js'` (and same for the other 4 names). Tests don't count as "outside server/streaming/" for dep-cruiser — the `__tests__/` directory is inside `server/streaming/` so the rule `pathNot: '^server/streaming/'` does NOT fire on test imports.

    **Step 4 — Content update for job-broadcaster.spec.ts (Pitfall 1 mitigation).**

    Inspect the renamed `job-broadcaster.spec.ts`. If it contains a helper like `makeMessage()` returning `JobMessage` shape (e.g. `{ type, data, timestamp }`), UPDATE to return `WsEnvelope` shape (`{ type, correlationId: randomUUID(), v: 1, ts: new Date().toISOString(), payload: data }`) because the JobBroadcaster signature now requires WsEnvelope (Plan 22-02 Task 2.1 tightened).

    If the test file uses `broadcaster.emit(jobId, { type, data, timestamp })` directly, update to `broadcaster.emit(jobId, { type, correlationId: randomUUID(), v: 1, ts: ..., payload: data })`. Update `broadcaster.subscribe` handler signatures from `(msg: JobMessage)` to `(envelope: WsEnvelope)`.

    If the file has zero `JobMessage` references and only tests the ring-buffer behavior abstractly (emit + subscribe + cleanup + getBufferSize), no content edit is needed — just the rename.

    **Step 5 — Run each renamed spec to verify it still passes:**
    ```bash
    npx vitest run server/streaming/__tests__/adapter-factory.spec.ts
    npx vitest run server/streaming/__tests__/android-preview-adapter.spec.ts
    npx vitest run server/streaming/__tests__/device-preview.spec.ts
    npx vitest run server/streaming/__tests__/ios-preview-adapter.spec.ts
    npx vitest run server/streaming/__tests__/job-broadcaster.spec.ts
    ```

    **Step 6 — Confirm no .test.ts remains:**
    ```bash
    find server/streaming/__tests__ -name '*.test.ts' | wc -l
    # Expected: 0
    ```
  </action>
  <verify>
    <automated>test -f server/streaming/__tests__/adapter-factory.spec.ts && test -f server/streaming/__tests__/android-preview-adapter.spec.ts && test -f server/streaming/__tests__/device-preview.spec.ts && test -f server/streaming/__tests__/ios-preview-adapter.spec.ts && test -f server/streaming/__tests__/job-broadcaster.spec.ts && test ! -f server/streaming/__tests__/adapter-factory.test.ts && test ! -f server/streaming/__tests__/android-preview-adapter.test.ts && test ! -f server/streaming/__tests__/device-preview.test.ts && test ! -f server/streaming/__tests__/ios-preview-adapter.test.ts && test ! -f server/streaming/__tests__/job-broadcaster.test.ts && npx vitest run server/streaming/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - All 5 `.spec.ts` files exist at new paths
    - All 5 `.test.ts` paths NO LONGER EXIST — `find server/streaming/__tests__ -name '*.test.ts' | wc -l` == 0
    - Git history preserved — `git log --follow --oneline server/streaming/__tests__/job-broadcaster.spec.ts | wc -l` > 1
    - `npx vitest run server/streaming/__tests__/` exits 0 (all 10 specs pass: 5 renamed + 5 new from plans 22-00..22-04)
    - `grep -r "from '../job-broadcaster.js'" server/streaming/__tests__/` returns 0 matches (imports updated to internal/)
    - `grep -r "from '../device-preview.js'" server/streaming/__tests__/` returns 0 matches
    - `grep -r "from '../types.js'" server/streaming/__tests__/` returns 0 matches
    - `grep -r "from '../ws-schemas.js'" server/streaming/__tests__/` returns 0 matches
    - `grep -c "from '../internal/" server/streaming/__tests__/*.spec.ts | awk -F: '{s+=$2} END {print s}'` >= 5 (at least some tests import from internal/)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    5 test files renamed .test.ts → .spec.ts via git mv (blame preserved); internal imports updated from ../ to ../internal/. MOD-04 contract complete. job-broadcaster.spec.ts content updated for WsEnvelope signature if helper existed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.4: Nyquist delta gate — confirm coverage delta >= -2pp vs Phase 15 baseline</name>
  <files>.planning/nyquist-baseline.json</files>
  <read_first>
    - .planning/nyquist-baseline.json (MUST NOT be modified; committed since Phase 15 commit 55ff8ac)
    - .planning/phases/22-streaming-module/22-RESEARCH.md §Wave 5 Nyquist gate (delta expected <= +1pp)
    - .planning/phases/21-artifacts-module/21-06-PLAN.md Nyquist gate task (precedent)
  </read_first>
  <action>
    **Run Nyquist delta check** — read-only gate; this task does NOT modify any file.

    **Step 1 — Verify baseline NOT modified:**
    ```bash
    git diff .planning/nyquist-baseline.json
    # Should output nothing (file unchanged since Phase 15 commit)
    git log --oneline -1 .planning/nyquist-baseline.json
    # Should show the Phase 15 Plan 15-09 commit (55ff8ac or whatever the exact SHA is)
    ```

    **Step 2 — Run coverage:**
    ```bash
    npm run coverage
    ```
    This produces `coverage/coverage-summary.json` (v8 coverage output). May take 2-5 minutes depending on host; runs the full vitest suite with coverage instrumentation.

    **Step 3 — Run Nyquist check:**
    ```bash
    npm run nyquist:check
    ```
    Exits 0 if coverage delta >= -2pp vs baseline's `lines.pct` field (48.29% per RESEARCH — confirm at runtime). Exits non-zero with structured error output if below -2pp.

    **Step 4 — Record the delta in Plan 22-05 SUMMARY.md** (created after this task) showing:
    - Baseline pct (from `.planning/nyquist-baseline.json`)
    - Current pct (from `coverage/coverage-summary.json lines.pct`)
    - Delta (current - baseline)
    - Verdict (green if >= -2pp)

    **Step 5 — Verify baseline still unmodified POST-coverage-run:**
    ```bash
    git diff .planning/nyquist-baseline.json
    # Must STILL output nothing — coverage run should not touch baseline
    ```

    **If delta < -2pp:** STOP. Do NOT proceed to Plan 22-06. Investigate which coverage area regressed and either:
    - Add tests to recover coverage (extend Plan 22-03 or add new spec).
    - Document the exception if the regression is truly justified (e.g. deleted dead code).

    Do NOT overwrite the baseline. Phase 15 Plan 15-09 established that the baseline is LOCKED and each phase must clear it. A legitimate baseline update would require its own plan (Phase 30 Test Migration Cleanup handles milestone-level baseline refresh).
  </action>
  <verify>
    <automated>test -f .planning/nyquist-baseline.json && git diff --exit-code .planning/nyquist-baseline.json && npm run nyquist:check</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/nyquist-baseline.json` EXISTS and is UNMODIFIED (git diff --exit-code exits 0)
    - `npm run nyquist:check` exits 0 (delta >= -2pp)
    - Coverage summary file exists at `coverage/coverage-summary.json` after `npm run coverage` runs
    - `git log --oneline -1 -- .planning/nyquist-baseline.json` shows the original Phase 15 commit (exact SHA may differ but should pre-date Phase 22)
    - Delta magnitude reasonable — if delta > +10pp, something is suspicious (Phase 22 adds expected ~800-1100 lines of tests + ~500-700 lines of source; delta in the -1pp to +2pp range is expected; large positive delta suggests baseline regression somewhere else masking)
    - Plan 22-05 SUMMARY.md (authored post-task) records the numeric delta for traceability
  </acceptance_criteria>
  <done>
    Nyquist delta gate PASSED. Baseline unmodified. Phase 22 has not regressed coverage beyond the -2pp threshold. Plan 22-06 phase-close can now run the final administrative sweep.
  </done>
</task>

</tasks>

<verification>
All 4 tasks green:

```bash
# Task 5.1 — MODULE.md full body
grep -c "^## " server/streaming/MODULE.md   # 9 or 10 (9 + Runnable)
grep -q "6TH SAMPLE POINT" server/streaming/MODULE.md
grep -q "SC2 non-violation" server/streaming/MODULE.md

# Task 5.2 — index.ts full barrel
grep -c "export " server/streaming/index.ts   # >= 15 (multiple exports)
npx tsc --noEmit
npm run dep-check

# Task 5.3 — 5 renames
find server/streaming/__tests__ -name '*.test.ts' | wc -l   # == 0
find server/streaming/__tests__ -name '*.spec.ts' | wc -l   # >= 9 (5 renamed + 5 new)
npx vitest run server/streaming/__tests__/   # all pass

# Task 5.4 — Nyquist gate
git diff --exit-code .planning/nyquist-baseline.json   # exits 0
npm run nyquist:check   # exits 0
```
</verification>

<success_criteria>
- MODULE.md has 9 canonical H2 sections + Runnable Example; Queue Produced/Consumed say "None"; 6TH SAMPLE POINT + SC2 non-violation documented in Non-Goals
- index.ts is a full back-compat barrel exporting streamingPlugin + createStreamingModule + classes + envelope + events surface
- 5 test files renamed .test.ts -> .spec.ts via git mv; blame preserved; imports updated from ../ to ../internal/
- Nyquist coverage delta >= -2pp; baseline unmodified
- `npx tsc --noEmit` green
- `npm run lint` green
- `npm run dep-check` green
- `npx vitest run server/streaming/__tests__/` all passing (5 renamed + 5 new = 10 specs)
- Plan 22-06 (Wave 6 administrative close) can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/22-streaming-module/22-05-SUMMARY.md` per the execute-plan template. Include the Nyquist delta number + baseline/current percentages.
</output>
