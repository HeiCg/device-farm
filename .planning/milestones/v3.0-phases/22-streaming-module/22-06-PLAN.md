---
phase: 22
plan: 06
type: execute
wave: 6
depends_on: [22-05]
files_modified:
  - .planning/phases/22-streaming-module/deferred-items.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TRACE-06]
estimated_minutes: 20
gap_closure: false
must_haves:
  truths:
    - "`.planning/phases/22-streaming-module/deferred-items.md` NEW — catalogs 6-10 deferred items carried forward from Phase 22 close. Inherits Phase 17 exclusions (3 files: routes.test + artifact-routes.test + auth-plugin.test fastify-zod-openapi v5 required-emission bug); Phase 22-specific deferrals: (a) DevicePreviewManager envelope wrapping (CONTEXT §Deferred Ideas; Phase 29 WEB-03); (b) shared wsEnvelopeSchema web-client artifact (Phase 29 WEB-03); (c) ring-buffer replay beyond 200 messages (out of scope); (d) SC2 stricter reading — replace this.jobBroadcaster!.cleanup with job.cleanup.requested event (Phase 23 Jobs Keystone scope); (e) 6TH SAMPLE POINT persistEnvelope consolidation (Phase 27+); (f) `import type { JobBroadcaster } from '../streaming/internal/job-broadcaster.js'` in server/jobs/job-service.ts — technically a deep import across modules, tolerated because dep-cruiser excludes type-only imports by default + Phase 23 Jobs Keystone will remove entirely."
    - "`.planning/STATE.md` UPDATED — Phase 22 entry changes from 'Not started' to 'CLOSED 2026-04-22' (or today's date). Increments current_plan and completed_plans counts per Phase 15-21 format (Phase 21 closed at completed_plans=57 = 50 + 7; Phase 22 adds 7 plans so new total = 64)."
    - "`.planning/ROADMAP.md` UPDATED — Phase 22 row in §Progress table changes from 'Not started' to 'Complete 2026-04-22'. Phase 22 section in §Phase Details adds `Plans: 7 plans across 7 waves` line (similar to Phase 20's `Plans: 7 plans across 7 waves`) and lists all 7 plans with ✅ marks + completion dates in the same format as Phases 20/21."
    - "Full-suite green sweep: `npm test` runs with the inherited Phase 17 exclusions (3 pre-existing failing files); ZERO new Phase-22-attributable failures. `npm run lint` clean. `npx tsc --noEmit` inherits baseline 6 Phase 15 Map-vs-RequestContext errors; ZERO new errors from Phase 22 (grep-verify: count pre-existing errors on a stashed main branch, then count on the Phase 22 branch; difference MUST be zero)."
    - "`npm run dep-check` exits 0 — no new dep-cruiser violations. Phase 22 added rule 6 (no-deep-imports-into-streaming-internal); on the committed codebase NO file outside server/streaming/ imports from server/streaming/internal/ (the type imports in server/jobs/job-service.ts are excluded by the dep-cruiser tsPreCompilationDeps + type-only exclusion; confirmed green in Plan 22-02 Task 2.6)."
    - "`.planning/nyquist-baseline.json` UNCHANGED — `diff -s` against any backup MUST report 'Files are identical' OR equivalently `git diff .planning/nyquist-baseline.json` produces no output. Plan 22-05 Task 5.4 already verified; this plan re-verifies as the final gate."
    - "Commit message: `docs(phase-22): complete phase execution` (format matches Phase 21 precedent at commit da7d436). Includes all 3 files_modified in this plan + any trailing close-out changes from prior plans not yet committed."
    - "Post-close verification: `git log --oneline HEAD~10..HEAD` shows a sequence of `docs/feat(22-XX)` commits per-plan from Plan 22-00 through Plan 22-06, ending with the phase-close commit. Each plan generated at minimum one commit; total Phase 22 commits approximate 10-15 (some plans like Plan 22-02 generate multiple commits for surgical work)."
  artifacts:
    - path: ".planning/phases/22-streaming-module/deferred-items.md"
      provides: "Catalog of Phase 22 close-out deferrals + inherited Phase 17 exclusions"
      contains: "DevicePreviewManager envelope wrapping"
    - path: ".planning/STATE.md"
      provides: "State.md updated with Phase 22 CLOSED entry + incremented plan counts"
      contains: "Phase 22"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap updated with Phase 22 Complete status + 7-plan list"
      contains: "Phase 22"
  key_links:
    - from: ".planning/STATE.md Phase 22 entry"
      to: "Phase 22 complete + frozen state"
      via: "completed_plans count incremented from Phase 21's 57 to 64; current phase advances to 23"
      pattern: "Phase 22.*Complete\\|Phase 22.*CLOSED"
    - from: ".planning/ROADMAP.md §Phase 22 section"
      to: "Phase 22 plan enumeration"
      via: "Adds Plans: line + 7-plan bullet list with completion dates"
      pattern: "- \\[x\\] 22-"
    - from: "deferred-items.md entries"
      to: "Future phase scope"
      via: "Each deferred item names the phase that will address it (Phase 23/27/29)"
      pattern: "Phase (23|27|29)"
---

<objective>
Wave 6 plan — administrative phase close. Plans 22-00 through 22-05 deposited all functional substance. Plan 22-06 performs the 10-step sweep that marks Phase 22 as CLOSED in project state:

1. Create `deferred-items.md` catalog of open-but-explicitly-deferred items that Phase 22 did not address (to ensure they don't get lost in future phases).
2. Run the full test suite + lint + typecheck to confirm green baseline.
3. Verify dep-cruiser + Nyquist baseline still green (re-verify Plan 22-05 Task 5.4 gate).
4. Update `.planning/STATE.md` — Phase 22 Complete entry; increment plan counts.
5. Update `.planning/ROADMAP.md` — Phase 22 row to Complete; list 7 plans with dates.
6. Final commit: `docs(phase-22): complete phase execution`.

This matches Phase 21 Plan 21-06 structure verbatim (10-step sweep). No source code changes. No new tests. Pure administrative close.

Purpose: Phase 22 transitions from IN-PROGRESS to CLOSED state. Subsequent phases (Phase 23 Jobs Keystone) can reference Phase 22's completion as a dependency. STATE.md + ROADMAP.md become the single source of truth that `/gsd:plan-phase 23` reads when it boots its planner context.

Sequential — depends on Plan 22-05 landing. No downstream plans in Phase 22.

Output: 1 NEW file (`deferred-items.md`); 2 EDITED files (STATE.md, ROADMAP.md); 1 git commit closing the phase.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-streaming-module/22-CONTEXT.md
@.planning/phases/22-streaming-module/22-RESEARCH.md
@.planning/phases/22-streaming-module/22-VALIDATION.md
@.planning/phases/22-streaming-module/22-05-PLAN.md
@.planning/phases/21-artifacts-module/21-06-PLAN.md
@.planning/phases/21-artifacts-module/deferred-items.md
@.planning/phases/20-pool-module-devices/deferred-items.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/nyquist-baseline.json

<interfaces>
<!-- Phase 21 Plan 21-06 structure (to mirror) -->

Phase 21 deferred-items.md structure (~3-8 entries; inherits Phase 17 exclusions + Phase 21-specific deferrals).

Phase 22 deferred-items.md expected entries (6-10):

Inherited from Phase 17 (pre-existing test failures — NOT introduced by Phase 22):
1. `server/api/__tests__/routes.test.ts` — fastify-zod-openapi v5 `required`-emission bug.
2. `server/artifacts/__tests__/artifact-routes.test.ts` — same Phase 17 bug.
3. `server/auth/__tests__/auth-plugin.test.ts` — same Phase 17 bug.

Inherited from Phase 15 (pre-existing tsc errors):
4. 6 Phase 15 Map-vs-RequestContext tsc errors — deferred resolution until the request-context migration lands (not Phase 22 scope).

Phase 22-specific deferrals:
5. DevicePreviewManager envelope wrapping (Phase 29 WEB-03).
6. Shared wsEnvelopeSchema web-client artifact (Phase 29 WEB-03).
7. Ring-buffer replay beyond 200 messages (out of scope).
8. SC2 stricter reading — replace `this.jobBroadcaster!.cleanup` at job-service.ts line ~479 with a `job.cleanup.requested` bus event (Phase 23 Jobs Keystone scope).
9. 6TH SAMPLE POINT — `persistEnvelope` middleware consolidation to `server/bus/persist-envelope.ts` (Phase 27+ scope).
10. `import type { JobBroadcaster }` + `import type { DevicePreviewManager }` in server/jobs/job-service.ts — type-only deep imports permitted today; Phase 23 Jobs Keystone will remove entirely.

STATE.md current Phase 22 row (before close):
```
| 22. Streaming Module | v3.0 | 0/TBD | Not started | - |
```

After close:
```
| 22. Streaming Module | v3.0 | 7/7 | Complete | 2026-04-22 |
```

ROADMAP.md current Phase 22 section (before close; line ~208):
```markdown
### Phase 22: Streaming Module
**Goal**: Fill the `JobBroadcaster` ring buffer from bus subscriptions instead of direct producer calls. Preserve WS replay semantics while routing every WebSocket frame through a Zod-validated envelope that carries `correlationId`.
**Depends on**: Phase 21 (artifacts emits via bus)
**Requirements**: TRACE-06
**Success Criteria** (what must be TRUE):
  [... 4 SCs ...]
Plans: TBD
```

After close (matches Phase 21 format):
```markdown
### Phase 22: Streaming Module
**Goal**: [same]
**Depends on**: Phase 21 (artifacts emits via bus)
**Requirements**: TRACE-06
**Success Criteria** (what must be TRUE):
  [... 4 SCs ...]
**Plans** (7 plans across 7 waves):
- [x] 22-00-PLAN.md — Wave 0: substrate (streaming/events stub + internal/module throw-stub + MODULE.md placeholder + index.ts stub + jobs/events 3-key extension + dep-cruiser 6th rule + fixture + spec extension) ✅ 2026-04-22
- [x] 22-01-PLAN.md — Wave 1: events + envelope + jobs bridgehead body (streaming/events full body + wsEnvelopeSchema strict + jobs/events 3 new payload schemas + registry entries + 6-helper makeJobsEmitters) ✅ 2026-04-22
- [x] 22-02-PLAN.md — Wave 2: factory + plugin rewire + 7-callsite surgery (git mv into internal/ + createStreamingModule full body + plugin.ts thin wirer + websocket-plugin.ts deletion + index.ts import + 7 job-service.ts callsites inverted + module.spec) ✅ 2026-04-22
- [x] 22-03-PLAN.md — Wave 3: DB-gated proofs (subscriber.spec + correlation.spec + envelope.spec — SC1/SC2/SC3/TRACE-06 all covered) ✅ 2026-04-22
- [x] 22-04-PLAN.md — Wave 4: lifecycle grep-guard + plugin-order extension + substring-bug fix (SC2 structural + wbIndex helper + 4 new Phase 22 positional/structural assertions) ✅ 2026-04-22
- [x] 22-05-PLAN.md — Wave 5: MODULE.md 9 H2 sections + index.ts full barrel + 5 MOD-04 renames + Nyquist delta gate ✅ 2026-04-22
- [x] 22-06-PLAN.md — Wave 6: deferred-items catalog + STATE.md + ROADMAP.md + full-suite green sweep + commit ✅ 2026-04-22
```

Progress table update — Phase 22 row from "Not started" to "Complete 2026-04-22":
```
| 22. Streaming Module | v3.0 | 7/7 | Complete | 2026-04-22 |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 6.1: Create .planning/phases/22-streaming-module/deferred-items.md</name>
  <files>.planning/phases/22-streaming-module/deferred-items.md</files>
  <read_first>
    - .planning/phases/21-artifacts-module/deferred-items.md (Phase 21 canonical — shape to mirror)
    - .planning/phases/20-pool-module-devices/deferred-items.md (Phase 20 canonical)
    - .planning/phases/22-streaming-module/22-CONTEXT.md §Deferred Ideas
    - .planning/phases/22-streaming-module/22-RESEARCH.md §Pitfall 6 (SC2 non-violation) + §Pitfall 11 (6TH SAMPLE POINT)
    - .planning/phases/22-streaming-module/22-05-PLAN.md (MODULE.md §Non-Goals)
  </read_first>
  <action>
    **Create new file `.planning/phases/22-streaming-module/deferred-items.md`** with the full catalog below.

    ```markdown
    # Phase 22 Streaming Module — Deferred Items Catalog

    **Phase closed:** 2026-04-22
    **Inherited from:** Phase 15 (tsc errors), Phase 17 (test failures); carried forward unchanged.
    **New deferrals from Phase 22:** 6 items targeting Phase 23 / 27 / 29.

    ---

    ## Inherited (pre-existing, NOT introduced by Phase 22)

    ### DEFERRED-17-A: fastify-zod-openapi v5 `required`-emission bug (3 test files)

    **Files failing:** `server/api/__tests__/routes.test.ts`, `server/artifacts/__tests__/artifact-routes.test.ts`, `server/auth/__tests__/auth-plugin.test.ts`

    **Failure:** fastify-zod-openapi v5 emits `required` fields with array representation instead of object; validator-compiler rejects request bodies that previously validated. Root cause documented in Phase 17 VERIFICATION.md.

    **Resolution planned:** Phase 29 Web Refactor + Phase 30 Test Migration Cleanup. Either upgrade fastify-zod-openapi to v6 (if/when released with bug fix) or swap for `@fastify/zod` / manual OpenAPI emit.

    **Impact on Phase 22:** None. Phase 22 adds zero new HTTP routes; does not touch fastify-zod-openapi surface.

    ### DEFERRED-15-A: 6 Phase 15 Map-vs-RequestContext tsc errors

    **Files:** assorted — `server/bus/helpers.ts`, `server/queue/plugin.ts`, related subscribers.

    **Failure:** TypeScript strict-mode errors related to the ALS store shape migration from Map to plain-object (Phase 15/20 pattern). Functional runtime unaffected (`readAls` helper is dual-shape tolerant).

    **Resolution planned:** Phase 26 (Auth Module) or Phase 30 (Test Migration Cleanup) — whichever lands the final ALS shape cleanup.

    **Impact on Phase 22:** None. All Phase 22 specs use plain-object ALS shape; errors don't increase.

    ---

    ## New Phase 22 deferrals

    ### DEFERRED-22-A: DevicePreviewManager envelope wrapping

    **Context:** Binary base64 frames on `/ws/devices/:id/preview` don't fit the Zod envelope shape. Phase 22 scope was job-channel envelope (TRACE-06 primary requirement); device preview kept its existing wire format unchanged.

    **Resolution planned:** Phase 29 Web Refactor WEB-03 ("Schemas Zod de WS messages compartilhados server↔web"). When the web client migrates to shared Zod schemas, device preview may gain a structured metadata envelope alongside the binary frame OR the binary frame may stay as-is with out-of-band metadata.

    **Impact if not addressed:** Web dev tools can't grep device-preview frames by correlationId (only job-channel frames are trackable). Acceptable trade-off for Phase 22 scope.

    ### DEFERRED-22-B: Shared wsEnvelopeSchema with web client

    **Context:** Phase 22 defined `wsEnvelopeSchema` at `server/streaming/internal/ws-schemas.ts` + exported via barrel. Web client today parses frames with its own ad-hoc logic.

    **Resolution planned:** Phase 29 WEB-03 — either relative imports from server tree OR npm workspace package. Web client calls `wsEnvelopeSchema.safeParse()` on every frame + drops malformed with a structured console warning.

    **Impact if not addressed:** Web client may silently render malformed frames if server ships a buggy envelope builder. Phase 22 server-side safeParse drop path protects clients from ever SEEING malformed frames, so this is a belt-and-suspenders concern only.

    ### DEFERRED-22-C: WS replay semantics beyond 200-message ring buffer

    **Context:** `JobBroadcaster` ring buffer holds max 200 messages/job. Reconnecting clients get the last 200. For long-running jobs (1 hour+ with 10-100 log lines/sec), early history is lost.

    **Resolution planned:** OUT OF SCOPE — v2 requirement territory (OBS-01 / ER-01 in REQUIREMENTS.md). Structured logging + persisted `job.completed` event in events table covers operator debug use cases. Client-side replay is a nice-to-have, not a contract requirement.

    **Impact if not addressed:** None. Users who need full history read the maestro.log artifact file.

    ### DEFERRED-22-D: SC2 stricter reading — replace this.jobBroadcaster!.cleanup with job.cleanup.requested event

    **Context:** `server/jobs/job-service.ts` line ~479 retains a single `this.jobBroadcaster!.cleanup(job.id)` call via `setTimeout(..., 5000)` to free the ring buffer 5s after job finish. SC2 ("no producer calls broadcaster.emit directly") is about EMIT, not cleanup — so strictly admissible. MODULE.md §Non-Goals documents + `lifecycle-ownership.spec.ts` asserts count <= 1.

    **Resolution planned:** Phase 23 Jobs Keystone will rewrite `executeJob` as a saga with an explicit `job.cleanup.requested` bus event. Streaming module subscriber listens → calls `broadcaster.cleanup(jobId)` internally. Zero `jobBroadcaster` references in job-service.ts.

    **Impact if not addressed:** Cosmetic only. SC2 non-violation explicitly documented; grep-guard locks count at 1.

    ### DEFERRED-22-E: 6TH SAMPLE POINT persistEnvelope consolidation

    **Context:** `server/streaming/internal/module.ts` lines ~80-110 contain a verbatim `persistEnvelope(envelope)` middleware copy — the 6th identical implementation across modules (Phase 16 hooks + Phase 18 lifecycle + Phase 19 reporting + Phase 20 pool + Phase 21 artifacts + Phase 22 streaming). Phase 15 ADR-001 foresaw this; consolidation waits for enough samples to inform the shared helper signature.

    **Resolution planned:** Phase 27+ API Aggregator & Events API → extract to `server/bus/persist-envelope.ts`; all 6 module factories call the shared helper with their registry.

    **Impact if not addressed:** 6 copies of ~30 lines each = ~180 LOC of duplication. Refactor risk localized — if persistence policy changes, all 6 need touching. Acceptable given the samples-before-abstraction discipline.

    ### DEFERRED-22-F: Cross-module type imports in server/jobs/job-service.ts

    **Context:** `server/jobs/job-service.ts` retains `import type { JobBroadcaster } from '../streaming/internal/job-broadcaster.js'` + `import type { DevicePreviewManager } from '../streaming/internal/device-preview.js'`. Technically these are deep imports into streaming/internal/ from outside server/streaming/. Dep-cruiser tsPreCompilationDeps option excludes type-only imports by default so the rule does not fire.

    **Resolution planned:** Phase 23 Jobs Keystone removes entirely when it rewrites executeJob to use the streaming module via `fastify.streamingModule.*` decorators only (no direct class references).

    **Impact if not addressed:** Structural coupling between jobs and streaming/internal/. Refactor risk: a breaking change to JobBroadcaster constructor signature would cascade into job-service.ts. Low practical risk since JobBroadcaster signature is stable (post-Phase-22).

    ---

    ## Close-out metadata

    - All deferred items were known BEFORE Phase 22 close — no surprises surfaced during Plan 22-05 or 22-06 execution.
    - Nyquist delta check PASSED (Plan 22-05 Task 5.4); `.planning/nyquist-baseline.json` unchanged.
    - Phase 22 specs: 10 total (5 renamed MOD-04 + events.spec + module.spec + subscriber.spec + correlation.spec + envelope.spec + lifecycle-ownership.spec + plugin-order.spec extension = across 2 directories).
    - 7 plans, 7 waves (0-6).
    - Close date: 2026-04-22.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/22-streaming-module/deferred-items.md && grep -q "DEFERRED-17-A" .planning/phases/22-streaming-module/deferred-items.md && grep -q "DEFERRED-22-A\|DEFERRED-22-B\|DEFERRED-22-C\|DEFERRED-22-D\|DEFERRED-22-E\|DEFERRED-22-F" .planning/phases/22-streaming-module/deferred-items.md && grep -q "6TH SAMPLE POINT" .planning/phases/22-streaming-module/deferred-items.md && grep -q "DevicePreviewManager envelope" .planning/phases/22-streaming-module/deferred-items.md && grep -q "SC2" .planning/phases/22-streaming-module/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/22-streaming-module/deferred-items.md` exits 0
    - `grep -c "^### DEFERRED-" .planning/phases/22-streaming-module/deferred-items.md` >= 6 (3 inherited + 6 Phase 22 = 9; minimum 6 captured)
    - All 6 new Phase 22 deferrals present: DEFERRED-22-A through DEFERRED-22-F
    - At least 2 inherited deferrals present: DEFERRED-17-A (fastify-zod-openapi) + DEFERRED-15-A (Map-vs-RequestContext)
    - `grep -c "Phase 23\\|Phase 27\\|Phase 29" .planning/phases/22-streaming-module/deferred-items.md` >= 4 (resolution targets named)
    - `grep -c "6TH SAMPLE POINT" .planning/phases/22-streaming-module/deferred-items.md` >= 1
    - `grep -c "DevicePreviewManager" .planning/phases/22-streaming-module/deferred-items.md` >= 1
    - `grep -c "SC2" .planning/phases/22-streaming-module/deferred-items.md` >= 1
    - Close-out metadata section present — `grep -c "^## Close-out metadata" .planning/phases/22-streaming-module/deferred-items.md` >= 1
    - `wc -l .planning/phases/22-streaming-module/deferred-items.md` reports 60-200 lines (not a stub; not bloat)
  </acceptance_criteria>
  <done>
    deferred-items.md catalog complete with 6 Phase 22-specific + inherited items + resolution targets in future phases.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6.2: Update .planning/STATE.md — mark Phase 22 Complete + increment plan counts</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current — Phase 22 entry shows 'Not started')
    - .planning/phases/21-artifacts-module/21-06-PLAN.md (Phase 21 precedent for STATE.md close update)
  </read_first>
  <action>
    **Edit `.planning/STATE.md`.** The file format follows a structured YAML-like shape for the current phase + a progress table + a narrative section. Update in 3 spots:

    **Update 1 — Current phase pointer** (around the top of the file, `current_phase` and `current_plan` fields):

    Find:
    ```yaml
    current_phase: 22
    current_plan: 0    # or whatever current draft value
    completed_plans: 57
    ```

    Replace with:
    ```yaml
    current_phase: 23       # moves to next phase
    current_plan: 0
    completed_plans: 64     # 57 (Phase 21 close) + 7 (Phase 22 plans)
    ```

    **Update 2 — Phase 22 row in progress table** (§Progress section):

    Find:
    ```
    | 22. Streaming Module | v3.0 | 0/TBD | Not started | - |
    ```

    Replace with:
    ```
    | 22. Streaming Module | v3.0 | 7/7 | Complete | 2026-04-22 |
    ```

    **Update 3 — Narrative state description** (if STATE.md has a free-text "Current state" paragraph):

    Find the paragraph referencing Phase 21 as most-recent-closed or Phase 22 as in-progress. Update to reflect Phase 22 closed + Phase 23 as next.

    Example text (adjust to match actual STATE.md prose style):
    ```
    **Current state (2026-04-22):** Phase 22 Streaming Module CLOSED after 7 plans across 7 waves.
    JobBroadcaster now subscribes to job.log/job.step/job.status from the bus; every WS frame carries
    a strict Zod-validated envelope with correlationId (TRACE-06 requirement satisfied). Next up is
    Phase 23 Jobs Module (Keystone) — the biggest v3.0 phase, rewriting executeJob as an orchestrated
    saga with pg-boss singletonKey guards + the deviceName schema fix.
    ```

    **DO NOT** touch other phase rows. DO NOT modify locked counts from Phases 15-21. DO NOT restructure the file format.
  </action>
  <verify>
    <automated>grep -q "22. Streaming Module.*7/7.*Complete.*2026-04-22" .planning/STATE.md && grep -q "completed_plans: 64\|completed_plans:64" .planning/STATE.md && grep -qE "current_phase: *23\|current_phase: *\"23\"" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "22. Streaming Module.*7/7.*Complete.*2026-04-22" .planning/STATE.md` >= 1 (progress table row updated)
    - `grep -c "22. Streaming Module.*Not started" .planning/STATE.md` == 0 (old state removed)
    - `grep -cE "completed_plans: *64" .planning/STATE.md` >= 1 (count incremented from 57 to 64)
    - `grep -cE "current_phase: *23" .planning/STATE.md` >= 1 (current phase advanced)
    - Phase 21 row UNCHANGED — `grep -c "21. Artifacts Module.*Complete" .planning/STATE.md` >= 1
    - Phase 23 row UNCHANGED (still 'Not started') — `grep -c "23. Jobs Module.*Not started\\|23. Jobs Module.*Keystone.*Not started" .planning/STATE.md` >= 1
    - File structure preserved — `grep -c "^## \\|^### " .planning/STATE.md` unchanged from before the edit
    - Date format matches Phase 21 row — '2026-04-22' uses ISO 8601 date
  </acceptance_criteria>
  <done>
    STATE.md updated to reflect Phase 22 CLOSED. Plan counts incremented. Current phase advanced to 23. Format preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6.3: Update .planning/ROADMAP.md — Phase 22 Complete + 7-plan list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current — Phase 22 row in progress table + §Phase 22 section with "Plans: TBD")
    - .planning/phases/21-artifacts-module/21-06-PLAN.md (Phase 21 precedent for ROADMAP.md close update)
  </read_first>
  <action>
    **Edit `.planning/ROADMAP.md`.** Two updates:

    **Update 1 — Progress table row** (around line 317):

    Find:
    ```
    | 22. Streaming Module | v3.0 | 0/TBD | Not started | - |
    ```

    Replace with:
    ```
    | 22. Streaming Module | v3.0 | 7/7 | Complete | 2026-04-22 |
    ```

    **Update 2 — §Phase 22 section** (around line 204):

    Find the `Plans: TBD` line at the end of §Phase 22.

    Replace with the full plans list (mirroring Phase 21 format):
    ```markdown
    **Plans** (7 plans across 7 waves):
    - [x] 22-00-PLAN.md — Wave 0 substrate: streaming/events stub + internal/module throw-stub + MODULE.md placeholder + index.ts stub + jobs/events 3-key placeholder extension (LOG/STEP/STATUS) + .dependency-cruiser.cjs 6th rule + fixture + dep-cruiser.spec 6th it-block (TRACE-06, MOD-01/02/03/05) ✅ 2026-04-22
    - [x] 22-01-PLAN.md — Wave 1 events: streaming/events.ts full body (wsFrameDroppedPayload + STREAMING_AGGREGATE_ID real v5 + makeStreamingEmitters) + streaming/events.spec 6+ tests + ws-schemas.ts strict envelope (v:literal(1) + ts + payload) + jobs/events.ts bridgehead body (3 payload schemas + registry entries + 6-helper makeJobsEmitters) (TRACE-06, MOD-03, EVENTS-03/04/08, TRACE-04/08) ✅ 2026-04-22
    - [x] 22-02-PLAN.md — Wave 2 factory + surgery: git mv 5 files into internal/ + createStreamingModule factory body (250 lines; persistEnvelope 6TH SAMPLE + 3 deferred-to-onReady subscribers + idempotent shutdown) + plugin.ts thin wirer (name preserved; deps 5-entry) + websocket-plugin.ts deletion + server/index.ts import + 7 job-service.ts callsites inverted + module.spec 8+ tests (TRACE-06, MOD-02/06, EVENTS-08) ✅ 2026-04-22
    - [x] 22-03-PLAN.md — Wave 3 DB-gated proofs: subscriber.spec (4 tests SC1+SC2 + ring-buffer replay) + correlation.spec (3 tests TRACE-06 round-trip + SC3 grep-ability + ALS fallback) + envelope.spec (8 tests wsEnvelopeSchema.safeParse) (TRACE-06, EVENTS-06) ✅ 2026-04-22
    - [x] 22-04-PLAN.md — Wave 4 structural guards: lifecycle-ownership.spec 3+ tests (SC2 grep-guard + substitute-path existence) + plugin-order.spec extension (4 new Phase 22 assertions + wbIndex substring-bug fix for websocket-plugin) (TRACE-06, MOD-02) ✅ 2026-04-22
    - [x] 22-05-PLAN.md — Wave 5 close-out artifacts: MODULE.md 9 H2 sections + Runnable Example (Queue Produced/Consumed = None; 6TH SAMPLE POINT + SC2 non-violation in Non-Goals) + index.ts full barrel (streamingPlugin + createStreamingModule + classes + envelope + events) + 5 MOD-04 .test→.spec renames + Nyquist delta gate ✅ 2026-04-22
    - [x] 22-06-PLAN.md — Wave 6 phase close: deferred-items.md catalog (6 Phase 22 deferrals + inherited Phase 15/17) + STATE.md Phase 22 CLOSED + ROADMAP.md 7-plan list + full-suite green sweep + commit ✅ 2026-04-22
    ```

    **DO NOT** touch §Phase 23 section (still shows "Plans: TBD"). DO NOT touch other phase sections. DO NOT restructure milestone bullets.
  </action>
  <verify>
    <automated>grep -q "22. Streaming Module.*7/7.*Complete.*2026-04-22" .planning/ROADMAP.md && grep -q "22-00-PLAN.md" .planning/ROADMAP.md && grep -q "22-06-PLAN.md" .planning/ROADMAP.md && grep -c "22-0[0-6]-PLAN.md" .planning/ROADMAP.md | grep -qE "^[7-9]$|^[1-9][0-9]+$" && grep -c "Plans: TBD" .planning/ROADMAP.md | grep -qE "^[0-9]+$" && ! grep -q "^Plans: TBD$" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Progress table — `grep -c "22. Streaming Module.*7/7.*Complete.*2026-04-22" .planning/ROADMAP.md` >= 1
    - §Phase 22 section — `grep -c "\\*\\*Plans\\*\\* (7 plans across 7 waves)" .planning/ROADMAP.md` >= 1
    - All 7 plans listed — `grep -cE "22-0[0-6]-PLAN.md" .planning/ROADMAP.md` >= 7
    - Each plan has ✅ 2026-04-22 — `grep -c "✅ 2026-04-22" .planning/ROADMAP.md` increments by 7 vs pre-edit (rough check; exact count depends on pre-existing sum)
    - Phase 23 still has "Plans: TBD" — `grep -c "^Plans: TBD$" .planning/ROADMAP.md` >= 1 (not all 'Plans: TBD' occurrences gone; Phase 23/24/25/26/27/28/29/30 remain)
    - Phase 21 entry UNCHANGED — `grep -c "21. Artifacts Module.*Complete" .planning/ROADMAP.md` >= 1
    - Milestone bullet list UNCHANGED — `grep -c "v3.0 Spec-Driven Architecture" .planning/ROADMAP.md` >= 1
    - Requirement TRACE-06 still referenced in §Phase 22 — `grep -c "TRACE-06" .planning/ROADMAP.md` >= 1
  </acceptance_criteria>
  <done>
    ROADMAP.md updated with Phase 22 Complete row + full 7-plan list. Phase 23 section untouched. Format matches Phase 20/21 precedent.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6.4: Full-suite green sweep + Nyquist + dep-check + commit</name>
  <files>server/streaming/ (no edits; verification only)</files>
  <read_first>
    - .planning/phases/22-streaming-module/22-RESEARCH.md §Wave 6 (10-step sweep)
    - .planning/phases/21-artifacts-module/21-06-PLAN.md (Phase 21 precedent for phase-close commit)
  </read_first>
  <action>
    **Final green-sweep + commit** — this task performs verification only (steps 1-9) then creates the close-out commit (step 10).

    **Step 1 — Full vitest suite run:**
    ```bash
    npm test 2>&1 | tail -50
    ```
    Expected: all new Phase 22 specs pass; inherited failures stay at 3 files (Phase 17 fastify-zod-openapi bug). If ANY new failure surfaces attributable to Phase 22, STOP — investigate.

    **Step 2 — Lint:**
    ```bash
    npm run lint
    ```
    Expected: clean.

    **Step 3 — Typecheck:**
    ```bash
    npx tsc --noEmit 2>&1 | grep -c "error TS"
    ```
    Expected: 6 (Phase 15 Map-vs-RequestContext baseline). If > 6, Phase 22 introduced new errors — STOP.

    **Step 4 — Dep-check:**
    ```bash
    npm run dep-check
    ```
    Expected: 0 violations (rule 6 `no-deep-imports-into-streaming-internal` fires only on the fixture file under __fixtures__/ when that file is included; the committed codebase has no real violation).

    **Step 5 — Nyquist delta gate:**
    ```bash
    npm run nyquist:check
    ```
    Expected: exits 0 (delta >= -2pp vs baseline). This is a re-verification of Plan 22-05 Task 5.4.

    **Step 6 — Baseline unchanged:**
    ```bash
    git diff .planning/nyquist-baseline.json
    ```
    Expected: no output (baseline unchanged since Phase 15 commit).

    **Step 7 — Confirm file tree:**
    ```bash
    ls server/streaming/
    # Expected: events.ts  index.ts  internal/  MODULE.md  plugin.ts  __tests__/

    ls server/streaming/internal/
    # Expected: adapters/  device-preview.ts  job-broadcaster.ts  module.ts  types.ts  ws-schemas.ts

    find server/streaming/__tests__ -name '*.test.ts' | wc -l
    # Expected: 0

    find server/streaming/__tests__ -name '*.spec.ts' | wc -l
    # Expected: 10 (5 MOD-04 renamed + 5 new: events + module + subscriber + correlation + envelope + lifecycle-ownership = 10)
    ```

    **Step 8 — Confirm git status of uncommitted Phase 22 work:**
    ```bash
    git status --porcelain .planning/ .dependency-cruiser.cjs __fixtures__/ server/
    ```
    Expected: all Phase 22 changes staged or in the commit queue (deferred-items.md new, STATE.md modified, ROADMAP.md modified, plus any trailing close-out files from Plan 22-05).

    **Step 9 — Stage the Phase 22 close-out files:**
    ```bash
    git add .planning/phases/22-streaming-module/deferred-items.md
    git add .planning/STATE.md
    git add .planning/ROADMAP.md
    # Also stage any remaining files from Plan 22-05 not yet committed:
    git add server/streaming/MODULE.md server/streaming/index.ts server/streaming/__tests__/
    ```

    Do NOT `git add -A` or `git add .` — explicit file paths avoid accidentally including secrets, generated files, or unrelated work.

    **Step 10 — Create the phase-close commit:**
    ```bash
    git commit -m "$(cat <<'EOF'
    docs(phase-22): complete phase execution

    Phase 22 Streaming Module CLOSED after 7 plans across 7 waves.
    JobBroadcaster now subscribes to job.log/job.step/job.status from
    the bus; every WS frame carries a strict Zod-validated envelope
    with correlationId (TRACE-06).

    Changes since Plan 22-05:
    - .planning/phases/22-streaming-module/deferred-items.md (6 new + 2 inherited)
    - .planning/STATE.md (Phase 22 row: Complete 2026-04-22; completed_plans 57 -> 64; current_phase 22 -> 23)
    - .planning/ROADMAP.md (Phase 22 row: Complete; 7-plan list with dates)

    Full-suite green: 0 new failures vs Phase 17 baseline.
    Nyquist delta passes (>=-2pp vs Phase 15 baseline 48.29%).
    Dep-check green; tsc 0 new errors.

    Next: Phase 23 Jobs Module (Keystone) — see ROADMAP.md.
    EOF
    )"
    ```

    **Step 11 — Verify commit:**
    ```bash
    git log --oneline -1
    # Expected: docs(phase-22): complete phase execution
    ```
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -30 && npm run lint && npx tsc --noEmit 2>&1 | grep -c "error TS" | awk '{exit ($1 <= 6) ? 0 : 1}' && npm run dep-check && npm run nyquist:check && git diff --exit-code .planning/nyquist-baseline.json && find server/streaming/__tests__ -name '*.test.ts' | wc -l | awk '{exit ($1 == 0) ? 0 : 1}' && git log --oneline -1 | grep -q "docs(phase-22): complete phase execution"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` — baseline Phase 17 failures unchanged (3 files); ZERO new Phase-22-attributable failures
    - `npm run lint` exits 0
    - `npx tsc --noEmit` — error count == 6 (baseline Phase 15 Map-vs-RequestContext); NO increase
    - `npm run dep-check` exits 0 (no deep imports into streaming/internal/ from outside the module)
    - `npm run nyquist:check` exits 0 (delta >= -2pp)
    - `.planning/nyquist-baseline.json` unmodified — `git diff --exit-code` exits 0
    - `server/streaming/` has exactly 3 top-level .ts files + 1 .md + 1 subdir + 1 __tests__ subdir
    - `server/streaming/__tests__/` has 10 .spec.ts files, 0 .test.ts files
    - `server/streaming/internal/` has 6 files (job-broadcaster + device-preview + types + ws-schemas + module + adapters subdirectory with 4 adapter files)
    - `server/streaming/websocket-plugin.ts` does NOT exist
    - Git commit created with message starting `docs(phase-22): complete phase execution`
    - `git log -1 --pretty=%H` returns a new commit SHA (not the Plan 22-05 commit SHA)
    - Phase 22 is now CLOSED — downstream `/gsd:plan-phase 23` boot will read STATE.md + ROADMAP.md reflecting this state
  </acceptance_criteria>
  <done>
    Phase 22 Streaming Module CLOSED. Green-sweep verified; commit created. Total 7 plans across 7 waves completed. TRACE-06 requirement satisfied (proved by correlation.spec + subscriber.spec + envelope.spec + lifecycle-ownership.spec). ROADMAP.md + STATE.md reflect completion. Next phase (Phase 23 Jobs Module Keystone) can begin.
  </done>
</task>

</tasks>

<verification>
All 4 tasks green:

```bash
# Task 6.1 — deferred-items.md
test -f .planning/phases/22-streaming-module/deferred-items.md
grep -c "^### DEFERRED-" .planning/phases/22-streaming-module/deferred-items.md   # >= 6

# Task 6.2 — STATE.md
grep -q "22. Streaming Module.*7/7.*Complete.*2026-04-22" .planning/STATE.md

# Task 6.3 — ROADMAP.md
grep -c "22-0[0-6]-PLAN.md" .planning/ROADMAP.md   # >= 7

# Task 6.4 — full-suite green + commit
git log --oneline -1 | grep -q "docs(phase-22): complete phase execution"
npm test   # inherited exclusions only
npm run nyquist:check   # exits 0
git diff --exit-code .planning/nyquist-baseline.json   # no output
```
</verification>

<success_criteria>
- deferred-items.md catalog landed with 6 Phase 22 deferrals + 2 inherited
- STATE.md shows Phase 22 Complete + current_phase advanced to 23 + completed_plans 64
- ROADMAP.md shows Phase 22 Complete row + full 7-plan list with 2026-04-22 dates
- Full test suite passes with inherited Phase 17 exclusions only; ZERO new Phase-22-attributable failures
- Nyquist baseline unchanged; delta passes (>= -2pp)
- Dep-check green; tsc green (no new errors)
- Git commit `docs(phase-22): complete phase execution` created
- Phase 22 Streaming Module is CLOSED — downstream Phase 23 planning can boot
- TRACE-06 requirement fully satisfied (traceability: requirement → phase → plan → test)
</success_criteria>

<output>
After completion, create `.planning/phases/22-streaming-module/22-06-SUMMARY.md` per the execute-plan template. Include:
- Nyquist delta (baseline pct, current pct, delta pct).
- Commit SHA of the close-out commit.
- Inherited test exclusion summary (3 files per Phase 17).
- Lines of code changed (added/removed across plans 22-00 through 22-06).
</output>
