---
phase: 19
plan: 06
type: execute
wave: 3
depends_on: [19-03, 19-04, 19-05]
files_modified:
  - server/reporting/MODULE.md
  - server/reporting/index.ts
  - server/reporting/__tests__/flaky-detector.spec.ts
  - server/reporting/__tests__/junit-generator.spec.ts
autonomous: true
requirements: [EVENTS-07, QUEUE-05]
gap_closure: false
must_haves:
  truths:
    - "`server/reporting/MODULE.md` exists with ALL 9 fixed H2 sections in exact order: Purpose / Public API / Events Emitted / Events Consumed / Queue Produced / Queue Consumed / Invariants / Non-Goals / Dependencies — plus an optional H3 'Runnable Example' block"
    - "MODULE.md Events Emitted lists all 4 events with persisted flags (`webhook.scheduled` thin, `webhook.delivered` terminal+persisted, `webhook.failed` transient, `webhook.failed.retryExhausted` terminal+persisted+EVENTS-07); Events Consumed lists `job.completed` (from jobs module); Queue Produced lists `webhook.deliver` (policy:standard, retryLimit:5, retryBackoff:true, deadLetter:webhook.deliver.dlq) AND `webhook.deliver.dlq` (policy:standard, retryLimit:0); Queue Consumed lists same two queues (self-loop); Dependencies matches plugin.ts dependencies array verbatim: `['config', 'db', 'queue', 'event-bus']`"
    - "`server/reporting/index.ts` barrel exists and exports ONLY public API: `reportingPlugin` (default-re-exported from plugin.js), `createReportingModule` (+ types `ReportingModule` + `CreateReportingModuleDeps`), `WebhookService` class (back-compat), `FlakyDetector` class (back-compat, unchanged), `reportingRegistry`, `REPORTING_EVENT_NAMES`, `REPORTING_AGGREGATE_ID`, `makeReportingEmitters`, all 4 payload schemas, types `ReportingRegistry` + `ReportingEmitters` + `ReportingEventName`, `WEBHOOK_DELIVER_QUEUE_NAME` + `WEBHOOK_DELIVER_DLQ_QUEUE_NAME`, `webhookDeliveryPayloadSchema`, `registerWebhookDeliveryWorkers`, `dlqJobSchema`, `dlqListResponseSchema`, types `DlqJob` + `DlqListResponse`"
    - "`server/reporting/index.ts` has EXACTLY ONE `from './internal/` re-export line using inline type modifier (`export { createReportingModule, type ReportingModule, type CreateReportingModuleDeps } from './internal/module.js'`) — matches Phase 18 lifecycle 1-line form stricter than Phase 16 hooks 2-line form"
    - "`server/reporting/__tests__/flaky-detector.test.ts` RENAMED to `flaky-detector.spec.ts` + `junit-generator.test.ts` RENAMED to `junit-generator.spec.ts` via `git mv` (100% similarity — content byte-identical; blame preserved through git's rename detection)"
    - "`npm run dep-check` passes — no deep imports into `server/reporting/internal/*` (dep-cruiser rule from plan 19-00 Task 0.4 enforces boundary structurally now that plan 19-03 shipped real internal content + plan 19-05 shipped real routes.ts)"
    - "`npm run nyquist:capture && npm run nyquist:check` exits 0 — coverage delta on `coverage.lines` vs `.planning/nyquist-baseline.json` (48.29%) is ≥ −2pp (Phase 16 convention; same exclusions Phase 18 Plan 18-04 documented for pre-existing fastify-zod-openapi v5 failures per STATE.md)"
    - "`npm test` full suite exits 0 (or matches pre-existing Phase 17/18 failure envelope documented in STATE.md); `npm run lint` exits 0; `npm run typecheck` exits 0"
  artifacts:
    - path: "server/reporting/MODULE.md"
      provides: "LLM-first public contract for the reporting module (MOD-01)"
      contains: "## Purpose"
    - path: "server/reporting/index.ts"
      provides: "Public barrel — the ONLY legitimate import surface outside server/reporting/ (MOD-02)"
      contains: "createReportingModule"
    - path: "server/reporting/__tests__/flaky-detector.spec.ts"
      provides: "Renamed from flaky-detector.test.ts for MOD-04 naming consistency"
      contains: "FlakyDetector"
    - path: "server/reporting/__tests__/junit-generator.spec.ts"
      provides: "Renamed from junit-generator.test.ts for MOD-04 naming consistency"
      contains: "JUnit"
  key_links:
    - from: "server/reporting/MODULE.md"
      to: "server/reporting/index.ts"
      via: "Public API section lists barrel exports"
      pattern: "index\\.ts"
    - from: "server/reporting/index.ts"
      to: "server/reporting/internal/module.ts"
      via: "The ONLY allowed internal/ re-export per MOD-02 barrel rule (createReportingModule + ReportingModule type + CreateReportingModuleDeps type, inline type modifier, ONE line)"
      pattern: "from './internal/module"
    - from: "server/reporting/MODULE.md Invariants"
      to: "server/reporting/__tests__/queue.spec.ts + correlation.spec.ts + terminal-event.spec.ts + dlq-route.spec.ts"
      via: "Each invariant maps to a spec file+test reference"
      pattern: "\\[Invariant"
---

<objective>
Close Phase 19. Ship the LLM-first `MODULE.md` with all 9 fixed H2 sections documenting the now-complete reporting module (plans 19-01..19-05 delivered the code; this plan documents + barrels it). Create the `server/reporting/index.ts` barrel (MOD-02) as the single public import surface — the `no-deep-imports-into-reporting-internal` dep-cruiser rule from plan 19-00 becomes structurally meaningful now that both the barrel AND real `internal/module.ts` content exist. Rename the remaining two `*.test.ts` files under `server/reporting/__tests__/` to `*.spec.ts` for MOD-04 naming consistency (plan 19-00 already renamed `webhook-service.test.ts` → `webhook-service.spec.ts`; this plan handles `flaky-detector.test.ts` + `junit-generator.test.ts`). Finally, run the Nyquist delta gate and confirm coverage delta ≥ −2pp vs baseline.

Pattern: mirrors Phase 18 Plan 18-04 exactly — MODULE.md + barrel + rename .test.ts → .spec.ts + Nyquist gate + phase close. Key difference from Phase 18: reporting has MORE public surface (webhook-service class + flakyDetector class + 4 events + 2 queues + DLQ route + DLQ schemas), so the barrel is ~45 lines vs lifecycle's ~30 lines.

Purpose: MOD-01 requires every v3.0 module to ship MODULE.md with 9 fixed sections so LLMs read a single file to understand the public contract. MOD-02 requires a barrel that `dependency-cruiser` enforces structurally. Phase 19 is not DONE until these two artifacts exist — otherwise plans 19-01..19-04 ship working code but Phase 16 convention parity isn't reached, and ROADMAP §Phase 19 SC3 ("Reporting module follows Phase 16 conventions") is false. Nyquist is the final CI gate per DEBT-03 (every v3.0 phase ends with coverage check); running it here closes the phase.

Output: `MODULE.md` (~100 lines per hooks + lifecycle templates — reporting surface is slightly richer), `index.ts` barrel (~45 lines), 2 `.test.ts → .spec.ts` renames, Nyquist delta captured + recorded in SUMMARY.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-reporting-migration-webhooks-dlq/19-CONTEXT.md
@.planning/phases/19-reporting-migration-webhooks-dlq/19-RESEARCH.md
@.planning/phases/19-reporting-migration-webhooks-dlq/19-VALIDATION.md
@.planning/phases/19-reporting-migration-webhooks-dlq/19-00-PLAN.md
@.planning/phases/19-reporting-migration-webhooks-dlq/19-01-PLAN.md
@.planning/phases/19-reporting-migration-webhooks-dlq/19-02-PLAN.md
@.planning/phases/19-reporting-migration-webhooks-dlq/19-03-PLAN.md
@.planning/phases/19-reporting-migration-webhooks-dlq/19-04-PLAN.md
@.planning/nyquist-baseline.json
@docs/adr/001-spec-driven-architecture.md
@docs/adr/002-file-naming.md
@server/hooks/MODULE.md
@server/hooks/index.ts
@server/lifecycle/MODULE.md
@server/lifecycle/index.ts
@server/reporting/events.ts
@server/reporting/queue.ts
@server/reporting/plugin.ts
@server/reporting/routes.ts
@server/reporting/internal/module.ts
@server/reporting/schemas.ts
@server/reporting/webhook-service.ts
@server/reporting/flaky-detector.ts

<interfaces>
<!-- Everything this plan imports/references is already committed by plans 19-00..19-04. -->

From server/hooks/MODULE.md (template — 9 sections in this order):
```
## Purpose
## Public API
## Events Emitted
## Events Consumed
## Queue Produced
## Queue Consumed
## Invariants
## Non-Goals
## Dependencies

### Runnable Example   (H3, optional — Phase 27 MOD-09 typechecks the block)
```

From server/lifecycle/index.ts (template — public barrel structure; ONE internal/ re-export line with inline type modifier):
```typescript
// Fastify plugin (default export — matches Phase 15 substrate convention).
export { default as lifecyclePlugin } from './plugin.js';

// Factory surface — the ONLY allowed internal/ re-export line per MOD-02.
export { createLifecycleModule, type LifecycleModule, type CreateLifecycleModuleDeps } from './internal/module.js';

// Stats / schemas / events / queue surfaces (many named re-exports)
export type { LifecycleStats } from './stats.js';
export { lifecycleJobPayloadSchema, ... } from './schemas.js';
export { lifecycleRegistry, LIFECYCLE_EVENT_NAMES, ..., makeLifecycleEmitters } from './events.js';
export type { LifecycleRegistry, LifecycleEmitters, LifecycleEventName } from './events.js';
export { LIFECYCLE_COMPRESS_DAILY_QUEUE_NAME, ..., registerLifecycleSchedulesAndWorkers } from './queue.js';
```

From server/reporting/events.ts (plan 19-01):
- Exports: reportingRegistry, REPORTING_EVENT_NAMES, REPORTING_AGGREGATE_ID, makeReportingEmitters, webhookScheduledPayload, webhookDeliveredPayload, webhookFailedPayload, webhookFailedRetryExhaustedPayload, types ReportingRegistry, ReportingEmitters, ReportingEventName

From server/reporting/queue.ts (plan 19-03):
- Exports: WEBHOOK_DELIVER_QUEUE_NAME, WEBHOOK_DELIVER_DLQ_QUEUE_NAME, webhookDeliveryPayloadSchema (re-exported), registerWebhookDeliveryWorkers, types WebhookDeliveryPayload, RegisterWebhookDeliveryWorkersDeps, WebhookDeliveryRegistration

From server/reporting/internal/module.ts (plan 19-03):
- Exports: createReportingModule, types ReportingModule, CreateReportingModuleDeps

From server/reporting/webhook-service.ts (plan 19-02):
- Exports: WebhookService class, WebhookConfig interface

From server/reporting/flaky-detector.ts (unchanged):
- Exports: FlakyDetector class (back-compat; untouched by Phase 19)

From server/reporting/schemas.ts (plan 19-00 extended):
- Exports: webhookCreateRequestSchema (Phase 17), webhookSchema (Phase 17), webhookDeliveryPayloadSchema, dlqJobSchema, dlqListResponseSchema, types WebhookDeliveryPayload, DlqJob, DlqListResponse, WebhookCreateRequest, Webhook

From server/reporting/routes.ts (plan 19-04):
- Exports: registerReportingRoutes (NOT re-exported from barrel — routes are plugin-internal)

Current test file layout:
- server/reporting/__tests__/webhook-service.spec.ts (renamed by plan 19-00; body rewritten by plan 19-02)
- server/reporting/__tests__/flaky-detector.test.ts (RENAME THIS PLAN to .spec.ts)
- server/reporting/__tests__/junit-generator.test.ts (RENAME THIS PLAN to .spec.ts)
- server/reporting/__tests__/events.spec.ts (plan 19-01)
- server/reporting/__tests__/module.spec.ts (plan 19-03)
- server/reporting/__tests__/queue.spec.ts (plan 19-03)
- server/reporting/__tests__/correlation.spec.ts (plan 19-03)
- server/reporting/__tests__/terminal-event.spec.ts (plan 19-03)
- server/reporting/__tests__/dlq-route.spec.ts (plan 19-04)

Nyquist baseline: `.planning/nyquist-baseline.json` — `coverage.lines` = 48.29% (captured at Phase 15 Plan 15-09; NEVER overwrite).
Nyquist delta rule: `npm run nyquist:check` fails if current `coverage.lines` is below baseline by more than 2pp.

Phase 18 Plan 18-04 documented the pre-existing-failure exclusion pattern required for Vitest v4 coverage capture to emit `coverage-summary.json` when certain test files fail at Fastify boot (routes.test, artifact-routes.test, auth-plugin.test, plugin-order.spec). Phase 19 inherits the same exclusion set — see Phase 18 18-04-SUMMARY.md for the exact `vitest run --exclude` flag pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Create server/reporting/MODULE.md with 9 fixed H2 sections</name>
  <files>server/reporting/MODULE.md</files>
  <read_first>
    - server/hooks/MODULE.md (canonical template — 9 sections + H3 Runnable Example)
    - server/lifecycle/MODULE.md (alternative template — slightly tighter style, adapted for module that emits scheduled events)
    - server/reporting/events.ts (plan 19-01 — 4 events + their persisted flags)
    - server/reporting/queue.ts (plan 19-03 — MAIN + DLQ queue options)
    - server/reporting/plugin.ts (plan 19-03 — dependencies array)
    - server/reporting/routes.ts (plan 19-04 — /api/queue/dlq route)
    - server/reporting/webhook-service.ts (plan 19-02 — deliverOnce contract)
    - server/reporting/__tests__/*.spec.ts (invariants to map in Invariants section)
  </read_first>
  <action>
    **Create `server/reporting/MODULE.md`.** Follow the 9-section template EXACTLY (ordered per MOD-01; copy-paste Phase 16 / Phase 18 structure; substitute reporting content):

    ```markdown
    # Reporting Module

    ## Purpose

    Deliver outbound webhook notifications for completed jobs via a durable, retryable, dead-letter-observable pipeline. Owns `WebhookService.deliverOnce` (single-attempt HTTP POST — throws on 5xx/network, resolves on 2xx/4xx), the `webhook.deliver` pg-boss queue (retry + backoff owned at the queue layer), the `webhook.deliver.dlq` dead-letter queue (terminal pipeline), and the read-only `GET /api/queue/dlq` operator endpoint. Also hosts `FlakyDetector` (background analytics — unchanged by Phase 19) and JUnit-report generation (unchanged).

    The module subscribes to `job.completed` from the bus and enqueues a webhook delivery when `config.webhooks.url` is configured; callers outside the bus may enqueue imperatively via `reportingModule.enqueueWebhookDelivery(url, payload)`.

    ## Public API

    Exports from `server/reporting/index.ts` (the ONLY legitimate import surface outside this module — enforced by `dependency-cruiser` rule `no-deep-imports-into-reporting-internal`):

    - `reportingPlugin` — Fastify plugin (thin wrapper around `createReportingModule`).
    - `createReportingModule(deps): ReportingModule` — factory returning `{webhookService, flakyDetector, emit, bus, registerWorkersAndSubscribers, enqueueWebhookDelivery, shutdown}`.
    - `WebhookService` — imperative class (back-compat); `.deliverOnce(url, payload)` single-attempt HTTP POST.
    - `FlakyDetector` — back-compat (untouched by Phase 19).
    - `reportingRegistry`, `REPORTING_EVENT_NAMES`, `REPORTING_AGGREGATE_ID`, `makeReportingEmitters` — events surface.
    - 4 payload schemas: `webhookScheduledPayload`, `webhookDeliveredPayload`, `webhookFailedPayload`, `webhookFailedRetryExhaustedPayload`.
    - `WEBHOOK_DELIVER_QUEUE_NAME`, `WEBHOOK_DELIVER_DLQ_QUEUE_NAME`, `webhookDeliveryPayloadSchema`, `registerWebhookDeliveryWorkers` — queue surface.
    - `dlqJobSchema`, `dlqListResponseSchema` — HTTP response schemas.
    - Types: `ReportingModule`, `CreateReportingModuleDeps`, `ReportingRegistry`, `ReportingEmitters`, `ReportingEventName`, `WebhookDeliveryPayload`, `RegisterWebhookDeliveryWorkersDeps`, `WebhookDeliveryRegistration`, `DlqJob`, `DlqListResponse`, `WebhookConfig`.

    HTTP routes (registered by the plugin):
    - `GET /api/queue/dlq` — lists items in `webhook.deliver.dlq` via `fastify.boss.findJobs`; Zod-validated response via `fastify-zod-openapi`.
    - Existing `POST /api/webhooks` (ping endpoint — Phase 17) and `GET /api/jobs/:id/reports/*` (report-routes.ts — unchanged).

    ## Events Emitted

    - `webhook.scheduled` — thin, NOT persisted. Fired after the `job.completed` bus subscriber (or `enqueueWebhookDelivery` facade) successfully enqueues a `webhook.deliver` job.
    - `webhook.delivered` — terminal, **persisted**. Fired by the MAIN worker after `deliverOnce` returns (2xx response from target).
    - `webhook.failed` — transient, NOT persisted. Fired by the MAIN worker on per-attempt failure (before pg-boss decides to retry or route to DLQ).
    - `webhook.failed.retryExhausted` — terminal, **persisted**. Fired by the DLQ worker after pg-boss moves the exhausted job to the DLQ queue. This is the **EVENTS-07** terminal event. Payload includes `payloadSnapshot` for operator debug visibility (EVENTS-04 documented exception for terminal events).

    All four events carry aggregate `'reporting'` and `aggregateId === REPORTING_AGGREGATE_ID` (the stable v5 UUID `bca46f4f-d5bd-5d65-bf73-0a59a7f3c6d7` derived from `'reporting'` under the URL namespace).

    ## Events Consumed

    - `job.completed` (from the jobs module — plan 19-01 added minimal `server/jobs/events.ts` declaring this event; Phase 23 Jobs Module Keystone extends the registry with the full saga).

    Phase 19's subscription handler is registered via `fastify.onPersisted('job.completed', handler)` inside `createReportingModule`. The handler reads `config.webhooks?.url` — no-ops if absent; otherwise builds a POST body `{event:'job.completed', job: envelope.payload, timestamp: ISO(envelope.occurredAt)}` and enqueues via `fastify.queue.send(WEBHOOK_DELIVER_QUEUE_NAME, {url, payload: body})`. The enqueued job inherits the current ALS correlationId (Phase 15/18 substrate) so all 5 retries + the DLQ row + the terminal event share ONE correlationId.

    ## Queue Produced

    - `webhook.deliver` — `policy:'standard'`, `retryLimit:5`, `retryBackoff:true`, `retryDelay:1`, `retryDelayMax:30`, `deadLetter:'webhook.deliver.dlq'`. Payload shape: `{url: string, payload: Record<string, unknown>}` validated by `webhookDeliveryPayloadSchema`. Policy is `'standard'` (NOT `'stately'`) because webhook deliveries are NOT idempotent by enqueue — two different `job.completed` events SHOULD both fire deliveries.
    - `webhook.deliver.dlq` — `policy:'standard'`, `retryLimit:0`, `retryBackoff:false`. pg-boss re-inserts exhausted main-queue payloads into this queue verbatim (including the original envelope's `correlationId`) via its maintenance loop. The DLQ worker emits `webhook.failed.retryExhausted` (EVENTS-07) and does not throw (retryLimit:0 prevents loops regardless).

    ## Queue Consumed

    - `webhook.deliver` — MAIN worker (calls `WebhookService.deliverOnce`; emits `webhook.delivered` on 2xx; emits `webhook.failed` and re-throws on 5xx/network for pg-boss retry accounting).
    - `webhook.deliver.dlq` — DLQ worker (emits terminal `webhook.failed.retryExhausted`; swallows emit errors; does not re-throw).

    Both queues are self-loops (producer and consumer live in the reporting module).

    ## Invariants

    Every invariant below has at least one test (MOD-08):

    - **(a) deliverOnce contract** — `WebhookService.deliverOnce` throws on 5xx or network error; resolves on 2xx (success) or 4xx (non-retryable per RESEARCH §Pitfall 7). Test: `__tests__/webhook-service.spec.ts` (12+ tests).
    - **(b) 5× 500 → DLQ without crash** — A local HTTP server returning 500 for every request produces a DLQ row after pg-boss exhausts retries + the maintenance loop; the Fastify app does not crash. Test: `__tests__/queue.spec.ts` `[SC1]`.
    - **(c) Single correlationId across retries + DLQ + events row** — A webhook enqueued inside an ALS fiber carries ONE correlationId through all 5 retry attempts (pg-boss re-uses the same row across retries), the DLQ row (pg-boss copies the envelope verbatim into the DLQ queue), and the terminal event persisted to the `events` table (envelope emitted by the DLQ worker inherits the restored ALS correlationId). Test: `__tests__/correlation.spec.ts` `[SC4]`.
    - **(d) Terminal event persistence** — The DLQ worker fires `webhook.failed.retryExhausted` which is persisted to the `events` table with `aggregate_type='reporting'`, `aggregate_id=REPORTING_AGGREGATE_ID`, non-null `correlation_id`, and a `payload` JSON containing `url`, `event`, `attempts`, `payloadSnapshot`. Test: `__tests__/terminal-event.spec.ts` `[EVENTS-07]`.
    - **(e) DLQ endpoint shape** — `GET /api/queue/dlq` returns 200 with a body that round-trips through `dlqListResponseSchema.safeParse`; items have flat shape with snake_case `correlation_id` + lowercase `retrycount` / `createdon` (matches CONTEXT.md §Specifics verbatim). Test: `__tests__/dlq-route.spec.ts` `[QUEUE-05]`.
    - **(f) Shutdown idempotency** — `reportingModule.shutdown()` called twice is a no-op; first call unsubscribes the `job.completed` bus subscriber and `boss.offWork`s each registered worker id; second call exits without additional side-effects. Test: `__tests__/module.spec.ts` `[MOD-06]`.

    ## Non-Goals

    - **DLQ replay endpoint** — `GET /api/queue/dlq` is read-only. Re-enqueueing from the DLQ back into the main queue is out of scope Phase 19.
    - **Webhook HMAC signing redesign** — current `WebhookService` HMAC-SHA256 signing (via `X-Signature-256` header) is preserved unchanged. Out of scope.
    - **Multi-target webhook fan-out** — current single-target model (`config.webhooks.url` → one POST) carries through. Fan-out is out of scope.
    - **Webhook event types beyond `job.completed`** — `device.*` webhooks are downstream scope once the pool module (Phase 20) emits `device.*` events.
    - **Full reporting route Zod coverage** — Phase 17 met the one-route-minimum (the `/api/webhooks` ping endpoint). Full Zod coverage across all reporting endpoints is downstream.
    - **Auth gating on `/api/queue/dlq`** — matches the existing `/api/webhooks` endpoint (no auth); Phase 26 (Auth Module) adds gating when it lands.
    - **`?state=` filter on `/api/queue/dlq`** — RESEARCH §Pitfall 5 notes `findJobs` returns all states; a future query-param filter is deliberately deferred.
    - **Consolidation of the `persistEnvelope` middleware** — duplicated 10 lines across hooks + lifecycle + reporting per RESEARCH Open Question #1; Phase 27+ consolidates.

    ## Dependencies

    - `config` — reads `fastify.config.webhooks?.{url, secret, timeout_ms, max_retries}`.
    - `db` — writes `events` rows via the persistEnvelope middleware.
    - `queue` — registers MAIN + DLQ workers via `fastify.queue.work`; enqueues deliveries via `fastify.queue.send`.
    - `event-bus` — subscribes to `job.completed` via `fastify.onPersisted` (decorated by the bus plugin — Phase 15).

    ---

    ### Runnable Example

    ```typescript
    // Inside a Fastify plugin that has already registered config + db + queue + event-bus + reporting:
    import {
      WEBHOOK_DELIVER_QUEUE_NAME,
      REPORTING_EVENT_NAMES,
      REPORTING_AGGREGATE_ID,
    } from 'server/reporting/index.js';

    // Enqueue a delivery directly (bypassing the bus — matches reportingModule.enqueueWebhookDelivery):
    const sentJobId = await app.queue.send(WEBHOOK_DELIVER_QUEUE_NAME, {
      url: 'https://example.com/hook',
      payload: { event: 'job.completed', job: { id: 'abc-123' } },
    }, {});

    // Listen for the terminal event (requires the reporting module's private bus —
    // access via app.reportingModule.bus.on for now; Phase 27+ may consolidate into a global bus):
    app.reportingModule.bus.on(REPORTING_EVENT_NAMES.FAILED_RETRY_EXHAUSTED, (payload) => {
      app.log.warn({ url: payload.url, attempts: payload.attempts }, 'Webhook retry-exhausted');
    });

    // Query the DLQ endpoint:
    // curl http://localhost:3000/api/queue/dlq
    // → { items: [{id, queue: 'webhook.deliver.dlq', state, retrycount, data, output, createdon, correlation_id}, ...], count }
    ```

    Phase 27 (MOD-09) will add CI-level typechecking of this example snippet. For Phase 19, reviewer spot-checks the block.
    ```

    **Do NOT** add sections beyond the 9 H2s + 1 optional H3. Phase 16 Plan 16-04 established the strict section invariant for grep-based MOD-01 checks.

    **Do NOT** re-document internal implementation details (persistEnvelope, ALS Map-shape plumbing, etc.) — those live in RESEARCH.md + STATE.md. MODULE.md is the consumer-facing contract.
  </action>
  <verify>
    <automated>test -f server/reporting/MODULE.md && for section in "## Purpose" "## Public API" "## Events Emitted" "## Events Consumed" "## Queue Produced" "## Queue Consumed" "## Invariants" "## Non-Goals" "## Dependencies"; do if ! grep -q "^${section}$" server/reporting/MODULE.md; then echo "MISSING section: ${section}"; exit 1; fi; done && echo "OK: all 9 H2 sections present" && grep -c "webhook.failed.retryExhausted" server/reporting/MODULE.md | awk '{ if ($1 < 2) { print "ERROR: EVENTS-07 terminal event should be mentioned >=2 times (Events Emitted + Invariants), got", $1; exit 1 } else { print "OK: EVENTS-07 referenced", $1, "times" } }' && grep -c "REPORTING_AGGREGATE_ID" server/reporting/MODULE.md | grep -q "^[1-9]" && wc -l server/reporting/MODULE.md | awk '{ if ($1 < 60) { print "WARN: MODULE.md unusually short (", $1, "lines) — hooks is 97, lifecycle is 104"; } else { print "OK", $1, "lines" } }'</automated>
  </verify>
  <acceptance_criteria>
    - `server/reporting/MODULE.md` exists
    - All 9 H2 sections present in exact order: `## Purpose`, `## Public API`, `## Events Emitted`, `## Events Consumed`, `## Queue Produced`, `## Queue Consumed`, `## Invariants`, `## Non-Goals`, `## Dependencies` (verified by loop in <verify>)
    - Events Emitted section lists all 4 event names with persisted flags (grep each of `webhook.scheduled`, `webhook.delivered`, `webhook.failed`, `webhook.failed.retryExhausted`)
    - `EVENTS-07` explicitly called out (grep match)
    - Queue Produced section names both queues (`webhook.deliver` + `webhook.deliver.dlq`) with policy + retry options
    - Events Consumed lists `job.completed`
    - Dependencies section matches plugin.ts dependencies array verbatim: `['config', 'db', 'queue', 'event-bus']` (grep each literal)
    - Invariants section lists at least 6 invariants, each with a test file reference
    - Line count between 60 and 200 (hooks: 97, lifecycle: 104 — reporting slightly richer ≈ 100-150)
    - H3 `### Runnable Example` present (optional per MOD-09 — Phase 27 typechecks it)
  </acceptance_criteria>
  <done>
    server/reporting/MODULE.md ships the 9-section LLM-first contract. Every invariant has a test reference. Runnable example present.
  </done>
</task>

<task type="auto">
  <name>Task 6.2: Create server/reporting/index.ts barrel (MOD-02 public surface)</name>
  <files>server/reporting/index.ts</files>
  <read_first>
    - server/hooks/index.ts (template — 2-line internal re-export form)
    - server/lifecycle/index.ts (stricter template — 1-line internal re-export form via inline type modifier)
    - server/reporting/events.ts (what to re-export)
    - server/reporting/queue.ts (what to re-export)
    - server/reporting/schemas.ts (Phase 17 + Phase 19 schemas to re-export)
    - server/reporting/webhook-service.ts (WebhookService class + WebhookConfig interface)
    - server/reporting/flaky-detector.ts (FlakyDetector class — back-compat)
    - server/reporting/internal/module.ts (createReportingModule + types)
    - server/reporting/plugin.ts (default plugin export to wrap)
  </read_first>
  <action>
    **Create `server/reporting/index.ts`** as the public barrel. Adopt the Phase 18 lifecycle 1-line internal/ re-export form (stricter than Phase 16 hooks' 2-line form — sets the precedent for Phase 20+).

    ```typescript
    /**
     * Phase 19 / Plan 19-06 — Reporting module public barrel (MOD-02).
     *
     * This is the ONLY import surface for consumers outside `server/reporting/`.
     * `dependency-cruiser` rule `no-deep-imports-into-reporting-internal` (added
     * to `.dependency-cruiser.cjs` in Phase 19 Plan 19-00) enforces the boundary
     * structurally — imports into `server/reporting/internal/**` from outside
     * the module fail CI.
     *
     * Exports in this file are PUBLIC and subject to semver-ish stability.
     * Anything under `./internal/` is module-private and may change without notice.
     *
     * The ONLY allowed internal/ re-export line is the factory entry point per
     * MOD-06. Runtime + types exported from a single statement (inline `type`
     * modifier) so plan 19-06 verify script sees exactly ONE internal/ re-export
     * line (MOD-02 structural invariant). Matches Phase 18 lifecycle 1-line form
     * which tightens on Phase 16 hooks' 2-line form.
     */

    // Fastify plugin (default export — matches Phase 15 substrate convention).
    export { default as reportingPlugin } from './plugin.js';

    // Factory surface (the v3.0 canonical way to instantiate the module). MOD-06.
    // ONE internal/ re-export line — MOD-02 structural invariant.
    export { createReportingModule, type ReportingModule, type CreateReportingModuleDeps } from './internal/module.js';

    // Back-compat class surfaces (preserved for existing consumers — webhookService
    // decorator on fastify is still read by any code holding a reference).
    export { WebhookService } from './webhook-service.js';
    export type { WebhookConfig } from './webhook-service.js';
    export { FlakyDetector } from './flaky-detector.js';

    // Schemas + derived TS types (SPEC-01 / SPEC-03 / QUEUE-05 / Phase 17 ping endpoint).
    export {
      webhookCreateRequestSchema,
      webhookSchema,
      webhookDeliveryPayloadSchema,
      dlqJobSchema,
      dlqListResponseSchema,
    } from './schemas.js';
    export type {
      WebhookCreateRequest,
      Webhook,
      WebhookDeliveryPayload,
      DlqJob,
      DlqListResponse,
    } from './schemas.js';

    // Events surface (MOD-03).
    export {
      reportingRegistry,
      REPORTING_EVENT_NAMES,
      REPORTING_AGGREGATE_ID,
      makeReportingEmitters,
      webhookScheduledPayload,
      webhookDeliveredPayload,
      webhookFailedPayload,
      webhookFailedRetryExhaustedPayload,
    } from './events.js';
    export type { ReportingRegistry, ReportingEmitters, ReportingEventName } from './events.js';

    // Queue surface (QUEUE-06 + QUEUE-05 DLQ).
    export {
      WEBHOOK_DELIVER_QUEUE_NAME,
      WEBHOOK_DELIVER_DLQ_QUEUE_NAME,
      registerWebhookDeliveryWorkers,
    } from './queue.js';
    export type {
      RegisterWebhookDeliveryWorkersDeps,
      WebhookDeliveryRegistration,
    } from './queue.js';

    // NOTE: routes.ts / registerReportingRoutes is NOT re-exported — routes are
    // plugin-internal (called by reporting/plugin.ts only). Barrel consumers who
    // need the route shape can import DlqJob / DlqListResponse schemas above.
    ```

    **Structural invariant**: MOD-02 requires EXACTLY ONE re-export line matching `from '\\./internal/` — the inline type modifier keeps `createReportingModule` + `ReportingModule` + `CreateReportingModuleDeps` on a single statement.

    **Do NOT export `registerReportingRoutes`** — the route registration function is an implementation detail of the plugin and is NOT consumable from outside the module.

    **Do NOT use `export *`** — per ADR-002 / MOD-02, named exports only (internal helpers would leak otherwise).
  </action>
  <verify>
    <automated>test -f server/reporting/index.ts && grep -c "from './internal/" server/reporting/index.ts | awk '{ if ($1 != 1) { print "ERROR: MOD-02 requires EXACTLY ONE internal/ re-export line; got", $1; exit 1 } else { print "OK: 1 internal/ line" } }' && grep -c "export { createReportingModule, type ReportingModule, type CreateReportingModuleDeps } from './internal/module.js'" server/reporting/index.ts | grep -q "^1$" && grep -c "export { default as reportingPlugin }" server/reporting/index.ts | grep -q "^1$" && grep -c "export { WebhookService }" server/reporting/index.ts | grep -q "^1$" && grep -c "export { FlakyDetector }" server/reporting/index.ts | grep -q "^1$" && grep -c "export { reportingRegistry" server/reporting/index.ts | grep -q "^1$" && grep -c "export { WEBHOOK_DELIVER_QUEUE_NAME" server/reporting/index.ts | grep -q "^1$" && grep -c "dlqJobSchema" server/reporting/index.ts | grep -q "^[1-9]" && grep -c "registerReportingRoutes" server/reporting/index.ts | awk '{ if ($1 != 0) { print "ERROR: registerReportingRoutes must NOT be re-exported from barrel (plugin-internal)"; exit 1 } else { print "OK: routes not exported" } }' && grep -c "export \*" server/reporting/index.ts | awk '{ if ($1 != 0) { print "ERROR: export * not allowed per ADR-002"; exit 1 } else { print "OK: named exports only" } }' && npx tsc --noEmit && npm run dep-check</automated>
  </verify>
  <acceptance_criteria>
    - `server/reporting/index.ts` exists
    - EXACTLY ONE line matches `from './internal/` (MOD-02 structural invariant — grep count = 1)
    - That one line uses inline `type` modifier: `export { createReportingModule, type ReportingModule, type CreateReportingModuleDeps } from './internal/module.js'` (grep exact match)
    - Exports `reportingPlugin` (default re-exported — grep match)
    - Exports `WebhookService`, `FlakyDetector`, `WebhookConfig` (back-compat classes + config — grep each)
    - Exports events surface: `reportingRegistry`, `REPORTING_EVENT_NAMES`, `REPORTING_AGGREGATE_ID`, `makeReportingEmitters`, 4 payload schemas, types (grep each)
    - Exports queue surface: `WEBHOOK_DELIVER_QUEUE_NAME`, `WEBHOOK_DELIVER_DLQ_QUEUE_NAME`, `registerWebhookDeliveryWorkers`, types (grep each)
    - Exports schema surface (Phase 17 `webhookCreateRequestSchema`/`webhookSchema` + Phase 19 `webhookDeliveryPayloadSchema`/`dlqJobSchema`/`dlqListResponseSchema`)
    - Does NOT export `registerReportingRoutes` (plugin-internal — grep count = 0)
    - Does NOT use `export *` (grep count = 0)
    - `npx tsc --noEmit` exits 0
    - `npm run dep-check` exits 0 (rule from plan 19-00 now enforces on real content; no deep-import violations anywhere in server/)
  </acceptance_criteria>
  <done>
    server/reporting/index.ts barrel ships with ONE internal/ re-export line (MOD-02 strict form). All public surface re-exported via named exports. dep-check green.
  </done>
</task>

<task type="auto">
  <name>Task 6.3: Rename remaining reporting test files .test.ts → .spec.ts via git mv</name>
  <files>server/reporting/__tests__/flaky-detector.spec.ts, server/reporting/__tests__/junit-generator.spec.ts</files>
  <read_first>
    - server/reporting/__tests__/flaky-detector.test.ts (current — content UNCHANGED by this rename)
    - server/reporting/__tests__/junit-generator.test.ts (current — content UNCHANGED by this rename)
    - .planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-04-PLAN.md Task 4.3 (precedent — `git mv .test.ts .spec.ts` with 100% similarity preserves blame)
    - Plan 19-00 Task 0.5 (precedent — `webhook-service.test.ts` → `.spec.ts` already done)
  </read_first>
  <action>
    **Run the renames via git mv.** Shell:

    ```bash
    git mv server/reporting/__tests__/flaky-detector.test.ts server/reporting/__tests__/flaky-detector.spec.ts
    git mv server/reporting/__tests__/junit-generator.test.ts server/reporting/__tests__/junit-generator.spec.ts
    ```

    **Do NOT modify the contents of either file.** Phase 30 (Test Migration Cleanup per MOD-04) will do the describe-tree alignment (matching MODULE.md Public API sections). This plan does the FILE-NAMING layer only — same scope as Phase 18 Plan 18-04.

    **If a file has already been renamed** (idempotent re-run), `test -f` on the new path returns 0 — treat as success.

    **Verify blame preservation.** `git log --follow` on each renamed file should show commits pre-dating Phase 19 (the files existed in v2.0 / earlier phases).

    **No test content adjustments.** The renamed files run verbatim. If any test depends on file-system path assumptions (e.g. a snapshot file named after the spec), fix those in a Phase 30 follow-up; the existing `.test.ts` tests don't rely on filename conventions.
  </action>
  <verify>
    <automated>test -f server/reporting/__tests__/flaky-detector.spec.ts && ! test -f server/reporting/__tests__/flaky-detector.test.ts && test -f server/reporting/__tests__/junit-generator.spec.ts && ! test -f server/reporting/__tests__/junit-generator.test.ts && ls server/reporting/__tests__/*.test.ts 2>&1 | awk '{ if (NF > 0) { print "ERROR: .test.ts files still present:", $0; exit 1 } else { print "OK: no .test.ts files in reporting" } }' && npx vitest run server/reporting/__tests__/flaky-detector.spec.ts server/reporting/__tests__/junit-generator.spec.ts --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - `test -f server/reporting/__tests__/flaky-detector.spec.ts` exits 0
    - `test -f server/reporting/__tests__/junit-generator.spec.ts` exits 0
    - `test -f server/reporting/__tests__/flaky-detector.test.ts` exits non-zero (old file gone)
    - `test -f server/reporting/__tests__/junit-generator.test.ts` exits non-zero (old file gone)
    - `ls server/reporting/__tests__/*.test.ts 2>&1` returns empty (no stale .test.ts in the directory)
    - `git log --follow -1 --format=%H server/reporting/__tests__/flaky-detector.spec.ts` returns a commit hash matching prior history (blame preserved)
    - `git log --follow -1 --format=%H server/reporting/__tests__/junit-generator.spec.ts` same
    - `npx vitest run server/reporting/__tests__/flaky-detector.spec.ts server/reporting/__tests__/junit-generator.spec.ts` exits 0 (renamed files still pass — content unchanged)
  </acceptance_criteria>
  <done>
    flaky-detector + junit-generator .test.ts renamed to .spec.ts via git mv. Content preserved; tests still pass. MOD-04 file-naming layer complete for the reporting module.
  </done>
</task>

<task type="auto">
  <name>Task 6.4: Nyquist delta gate — capture coverage + check against .planning/nyquist-baseline.json</name>
  <files>.planning/phases/19-reporting-migration-webhooks-dlq/19-06-SUMMARY.md</files>
  <read_first>
    - .planning/nyquist-baseline.json (baseline 48.29% lines at Phase 15 commit 55ff8ac — DO NOT overwrite)
    - scripts/capture-nyquist.mjs (Phase 15 Plan 15-09 script)
    - scripts/check-nyquist.mjs (Phase 15 Plan 15-09 script)
    - .planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-04-PLAN.md Task 4.4 + 18-04-SUMMARY.md (precedent — exclusion set for pre-existing fastify-zod-openapi v5 failures)
  </read_first>
  <action>
    **Step 1 — Run the full test suite with coverage via the nyquist:capture script.**

    ```bash
    npm run nyquist:capture
    ```

    If the capture fails due to pre-existing Phase 17 `fastify-zod-openapi` v5 `required` emission bug (documented in STATE.md §Blockers/Concerns and Phase 18 Plan 18-04 SUMMARY), use the Phase 18 exclusion pattern — exclude the failing boot-at-load test files:

    ```bash
    npx vitest run --coverage --exclude 'server/api/__tests__/routes.test.ts' --exclude 'server/api/__tests__/artifact-routes.test.ts' --exclude 'server/auth/__tests__/auth-plugin.test.ts' --exclude 'server/__tests__/plugin-order.spec.ts'
    # Then manually copy coverage-summary.json to the capture script's expected location
    # OR use an ad-hoc capture helper — see Phase 18 Plan 18-04 SUMMARY for the exact command line
    ```

    **IMPORTANT: Do NOT overwrite `.planning/nyquist-baseline.json`.** The baseline is frozen at Phase 15 Plan 15-09's snapshot (commit 55ff8ac, 48.29% lines). Overwriting would make the gate trivially pass against itself. If the capture script writes to baseline by default, either (a) redirect its output to a scratch location OR (b) git-restore the baseline file after capture.

    **Step 2 — Run the nyquist:check gate.**

    ```bash
    npm run nyquist:check
    ```

    Expected outcome: current `coverage.lines` (after Phase 19 adds new code in queue.ts + internal/module.ts + events.ts + routes.ts + 5 new spec files) should meet or exceed `baseline.coverage.lines - 2pp` = `48.29% - 2% = 46.29%`.

    RESEARCH §Metadata §Nyquist delta forecast predicted Phase 19 net delta ≈ +1 to +3pp (new specs pull in new lines). Phase 18 Plan 18-04 achieved +7.54pp delta. Phase 19 should be similar or slightly higher because reporting's new specs cover more lines relative to the module size.

    **Step 3 — Record outcome in SUMMARY.**

    Capture the exact numbers into the SUMMARY.md:

    - Current `coverage.lines`: <recorded %>
    - Baseline: 48.29%
    - Delta: <+/- pp>
    - Gate: PASSED (delta ≥ −2pp) / FAILED

    If gate FAILS: investigate which spec files weren't run (skipIf(!HAS_DB) for DB-gated specs; pre-existing boot failures). Phase 18 precedent documents that DB-gated specs DO run in capture (they either have TEST_DATABASE_URL or skip; skipping doesn't count against baseline). If root-cause is missing coverage on Phase 19 specifically (new code without tests), add follow-up plan OR extend existing specs.

    **Step 4 — Document pre-existing failure envelope.**

    Per Phase 18 precedent, document in SUMMARY every known pre-existing failure that's NOT Phase 19's fault (fastify-zod-openapi v5 `required` emission bug, Phase 15 Map-vs-RequestContext divergence, etc.). These go into `.planning/phases/19-reporting-migration-webhooks-dlq/deferred-items.md` (create if not present) so Phase 20+ inherit the knowledge.

    **Step 5 — Full verification sweep.** Run all the phase-close checks:

    ```bash
    npx tsc --noEmit
    npm run lint
    npm run dep-check
    DATABASE_URL=postgres://localhost/device_farm_test TEST_DATABASE_URL=postgres://localhost/device_farm_test npx vitest run server/reporting/__tests__/ server/hooks/__tests__/dep-cruiser.spec.ts server/__tests__/plugin-order.spec.ts server/queue/__tests__/plugin.spec.ts
    ```

    All must exit 0 OR match the documented pre-existing-failure envelope. Any NEW regression (a test that was green before Phase 19 and is now red due to Phase 19 code) is a blocker — fix before declaring phase close.
  </action>
  <verify>
    <automated>npm run nyquist:capture 2>&1 | tee /tmp/p19-05-nyquist-capture.log; tail -20 /tmp/p19-05-nyquist-capture.log; npm run nyquist:check 2>&1 | tee /tmp/p19-05-nyquist-check.log; tail -20 /tmp/p19-05-nyquist-check.log; test -f .planning/nyquist-baseline.json && node -e "const b = require('./.planning/nyquist-baseline.json'); console.log('Baseline lines:', b.coverage?.lines ?? 'MISSING'); if ((b.coverage?.lines ?? 0) !== 48.29) { console.error('Baseline was overwritten! Expected 48.29%, got', b.coverage?.lines); process.exit(1) } else { console.log('OK: baseline preserved') }"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run nyquist:capture` runs to completion (exit 0 OR exits with exclusions per Phase 18 precedent documented in 19-05-SUMMARY.md)
    - `npm run nyquist:check` exits 0 — current delta ≥ −2pp vs 48.29% baseline
    - `.planning/nyquist-baseline.json` `coverage.lines` is STILL 48.29% (NOT overwritten — verified in <verify>)
    - SUMMARY.md records exact current/baseline/delta numbers
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `npm run dep-check` exits 0
    - `DATABASE_URL=... npx vitest run server/reporting/__tests__/` exits 0 — all 9 reporting specs pass (webhook-service + events + module + queue + correlation + terminal-event + dlq-route + flaky-detector + junit-generator)
    - Pre-existing failures documented in `.planning/phases/19-reporting-migration-webhooks-dlq/deferred-items.md` (create if needed) per Phase 18 precedent
    - No NEW regressions introduced (any red test must have been red before Phase 19 — demonstrable via git checkout HEAD~5 or similar pre-Phase-19 commit)
  </acceptance_criteria>
  <done>
    Nyquist delta gate: current vs baseline (48.29%) ≥ −2pp. Phase 19 closes with full suite green + dep-check green + typecheck green. Pre-existing failures documented in deferred-items.md. Baseline file preserved.
  </done>
</task>

</tasks>

<verification>
- `server/reporting/MODULE.md` exists with 9 H2 sections in exact order; every invariant has a test reference
- `server/reporting/index.ts` barrel exists with ONE internal/ re-export line (MOD-02 strict form)
- `server/reporting/__tests__/flaky-detector.spec.ts` + `junit-generator.spec.ts` renamed from .test.ts via git mv (content preserved, blame preserved)
- No .test.ts files remain in `server/reporting/__tests__/` (`ls *.test.ts` returns empty)
- `npm run dep-check` green — no deep-import violations
- Nyquist delta gate: `npm run nyquist:check` exits 0; current `coverage.lines` ≥ `48.29% - 2% = 46.29%`
- `.planning/nyquist-baseline.json` untouched (still 48.29% baseline)
- Full suite verification: `npx tsc --noEmit` + `npm run lint` + `npm run dep-check` + `DATABASE_URL=... npx vitest run server/reporting/__tests__/ server/hooks/__tests__/dep-cruiser.spec.ts server/__tests__/plugin-order.spec.ts` all exit 0 (or match documented pre-existing-failure envelope)
</verification>

<success_criteria>
- MOD-01: reporting/MODULE.md ships LLM-first public contract (9 fixed sections + runnable example)
- MOD-02: reporting/index.ts barrel is the ONLY import surface outside server/reporting/; dep-cruiser enforces structurally; ONE internal/ re-export line
- MOD-04: `.test.ts → .spec.ts` naming alignment complete for server/reporting/__tests__/ (file-naming layer; describe-tree alignment deferred to Phase 30)
- DEBT-03: Nyquist delta gate passes — coverage delta ≥ −2pp vs Phase 15 baseline
- ROADMAP §Phase 19 SC3: "Reporting module follows Phase 16 conventions" — TRUE (MODULE.md + barrel + events.ts + queue.ts + internal/ + tests-as-spec naming all in place)
- Phase 19 CLOSED: EVENTS-07 + QUEUE-05 requirements complete; reporting module fully migrated; downstream Phase 20+ can extend the pattern
- Pre-existing failures logged to deferred-items.md per Phase 18 precedent so Phase 20+ can decide whether to hotfix before or after their scope
</success_criteria>

<output>
Create `.planning/phases/19-reporting-migration-webhooks-dlq/19-06-SUMMARY.md` with:
- server/reporting/MODULE.md line count + word count + section list (confirm all 9 H2s present)
- server/reporting/index.ts line count + full list of exported symbols + the ONE internal/ re-export line verbatim
- git mv output for both flaky-detector + junit-generator renames (showing 100% similarity)
- `git log --follow -1` output for both renamed files showing blame preservation
- Nyquist delta capture + check output (current %, baseline %, delta pp, gate status)
- Confirmation that `.planning/nyquist-baseline.json` was NOT overwritten (still 48.29%)
- Full pre-existing-failure envelope documented in `.planning/phases/19-reporting-migration-webhooks-dlq/deferred-items.md` (create if needed; reference Phase 18 precedent)
- `npm run dep-check` + `npx tsc --noEmit` + `npm run lint` + `DATABASE_URL=... npx vitest run server/reporting/__tests__/` outputs
- Phase 19 final status: CLOSED (or flagged with specific deferred items)
- Roll-up across Phase 19: all 7 plans (19-00 substrate + 19-01 events + 19-02 webhook-service + 19-03 queue+module+plugin + 19-04 DB-gated specs + 19-05 DLQ route + 19-06 close) with per-plan test counts + line counts
</output>
