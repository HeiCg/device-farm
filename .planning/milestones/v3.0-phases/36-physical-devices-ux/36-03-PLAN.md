---
phase: 36-physical-devices-ux
plan: 03
type: execute
wave: 2
depends_on: ["36-00"]
files_modified:
  - web/src/lib/components/CommandPalette.svelte
  - web/src/lib/command-palette/registry.ts
  - web/src/lib/command-palette/recent.svelte.ts
  - web/src/lib/command-palette/fuzzy.ts
  - web/src/routes/+layout.svelte
  - web/src/lib/components/__tests__/CommandPalette.spec.ts
  - web/src/lib/command-palette/__tests__/recent.spec.ts
autonomous: true
requirements:
  - CMD-PALETTE

must_haves:
  truths:
    - "⌘K (or Ctrl+K on non-mac) opens the CommandPalette dialog"
    - "Esc closes the dialog"
    - "Typing fuzzy-ranks devices + jobs + pages + actions; Enter runs the selected item"
    - "Recent action ids persisted in localStorage under key `device-farm-palette-recent` (FIFO max 5)"
    - "Hand-rolled Svelte 5 component — NO cmdk-sv, NO bits-ui dependency"
    - "Devices source flows from deviceStore (Plan 36-02 Task 3 created the WS-backed store)"
  artifacts:
    - path: "web/src/lib/components/CommandPalette.svelte"
      provides: "Full dialog + input + listbox + keyboard navigation"
      min_lines: 150
    - path: "web/src/lib/command-palette/registry.ts"
      provides: "PaletteAction + PaletteStep types + getActions() returning real action list"
      min_lines: 60
    - path: "web/src/lib/command-palette/fuzzy.ts"
      provides: "rank(query, items) wrapping fuzzysort.go with keys + threshold"
      min_lines: 30
    - path: "web/src/lib/command-palette/recent.svelte.ts"
      provides: "getRecent + pushRecent with localStorage + MAX=5 FIFO"
      min_lines: 30
    - path: "web/src/routes/+layout.svelte"
      provides: "Global ⌘K / Ctrl+K handler wired to CommandPalette"
      contains: "openPalette"
  key_links:
    - from: "web/src/routes/+layout.svelte"
      to: "web/src/lib/components/CommandPalette.svelte"
      via: "import + global keydown handler"
      pattern: "openPalette"
    - from: "web/src/lib/components/CommandPalette.svelte"
      to: "web/src/lib/command-palette/fuzzy.ts rank()"
      via: "$derived.by ranking"
      pattern: "rank\\("
    - from: "web/src/lib/components/CommandPalette.svelte"
      to: "web/src/lib/stores/devices.svelte.ts deviceStore"
      via: "import + items aggregation"
      pattern: "deviceStore"
---

<objective>
Implement T-36.5 CommandPalette — a hand-rolled Svelte 5 ⌘K command palette with fuzzy search across devices, jobs, pages, and actions. NO `cmdk-sv` (deprecated), NO `bits-ui` (too heavy). ~150 LOC Svelte 5 + `fuzzysort@^3` (installed in Plan 36-00).

Purpose: Keyboard-first operator UX. Press ⌘K → type "pixel" → ranks Pixel 8 first → Enter opens its stream. Press ⌘K → type "pair" → ranks "Pair device" action first → Enter navigates to `/devices/pair`.

Plan runs in parallel with Plan 36-02 (no file overlap; only depends on Wave 0 substrate from Plan 36-00). Web-only changes.

Output: 4 source files populated, 1 layout edit for global hotkey, 2 vitest specs converted (recent + CommandPalette behavior).
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-physical-devices-ux/36-RESEARCH.md
@.planning/phases/36-physical-devices-ux/36-VALIDATION.md
@.planning/phases/36-physical-devices-ux/36-00-SUMMARY.md
@web/src/lib/components/CommandPalette.svelte
@web/src/lib/command-palette/registry.ts
@web/src/lib/command-palette/recent.svelte.ts
@web/src/lib/command-palette/fuzzy.ts
@web/src/routes/+layout.svelte
@web/src/lib/stores/devices.svelte.ts

<interfaces>
<!-- Wave 0 stubs (Plan 36-00) — populate against these. -->

From web/src/lib/command-palette/registry.ts (Wave 0 stub):
```typescript
export type PaletteStep =
  | { type: 'device-select'; multi: boolean; filter?: (d: Device) => boolean }
  | { type: 'parameter'; key: string; placeholder?: string }
  | { type: 'confirm'; message: string; destructive?: boolean };
export interface ActionContext { goto: (href: string) => void; }
export type PaletteAction = {
  kind: 'action';
  id: string;
  label: string;
  description: string;
  keywords: string[];
  steps?: PaletteStep[];
  run: (ctx: ActionContext) => Promise<void> | void;
};
export function getActions(): PaletteAction[];
```

From web/src/lib/stores/devices.svelte.ts (Plan 36-02 Task 3 — may not exist yet if Plan 36-02 hasn't run):
```typescript
// MUST handle gracefully — if devices array is empty (Plan 36-02 not yet executed),
// the palette still works with actions + pages + jobs sources.
export const deviceStore: { devices: Device[]; connect(): void; disconnect(): void };
```

From fuzzysort@^3 (Plan 36-00 installed):
```typescript
import fuzzysort from 'fuzzysort';
const results = fuzzysort.go(query, items, { keys: ['label', 'sub'], threshold: -10000, limit: 50 });
// Each result has { obj, score, [key]: highlight }
```

From SvelteKit (current project):
- `$app/navigation` exports `goto(href)`
- `$app/stores` exports `page` store
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement registry + fuzzy + recent (data layer)</name>
  <files>
    web/src/lib/command-palette/registry.ts,
    web/src/lib/command-palette/fuzzy.ts,
    web/src/lib/command-palette/recent.svelte.ts,
    web/src/lib/command-palette/__tests__/recent.spec.ts
  </files>
  <read_first>
    - All Wave 0 stubs under `web/src/lib/command-palette/`
    - .planning/phases/36-physical-devices-ux/36-RESEARCH.md §"CommandPalette" → Component Skeleton + Action Registry Shape + fuzzysort Tuning + Recent Actions
    - node_modules/fuzzysort/fuzzysort.d.ts (TS types for `fuzzysort.go`)
    - web/src/routes/ (page list — feeds the "pages" source)
  </read_first>
  <behavior>
    **registry.ts:**
    - `getActions()` returns a populated array (≥6 actions):
      - `{ id: 'pair-device', label: 'Pair device', description: 'Wireless ADB pairing wizard', keywords: ['adb','wifi','pair'], run: ctx => ctx.goto('/devices/pair') }`
      - `{ id: 'go-jobs', label: 'Go to Jobs', description: 'Open jobs list', keywords: ['job','runs'], run: ctx => ctx.goto('/jobs') }`
      - `{ id: 'go-devices', label: 'Go to Devices', description: 'Open devices list', keywords: ['devices','pool'], run: ctx => ctx.goto('/devices') }`
      - `{ id: 'go-pipelines', label: 'Go to Pipelines', description: 'Open pipelines list', keywords: ['pipeline','schedule'], run: ctx => ctx.goto('/pipelines') }`
      - `{ id: 'go-settings', label: 'Go to Settings', description: 'Open settings', keywords: ['config'], run: ctx => ctx.goto('/settings') }`
      - `{ id: 'clear-logs', label: 'Clear logs viewer', description: 'Clear current job logs viewer', keywords: ['logs','clear'], run: async () => { /* document deferral if no logs viewer state */ } }`
    - Type contracts unchanged from Wave 0 stub.

    **fuzzy.ts:**
    - `rank(query, items)`:
      - When `query.trim()` is empty → return `items.slice(0, 50)` (unranked, alphabetic by label)
      - Else: `fuzzysort.go(query, items, { keys: ['label', 'sub', 'keywords'], threshold: -10000, limit: 50 })` → return `results.map(r => r.obj)`
    - `FuzzyItem` shape (extended from Wave 0 stub):
      ```typescript
      export interface FuzzyItem {
        id: string;
        label: string;
        sub?: string;
        keywords?: string[];
        kind: 'action' | 'device' | 'job' | 'page';
        run: (ctx: ActionContext) => Promise<void> | void;
      }
      ```
    - Note: fuzzysort expects strings for keyed-search, not arrays. If `keywords` is `string[]`, join with space before passing OR pre-flatten in the items mapper.

    **recent.svelte.ts:**
    - Module-level constants `STORAGE_KEY = 'device-farm-palette-recent'`, `MAX = 5`
    - `getRecent(): string[]`:
      - Reads `localStorage.getItem(STORAGE_KEY)`
      - `JSON.parse` with try/catch → returns `[]` on failure
      - Returns array slice to MAX
    - `pushRecent(actionId: string): void`:
      - Read current list
      - Remove existing occurrence (move-to-head behavior)
      - Prepend new id
      - Slice to MAX
      - Write back via `localStorage.setItem`
    - Guards against SSR: `if (typeof localStorage === 'undefined') return [...]`
  </behavior>
  <action>
    1. **registry.ts** — replace `getActions()` stub with the 6-action list per Behavior. Keep `PaletteAction` + `PaletteStep` + `ActionContext` types unchanged.

    2. **fuzzy.ts** — implement `rank` + extend `FuzzyItem` shape. Use `fuzzysort.go` with `keys: ['label', 'sub', 'keywords']` (NOTE: pre-flatten keywords array to string in `items` mapper OR use a wrapper that joins arrays). `threshold: -10000`, `limit: 50`.

    3. **recent.svelte.ts** — implement `getRecent` + `pushRecent` with localStorage. Add SSR guard.

    4. **Spec — recent.spec.ts:** Mock `localStorage` via `vi.stubGlobal('localStorage', { ... })` or jsdom default. Tests:
       - "pushRecent appends to localStorage"
       - "pushRecent respects MAX=5 FIFO (6th push drops oldest)"
       - "getRecent returns [] on empty storage"
       - "getRecent returns [] when JSON.parse fails"
       - "duplicate pushRecent moves entry to head without duplicating (list length unchanged)"
       - "SSR safe — getRecent in environment with no localStorage returns []"

    Optional but recommended: also write a fuzzy-rank micro-spec under `web/src/lib/command-palette/__tests__/fuzzy.spec.ts` (not in files_modified — can be added in this task as extra) with 2 tests:
       - "rank('') returns first 50 items unranked"
       - "rank('pixel') with mock items including Pixel 8 ranks Pixel 8 first"
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run check &amp;&amp; npx vitest run src/lib/command-palette/__tests__/ &amp;&amp; cd ..</automated>
  </verify>
  <done>
    - `getActions()` returns 6+ real `PaletteAction` entries
    - `rank()` wraps fuzzysort.go with correct keys + threshold
    - `getRecent` + `pushRecent` persist via localStorage with MAX=5 FIFO + move-to-head + SSR guard
    - 6+ tests pass in recent.spec.ts (+ optional 2 in fuzzy.spec.ts)
    - `cd web && npm run check` clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CommandPalette.svelte (dialog + input + listbox + keyboard nav)</name>
  <files>
    web/src/lib/components/CommandPalette.svelte,
    web/src/lib/components/__tests__/CommandPalette.spec.ts
  </files>
  <read_first>
    - web/src/lib/components/CommandPalette.svelte (Wave 0 stub — already exports openPalette/closePalette)
    - web/src/lib/command-palette/registry.ts (Task 1 output)
    - web/src/lib/command-palette/fuzzy.ts (Task 1 output)
    - web/src/lib/command-palette/recent.svelte.ts (Task 1 output)
    - web/src/lib/stores/devices.svelte.ts (consume — may be empty array if Plan 36-02 hasn't run yet; handle gracefully)
    - .planning/phases/36-physical-devices-ux/36-RESEARCH.md §"Component Skeleton" + §"Keyboard Nav" + §"Pitfall 7 Svelte 5 derived vs effect"
    - Svelte 5 docs (via svelte MCP if needed) for `$state`, `$derived.by`, `$effect`, `<dialog>` integration
  </read_first>
  <behavior>
    - Component exports `openPalette()` + `closePalette()` (keep Wave 0 signature)
    - Internal state:
      - `let open = $state(false)`
      - `let search = $state('')`
      - `let selected = $state(0)` — index into filtered list
      - `let dialogEl: HTMLDialogElement | undefined`
    - `openPalette()`: `open = true; search = ''; selected = 0; requestAnimationFrame(() => dialogEl?.showModal())` — focus the input on next tick
    - `closePalette()`: `open = false; dialogEl?.close()`
    - Items aggregation via `$derived.by`:
      ```typescript
      let items = $derived.by(() => {
        const actions = getActions().map(a => ({ id: a.id, label: a.label, sub: a.description, keywords: a.keywords.join(' '), kind: 'action' as const, run: a.run }));
        const devices = deviceStore.devices.map(d => ({ id: 'device:' + d.id, label: d.name, sub: `${d.platform} · ${d.state}`, kind: 'device' as const, run: () => goto(`/devices/${d.id}`) }));
        const pages = [
          { id: 'page:pair', label: 'Pair device', sub: 'Pair a new wireless Android', kind: 'page' as const, run: () => goto('/devices/pair') },
          // more pages: jobs, devices, pipelines, settings
        ];
        return [...actions, ...devices, ...pages];
      });
      ```
    - Filtered list via `$derived.by` (NOT `$effect` — Pitfall 7):
      ```typescript
      let filtered = $derived.by(() => rank(search, items));
      ```
    - Reset `selected = 0` when filtered length changes (via `$effect` watching `filtered.length`)
    - Keyboard handlers on the input element (inside dialog):
      - `ArrowDown` → `selected = (selected + 1) % filtered.length`
      - `ArrowUp` → `selected = (selected - 1 + filtered.length) % filtered.length`
      - `Enter` → if `filtered[selected]` exists: run it with `{ goto }` context, pushRecent(filtered[selected].id), closePalette()
      - `Escape` → closePalette()
    - Renders a `<dialog bind:this={dialogEl}>` with:
      - `<input bind:value={search}>` autofocus
      - `<ul role="listbox">` with `<li role="option" aria-selected={i === selected}>` for each filtered item
      - Hover/click on `<li>` runs the item
    - Imports `goto` from `$app/navigation`
    - Imports `deviceStore` from `$lib/stores/devices.svelte.js` — wrap in `try` import or use a defensive fallback if module doesn't exist yet (it does — Wave 0 created the stub)
  </behavior>
  <action>
    1. Implement `web/src/lib/components/CommandPalette.svelte` per Behavior. Hand-rolled, ~150 LOC. NO third-party UI library beyond `fuzzysort` (used inside `rank()`).

    2. Add minimal CSS — small styled dialog (centered, ~600px wide, max-h-80vh). Use Tailwind utility classes if the project uses Tailwind v4 (per project CLAUDE.md it does).

    3. **Spec — `CommandPalette.spec.ts`:** Use `@testing-library/svelte` (verify it's installed in web; if not, document gap and use vanilla DOM testing). Tests:
       - "openPalette() sets open=true and opens the dialog"
       - "closePalette() sets open=false"
       - "Esc keydown closes the dialog"
       - "typing in input updates filtered list (via rank)"
       - "ArrowDown advances selected index; wraps at end"
       - "ArrowUp decrements selected index; wraps at start"
       - "Enter runs filtered[selected].run() then closes the dialog"
       - "Enter pushes action.id to recent (mock pushRecent + assert called with correct id)"
       - "typing 'pair' with default actions ranks 'Pair device' first"

       If `@testing-library/svelte` is NOT installed, fall back to a simpler approach: import the component, mount via jsdom, dispatch keyboard events via `new KeyboardEvent('keydown', {key: ...})` on the input, assert state via the rendered DOM. Document the testing approach choice in the spec file header.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run check &amp;&amp; npx vitest run src/lib/components/__tests__/CommandPalette.spec.ts &amp;&amp; cd ..</automated>
  </verify>
  <done>
    - `CommandPalette.svelte` is ~150 LOC, hand-rolled, NO cmdk-sv NOR bits-ui imports
    - 8+ tests pass in CommandPalette.spec.ts
    - `cd web && npm run check` clean
    - Dialog opens/closes via openPalette/closePalette exports — verified by spec
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire global ⌘K handler in +layout.svelte + connect deviceStore</name>
  <files>
    web/src/routes/+layout.svelte
  </files>
  <read_first>
    - web/src/routes/+layout.svelte (current — see how it integrates other components)
    - web/src/lib/components/CommandPalette.svelte (Task 2 output)
    - web/src/lib/stores/devices.svelte.ts (call connect() on mount)
  </read_first>
  <behavior>
    - Import `CommandPalette` and `deviceStore`
    - Use Svelte 5 component reference binding to access `openPalette` / `closePalette`
    - `$effect` on mount:
      - Add `window.addEventListener('keydown', onKey)` where `onKey` checks `(e.metaKey || e.ctrlKey) && e.key === 'k'`:
        - `e.preventDefault()`
        - call `paletteRef.openPalette()`
      - Cleanup: remove the listener on unmount
    - `$effect` for store connection: call `deviceStore.connect()` on mount; `deviceStore.disconnect()` on unmount
    - Render `<CommandPalette bind:this={paletteRef} />` at the end of the layout (after `<slot />` or `{@render children()}` depending on Svelte 5 convention used in the project)
  </behavior>
  <action>
    1. Edit `web/src/routes/+layout.svelte`:
       - Add imports for `CommandPalette` + `deviceStore`
       - Add `let paletteRef: { openPalette(): void; closePalette(): void } | undefined = $state()`
       - Add `$effect(() => { ... })` block with the global keydown handler + cleanup return
       - Add another `$effect(() => { deviceStore.connect(); return () => deviceStore.disconnect(); })` for the WS connection
       - Add `<CommandPalette bind:this={paletteRef} />` rendering

    2. If the project's `+layout.svelte` uses Svelte 4 patterns (no runes), upgrade just those lines to Svelte 5 syntax while keeping the rest of the file untouched. Per project CLAUDE.md, web is already on Svelte 5 runes — assume runes context.

    3. Smoke verify (manual checkpoint inline — not a checkpoint task): `cd web && npm run dev` and document that ⌘K opens the palette in the SUMMARY (no automated test for this wiring — it would need full SvelteKit jsdom mount; if the project has Playwright, write a smoke test; otherwise document deferral).
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run check &amp;&amp; npm run build &amp;&amp; cd ..</automated>
  </verify>
  <done>
    - `+layout.svelte` imports `CommandPalette` + `deviceStore`
    - Global keydown handler registered on mount + cleaned up on unmount
    - `deviceStore.connect()` called on mount + `disconnect()` on unmount
    - `cd web && npm run check` clean; `cd web && npm run build` succeeds (validates SvelteKit can compile the layout + component together)
  </done>
</task>

</tasks>

<verification>
After all 3 tasks:

1. `cd web && npm run check` — clean
2. `cd web && npx vitest run src/lib/command-palette/ src/lib/components/__tests__/CommandPalette.spec.ts` — all green
3. `cd web && npm run build` — succeeds (validates production build of palette + layout + dialog)
4. Manual smoke (documented in summary, not enforced): `cd web && npm run dev`, open http://localhost:5173, press ⌘K, type "pair", press Enter → navigates to `/devices/pair`
</verification>

<success_criteria>
- CMD-PALETTE requirement complete: hand-rolled Svelte 5 component with ⌘K global hotkey, fuzzysort ranking, ≥6 actions, devices + jobs + pages sources, localStorage recents, keyboard nav (ArrowUp/Down/Enter/Esc)
- NO `cmdk-sv` NOR `bits-ui` imports in package.json or any component (grep verification)
- ~150 LOC for CommandPalette.svelte (component LOC budget per RESEARCH §"Component Skeleton")
- Layout integration: deviceStore.connect() is called on mount so palette has fresh device data immediately
- All tests pass; production build of web succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/36-physical-devices-ux/36-03-SUMMARY.md` documenting:
- LOC of CommandPalette.svelte
- Action registry contents (6+ actions)
- Test counts (recent + CommandPalette + optional fuzzy specs)
- Manual smoke result (⌘K opens palette in dev mode)
- Pointer: "Plan 36-04 builds the pairing wizard UI which uses one of the palette actions"
</output>
