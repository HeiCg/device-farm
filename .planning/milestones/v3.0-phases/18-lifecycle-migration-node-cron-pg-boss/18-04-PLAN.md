---
phase: 18
plan: 04
type: execute
wave: 3
depends_on: [18-03]
files_modified:
  - server/lifecycle/MODULE.md
  - server/lifecycle/index.ts
  - server/lifecycle/__tests__/compression-task.spec.ts
  - server/lifecycle/__tests__/retention-task.spec.ts
  - server/lifecycle/__tests__/disk-pressure-task.spec.ts
autonomous: true
requirements: [QUEUE-08]
must_haves:
  truths:
    - "`server/lifecycle/MODULE.md` exists with ALL 9 fixed H2 sections in exact order: Purpose / Public API / Events Emitted / Events Consumed / Queue Produced / Queue Consumed / Invariants / Non-Goals / Dependencies"
    - "MODULE.md Events Emitted lists all 4 persisted events; Queue Produced lists all 3 queues with crons + policy:'stately' + singletonKey documentation; Queue Consumed lists same 3 queues (self-loop); Dependencies matches plugin.ts dependencies array verbatim"
    - "`server/lifecycle/index.ts` barrel exists and exports ONLY public API: `lifecyclePlugin` (default), `createLifecycleModule`, type `LifecycleModule`, type `LifecycleStats`, type `LifecycleEmitters`, type `LifecycleRegistry`, `lifecycleRegistry`, `LIFECYCLE_EVENT_NAMES`, `makeLifecycleEmitters`, `LIFECYCLE_COMPRESS_DAILY_QUEUE_NAME` + 2 siblings, `lifecycleJobPayloadSchema`, type `LifecycleJobPayload`"
    - "`server/lifecycle/__tests__/*.test.ts` RENAMED to `*.spec.ts` for MOD-04 consistency: compression-task.spec.ts, retention-task.spec.ts, disk-pressure-task.spec.ts — each still passes `npx vitest run`"
    - "`npm run dep-check` passes — no deep imports into `server/lifecycle/internal/*` exist in the committed tree (dep-cruiser rule from plan 18-00 Task 0.4 is now enforced on real code: `internal/module.ts` exists from plan 18-03)"
    - "`npm run nyquist:capture` + `npm run nyquist:check` exits 0 — coverage delta on `coverage.lines` vs `.planning/nyquist-baseline.json` (48.29%) is ≥ −2pp (SC3 Nyquist gate per Phase 16 convention)"
    - "`npm test` full suite exits 0 with `DATABASE_URL` set; `npm run lint` exits 0; `npm run typecheck` exits 0"
  artifacts:
    - path: "server/lifecycle/MODULE.md"
      provides: "LLM-first public contract for the lifecycle module (MOD-01)"
      contains: "## Purpose"
    - path: "server/lifecycle/index.ts"
      provides: "Public barrel — ONLY legitimate import surface outside server/lifecycle/ (MOD-02)"
      contains: "createLifecycleModule"
    - path: "server/lifecycle/__tests__/compression-task.spec.ts"
      provides: "Renamed from compression-task.test.ts for MOD-04 naming consistency"
      contains: "runCompressionTask"
  key_links:
    - from: "server/lifecycle/MODULE.md"
      to: "server/lifecycle/index.ts"
      via: "Public API section lists barrel exports"
      pattern: "index\\.ts"
    - from: "server/lifecycle/index.ts"
      to: "server/lifecycle/internal/module.ts"
      via: "The ONLY allowed internal/ re-export per MOD-02 barrel rule (createLifecycleModule + LifecycleModule type)"
      pattern: "from './internal/module"
    - from: "server/lifecycle/MODULE.md Invariants"
      to: "server/lifecycle/__tests__/queue.spec.ts + correlation.spec.ts + graceful-shutdown.spec.ts"
      via: "Each invariant maps to a spec file+test reference"
      pattern: "\\[Invariant"
---

<objective>
Close Phase 18. Ship the LLM-first `MODULE.md` with all 9 fixed H2 sections documenting the now-complete lifecycle module (plans 18-01/02/03 delivered the code; this plan documents + barrels it). Create the `server/lifecycle/index.ts` barrel (MOD-02) as the single public import surface — dependency-cruiser's `no-deep-imports-into-lifecycle-internal` rule from plan 18-00 Task 0.4 becomes structurally meaningful the moment this barrel exists alongside `internal/module.ts` (from plan 18-03). Rename the three existing `*.test.ts` files under `server/lifecycle/__tests__/` to `*.spec.ts` for MOD-04 naming consistency (pre-empting Phase 30's repo-wide migration; CONTEXT open-question #5 pre-resolved to rename-in-this-phase). Finally, run the Nyquist delta gate and confirm coverage delta ≥ −2pp vs baseline.

Purpose: MOD-01 requires every v3.0 module to ship MODULE.md with 9 fixed sections so LLMs read a single file to understand the public contract. MOD-02 requires a barrel that `dependency-cruiser` enforces structurally. Phase 18 is not DONE until these two artifacts exist — otherwise plan 18-03 ships working code but Phase 16 convention parity isn't reached, and the ROADMAP §Phase 18 SC3 ("Lifecycle module follows Phase 16 conventions") is false. Nyquist is the final CI gate per DEBT-03 (every v3.0 phase ends with the coverage check); running it here closes the phase.

Output: `MODULE.md` (~75 lines per hooks template), `index.ts` barrel (~30 lines per hooks template), 3 `.test.ts → .spec.ts` renames, Nyquist delta captured + recorded in SUMMARY.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-CONTEXT.md
@.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-RESEARCH.md
@.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-VALIDATION.md
@.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-00-PLAN.md
@.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-01-PLAN.md
@.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-02-PLAN.md
@.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-03-PLAN.md
@.planning/nyquist-baseline.json
@docs/adr/001-spec-driven-architecture.md
@docs/adr/002-file-naming.md
@server/hooks/MODULE.md
@server/hooks/index.ts
@server/lifecycle/events.ts
@server/lifecycle/queue.ts
@server/lifecycle/plugin.ts
@server/lifecycle/internal/module.ts
@server/lifecycle/schemas.ts
@server/lifecycle/stats.ts

<interfaces>
<!-- Everything this plan imports/references is already committed by plans 18-00..18-03. -->

From server/hooks/MODULE.md (template — 9 sections in this order):
```
## Purpose
## Public API
## Events Emitted
## Events Consumed
## Queue Produced
## Queue Consumed
## Invariants
## Non-Goals
## Dependencies

### Runnable Example   (H3, optional — phase 27 MOD-09 typechecks the block)
```

From server/hooks/index.ts (template — public barrel structure):
```typescript
// Fastify plugin
export { default as hooksPlugin } from './plugin.js';
// Factory surface + types
export { createHooksModule } from './internal/module.js';
export type { HooksModule, CreateHooksModuleDeps } from './internal/module.js';
// Schemas + derived types
export { hookDefinitionSchema } from './schemas.js';
export type { HookDefinition, HookEvent } from './schemas.js';
// Events surface
export { hooksRegistry, HOOK_EVENT_NAMES, makeHookEmitters, /* payloads */ } from './events.js';
export type { HooksRegistry, HookEmitters, HookEventName } from './events.js';
// Queue surface
export { HOOK_RUN_QUEUE_NAME, hookRunPayloadSchema, registerHookRunWorker } from './queue.js';
export type { HookRunPayload, RegisterHookRunWorkerDeps } from './queue.js';
```

From server/lifecycle/events.ts (plan 18-01):
- Exports: lifecycleRegistry, LIFECYCLE_EVENT_NAMES, makeLifecycleEmitters, compressionCompletedPayload, retentionCompletedPayload, diskCheckedPayload, taskFailedPayload, types LifecycleRegistry, LifecycleEmitters, LifecycleEventName

From server/lifecycle/queue.ts (plan 18-02):
- Exports: LIFECYCLE_COMPRESS_DAILY_QUEUE_NAME, LIFECYCLE_RETENTION_DAILY_QUEUE_NAME, LIFECYCLE_DISK_HOURLY_QUEUE_NAME, COMPRESS_CRON, RETENTION_CRON, DISK_CRON, registerLifecycleSchedulesAndWorkers, lifecycleJobPayloadSchema, type LifecycleJobPayload

From server/lifecycle/schemas.ts (plan 18-00):
- Exports: lifecycleJobPayloadSchema, compressionResultSchema, retentionResultSchema, diskPressureResultSchema, types LifecycleJobPayload, CompressionResultParsed, RetentionResultParsed, DiskPressureResultParsed

From server/lifecycle/stats.ts (plan 18-02):
- Exports: type LifecycleStats

From server/lifecycle/internal/module.ts (plan 18-03):
- Exports: createLifecycleModule, types CreateLifecycleModuleDeps, LifecycleModule

Nyquist baseline:
```json
// .planning/nyquist-baseline.json (committed)
{
  "lines": { "pct": 48.29 },
  "branches": { "pct": 36 },
  "functions": { "pct": 45.21 },
  "statements": { "pct": 47.06 }
}
```
Gate: delta on `coverage.lines` >= -2pp (i.e. current >= 46.29%). Phase 16 Plan 16-04 measured +3.63pp delta; Phase 17 stayed green. Phase 18 should stay in the same band given the unit + DB-gated spec additions offset the deleted mutex/cron branches per RESEARCH §Pitfall 7.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Write server/lifecycle/MODULE.md (9 fixed sections + runnable example)</name>
  <files>server/lifecycle/MODULE.md</files>
  <read_first>
    - server/hooks/MODULE.md (canonical 9-section template — mirror structure verbatim)
    - docs/adr/002-file-naming.md (MODULE.md section conventions)
    - server/lifecycle/events.ts (Events Emitted — 4 events listed)
    - server/lifecycle/queue.ts (Queue Produced — 3 queues, crons, singletonKey policy)
    - server/lifecycle/plugin.ts (Dependencies section — exact dependencies array)
    - server/lifecycle/__tests__/queue.spec.ts (invariant citations)
    - server/lifecycle/__tests__/correlation.spec.ts (invariant citations)
    - server/lifecycle/__tests__/graceful-shutdown.spec.ts (invariant citations)
    - .planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-RESEARCH.md §Example 4 (MODULE.md skeleton sketch)
  </read_first>
  <action>
    **Create `server/lifecycle/MODULE.md`** with EXACTLY 9 H2 sections in this order. Prose style matches hooks/MODULE.md — concise, ≤5 lines per section where possible.

    ```markdown
    # Lifecycle Module

    ## Purpose

    Run durable housekeeping tasks on pg-boss schedules: video compression (daily), expired-artifact retention (daily), disk-pressure-driven cleanup (hourly). Replaces v2.0's `node-cron` + `async-mutex` scheduling with queue-layer overlap protection (`policy: 'stately'` + `singletonKey`) and per-fire correlationId traceability. Tasks are pure functions invoked by worker handlers; worker handlers emit persisted telemetry events for observability.

    ## Public API

    Exports from `server/lifecycle/index.ts` (the ONLY legitimate import surface outside this module — enforced by `dependency-cruiser` rule `no-deep-imports-into-lifecycle-internal`):

    - `lifecyclePlugin` — Fastify plugin (thin wrapper around `createLifecycleModule`).
    - `createLifecycleModule(deps): LifecycleModule` — factory returning `{stats, emit, bus, registerSchedulesAndWorkers, shutdown}`.
    - `LifecycleStats` type — stats decorator shape (back-compat with `/health` endpoint).
    - `lifecycleRegistry`, `LIFECYCLE_EVENT_NAMES`, `makeLifecycleEmitters` — per-module event registry + typed emit helpers.
    - `LIFECYCLE_COMPRESS_DAILY_QUEUE_NAME`, `LIFECYCLE_RETENTION_DAILY_QUEUE_NAME`, `LIFECYCLE_DISK_HOURLY_QUEUE_NAME` — queue name constants.
    - `lifecycleJobPayloadSchema` + `LifecycleJobPayload` — Zod payload shape (SPEC-01/03).

    ## Events Emitted

    All four events are **persisted** (terminal operational telemetry per TRACE-08); aggregate `'lifecycle'`:

    - `lifecycle.compression.completed` — `{compressed, savedBytes, durationMs}`. Fired after a successful compression fire.
    - `lifecycle.retention.completed` — `{deleted, freedBytes, durationMs}`. Fired after a successful retention fire.
    - `lifecycle.disk.checked` — `{currentUsageBytes, maxBytes, deleted, freedBytes, durationMs}`. Fired after a successful disk-pressure fire.
    - `lifecycle.task.failed` — `{task: 'compression'|'retention'|'disk-pressure', error, durationMs}`. Fired when any task throws; handler re-throws for pg-boss retry accounting.

    ## Events Consumed

    None. Lifecycle is a scheduled producer only — no bus subscriptions in Phase 18. Deferred to future phases if event-driven housekeeping is introduced.

    ## Queue Produced

    All three use `policy: 'stately'` + `singletonKey: <queue-name>` so a second fire is DROPPED at the queue boundary when the first is still in-flight (RESEARCH §Pitfall 1). `retryLimit: 1`, `retryBackoff: true`, `retryDelay: 30`.

    - `lifecycle.compress.daily` — cron `0 3 * * *` (daily at 3 AM UTC). Runs `runCompressionTask`.
    - `lifecycle.retention.daily` — cron `0 3 * * *` (daily at 3 AM UTC). Runs `runRetentionTask` CONCURRENTLY with compression — deletes expired artifacts (orthogonal to compression's artifact set).
    - `lifecycle.disk.hourly` — cron `0 * * * *` (hourly on the hour). Runs `runDiskPressureTask`.

    ## Queue Consumed

    Same three queues (self-loop: producer + consumer in same module). Workers are registered in `server/lifecycle/queue.ts` via `registerLifecycleSchedulesAndWorkers`. Each worker handler restores ALS correlationId from the per-fire envelope (Phase 15 substrate `fastify.queue.work`) then invokes the respective pure task body.

    ## Invariants

    - **(a) No overlapping fires** — `policy: 'stately'` + `singletonKey` drops duplicate sends at the queue layer. Test: `__tests__/queue.spec.ts` (`[RESEARCH §Pitfall 1]`).
    - **(b) Per-fire correlationId** — every scheduled fire gets a fresh UUID; consecutive fires observe DIFFERENT ids. Test: `__tests__/correlation.spec.ts` (`[QUEUE-08 SC2 + Option B]`).
    - **(c) Worker failure does not crash the plugin** — task throws → handler emits `lifecycle.task.failed` then re-throws for pg-boss retry. Covered by the try/catch in each of the 3 worker handlers; operational verification is manual (SC4 drain spec asserts no unhandled rejections).
    - **(d) `fastify.lifecycleStats` updates after each successful run** — back-compat for `server/api/routes.ts:439` /health endpoint. Exercised structurally by module.spec.ts + graceful-shutdown.spec.ts.
    - **(e) Graceful shutdown drains in-flight within timeout** — `boss.stop({graceful: true, timeout: 30_000})` owned by queue plugin; lifecycle's onClose calls `module.shutdown()` which `offWork`s the 3 workers. Test: `__tests__/graceful-shutdown.spec.ts` (`[SC4]`).

    ## Non-Goals

    - **Event-driven housekeeping** (e.g. cleanup-on-job-completed) — future phase.
    - **DLQ surface for retry-exhausted lifecycle jobs** — Phase 19 (EVENTS-07, QUEUE-05) handles repo-wide DLQ pipeline; lifecycle events will flow through it without code change here.
    - **Removing `node-cron` from `package.json`** — Phase 25 (`server/pipelines/scheduler.ts` still imports it; full removal is the pipelines module refactor).
    - **Changing the three task bodies' signatures or logic** — Phase 18 is pure substrate + wiring; `runCompressionTask` / `runRetentionTask` / `runDiskPressureTask` behave identically to v2.0.

    ## Dependencies

    Declared in `server/lifecycle/plugin.ts` `dependencies: ['config', 'db', 'queue', 'event-bus']`:

    - `config` — reads `fastify.config.storage.artifacts` for retention_days / compress_after_days / max_storage_gb.
    - `db` — reads `fastify.db` for artifact queries (task bodies) + INSERTs into `events` table (persistEnvelope middleware).
    - `queue` — uses `fastify.boss.createQueue` + `fastify.queue.schedule` + `fastify.queue.work`.
    - `event-bus` — persistEnvelope side-channel fires `<type>.envelope` events that the bus's `onPersisted` subscribers observe.

    The `api-plugin` depends on `lifecycle-plugin` (Phase 17 Plan 17-07 added that declaration to `server/api/plugin.ts:dependencies`) so that `server/api/routes.ts:439` can read `fastify.lifecycleStats` at /health time without a registration race.

    ---

    ### Runnable Example

    ```typescript
    // Inside a Fastify plugin that has already registered db + queue + event-bus + lifecycle:
    import {
      createLifecycleModule,
      LIFECYCLE_COMPRESS_DAILY_QUEUE_NAME,
    } from 'server/lifecycle/index.js';

    // Direct factory usage (rare — the lifecyclePlugin does this for you):
    const module = createLifecycleModule({
      fastify: app,
      db: app.db,
      config: app.config,
      logger: app.log,
    });
    await module.registerSchedulesAndWorkers();

    // Listen for the terminal lifecycle.compression.completed event:
    app.lifecycleModule.bus.on('lifecycle.compression.completed', (payload) => {
      app.log.info(
        { compressed: payload.compressed, savedBytes: payload.savedBytes, durationMs: payload.durationMs },
        'Compression fire completed',
      );
    });

    // Inspect the lifecycle queue name:
    app.log.info({ queue: LIFECYCLE_COMPRESS_DAILY_QUEUE_NAME }, 'lifecycle.compress.daily');
    ```

    Phase 27 (MOD-09) will add CI typechecking for this snippet. Phase 18 leaves it reviewer-spot-checked.
    ```

    Section count MUST be EXACTLY 9 H2 headers. Invariant letters (a)-(e) MUST each appear with a test-file citation. Events list MUST include all four persisted events. Queue list MUST include all three queues with cron + policy notes.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const m = fs.readFileSync('server/lifecycle/MODULE.md','utf8'); const sections = ['## Purpose','## Public API','## Events Emitted','## Events Consumed','## Queue Produced','## Queue Consumed','## Invariants','## Non-Goals','## Dependencies']; const missing = sections.filter(s => !m.includes(s)); if (missing.length) { console.error('MODULE.md missing sections:', missing); process.exit(1); } const ordered = sections.map(s => m.indexOf(s)); for (let i = 1; i < ordered.length; i++) { if (ordered[i] < ordered[i-1]) { console.error('Section ordering incorrect at', sections[i]); process.exit(1); } } for (const inv of ['**(a)','**(b)','**(c)','**(d)','**(e)']) { if (!m.includes(inv)) { console.error('Invariant missing from MODULE.md:', inv); process.exit(1); } } for (const e of ['lifecycle.compression.completed','lifecycle.retention.completed','lifecycle.disk.checked','lifecycle.task.failed']) { if (!m.includes(e)) { console.error('Event missing:', e); process.exit(1); } } for (const q of ['lifecycle.compress.daily','lifecycle.retention.daily','lifecycle.disk.hourly']) { if (!m.includes(q)) { console.error('Queue missing:', q); process.exit(1); } } if (!/'0 3 \* \* \*'/.test(m) || !/'0 \* \* \* \*'/.test(m)) { console.error('Cron expressions missing'); process.exit(1); } if (!/policy: 'stately'/.test(m)) { console.error('policy:stately documentation missing'); process.exit(1); } if (!/singletonKey/.test(m)) { console.error('singletonKey mention missing'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `server/lifecycle/MODULE.md` exists
    - All 9 required H2 sections present in order: Purpose, Public API, Events Emitted, Events Consumed, Queue Produced, Queue Consumed, Invariants, Non-Goals, Dependencies (verify script)
    - Invariants section references all 5 invariants tagged `(a)`, `(b)`, `(c)`, `(d)`, `(e)` (verify script)
    - All 4 emitted events listed: `lifecycle.compression.completed`, `lifecycle.retention.completed`, `lifecycle.disk.checked`, `lifecycle.task.failed` (verify script)
    - All 3 queue names listed: `lifecycle.compress.daily`, `lifecycle.retention.daily`, `lifecycle.disk.hourly` (verify script)
    - Cron expressions `'0 3 * * *'` and `'0 * * * *'` both appear (verify script)
    - `policy: 'stately'` and `singletonKey` both documented (verify script)
    - Dependencies section lists exactly `['config', 'db', 'queue', 'event-bus']` (grep)
    - Mentions Phase 25 as the phase that removes `node-cron` from `package.json` (grep "Phase 25")
    - File word count ≥ 400 (substantive — `wc -w server/lifecycle/MODULE.md` ≥ 400)
    - Runnable Example H3 section exists with ```typescript fenced code block (grep for `### Runnable Example`)
  </acceptance_criteria>
  <done>
    MODULE.md exists with all 9 sections + invariants + events + queues + crons + runnable example.
  </done>
</task>

<task type="auto">
  <name>Task 4.2: Create server/lifecycle/index.ts barrel (MOD-02 public API surface)</name>
  <files>server/lifecycle/index.ts</files>
  <read_first>
    - server/hooks/index.ts (template — 46 lines, section-commented, only ONE re-export from internal/)
    - server/lifecycle/plugin.ts (default export — re-export via `export { default as lifecyclePlugin }`)
    - server/lifecycle/internal/module.ts (createLifecycleModule + LifecycleModule type)
    - server/lifecycle/events.ts (all events-surface exports)
    - server/lifecycle/queue.ts (all queue-surface exports)
    - server/lifecycle/schemas.ts (lifecycleJobPayloadSchema + type)
    - server/lifecycle/stats.ts (LifecycleStats type)
  </read_first>
  <action>
    **Create `server/lifecycle/index.ts`** mirroring `server/hooks/index.ts`:

    ```typescript
    /**
     * Phase 18 / Plan 18-04 — Lifecycle module public barrel (MOD-02).
     *
     * This is the ONLY import surface for consumers outside `server/lifecycle/`.
     * `dependency-cruiser` (rule `no-deep-imports-into-lifecycle-internal` added
     * in Phase 18 Plan 18-00) enforces the boundary structurally — imports into
     * `server/lifecycle/internal/**` from outside the module fail CI.
     *
     * Exports in this file are PUBLIC and subject to semver-ish stability.
     * Anything under `./internal/` is module-private and may change without notice.
     *
     * The ONLY allowed internal/ re-export is `createLifecycleModule` + the
     * `LifecycleModule` / `CreateLifecycleModuleDeps` types — the canonical
     * factory entry point per MOD-06. Matches Phase 16 hooks pilot pattern
     * (`server/hooks/index.ts:20-21`).
     */

    // Fastify plugin (default export — matches Phase 15 substrate convention).
    export { default as lifecyclePlugin } from './plugin.js';

    // Factory surface (the v3.0 canonical way to instantiate the module).
    export { createLifecycleModule } from './internal/module.js';
    export type { LifecycleModule, CreateLifecycleModuleDeps } from './internal/module.js';

    // Stats surface (back-compat with fastify.lifecycleStats decorator + /health endpoint).
    export type { LifecycleStats } from './stats.js';

    // Schemas + derived TS types (SPEC-01 / SPEC-03).
    export {
      lifecycleJobPayloadSchema,
      compressionResultSchema,
      retentionResultSchema,
      diskPressureResultSchema,
    } from './schemas.js';
    export type {
      LifecycleJobPayload,
      CompressionResultParsed,
      RetentionResultParsed,
      DiskPressureResultParsed,
    } from './schemas.js';

    // Events surface (MOD-03).
    export {
      lifecycleRegistry,
      LIFECYCLE_EVENT_NAMES,
      makeLifecycleEmitters,
      compressionCompletedPayload,
      retentionCompletedPayload,
      diskCheckedPayload,
      taskFailedPayload,
    } from './events.js';
    export type { LifecycleRegistry, LifecycleEmitters, LifecycleEventName } from './events.js';

    // Queue surface (QUEUE-06).
    export {
      LIFECYCLE_COMPRESS_DAILY_QUEUE_NAME,
      LIFECYCLE_RETENTION_DAILY_QUEUE_NAME,
      LIFECYCLE_DISK_HOURLY_QUEUE_NAME,
      COMPRESS_CRON,
      RETENTION_CRON,
      DISK_CRON,
      registerLifecycleSchedulesAndWorkers,
    } from './queue.js';
    export type { RegisterLifecycleSchedulesAndWorkersDeps, LifecycleQueueRegistration } from './queue.js';
    ```

    **Do NOT re-export** `hook-executor`-style imperative internal helpers (there are none — lifecycle doesn't have an equivalent of `HookExecutor` class). Do NOT re-export task bodies (`runCompressionTask` etc.) — those are `internal` implementation details that are at the root level by convention but are NOT part of the public surface. Consumers who want to invoke them directly should go through the plugin + schedule fires.

    **Update plugin.ts dependency list NOT required** — the barrel is a new file; plugin.ts was already landed by plan 18-03.
  </action>
  <verify>
    <automated>test -f server/lifecycle/index.ts && npx tsc --noEmit && node -e "const s = require('fs').readFileSync('server/lifecycle/index.ts','utf8'); for (const sym of ['lifecyclePlugin','createLifecycleModule','LifecycleModule','LifecycleStats','lifecycleJobPayloadSchema','lifecycleRegistry','LIFECYCLE_EVENT_NAMES','makeLifecycleEmitters','LIFECYCLE_COMPRESS_DAILY_QUEUE_NAME','LIFECYCLE_RETENTION_DAILY_QUEUE_NAME','LIFECYCLE_DISK_HOURLY_QUEUE_NAME','registerLifecycleSchedulesAndWorkers']) { if (!s.includes(sym)) { console.error('Missing re-export:', sym); process.exit(1); } } const internalReexports = (s.match(/from '\.\/internal\//g) || []).length; if (internalReexports > 1) { console.error('Too many internal/ re-export lines:', internalReexports, '(only ./internal/module.js allowed)'); process.exit(1); } if (!/from '\.\/internal\/module/.test(s)) { console.error('Missing the single allowed internal/module re-export'); process.exit(1); } for (const leaked of ['runCompressionTask','runRetentionTask','runDiskPressureTask','makePersistEnvelope']) { if (s.includes(leaked)) { console.error('LEAKED internal symbol in barrel:', leaked); process.exit(1); } } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `server/lifecycle/index.ts` exists
    - Default-exports re-export: `lifecyclePlugin` (via `export { default as lifecyclePlugin }`)
    - Factory re-export: `createLifecycleModule`, types `LifecycleModule` + `CreateLifecycleModuleDeps` (from `./internal/module.js`)
    - Schemas re-exports: `lifecycleJobPayloadSchema`, 3 result schemas, 4 type aliases
    - Events re-exports: `lifecycleRegistry`, `LIFECYCLE_EVENT_NAMES`, `makeLifecycleEmitters`, 4 payload schemas, 3 type aliases
    - Queue re-exports: 3 queue-name constants, 3 cron constants, `registerLifecycleSchedulesAndWorkers`, 2 type aliases
    - Stats re-export: type `LifecycleStats`
    - EXACTLY ONE `from './internal/` line in the file (only `internal/module.js` is a legitimate re-export target — verify script)
    - Does NOT re-export `runCompressionTask` / `runRetentionTask` / `runDiskPressureTask` / `makePersistEnvelope` (verify script)
    - `npx tsc --noEmit` exits 0
    - `npm run dep-check` exits 0 — the committed tree has NO consumer reaching into `server/lifecycle/internal/` via a non-barrel path (no bad-lifecycle fixture counts because it's outside `server/`)
  </acceptance_criteria>
  <done>
    server/lifecycle/index.ts barrel shipped; only ONE internal/ re-export (MOD-02); no leaked symbols; dep-check green.
  </done>
</task>

<task type="auto">
  <name>Task 4.3: Rename server/lifecycle/__tests__/*.test.ts → *.spec.ts (MOD-04 consistency)</name>
  <files>server/lifecycle/__tests__/compression-task.spec.ts, server/lifecycle/__tests__/retention-task.spec.ts, server/lifecycle/__tests__/disk-pressure-task.spec.ts</files>
  <read_first>
    - server/lifecycle/__tests__/compression-task.test.ts (source content — rename only, no content changes)
    - server/lifecycle/__tests__/retention-task.test.ts (source)
    - server/lifecycle/__tests__/disk-pressure-task.test.ts (source)
    - vitest.config.ts (confirm `include` glob matches both `*.test.ts` and `*.spec.ts` — if not, extend)
    - .planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-RESEARCH.md §Open Question #5 (rename pre-resolved as in-scope for this phase)
    - .planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-CONTEXT.md (CONTEXT open-question #5 pre-resolution: "Rename lifecycle `*.test.ts` → `*.spec.ts` in this phase")
  </read_first>
  <action>
    **Step 1 — Use `git mv` for each of the three files** (preserves history — do NOT copy-then-delete):

    ```bash
    git mv server/lifecycle/__tests__/compression-task.test.ts server/lifecycle/__tests__/compression-task.spec.ts
    git mv server/lifecycle/__tests__/retention-task.test.ts   server/lifecycle/__tests__/retention-task.spec.ts
    git mv server/lifecycle/__tests__/disk-pressure-task.test.ts server/lifecycle/__tests__/disk-pressure-task.spec.ts
    ```

    If `git mv` is unavailable, `mv` is acceptable — the commit step will record the rename as delete+add; git heuristics may still recognize it as a rename if content is identical.

    **Step 2 — Do NOT modify file contents.** Test bodies stay identical; only the file extension changes. Rationale: Phase 18 does NOT rewrite these specs as tests-as-spec (describe-tree per MOD-04 tree-matches-MODULE.md) — that alignment is Phase 30 repo-wide. This phase only aligns the file NAMING convention.

    **Step 3 — Verify vitest config globs match the new names.**

    Read `vitest.config.ts`. The `include` entry MUST match both `*.test.ts` and `*.spec.ts`. Current repo convention (verified via `server/hooks/__tests__/` — all `.spec.ts`, passing): vitest's default include is `['**/*.{test,spec,test-d,spec-d}.{js,mjs,cjs,ts,mts,cts,jsx,tsx}']` which picks up both; no config change needed. If a project-specific include is narrower (only `.test.ts`), EXTEND it to include `.spec.ts`:

    ```typescript
    // vitest.config.ts — if an explicit include exists:
    include: ['**/*.{test,spec}.ts'],
    ```

    **Step 4 — Run the renamed specs.**

    ```bash
    npx vitest run server/lifecycle/__tests__/compression-task.spec.ts
    npx vitest run server/lifecycle/__tests__/retention-task.spec.ts
    npx vitest run server/lifecycle/__tests__/disk-pressure-task.spec.ts
    ```

    Each MUST exit 0. The tests inside do not reference `__filename` or the file extension anywhere, so rename is transparent.

    **Step 5 — Grep for stale filename references.**

    ```bash
    grep -rn "compression-task.test\|retention-task.test\|disk-pressure-task.test" server/ scripts/ .planning/
    ```

    Expected: ZERO hits in source code. The only acceptable hits are inside plan/summary markdown files (historical references, not code). If a test explicitly imports from another `*.test.ts` file (unusual), update the import.
  </action>
  <verify>
    <automated>test -f server/lifecycle/__tests__/compression-task.spec.ts && test -f server/lifecycle/__tests__/retention-task.spec.ts && test -f server/lifecycle/__tests__/disk-pressure-task.spec.ts && test ! -f server/lifecycle/__tests__/compression-task.test.ts && test ! -f server/lifecycle/__tests__/retention-task.test.ts && test ! -f server/lifecycle/__tests__/disk-pressure-task.test.ts && npx vitest run server/lifecycle/__tests__/compression-task.spec.ts server/lifecycle/__tests__/retention-task.spec.ts server/lifecycle/__tests__/disk-pressure-task.spec.ts --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - Three `.spec.ts` files exist: compression-task.spec.ts, retention-task.spec.ts, disk-pressure-task.spec.ts (test -f each)
    - Three `.test.ts` originals DO NOT exist (test ! -f each)
    - File content byte-identical to pre-rename state EXCEPT for filename (diff would show rename-only if `git mv` used)
    - Each renamed spec passes: `npx vitest run <each>` exits 0
    - `grep -rn "compression-task.test\|retention-task.test\|disk-pressure-task.test" server/ scripts/` returns no code hits (markdown hits allowed)
    - `npx vitest run server/lifecycle/__tests__/` picks up ALL specs (events, queue, correlation, module, graceful-shutdown, compression-task, retention-task, disk-pressure-task = 8 spec files)
  </acceptance_criteria>
  <done>
    Three lifecycle task specs renamed .test.ts → .spec.ts with identical content; all vitest still green.
  </done>
</task>

<task type="auto">
  <name>Task 4.4: Run Nyquist delta gate + full suite + dep-check + lint</name>
  <files>.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-04-SUMMARY.md</files>
  <read_first>
    - .planning/nyquist-baseline.json (baseline lines: 48.29%)
    - scripts/check-nyquist.mjs (delta gate logic)
    - .planning/phases/16-pilot-module-hooks/16-04-SUMMARY.md (phase-close Nyquist output format — mirror)
    - .planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-VALIDATION.md (gate requirements)
  </read_first>
  <action>
    **Step 1 — Run the full local CI gate.** Each command MUST exit 0:

    ```bash
    # 1. Typecheck — no TS errors from barrel/plugin/factory wiring
    npx tsc --noEmit

    # 2. Lint — no ESLint violations (incl. custom rules no-imperative-event-names, no-direct-bus-emit from Phase 15)
    npm run lint

    # 3. Dep-check — structural enforcement of MOD-02 barrel boundary
    npm run dep-check

    # 4. Full suite — all module specs + renamed task specs + integration specs with DB
    DATABASE_URL=postgres://localhost/device_farm_test TEST_DATABASE_URL=postgres://localhost/device_farm_test npm test

    # 5. OpenAPI / contracts check — Phase 17 drift detector (should stay green — Phase 18 doesn't touch server routes)
    npm run contracts:check 2>/dev/null || true  # optional if contracts-check script exists
    ```

    **Step 2 — Run the Nyquist coverage gate.**

    ```bash
    DATABASE_URL=postgres://localhost/device_farm_test TEST_DATABASE_URL=postgres://localhost/device_farm_test npm run nyquist:capture
    npm run nyquist:check
    ```

    `nyquist:capture` writes current coverage to `coverage/coverage-summary.json` (full suite with DB). `nyquist:check` reads that + `.planning/nyquist-baseline.json` and asserts delta on `coverage.lines.pct` is `>= -2pp` (current `>= 46.29%`). Exit 0 = pass.

    Phase 16 Plan 16-04 delta was +3.63pp; Phase 17 stayed green. Phase 18 adds 5 new specs (events, queue, correlation, module, graceful-shutdown) that cover ~500 new lines of production code (events.ts + queue.ts + internal/module.ts + plugin.ts); deletes ~30 lines of lifecycle-plugin.ts (node-cron + mutex branches). Net delta should be +1-3pp. If delta < -2pp: STOP and investigate which file dropped (read `coverage/coverage-summary.json`, sort by delta, identify the drop). Common causes: a module spec that skipped due to missing DATABASE_URL when a DB should have been available; a new file with branches that no test hits.

    **Step 3 — Write `18-04-SUMMARY.md`.**

    Template (mirror `.planning/phases/16-pilot-module-hooks/16-04-SUMMARY.md` shape):

    ```markdown
    # Phase 18 Plan 04 — Summary

    **Completed:** <DATE>
    **Plans in phase:** 18-00 (substrate), 18-01 (events), 18-02 (queue), 18-03 (factory + plugin), 18-04 (MODULE + barrel + rename + gate) — ALL SHIPPED
    **Requirement closed:** QUEUE-08 (lifecycle half — pipelines half remains Phase 25)

    ## Artifacts landed

    - `server/lifecycle/MODULE.md` — N lines, 9 H2 sections + runnable example
    - `server/lifecycle/index.ts` — N lines, barrel re-exports from 5 module files + ONE internal/module.js re-export
    - 3 renamed task specs: compression-task.spec.ts, retention-task.spec.ts, disk-pressure-task.spec.ts (byte-identical content)

    ## Test suite

    ```
    <paste: npx vitest run server/lifecycle/__tests__/ output — 8 spec files, N tests>
    ```

    ## Gate commands output

    ```
    $ npx tsc --noEmit
    <paste>

    $ npm run lint
    <paste>

    $ npm run dep-check
    <paste>

    $ npm test (with DATABASE_URL)
    <paste tail, full counts>

    $ npm run nyquist:capture
    <paste: current coverage.lines.pct>

    $ npm run nyquist:check
    <paste: baseline.lines = 48.29, current.lines = X.XX, delta = +/-Y.YYpp — gate GREEN/RED>
    ```

    ## ROADMAP success criteria verification (Phase 18)

    - [x] **SC1**: `node-cron` no longer imported by `server/lifecycle/*` — verified by `grep -rn 'node-cron' server/lifecycle/` → no matches. Three pg-boss schedules registered: `lifecycle.compress.daily` (0 3 * * *), `lifecycle.retention.daily` (0 3 * * *), `lifecycle.disk.hourly` (0 * * * *) with `singletonKey` + `policy: 'stately'` preventing overlapping fires — queue.spec.ts test [RESEARCH §Pitfall 1] proves dedup.
    - [x] **SC2**: `boss.send()` wrapper (`fastify.queue.send`) injects correlationId from ALS (Phase 15 substrate). Consumers restore ALS via `fastify.queue.work` (not `logContext.run` per se — codebase primitive is `asyncLocalStorage.run`; naming divergence documented in MODULE.md prose per CONTEXT.md open-question #6 pre-resolution). Integration test `correlation.spec.ts` [QUEUE-08 SC2] proves schedule-triggered work carries a UUID correlationId to worker context; [QUEUE-08 SC2 + Option B] proves two consecutive fires get DIFFERENT ids.
    - [x] **SC3**: Lifecycle module follows Phase 16 conventions — MODULE.md (9 sections), barrel `index.ts` (MOD-02), `events.ts` (MOD-03), `queue.ts` (QUEUE-06), `internal/module.ts` factory (MOD-06), tests-as-spec file naming (MOD-04 file-naming alignment done; describe-tree alignment deferred to Phase 30). Nyquist delta: +X.XXpp (within ≥ −2pp budget).
    - [x] **SC4**: Graceful shutdown drains in-flight schedule jobs inside 30s timeout without dropping work — `graceful-shutdown.spec.ts` [SC4] proves `app.close()` resolves within 10s + idempotent double-close + zero unhandled rejections.

    ## Scope boundaries honored

    - `node-cron` package still in `package.json` — `server/pipelines/scheduler.ts` still imports it; removal is Phase 25 (CONTEXT §Deferred + planning_context preamble "Known scope boundary").
    - `async-mutex` package still in `package.json` — `server/pool/*` (device allocation) uses it (RESEARCH §Pitfall 8); keep the dep.
    - `lifecycle-plugin` plugin name unchanged — `server/api/plugin.ts` from Phase 17 Plan 17-07 depends on the string; renaming would cascade a 2-line change + plugin-order.spec.ts update that's out-of-scope.
    ```

    Fill in the `<paste>` placeholders with actual command output during execution.

    **Step 4 — Commit (handled by gsd-tools commit at the end of `/gsd:execute-phase`):** Nothing to do manually; the orchestrator will commit.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run dep-check && DATABASE_URL=postgres://localhost/device_farm_test TEST_DATABASE_URL=postgres://localhost/device_farm_test npm test -- --reporter=default && DATABASE_URL=postgres://localhost/device_farm_test TEST_DATABASE_URL=postgres://localhost/device_farm_test npm run nyquist:capture && npm run nyquist:check && test -f .planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0 (ESLint + custom rules from Phase 15)
    - `npm run dep-check` exits 0 (dep-cruiser — lifecycle rule structurally enforced because internal/ now exists)
    - `DATABASE_URL=postgres://localhost/device_farm_test npm test` exits 0 — full suite green including 8 lifecycle specs (events, queue, correlation, module, graceful-shutdown, compression-task, retention-task, disk-pressure-task) + queue/plugin.spec.ts from plan 18-00 + all other phases' specs
    - `npm run nyquist:capture` writes `coverage/coverage-summary.json` successfully
    - `npm run nyquist:check` exits 0 — delta on `coverage.lines` vs baseline 48.29% is >= -2pp (current >= 46.29%)
    - `.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-04-SUMMARY.md` exists with all 4 SCs marked `[x]` and filled-in gate outputs
    - `grep -rn 'node-cron\|async-mutex' server/lifecycle/` returns NO matches (final confirmation — plan 18-03 deleted the imports; this task confirms after all renames + barrel land)
  </acceptance_criteria>
  <done>
    All Phase 18 gate commands exit 0; Nyquist delta green; SUMMARY.md records every command's output + SC checklist. Phase 18 complete.
  </done>
</task>

</tasks>

<verification>
- `server/lifecycle/MODULE.md` has all 9 fixed H2 sections in order
- `server/lifecycle/index.ts` barrel re-exports public surface; only ONE internal/ re-export allowed (MOD-02)
- Three task tests renamed `.test.ts` → `.spec.ts` (content byte-identical; rename-only)
- `npx tsc --noEmit` green
- `npm run lint` green
- `npm run dep-check` green — lifecycle/internal denylist now structurally enforced (internal/module.ts landed in plan 18-03)
- `DATABASE_URL=... npm test` full suite green
- `npm run nyquist:check` exits 0 — delta ≥ -2pp
- `grep -rn 'node-cron\|async-mutex' server/lifecycle/` returns NO matches
- 18-04-SUMMARY.md records every gate output + SC checklist
</verification>

<success_criteria>
- MOD-01: `server/lifecycle/MODULE.md` committed with 9 fixed sections; LLM reads single file for full contract
- MOD-02: `server/lifecycle/index.ts` is the sole import surface; dep-cruiser rule from plan 18-00 now structurally enforced because `server/lifecycle/internal/` has real code (plan 18-03)
- MOD-04 (file naming): three task specs renamed `.test.ts` → `.spec.ts`; describe-tree alignment with MODULE.md deferred to Phase 30
- SC3 Phase 16 conventions: MODULE.md + barrel + events.ts + queue.ts + internal/module.ts + tests-as-spec (file-naming layer) — all in place
- Nyquist delta ≥ -2pp — DEBT-03 gate held
- ROADMAP §Phase 18 success criteria ALL satisfied end-to-end across plans 18-00..18-04
- QUEUE-08 LIFECYCLE HALF CLOSED (Phase 25 still owns pipelines half + package.json dep removal)
</success_criteria>

<output>
Create `.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-04-SUMMARY.md` populated per the template in Task 4.4 Step 3. All 4 ROADMAP SCs marked [x] with one-line verification references. Final gate outputs pasted verbatim. Nyquist delta explicit (baseline → current → delta in pp).

Phase 18 ships. Next: `/gsd:execute-phase 19` (Reporting Migration — Webhooks + DLQ).
</output>
