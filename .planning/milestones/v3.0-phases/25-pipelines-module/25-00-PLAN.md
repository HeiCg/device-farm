---
phase: 25
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - server/pipelines/events.ts
  - server/pipelines/queue.ts
  - server/pipelines/internal/module.ts
  - server/pipelines/MODULE.md
  - server/pipelines/index.ts
  - server/pipelines/__tests__/events.spec.ts
  - server/queue/names.ts
  - server/queue/__tests__/names.spec.ts
  - .dependency-cruiser.cjs
  - __fixtures__/dep-cruiser/bad-pipelines-deep-import.ts
  - server/hooks/__tests__/dep-cruiser.spec.ts
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, MOD-05]
estimated_minutes: 12
gap_closure: false
must_haves:
  truths:
    - "`server/pipelines/events.ts` stub exists exporting `PIPELINE_EVENT_NAMES` (5 keys: RUN_STARTED='pipeline.run.started', STAGE_ADVANCED='pipeline.stage.advanced', RUN_COMPLETED='pipeline.run.completed', RUN_FAILED='pipeline.run.failed', SCHEDULE_UPSERTED='pipeline.schedule.upserted'), placeholder `PIPELINE_RUN_AGGREGATE_TYPE='pipeline-run'` + `PIPELINE_SCHEDULE_AGGREGATE_TYPE='pipeline-schedule'`, `PipelineEventName` type, and stub `pipelinesRegistry = {} as const` (full body lands in 25-01)."
    - "`server/pipelines/queue.ts` stub exists exporting `PIPELINE_SCHEDULED_EXECUTE_QUEUE_NAME = QUEUE_NAMES.PIPELINE_SCHEDULED_EXECUTE` ALIAS only — no payload schema, no register helpers (full body in 25-01)."
    - "`server/pipelines/internal/module.ts` is an 8-line throw-stub exporting `createPipelinesModule` with body `throw new Error('Plan 25-03 not yet executed');` — required per Phase 18/19/20/21/22/23/24 empirical pattern (depcruise silently drops unresolvable imports — 8th repeat)."
    - "`server/pipelines/MODULE.md` placeholder with `## Purpose` section only (MOD-01 file-existence passes; full 9-section body lands in 25-05)."
    - "`server/pipelines/index.ts` MOD-02 strict 1-line internal/ re-export barrel: `export { createPipelinesModule, type PipelinesModule } from './internal/module.js';` (full surface in 25-05)."
    - "`server/pipelines/__tests__/events.spec.ts` stub with 1 test asserting `PIPELINE_EVENT_NAMES` shape — 5 keys + dot-separated past-tense + uniqueness per EVENTS-03. No DB."
    - "`server/queue/names.ts` extended — `QUEUE_NAMES.PIPELINE_SCHEDULED_EXECUTE='pipeline.scheduled.execute'` added alphabetically between `LIFECYCLE_RETENTION_DAILY` and `RECORDING_UPLOAD`. JSDoc comment-block on `QUEUE_NAMES` extended with Phase 25 paragraph documenting the entry as last-mile node-cron migration."
    - "`server/queue/__tests__/names.spec.ts` extends with one assertion that `QUEUE_NAMES.PIPELINE_SCHEDULED_EXECUTE === 'pipeline.scheduled.execute'` and is a valid pg-boss queue name."
    - "`.dependency-cruiser.cjs` gains a NINTH forbidden rule `no-deep-imports-into-pipelines-internal` mirroring rules 5/6/7/8 verbatim. The header comment-block is extended with rule-9 paragraph following the pattern of rules 5-8."
    - "`__fixtures__/dep-cruiser/bad-pipelines-deep-import.ts` fixture imports from `server/pipelines/internal/module.js` with `@ts-expect-error` — fires the new rule when invoked from spec."
    - "`server/hooks/__tests__/dep-cruiser.spec.ts` extended with `[MOD-02 pipelines extension]` it-block (two-pass err+json pattern matching Phase 20-24)."
    - "NO file moves yet. Existing `server/pipelines/{scheduler,executor,service,routes,parser,schema,schemas,secrets,variables,git-service,broadcaster,plugin}.ts` STAY at current locations. NO edits to package.json. NO `npm install`."
  artifacts:
    - path: "server/pipelines/events.ts"
      provides: "Events stub — 5 placeholder names + empty registry; full body in 25-01"
      contains: "PIPELINE_EVENT_NAMES"
    - path: "server/pipelines/queue.ts"
      provides: "Queue-name alias only; payload schema + register helpers in 25-01"
      contains: "PIPELINE_SCHEDULED_EXECUTE_QUEUE_NAME"
    - path: "server/pipelines/internal/module.ts"
      provides: "Throw-stub for dep-cruiser resolvable target (8th repeat)"
      contains: "createPipelinesModule"
    - path: "server/pipelines/MODULE.md"
      provides: "Placeholder; full body in 25-05"
      contains: "## Purpose"
    - path: "server/pipelines/index.ts"
      provides: "MOD-02 1-line strict internal re-export"
      contains: "from './internal/module"
    - path: "server/pipelines/__tests__/events.spec.ts"
      provides: "1-test stub asserting EVENTS-03 shape (5 keys); extends to 8+ tests in 25-01"
      contains: "PIPELINE_EVENT_NAMES"
    - path: "server/queue/names.ts"
      provides: "QUEUE_NAMES extended with PIPELINE_SCHEDULED_EXECUTE entry"
      contains: "PIPELINE_SCHEDULED_EXECUTE"
    - path: ".dependency-cruiser.cjs"
      provides: "9th forbidden rule (no-deep-imports-into-pipelines-internal)"
      contains: "no-deep-imports-into-pipelines-internal"
    - path: "__fixtures__/dep-cruiser/bad-pipelines-deep-import.ts"
      provides: "Fixture firing rule 9"
      contains: "server/pipelines/internal"
    - path: "server/hooks/__tests__/dep-cruiser.spec.ts"
      provides: "9th it-block proves pipelines rule fires structurally"
      contains: "[MOD-02 pipelines extension]"
  key_links:
    - from: "server/pipelines/internal/** (future plan 25-03)"
      to: ".dependency-cruiser.cjs rule 9"
      via: "Rule denies import into pipelines/internal/ from outside server/pipelines/"
      pattern: "no-deep-imports-into-pipelines-internal"
    - from: "__fixtures__/dep-cruiser/bad-pipelines-deep-import.ts"
      to: "server/pipelines/internal/module.js"
      via: "Fixture imports throw-stub via @ts-expect-error"
      pattern: "server/pipelines/internal/module"
    - from: "server/queue/names.ts QUEUE_NAMES.PIPELINE_SCHEDULED_EXECUTE"
      to: "server/pipelines/queue.ts (this plan + 25-01)"
      via: "Alias re-exported under PIPELINE_SCHEDULED_EXECUTE_QUEUE_NAME"
      pattern: "QUEUE_NAMES.PIPELINE_SCHEDULED_EXECUTE"
---

<objective>
Wave 0 substrate for Phase 25 Pipelines Module. Ships scaffolding that plans 25-01..25-05 depend on WITHOUT touching any runtime file in `server/pipelines/`.

Steps:
1. Creates `server/pipelines/events.ts` stub (5 placeholder event names + empty registry; full body in 25-01).
2. Creates `server/pipelines/queue.ts` stub (queue-name alias only; full payload schema + register helpers in 25-01).
3. Creates `server/pipelines/internal/module.ts` throw-stub for dep-cruiser resolvable target (8th repeat of Phase 18-24 pattern).
4. Creates `server/pipelines/MODULE.md` Purpose-only placeholder + `server/pipelines/index.ts` 1-line strict barrel (MOD-01 + MOD-02 file-existence pass for downstream plans).
5. Creates `server/pipelines/__tests__/events.spec.ts` stub (1 EVENTS-03 shape test).
6. EXTENDS `server/queue/names.ts` with new entry `PIPELINE_SCHEDULED_EXECUTE='pipeline.scheduled.execute'` + JSDoc Phase 25 paragraph.
7. EXTENDS `server/queue/__tests__/names.spec.ts` with 1 assertion proving the entry exists + is a valid pg-boss name.
8. Adds NINTH forbidden rule `no-deep-imports-into-pipelines-internal` to `.dependency-cruiser.cjs` mirroring rules 5/6/7/8; ships fixture + 9th it-block in dep-cruiser.spec.

**What Wave 0 INTENTIONALLY does NOT do:**
- NO `git mv` of existing pipelines/ top-level files under `internal/` — Plan 25-03 atomic work.
- NO edits to `server/pipelines/scheduler.ts` (node-cron import stays until 25-02).
- NO edits to `server/pipelines/service.ts` or `executor.ts` (Promise chain stays until 25-03).
- NO edits to `server/pipelines/plugin.ts` (thin rewrite in 25-03).
- NO edits to `package.json` (node-cron drop happens in 25-03).
- NO emission wiring (events bodies + queue worker land in 25-01..25-03).

Mirrors Phase 24 Plan 24-00 sequencing exactly.

Output: `server/pipelines/{events.ts, queue.ts, internal/module.ts, MODULE.md, index.ts, __tests__/events.spec.ts}` stubs exist; `server/queue/names.ts` has 12 entries (was 11); `.dependency-cruiser.cjs` has 9 forbidden module rules + the no-direct-bus-emit rule; fixture + dep-cruiser spec extension shipped.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-pipelines-module/25-CONTEXT.md
@.planning/phases/25-pipelines-module/25-RESEARCH.md
@.planning/phases/25-pipelines-module/25-VALIDATION.md
@.planning/phases/24-maestro-module/24-00-PLAN.md
@.planning/phases/23-jobs-module-keystone/23-00-PLAN.md
@server/queue/names.ts
@server/maestro/events.ts
@server/maestro/index.ts
@server/maestro/internal/module.ts
@server/maestro/__tests__/events.spec.ts
@.dependency-cruiser.cjs

<interfaces>
<!-- Existing artifacts that this Wave 0 plan extends -->

server/queue/names.ts current shape:
```typescript
export const QUEUE_NAMES = {
  DEMO: 'demo',
  DEVICE_BOOT: 'device.boot',
  DEVICE_REAP: 'device.reap',
  HOOK_RUN: 'hook.run',
  JOB_EXECUTE: 'job.execute',
  LIFECYCLE_COMPRESS_DAILY:  'lifecycle.compress.daily',
  LIFECYCLE_DISK_HOURLY:     'lifecycle.disk.hourly',
  LIFECYCLE_RETENTION_DAILY: 'lifecycle.retention.daily',
  RECORDING_UPLOAD:    'recording.upload',
  WEBHOOK_DELIVER:     'webhook.deliver',
  WEBHOOK_DELIVER_DLQ: 'webhook.deliver.dlq',
} as const;
```
Phase 25 inserts `PIPELINE_SCHEDULED_EXECUTE: 'pipeline.scheduled.execute'` alphabetically between `LIFECYCLE_RETENTION_DAILY` and `RECORDING_UPLOAD`.

.dependency-cruiser.cjs forbidden rules (8 module rules + 1 direct-bus-emit rule). Rule 8 (no-deep-imports-into-maestro-internal) is the most recent. Plan 25-00 inserts rule 9 mirroring rule 8 verbatim with `maestro` → `pipelines`.

Phase 24 Plan 24-00 reference: `server/maestro/__tests__/events.spec.ts` (events shape stub), `server/maestro/internal/module.ts` (throw-stub), `server/maestro/index.ts` (barrel).

Phase 16 baseline barrel pattern (MOD-02 strict 1-line):
```typescript
export { createPipelinesModule, type PipelinesModule } from './internal/module.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0.1: Extend server/queue/names.ts + names.spec.ts with PIPELINE_SCHEDULED_EXECUTE entry</name>
  <files>server/queue/names.ts, server/queue/__tests__/names.spec.ts</files>
  <read_first>
    - server/queue/names.ts (CURRENT 11 entries — verify alphabetic order)
    - server/queue/__tests__/names.spec.ts (read to find extension point)
    - .planning/phases/25-pipelines-module/25-RESEARCH.md (Pattern 1 + Standard Stack)
    - .planning/phases/25-pipelines-module/25-CONTEXT.md (queue-name policy)
    - server/lifecycle/internal/module.ts (Phase 18 boss.schedule reference for downstream awareness)
  </read_first>
  <action>
    EDIT `server/queue/names.ts`:
    1. Append a new paragraph to the existing JSDoc block above `QUEUE_NAMES` (Phase 25 paragraph; pattern matches Phase 18-24 paragraphs verbatim):

    ```
     * Phase 25 adds PIPELINE_SCHEDULED_EXECUTE for the pipelines module's
     * boss.schedule()-based pipeline scheduler (last node-cron consumer in tree
     * migrates to pg-boss; ROADMAP §Phase 25 SC1). Registered via boss.createQueue
     * with policy:'standard' + retryLimit:0; per-pipelineSchedules.id schedules
     * upserted via boss.schedule(name, cron, data, {key: scheduleId}) — `key` is
     * the pg-boss per-schedule disambiguator (NOT singletonKey, NOT pipelineId).
    ```

    2. Insert a new entry into the `QUEUE_NAMES` object literal in alphabetical order between `LIFECYCLE_RETENTION_DAILY: 'lifecycle.retention.daily',` and `RECORDING_UPLOAD: 'recording.upload',`:

    ```typescript
      PIPELINE_SCHEDULED_EXECUTE: 'pipeline.scheduled.execute',
    ```

    3. The result MUST have exactly 12 entries (was 11).

    EDIT `server/queue/__tests__/names.spec.ts`:
    - Find the existing assertion that proves all entries match `QUEUE_NAME_RE` (or the exhaustive entry list test). Add the new assertion in the same block:

    ```typescript
    it('Phase 25 — PIPELINE_SCHEDULED_EXECUTE entry exists + valid', () => {
      expect(QUEUE_NAMES.PIPELINE_SCHEDULED_EXECUTE).toBe('pipeline.scheduled.execute');
      expect(isValidQueueName(QUEUE_NAMES.PIPELINE_SCHEDULED_EXECUTE)).toBe(true);
    });
    ```

    - If the file uses an exhaustive `expect(Object.keys(QUEUE_NAMES).sort()).toEqual([...])` literal-array assertion, add `'PIPELINE_SCHEDULED_EXECUTE'` to that array in alphabetical position (between `'LIFECYCLE_RETENTION_DAILY'` and `'RECORDING_UPLOAD'`).

    Do NOT delete or reorder any existing entries. Do NOT regenerate the comment from scratch — extend the existing block.
  </action>
  <verify>
    <automated>npx vitest run server/queue/__tests__/names.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PIPELINE_SCHEDULED_EXECUTE: 'pipeline.scheduled.execute'" server/queue/names.ts` returns 1
    - `npx vitest run server/queue/__tests__/names.spec.ts` exits 0
    - `npx tsc --noEmit` produces ZERO new errors (existing pre-existing errors unchanged)
    - QUEUE_NAMES literal entries count is exactly 12 (`grep -cE "^  [A-Z_]+: '[a-z][a-z0-9._-]*'," server/queue/names.ts` returns 12)
  </acceptance_criteria>
  <done>
    QUEUE_NAMES extended with PIPELINE_SCHEDULED_EXECUTE in alphabetical position; names.spec proves entry shape; typecheck unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 0.2: Create pipelines module Wave 0 stubs (events.ts, queue.ts, internal/module.ts, MODULE.md, index.ts, events.spec.ts)</name>
  <files>server/pipelines/events.ts, server/pipelines/queue.ts, server/pipelines/internal/module.ts, server/pipelines/MODULE.md, server/pipelines/index.ts, server/pipelines/__tests__/events.spec.ts</files>
  <read_first>
    - server/maestro/events.ts (Phase 24 stub-then-body pattern, current full body for shape mirroring)
    - server/maestro/internal/module.ts (throw-stub format)
    - server/maestro/index.ts (full barrel for back-compat reference; Wave 0 SHIPS THE 1-LINE STRICT FORM ONLY)
    - server/maestro/__tests__/events.spec.ts (Wave 0 stub shape)
    - .planning/phases/24-maestro-module/24-00-PLAN.md (precedent task body)
    - .planning/phases/25-pipelines-module/25-RESEARCH.md (Code Examples §1 — events.ts target shape)
  </read_first>
  <action>
    Create `server/pipelines/events.ts` with the following EXACT body (placeholder — full body lands in 25-01):

    ```typescript
    /**
     * Pipelines module — typed event surface (Phase 25 Plan 25-00 STUB).
     *
     * Wave 0 ships placeholder names + empty registry. Plan 25-01 adds the
     * 5 payload schemas + makePipelinesEmitters factory + persistence flags
     * (TRACE-08 — terminal events run.completed/run.failed persisted; transitional
     * run.started/stage.advanced/schedule.upserted transient).
     *
     * Phase 25 ROADMAP SC2: bus subscription on `job.completed` (Phase 23 surface)
     * drives stage advancement; pipelines emits its own `pipeline.run.*` lifecycle.
     *
     * MOD-03 + EVENTS-03 (`noun.verbed` past-tense dotted) + EVENTS-08 (emit
     * helpers in events.ts; direct bus.emit forbidden by lint).
     */

    import type { EventRegistry } from '../bus/types.js';

    export const PIPELINE_EVENT_NAMES = {
      RUN_STARTED:        'pipeline.run.started',
      STAGE_ADVANCED:     'pipeline.stage.advanced',
      RUN_COMPLETED:      'pipeline.run.completed',
      RUN_FAILED:         'pipeline.run.failed',
      SCHEDULE_UPSERTED:  'pipeline.schedule.upserted',
    } as const;

    export type PipelineEventName = typeof PIPELINE_EVENT_NAMES[keyof typeof PIPELINE_EVENT_NAMES];

    /** Aggregate-type literals for envelope.aggregateType field. */
    export const PIPELINE_RUN_AGGREGATE_TYPE = 'pipeline-run' as const;
    export const PIPELINE_SCHEDULE_AGGREGATE_TYPE = 'pipeline-schedule' as const;

    /**
     * Empty registry — full body (5 payload schemas + persistence flags + makePipelinesEmitters)
     * lands in Plan 25-01.
     */
    export const pipelinesRegistry = {} as const satisfies EventRegistry;

    export type PipelinesRegistry = typeof pipelinesRegistry;
    ```

    Create `server/pipelines/queue.ts` with the following EXACT body (placeholder — full body in 25-01):

    ```typescript
    /**
     * Pipelines module — pg-boss queue surface (Phase 25 Plan 25-00 STUB).
     *
     * Wave 0 ships the queue-name alias only. Plan 25-01 adds:
     *   - pipelineScheduledExecutePayloadSchema (Zod)
     *   - registerPipelineScheduledExecuteQueue helper (createQueue with policy:'standard', retryLimit:0)
     *   - registerPipelineScheduledExecuteWorker helper (boss.work + per-fire correlationId via Phase 18 wrapper)
     *
     * Plan 25-02 wires this queue into the scheduler.ts boss.schedule migration
     * (idempotent upsert per pipelineSchedules.id row via `key` parameter — NOT singletonKey).
     *
     * QUEUE-06 (queue.ts colocated per module) + QUEUE-08 (boss.schedule replaces node-cron).
     */

    import { QUEUE_NAMES } from '../queue/names.js';

    export const PIPELINE_SCHEDULED_EXECUTE_QUEUE_NAME = QUEUE_NAMES.PIPELINE_SCHEDULED_EXECUTE;
    ```

    Create `server/pipelines/internal/module.ts` with EXACT 8-line throw-stub:

    ```typescript
    /**
     * Pipelines module factory — STUB (Plan 25-00).
     * Real body lands in Plan 25-03 (createPipelinesModule + executor + service rewrite +
     * subscribers wiring job.completed → stage advance via DB-driven join).
     *
     * Throw-stub kept so .dependency-cruiser.cjs rule 9 has a resolvable
     * import target (depcruise 17.x silently drops unresolvable imports —
     * 8th repeat of empirical Phase 18/19/20/21/22/23/24 pattern).
     */
    export function createPipelinesModule(): never {
      throw new Error('Plan 25-03 not yet executed; createPipelinesModule body pending.');
    }
    export type PipelinesModule = ReturnType<typeof createPipelinesModule>;
    ```

    Create `server/pipelines/MODULE.md` with PURPOSE-ONLY placeholder:

    ```markdown
    # `server/pipelines/` — MODULE.md

    > Phase 25 Plan 25-00 placeholder. Full 9-section body lands in Plan 25-05 (close-out).

    ## Purpose

    The pipelines module owns CI-style multi-stage execution: a pipeline definition declares
    sequential stages (script + maestro), runs are scheduled (cron) or triggered (HTTP/manual),
    and stage advancement is driven by a `job.completed` bus subscriber that joins
    `pipeline_stage_jobs` to identify which stages depend on a completed job. Last node-cron
    consumer in the codebase migrates to `boss.schedule()` here (Phase 25 SC1).
    ```

    Create `server/pipelines/index.ts` with EXACT 1-line MOD-02 strict re-export:

    ```typescript
    /**
     * Pipelines module — public barrel (MOD-02 strict 1-line internal re-export).
     * Plan 25-00 ships the strict form; Plan 25-05 expands with back-compat
     * surface re-exports (PipelineService, PipelineBroadcaster, etc) only as
     * needed for plugin-internal consumers.
     */
    export { createPipelinesModule, type PipelinesModule } from './internal/module.js';
    ```

    Create `server/pipelines/__tests__/events.spec.ts` stub (1 test):

    ```typescript
    /**
     * Pipelines module — events shape spec (Plan 25-00 STUB).
     * Full body (registry shape, payload schemas, makePipelinesEmitters helpers,
     * TRACE-08 persistence flags, ALS correlationId stamping) lands in Plan 25-01.
     */
    import { describe, it, expect } from 'vitest';
    import { PIPELINE_EVENT_NAMES } from '../events.js';

    describe('Phase 25 — pipelines events (EVENTS-03 shape)', () => {
      it('PIPELINE_EVENT_NAMES has 5 dot-separated past-tense names with no duplicates', () => {
        const values = Object.values(PIPELINE_EVENT_NAMES);
        expect(values.length).toBe(5);
        expect(new Set(values).size).toBe(5); // all unique
        // Phase 25 spec: noun.verbed (past tense) dotted
        expect(values).toEqual([
          'pipeline.run.started',
          'pipeline.stage.advanced',
          'pipeline.run.completed',
          'pipeline.run.failed',
          'pipeline.schedule.upserted',
        ]);
        for (const name of values) {
          expect(name).toMatch(/^[a-z][a-z0-9._-]*\.[a-z][a-z0-9._-]*$/);
        }
      });
    });
    ```

    Run `npx vitest run server/pipelines/__tests__/events.spec.ts` — expect 1/1 pass.
  </action>
  <verify>
    <automated>npx vitest run server/pipelines/__tests__/events.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f server/pipelines/events.ts` succeeds
    - `test -f server/pipelines/queue.ts` succeeds
    - `test -f server/pipelines/internal/module.ts` succeeds
    - `test -f server/pipelines/MODULE.md` succeeds
    - `test -f server/pipelines/index.ts` succeeds
    - `test -f server/pipelines/__tests__/events.spec.ts` succeeds
    - `grep -c "PIPELINE_EVENT_NAMES" server/pipelines/events.ts` >= 1
    - `grep -c "createPipelinesModule" server/pipelines/internal/module.ts` >= 1
    - `grep -cE "^export \\{ createPipelinesModule, type PipelinesModule \\} from './internal/module.js';$" server/pipelines/index.ts` returns 1 (MOD-02 strict 1-line)
    - `grep -c "## Purpose" server/pipelines/MODULE.md` returns 1
    - `npx tsc --noEmit` produces ZERO new errors
    - `npx vitest run server/pipelines/__tests__/events.spec.ts` 1/1 pass
  </acceptance_criteria>
  <done>
    All 6 substrate files exist with correct stub content; events.spec passes; typecheck unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 0.3: Add 9th dep-cruiser rule no-deep-imports-into-pipelines-internal + fixture + spec extension</name>
  <files>.dependency-cruiser.cjs, __fixtures__/dep-cruiser/bad-pipelines-deep-import.ts, server/hooks/__tests__/dep-cruiser.spec.ts</files>
  <read_first>
    - .dependency-cruiser.cjs (find rules 5/6/7/8 to mirror exactly — the maestro rule 8 is the most recent)
    - __fixtures__/dep-cruiser/bad-maestro-deep-import.ts (Phase 24 fixture exact shape)
    - server/hooks/__tests__/dep-cruiser.spec.ts (find Phase 24 [MOD-02 maestro extension] it-block — mirror its full body)
    - .planning/phases/24-maestro-module/24-00-PLAN.md (Task 0.3 precedent)
  </read_first>
  <action>
    EDIT `.dependency-cruiser.cjs`:

    1. Extend the header comment block above the forbidden array. Find the lines starting with `//   8. no-deep-imports-into-maestro-internal` and append:

    ```
    //   9. no-deep-imports-into-pipelines-internal — Phase 25 MOD-02 mirror of rules 1/2/3/4/5/6/7/8
    //      for the pipelines module (plan 25-00 adds this rule; plan 25-03 creates real
    //      server/pipelines/internal/module.ts body that replaces the stub).
    ```

    2. Find the rule-8 block (`name: 'no-deep-imports-into-maestro-internal'`) and DUPLICATE it verbatim immediately after, then change ALL `maestro` occurrences to `pipelines`:

    ```javascript
    {
      name: 'no-deep-imports-into-pipelines-internal',
      comment:
        'Nothing outside server/pipelines/** may reach into server/pipelines/internal/**. ' +
        'Public API comes from server/pipelines/index.ts barrel.',
      severity: 'error',
      from: { pathNot: '^server/pipelines/' },
      to:   { path: '^server/pipelines/internal/' },
    },
    ```

    Insert AFTER the rule-8 block, BEFORE the `no-direct-bus-emit-outside-events` rule.

    3. Verify the file is valid JS by running `node -e "require('./.dependency-cruiser.cjs')"` (must not throw).

    Create `__fixtures__/dep-cruiser/bad-pipelines-deep-import.ts` (mirrors `bad-maestro-deep-import.ts`):

    ```typescript
    /**
     * Fixture for Phase 25 dep-cruiser rule 9 (no-deep-imports-into-pipelines-internal).
     * This file deliberately reaches into server/pipelines/internal/ from OUTSIDE the
     * pipelines module — depcruise must flag it as a violation when invoked against
     * this fixture. Used by server/hooks/__tests__/dep-cruiser.spec.ts to prove
     * structural enforcement (test at runtime; CI graph excludes __fixtures__/ via
     * the `includeOnly: '^server/'` filter, so this fixture does NOT add to the
     * baseline violation count).
     */
    // @ts-expect-error — intentional rule violation for spec
    import { createPipelinesModule } from '../../server/pipelines/internal/module.js';

    export const _violation = createPipelinesModule;
    ```

    EDIT `server/hooks/__tests__/dep-cruiser.spec.ts`:

    Find the existing `[MOD-02 maestro extension]` it-block (Phase 24). Insert a new it-block immediately after it, mirroring the SAME helper invocation pattern (do NOT add new helper functions — reuse whatever runDepCruise/spawnDepCruise helper rule 8 uses):

    ```typescript
    it('[MOD-02 pipelines extension] rule 9 fires on bad-pipelines-deep-import fixture', async () => {
      const fixture = '__fixtures__/dep-cruiser/bad-pipelines-deep-import.ts';
      // Mirror the rule-8 helper invocation EXACTLY — same two-pass err+json
      // pattern documented at the top of this file.
      const result = await runDepCruise(fixture, 'json');
      const violations = result.summary.violations.filter(
        (v: any) => v.rule.name === 'no-deep-imports-into-pipelines-internal',
      );
      expect(violations.length).toBeGreaterThanOrEqual(1);
      expect(violations[0].from).toMatch(/bad-pipelines-deep-import/);
    });
    ```

    (Adapt the helper-call signature to match whatever rule 8 uses verbatim — do NOT introduce a new helper. If rule 8's it-block uses inline child_process via the existing imports at the top of dep-cruiser.spec.ts, mirror that exact call pattern with `pipelines` substituted for `maestro`.)
  </action>
  <verify>
    <automated>npx vitest run server/hooks/__tests__/dep-cruiser.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "no-deep-imports-into-pipelines-internal" .dependency-cruiser.cjs` >= 2 (one in comment block + one in rule body)
    - `node -e "require('./.dependency-cruiser.cjs')"` exits 0 (file is valid JS)
    - `test -f __fixtures__/dep-cruiser/bad-pipelines-deep-import.ts` succeeds
    - `grep -c "server/pipelines/internal/module" __fixtures__/dep-cruiser/bad-pipelines-deep-import.ts` returns 1
    - `grep -c "MOD-02 pipelines extension" server/hooks/__tests__/dep-cruiser.spec.ts` returns 1
    - `npx vitest run server/hooks/__tests__/dep-cruiser.spec.ts` ALL it-blocks pass (existing + new = +1)
    - `npm run dep-check` shows ZERO NEW violations on committed codebase (baseline unchanged; fixture lives in `__fixtures__/` outside `includeOnly:'^server/'`)
  </acceptance_criteria>
  <done>
    Rule 9 added; fixture file fires it; spec proves enforcement; baseline `npm run dep-check` violation count unchanged.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` produces ZERO new errors
- `npx vitest run server/queue/__tests__/names.spec.ts server/pipelines/__tests__/events.spec.ts server/hooks/__tests__/dep-cruiser.spec.ts` all green
- `npm run dep-check` baseline violation count UNCHANGED (rule 9 contributes 0 on committed code)
- 6 new files exist (events, queue, internal/module, MODULE.md, index.ts, events.spec)
- 1 new fixture file exists
- 4 files extended (queue/names.ts, queue/__tests__/names.spec.ts, .dependency-cruiser.cjs, dep-cruiser.spec.ts)
- NO edits to: scheduler.ts, executor.ts, service.ts, plugin.ts, package.json, server/index.ts
</verification>

<success_criteria>
- ROADMAP §Phase 25 SC1/SC2/SC3 substrate gates: dep-cruiser rule 9, queue-name registry entry, throw-stub for resolvable import, MOD-01 file existence — all unblocked for plans 25-01..25-05.
- Wave 0 commit lands cleanly; downstream plans can execute in parallel up to file-conflict boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/25-pipelines-module/25-00-SUMMARY.md` documenting:
- Files created (6) + extended (3 + 1 fixture)
- QUEUE_NAMES count: 12 (was 11)
- dep-cruiser rules count: 9 forbidden module rules + 1 direct-bus-emit
- Tests run + green count
- Plans 25-01..25-05 explicitly unblocked
- Confirmation that NO runtime files in server/pipelines/ were edited
</output>
