---
phase: 20
plan: 05
type: execute
wave: 5
depends_on: [20-03]
files_modified:
  - server/pool/MODULE.md
  - server/pool/index.ts
  - server/pool/__tests__/process-tracker.spec.ts
  - server/pool/__tests__/zombie-detector.spec.ts
  - server/pool/__tests__/cleanup.spec.ts
autonomous: true
requirements: [MOD-01, MOD-02, MOD-04, MOD-08]
estimated_minutes: 12
gap_closure: false
must_haves:
  truths:
    - "`server/pool/MODULE.md` fully rewritten (replaces 20-00 placeholder) with ALL 9 fixed H2 sections in exact order: Purpose / Public API / Events Emitted / Events Consumed / Queue Produced / Queue Consumed / Invariants / Non-Goals / Dependencies — plus H3 `### Runnable Example` block. Total ≈120-140 lines (matches Phase 19 reporting MODULE.md 114 lines; pool is similarly rich)."
    - "MODULE.md Events Emitted lists all 4 events with persisted flags: `device.state.changed` NOT persisted (high-freq derivable), `device.allocated` NOT persisted, `device.released` NOT persisted, `device.health.failed` **PERSISTED** (operational telemetry per TRACE-08). All 4 share `aggregateType: 'pool'` + `aggregateId: deviceId` (NOT POOL_AGGREGATE_ID — POOL_AGGREGATE_ID is reserved for future pool-wide telemetry events)."
    - "MODULE.md Events Consumed section EXPLICITLY states `(none — pool is a publisher-only module in Phase 20)` per CONTEXT §Deferred Ideas and RESEARCH §Summary. Phase 21/23/24 will have artifacts/jobs/maestro subscribe to `device.*`."
    - "MODULE.md Queue Produced lists `device.reap` (policy:'stately', retryLimit:1, retryBackoff:true, retryDelay:30, cron '* * * * *' via boss.schedule, singletonKey:DEVICE_REAP) AND notes `device.boot` as a reserved NAME constant NOT registered in Phase 20 (Phase 23 jobs keystone owns the consumer). Queue Consumed lists `device.reap` only (self-loop)."
    - "MODULE.md Invariants section lists 7 invariants (a)-(g) each citing a spec file+test, matching RESEARCH §Invariants Enumeration: (a) VALID_TRANSITIONS gating → device-state.spec; (b) every transition emits state.changed → events.spec + allocation.spec; (c) allocateMutex serialises → allocation.spec; (d) health.failed on every probe fail → health-checker.spec; (e) reaper filters trackedPids → process-tracker.spec; (f) processTracker.register BEFORE device.transition(Idle) → allocation.spec; (g) module.shutdown idempotent → module.spec."
    - "MODULE.md Dependencies section matches plugin.ts dependencies array VERBATIM: `['config', 'db', 'queue', 'event-bus']`."
    - "`server/pool/index.ts` barrel fully rewritten (replaces 20-00 stub). Exactly ONE line matches `from './internal/` (MOD-02 strict 1-line form per Phase 18/19). Re-exports public surface: `poolPlugin` (default via plugin.js) + `createPoolModule` + types + PoolManager/HealthChecker/ProcessTracker/DeviceInfoCollector classes (back-compat for api/jobs/maestro/hooks consumers) + event surface (POOL_EVENT_NAMES/POOL_AGGREGATE_ID/makePoolEmitters/4 payload schemas/types) + queue surface (DEVICE_REAP_QUEUE_NAME/DEVICE_BOOT_QUEUE_NAME/REAP_CRON/registerPoolQueues) + schema surface (deviceStateSchema/platformSchema/deviceMetadataSchema/deviceSummarySchema/deviceListSchema). ≈65 lines."
    - "3 remaining `*.test.ts` → `*.spec.ts` renames via `git mv` (process-tracker, zombie-detector, cleanup). Plan 20-02 already renamed device-state/allocation/health-checker; this plan closes the remaining 3 so ALL pool `__tests__/*.test.ts` files are gone."
    - "`find server/pool/__tests__ -name '*.test.ts' | wc -l` returns 0. MOD-04 file-naming complete for pool module."
    - "No changes to runtime `server/pool/*.ts` files OR `server/index.ts` — purely docs/barrel/file-rename."
  artifacts:
    - path: "server/pool/MODULE.md"
      provides: "LLM-first public contract (MOD-01) — 9 fixed H2 sections + Runnable Example"
      contains: "## Purpose"
    - path: "server/pool/index.ts"
      provides: "Public barrel (MOD-02) — single legitimate import surface; 1-line internal/ re-export"
      contains: "createPoolModule"
    - path: "server/pool/__tests__/process-tracker.spec.ts"
      provides: "Renamed from .test.ts (MOD-04)"
      contains: "ProcessTracker"
    - path: "server/pool/__tests__/zombie-detector.spec.ts"
      provides: "Renamed from .test.ts (MOD-04)"
      contains: "isZombieStat"
    - path: "server/pool/__tests__/cleanup.spec.ts"
      provides: "Renamed from .test.ts (MOD-04)"
      contains: "cleanup"
  key_links:
    - from: "server/pool/MODULE.md Public API"
      to: "server/pool/index.ts"
      via: "Public API section lists barrel exports; MODULE.md Dependencies matches plugin.ts array"
      pattern: "index\\.ts"
    - from: "server/pool/index.ts"
      to: "server/pool/internal/module.js"
      via: "The ONLY allowed internal/ re-export per MOD-02 (createPoolModule + PoolModule type + CreatePoolModuleDeps type, inline type modifier, ONE line)"
      pattern: "from './internal/module"
    - from: "server/pool/MODULE.md Invariants"
      to: "server/pool/__tests__/*.spec.ts"
      via: "Each of 7 invariants cites a [Invariant X] test reference"
      pattern: "\\[Invariant"
---

<objective>
Close the module-convention artifacts for pool. Plan 20-00..20-04 shipped all runtime + test code; this plan ships the LLM-first documentation + public import surface: (a) full `MODULE.md` with 9 fixed H2 sections + Runnable Example (matches Phase 19 reporting MODULE.md shape exactly); (b) complete public barrel at `server/pool/index.ts` (replaces 20-00 stub) with MOD-02 strict 1-line internal/ re-export + all public surface re-exported via named exports (no `export *`); (c) 3 remaining `*.test.ts → *.spec.ts` renames via `git mv` closing MOD-04 for the pool directory.

MOD-01 requires every v3.0 module to ship MODULE.md so LLMs read a single file to understand the public contract. MOD-02 requires a dep-cruiser-enforced barrel. Phase 20 is not complete until these two artifacts exist — otherwise plans 20-01..20-04 ship working code but MOD-01/02/04 is false and ROADMAP §Phase 20 SC3 fails.

This plan mirrors Phase 19 Plan 19-06 architecturally — MODULE.md + barrel + rename .test.ts → .spec.ts — adapted for pool's richer public surface (PoolManager + HealthChecker + ProcessTracker + DeviceInfoCollector back-compat classes + 4 events + 1 queue registered).

This plan is PARALLEL with Plan 20-04 (both depend on 20-03 only; neither depends on each other's output). Executing in Wave 5 rather than strictly after 20-04 is possible; keeping wave=5 because Nyquist gate wants the DB-gated specs from 20-04 counted in coverage delta. Plan 20-06 (phase close) depends on both 20-04 + 20-05.

Output: `MODULE.md` (~130 lines), `index.ts` barrel (~65 lines), 3 `git mv` renames. No Nyquist capture here — Plan 20-06 owns that (reads the final test-file list + full test suite).
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pool-module-devices/20-CONTEXT.md
@.planning/phases/20-pool-module-devices/20-RESEARCH.md
@.planning/phases/20-pool-module-devices/20-00-PLAN.md
@.planning/phases/20-pool-module-devices/20-01-PLAN.md
@.planning/phases/20-pool-module-devices/20-02-PLAN.md
@.planning/phases/20-pool-module-devices/20-03-PLAN.md
@.planning/phases/20-pool-module-devices/20-04-PLAN.md
@server/hooks/MODULE.md
@server/hooks/index.ts
@server/lifecycle/MODULE.md
@server/lifecycle/index.ts
@server/reporting/MODULE.md
@server/reporting/index.ts
@server/pool/events.ts
@server/pool/queue.ts
@server/pool/plugin.ts
@server/pool/internal/module.ts
@server/pool/pool-manager.ts
@server/pool/health-checker.ts
@server/pool/process-tracker.ts
@server/pool/device-info-collector.ts
@server/pool/schemas.ts
@docs/adr/001-spec-driven-architecture.md
@docs/adr/002-file-naming.md

<interfaces>
<!-- Everything imported/referenced is already committed by plans 20-00..20-04 (20-04 parallels this plan; MODULE.md references are Phase 20 canon). -->

From server/reporting/MODULE.md (canonical — copy section order 1:1):
```
## Purpose
## Public API
## Events Emitted
## Events Consumed
## Queue Produced
## Queue Consumed
## Invariants
## Non-Goals
## Dependencies
### Runnable Example   (H3, optional — Phase 27 MOD-09 typechecks)
```

From server/reporting/index.ts (canonical barrel — strict 1-line internal/ form):
```typescript
export { createReportingModule, type ReportingModule, type CreateReportingModuleDeps } from './internal/module.js';
```
→ pool mirrors this with `createPoolModule + PoolModule + CreatePoolModuleDeps`.

From server/pool/events.ts (Plan 20-01 committed):
- Exports: POOL_EVENT_NAMES, POOL_AGGREGATE_ID, poolRegistry, makePoolEmitters,
  4 payload schemas (deviceStateChangedPayload, deviceAllocatedPayload, deviceReleasedPayload, deviceHealthFailedPayload),
  types PoolRegistry, PoolEmitters, PoolEventName

From server/pool/queue.ts (Plan 20-03 committed):
- Exports: DEVICE_REAP_QUEUE_NAME, DEVICE_BOOT_QUEUE_NAME, REAP_CRON, registerPoolQueues,
  types RegisterPoolQueuesDeps, PoolQueueRegistration

From server/pool/internal/module.ts (Plan 20-03 committed):
- Exports: createPoolModule, types PoolModule, CreatePoolModuleDeps

From server/pool/schemas.ts (Phase 17 — unchanged):
- Exports: deviceStateSchema, platformSchema, deviceMetadataSchema, deviceSummarySchema, deviceListSchema, type DeviceSummary

From server/pool/device-info-collector.ts (unchanged; consumed by server/maestro/plugin.ts:22):
- Exports: DeviceInfoCollector class

Back-compat requirement (RESEARCH §Pitfall 9): fastify.pool : PoolManager surface must be preserved. Consumers in api/routes.ts + jobs/plugin.ts + maestro/plugin.ts + artifacts/recording-service.ts read `fastify.pool.getDevice/.getDevices/.getDeviceMap/.getDriver` → barrel must export `PoolManager` class for type reference.

File layout after this plan:
```
server/pool/
├── MODULE.md                 ← THIS plan (full body replaces 20-00 placeholder)
├── index.ts                  ← THIS plan (full barrel replaces 20-00 stub)
├── plugin.ts                 ← Plan 20-03 (thin wirer; unchanged here)
├── events.ts                 ← Plan 20-01 (full MOD-03 body)
├── queue.ts                  ← Plan 20-03 (registerPoolQueues factory body)
├── schemas.ts                ← Phase 17 (unchanged)
├── ws-schemas.ts             ← Phase 17 (unchanged)
├── types.ts                  ← unchanged (DeviceDriver interface)
├── device.ts                 ← Plan 20-02 (transition returns {from,to})
├── pool-manager.ts           ← Plan 20-02 (emits at 6+ sites; 4-param ctor)
├── health-checker.ts         ← Plan 20-02 (emits at 4 failure sites; 4-param ctor)
├── process-tracker.ts        ← Plan 20-03 (startReaper removed; stop() no-op)
├── zombie-detector.ts        ← unchanged
├── device-info-collector.ts  ← unchanged
├── internal/
│   └── module.ts             ← Plan 20-03 (full createPoolModule factory)
├── android/                  ← unchanged (driver impl)
├── ios/                      ← unchanged (driver impl)
└── __tests__/
    ├── device-state.spec.ts         ← Plan 20-02 renamed + extended
    ├── allocation.spec.ts           ← Plan 20-02 renamed + extended
    ├── health-checker.spec.ts       ← Plan 20-02 renamed + extended
    ├── events.spec.ts               ← Plan 20-00 stub + Plan 20-01 extended
    ├── module.spec.ts               ← Plan 20-03 (new)
    ├── lifecycle-ownership.spec.ts  ← Plan 20-03 (new)
    ├── subscriber.spec.ts           ← Plan 20-04 (DB-gated, new)
    ├── correlation.spec.ts          ← Plan 20-04 (DB-gated, new)
    ├── process-tracker.spec.ts      ← THIS plan renamed from .test.ts
    ├── zombie-detector.spec.ts      ← THIS plan renamed from .test.ts
    └── cleanup.spec.ts              ← THIS plan renamed from .test.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Write server/pool/MODULE.md with 9 fixed H2 sections + Runnable Example</name>
  <files>server/pool/MODULE.md</files>
  <read_first>
    - server/reporting/MODULE.md (Phase 19 canonical 114-line template — copy section ordering + voice)
    - server/lifecycle/MODULE.md (Phase 18 canonical ~100 lines)
    - server/hooks/MODULE.md (Phase 16 canonical 97 lines)
    - server/pool/events.ts (4 events + their persisted flags for Events Emitted section)
    - server/pool/queue.ts (device.reap policy + DEVICE_BOOT reserved-name note for Queue Produced section)
    - server/pool/plugin.ts (dependencies array verbatim for Dependencies section)
    - server/pool/__tests__/*.spec.ts (all specs — Invariants section cites test file+name)
  </read_first>
  <action>
    **Create** `server/pool/MODULE.md`. REPLACE the Plan 20-00 placeholder. Follow the 9-section canonical ordering — DO NOT add extra H2 sections.

    ```markdown
    # Pool Module

    ## Purpose

    Own the device pool: the `Device` state machine (`VALID_TRANSITIONS`-gated; 7 states + 2 platforms), the allocation mutex (`PoolManager.allocateMutex`), the 30-second in-process `HealthChecker` probe loop, and the `device.reap` pg-boss schedule (every minute; replaces the v2.0 raw `setInterval` reaper). Emits 4 typed events — `device.state.changed`, `device.allocated`, `device.released`, `device.health.failed` — so Phase 21+ modules (`artifacts`, `jobs`, `maestro`, `hooks`, `streaming`) can react to device lifecycle without reaching into pool internals.

    The module is a **publisher only** in Phase 20. No bus subscribers live here. Downstream phases add the consumer side: Phase 21 (`artifacts` subscribes to state.changed); Phase 23 (`jobs` keystone consumes allocated/released); Phase 24 (`maestro` subscribes to state.changed filtered on Booting→Idle for hierarchy/device-info collection).

    ## Public API

    Exports from `server/pool/index.ts` (the ONLY legitimate import surface outside this module — enforced by `dependency-cruiser` rule `no-deep-imports-into-pool-internal` added in Phase 20 Plan 20-00):

    - `poolPlugin` — Fastify plugin (thin wrapper around `createPoolModule`).
    - `createPoolModule(deps): PoolModule` — factory returning `{pool, healthChecker, emit, bus, registerWorkersAndSubscribers, shutdown}`.
    - `PoolManager` class — back-compat. `fastify.pool: PoolManager` decorator read by api/jobs/maestro/hooks/streaming.
    - `HealthChecker` class — back-compat. `fastify.healthChecker: HealthChecker` decorator.
    - `ProcessTracker` class — back-compat. `fastify.processTracker: ProcessTracker` decorator.
    - `DeviceInfoCollector` class — back-compat. Consumed by `server/maestro/plugin.ts:22`.
    - `Device`, `InvalidTransitionError` — entity class + error.
    - `reportingPlugin`-style module surface: `poolModule: PoolModule` (Fastify decorator; gives consumers `poolModule.bus.on(...)` access without deep-import).
    - Events: `poolRegistry`, `POOL_EVENT_NAMES`, `POOL_AGGREGATE_ID`, `makePoolEmitters`, 4 payload schemas (`deviceStateChangedPayload`, `deviceAllocatedPayload`, `deviceReleasedPayload`, `deviceHealthFailedPayload`).
    - Queue: `DEVICE_REAP_QUEUE_NAME`, `DEVICE_BOOT_QUEUE_NAME`, `REAP_CRON`, `registerPoolQueues`.
    - Schemas (from Phase 17): `deviceStateSchema`, `platformSchema`, `deviceMetadataSchema`, `deviceSummarySchema`, `deviceListSchema`, type `DeviceSummary`.
    - Types: `PoolModule`, `CreatePoolModuleDeps`, `PoolRegistry`, `PoolEmitters`, `PoolEventName`, `RegisterPoolQueuesDeps`, `PoolQueueRegistration`.

    Fastify decorators exposed by the plugin:
    - `fastify.pool: PoolManager`
    - `fastify.processTracker: ProcessTracker`
    - `fastify.healthChecker: HealthChecker`
    - `fastify.poolModule: PoolModule`

    ## Events Emitted

    - `device.state.changed` — **NOT persisted**. Fires on every `VALID_TRANSITIONS`-allowed transition. Payload: `{deviceId, from, to}`. High-frequency — skipping events-table persistence per TRACE-08 (derivable from Phase 21+ consumers if needed; flip the registry flag to turn it on).
    - `device.allocated` — **NOT persisted**. Fires from `PoolManager.allocate()` INSIDE `allocateMutex`, AFTER `device.allocate(jobId)` succeeds. Payload: `{deviceId, jobId, platform}`.
    - `device.released` — **NOT persisted**. Fires from `PoolManager.release()` AFTER `device.release()` returns. Payload: `{deviceId, jobId: string | null, platform}` — `jobId` captured BEFORE `device.release()` clears `currentJobId`, so consumers can link the release to its originating job.
    - `device.health.failed` — **PERSISTED** (TRACE-08 operational telemetry). Fires from `HealthChecker` on EVERY failed probe regardless of whether the state actually transitions. Payload: `{deviceId, platform, reason, failureCount, willReplace, lastError}` where `reason` is one of `'unhealthy' | 'zombie' | 'max-retries' | 'timeout'` (discriminator). Phase 27 trace-tree endpoint surfaces persisted rows.

    All 4 events share `aggregateType: 'pool'` and use `aggregateId: deviceId` (per-device, NOT `POOL_AGGREGATE_ID`). `POOL_AGGREGATE_ID = '9fabf52b-a6a3-599f-a7fc-1c1e22ad9b8b'` (stable v5 UUID derived from `'pool'` under the URL namespace, mirroring Phase 18 `LIFECYCLE_AGGREGATE_ID` + Phase 19 `REPORTING_AGGREGATE_ID` pattern) is **reserved** for future pool-wide telemetry (e.g. `pool.initialized`) but NOT used by the 4 events above.

    Derivable alias: consumers wanting `device.booted` semantics subscribe to `device.state.changed` filtered on `payload.from === 'booting' && payload.to === 'idle'`. Phase 24 (Maestro) may add `device.booted` as a convenience alias if this filter pattern proves painful in practice.

    ## Events Consumed

    **None in Phase 20** — pool is a publisher-only module. Phase 20 CONTEXT §Deferred Ideas + RESEARCH §Summary both explicitly document this.

    Downstream consumer phases:
    - Phase 21 Artifacts Module — subscribes to `device.state.changed` (filter on running→cleanup) to trigger recording upload.
    - Phase 23 Jobs Keystone — subscribes to `device.allocated` / `device.released` as saga transition signals.
    - Phase 24 Maestro Module — subscribes to `device.state.changed` (filter on booting→idle) for hierarchy/device-info collection.
    - Phase 27 API Aggregator — surfaces `device.health.failed` persisted rows via `GET /api/events?correlationId=...` trace-tree endpoint.

    ## Queue Produced

    - `device.reap` — `policy: 'stately'`, `retryLimit: 1`, `retryBackoff: true`, `retryDelay: 30`. Schedule: `'* * * * *'` (every minute) via `fastify.queue.schedule(DEVICE_REAP, '* * * * *', {}, {singletonKey: 'device.reap'})`. Registered by `registerPoolQueues` from `server/pool/queue.ts`. Payload: empty object `{}` (the worker invokes `processTracker.reapOrphans()` with no input). `singletonKey + policy:'stately'` prevents overlap if a reap cycle runs long (e.g. slow `ps axo` on macOS).
    - `device.boot` — **RESERVED NAME ONLY**. The constant `QUEUE_NAMES.DEVICE_BOOT = 'device.boot'` is exported for Phase 23 forward-compat but NOT registered as a pg-boss queue in Phase 20. Phase 23 jobs keystone owns the on-demand-boot consumer.

    ## Queue Consumed

    - `device.reap` — worker handler (self-loop — producer + consumer both in pool module) calls `processTracker.reapOrphans()`. ALS is restored from envelope.correlationId BEFORE the handler fires (Phase 15 substrate `server/queue/plugin.ts`); scheduled fires per Plan 18-00 Option B generate a fresh correlationId per fire (envelope stored with `correlationId: null`).

    ## Invariants

    Every invariant below has at least one test (MOD-08). Test file citations point to the canonical spec file under `server/pool/__tests__/`:

    - **(a) `VALID_TRANSITIONS`-only state changes** — `Device.transition(newState)` throws `InvalidTransitionError` when `newState` is not in `VALID_TRANSITIONS[this._state]`. Test: `device-state.spec.ts` `[Invariant MOD-08 (a)]`.
    - **(b) Every successful transition emits exactly one `device.state.changed` envelope** — the 6 call sites in `pool-manager.ts` + the 4 failure code paths in `health-checker.ts` + restart/replace paths all invoke `emit.stateChanged` AFTER the transition. Tests: `allocation.spec.ts` `[SC1]`, `events.spec.ts` `[Invariant MOD-08 (b)]`.
    - **(c) `PoolManager.allocate()` is mutex-protected** — concurrent allocate calls for the same platform serialise via `allocateMutex.runExclusive`; exactly one `device.allocated` envelope per successful allocation. Test: `allocation.spec.ts` `[SC1]` concurrency test.
    - **(d) `device.health.failed` fires on every failed probe** — regardless of whether the device state changes (RESEARCH §Pitfall 2). `reason` discriminator identifies the failure path (`unhealthy` / `zombie` / `max-retries` / `timeout`). Test: `health-checker.spec.ts` `[SC1]` per-reason tests.
    - **(e) Reaper never kills allocated-device PIDs** — `processTracker.scanOrphans()` filters by `!trackedPids.has(pid) && !trackedPids.has(pgid)`; reaper worker body preserves this guard. Test: `process-tracker.spec.ts`.
    - **(f) `processTracker.register(pid)` called BEFORE `device.transition(Idle)`** — prevents the register/reap race (RESEARCH §Pitfall 3). Test: `allocation.spec.ts` (spy-order assertion).
    - **(g) `poolModule.shutdown()` is idempotent** — second call is no-op via `stopped` flag; no extra `healthChecker.stop()` or `offWork` calls. Test: `module.spec.ts` `[Invariant MOD-08 (g)]`.

    ## Non-Goals

    - **`device.booted` as first-class event** — CONTEXT §Specifics + RESEARCH §Pitfall 4 defer to Phase 24 Maestro. Consumers filter `state.changed {booting→idle}` today.
    - **`pool.shutdown()` migration into `module.shutdown`** — RESEARCH §Open Question 1 documents the timing constraint (current `server/index.ts` flow needs `pool.shutdown()` AFTER `jobService.shutdown()` but BEFORE `app.close()`). Keeping it imperative in `server/index.ts` with a TODO comment; Phase 23 jobs keystone may revisit.
    - **Android/iOS driver internal refactor** — CONTEXT §Deferred. Drivers stay under `android/` + `ios/` sub-directories with their current kebab-case files.
    - **Hot-plug device detection** — `detectPhysicalDevices` is a one-shot scan in `initPool()` (RESEARCH §Pitfall 7). A watcher + `device.detected` event is deferred.
    - **`device.*` webhook fan-out** — reporting module subscribes only to `job.completed` today. Adding device webhooks is downstream.
    - **Consolidation of duplicated `persistEnvelope` middleware** — 4th sample point reached with pool (hooks + lifecycle + reporting + pool). Phase 27+ consolidation trigger noted in 4 factory files; do NOT consolidate yet.
    - **Registering `device.boot` pg-boss queue in Phase 20** — Phase 23 jobs keystone owns the consumer; registering without a consumer traps sends (RESEARCH §Anti-Patterns).
    - **Rewiring existing deep imports** (`jobs/plugin.ts` → `pool/pool-manager.ts`, etc.) — CONTEXT §Deferred Ideas. New code MUST use the barrel; existing deep imports are Phase 21/23/24 cleanup scope.

    ## Dependencies

    Declared in `server/pool/plugin.ts` `dependencies: ['config', 'db', 'queue', 'event-bus']`:

    - `config` — reads `fastify.config.pool.{android, ios}` for driver enablement + `max_instances` + platform-specific boot options.
    - `db` — writes `events` rows via the `persistEnvelope` middleware (persisted: `device.health.failed` only).
    - `queue` — registers the `device.reap` schedule + worker via `fastify.boss.createQueue` + `fastify.queue.schedule` + `fastify.queue.work`; offWorks on shutdown.
    - `event-bus` — structural dependency: pool maintains its own per-module `TypedBus<PoolRegistry>` but `fastify.bus` + `fastify.onPersisted` decorators from the bus plugin are required for downstream subscriber wiring (Phases 21/23/24) + the `subscriber.spec.ts` DB-gated proof uses `fastify.onPersisted`.

    ---

    ### Runnable Example

    ```typescript
    // Inside a Fastify plugin that has already registered config + db + queue + event-bus + pool:
    import {
      POOL_EVENT_NAMES,
      POOL_AGGREGATE_ID,
      DEVICE_REAP_QUEUE_NAME,
    } from 'server/pool/index.js';

    // Listen for device.state.changed via the module's per-module bus:
    app.poolModule.bus.on(POOL_EVENT_NAMES.STATE_CHANGED, (payload) => {
      app.log.info(
        { deviceId: payload.deviceId, from: payload.from, to: payload.to },
        'Device state changed',
      );
    });

    // Listen for device.health.failed via the persisted side-channel (same payload + envelope):
    app.onPersisted('device.health.failed', (envelope) => {
      app.log.warn(
        { deviceId: envelope.aggregateId, reason: envelope.payload.reason },
        'Device health failed — persisted event row in events table',
      );
    });

    // Query the events table by correlation for operational debug:
    // SELECT * FROM events WHERE aggregate_type = 'pool' AND correlation_id = $1 ORDER BY occurred_at;
    ```

    Phase 27 (MOD-09) will add CI-level typechecking of this snippet. For Phase 20, reviewer spot-checks the block.
    ```

    **Do NOT** add sections beyond the 9 H2s + 1 H3 Runnable Example. Phase 16/18/19 established the strict section invariant for grep-based MOD-01 checks.

    **Do NOT** re-document internal implementation details (persistEnvelope, ALS plumbing, emit helper internals). MODULE.md is the consumer-facing contract; those live in RESEARCH.md.
  </action>
  <verify>
    <automated>test -f server/pool/MODULE.md && for section in "## Purpose" "## Public API" "## Events Emitted" "## Events Consumed" "## Queue Produced" "## Queue Consumed" "## Invariants" "## Non-Goals" "## Dependencies"; do if ! grep -q "^${section}$" server/pool/MODULE.md; then echo "MISSING section: ${section}"; exit 1; fi; done && echo "OK: all 9 H2 sections" && grep -c "device.health.failed" server/pool/MODULE.md | awk '{ if ($1 < 3) { print "ERROR: EVENTS-07-style terminal event persisted flag mentioned only", $1, "times"; exit 1 } else { print "OK:", $1, "refs" } }' && grep -q "### Runnable Example" server/pool/MODULE.md && grep -q "'config', 'db', 'queue', 'event-bus'" server/pool/MODULE.md && grep -q "POOL_AGGREGATE_ID" server/pool/MODULE.md && grep -c "\[Invariant" server/pool/MODULE.md | awk '{ if ($1 < 5) { print "ERROR: Invariants section should reference >=5 tests; got", $1; exit 1 } }' && wc -l server/pool/MODULE.md | awk '{ if ($1 < 80 || $1 > 250) { print "WARN: MODULE.md line count", $1, "(expected 100-200)"; } }'</automated>
  </verify>
  <acceptance_criteria>
    - `server/pool/MODULE.md` exists
    - All 9 H2 sections present in exact order: Purpose, Public API, Events Emitted, Events Consumed, Queue Produced, Queue Consumed, Invariants, Non-Goals, Dependencies
    - H3 `### Runnable Example` present
    - Events Emitted lists all 4 events (grep each: `device.state.changed`, `device.allocated`, `device.released`, `device.health.failed`)
    - Exactly ONE event marked PERSISTED (`device.health.failed`)
    - Events Consumed explicitly states `(None in Phase 20 — pool is a publisher-only module)` — grep for "publisher-only" or equivalent
    - Queue Produced section names `device.reap` AND explicitly describes `device.boot` as reserved/NOT registered
    - Queue Consumed describes `device.reap` self-loop
    - Invariants section lists at least 5 invariants each with a `[Invariant` test reference
    - Dependencies section matches plugin.ts array verbatim: `'config', 'db', 'queue', 'event-bus'` (grep match)
    - Line count between 80 and 250 (hooks 97, lifecycle ~100, reporting 114)
    - Runnable Example block contains valid-looking TypeScript (imports from `server/pool/index.js`, uses `app.poolModule.bus.on` + `app.onPersisted`)
  </acceptance_criteria>
  <done>
    MODULE.md ships the LLM-first 9-section contract. Every invariant cites a spec. Runnable Example compiles (will be typechecked by Phase 27 MOD-09 later).
  </done>
</task>

<task type="auto">
  <name>Task 5.2: Write full server/pool/index.ts barrel (MOD-02 public surface)</name>
  <files>server/pool/index.ts</files>
  <read_first>
    - server/pool/index.ts (current — Plan 20-00 stub with 1 export line)
    - server/reporting/index.ts (Phase 19 canonical 75-line template — strict 1-line internal/ form)
    - server/lifecycle/index.ts (Phase 18 canonical — stricter form precedent)
    - server/pool/events.ts (what to re-export — 11 named exports)
    - server/pool/queue.ts (3 constants + registerPoolQueues + 2 types)
    - server/pool/internal/module.ts (factory + 2 types — the ONLY internal/ re-export)
    - server/pool/plugin.ts (default plugin export)
    - server/pool/pool-manager.ts (PoolManager class — back-compat)
    - server/pool/health-checker.ts (HealthChecker class — back-compat)
    - server/pool/process-tracker.ts (ProcessTracker class — back-compat)
    - server/pool/device-info-collector.ts (DeviceInfoCollector class)
    - server/pool/device.ts (Device class + InvalidTransitionError)
    - server/pool/schemas.ts (5 schemas + DeviceSummary type)
  </read_first>
  <action>
    **Overwrite** `server/pool/index.ts` (replaces Plan 20-00 stub).

    ```typescript
    /**
     * Phase 20 / Plan 20-05 — Pool module public barrel (MOD-02).
     *
     * This is the ONLY import surface for consumers outside `server/pool/`.
     * `dependency-cruiser` rule `no-deep-imports-into-pool-internal` (added
     * to `.dependency-cruiser.cjs` in Phase 20 Plan 20-00) enforces the boundary
     * structurally — imports into `server/pool/internal/**` from outside
     * the module fail CI.
     *
     * Exports in this file are PUBLIC and subject to semver-ish stability.
     * Anything under `./internal/` is module-private and may change without notice.
     *
     * The ONLY allowed internal/ re-export line is the factory entry point per
     * MOD-06. Runtime + types exported from a single statement (inline `type`
     * modifier) so plan 20-05 verify script sees exactly ONE internal/ re-export
     * line (MOD-02 structural invariant). Matches Phase 18 lifecycle + Phase 19
     * reporting 1-line form, stricter than Phase 16 hooks' 2-line form.
     */

    // Fastify plugin (default export — matches Phase 15 substrate convention).
    export { default as poolPlugin } from './plugin.js';

    // Factory surface (the v3.0 canonical way to instantiate the module). MOD-06.
    // ONE internal/ re-export line — MOD-02 structural invariant.
    export { createPoolModule, type PoolModule, type CreatePoolModuleDeps } from './internal/module.js';

    // Back-compat class surfaces (preserved for existing consumers — PoolManager
    // decorator on fastify is still read by api/jobs/maestro/hooks/streaming).
    export { PoolManager } from './pool-manager.js';
    export { HealthChecker } from './health-checker.js';
    export { ProcessTracker } from './process-tracker.js';
    export type { OrphanInfo, KillResult } from './process-tracker.js';
    export { DeviceInfoCollector } from './device-info-collector.js';
    export { Device, InvalidTransitionError } from './device.js';

    // Schemas + derived TS types (Phase 17 SPEC-06 unchanged).
    export {
      deviceStateSchema,
      platformSchema,
      deviceMetadataSchema,
      deviceSummarySchema,
      deviceListSchema,
    } from './schemas.js';
    export type { DeviceSummary } from './schemas.js';

    // Events surface (MOD-03 — Plan 20-01).
    export {
      poolRegistry,
      POOL_EVENT_NAMES,
      POOL_AGGREGATE_ID,
      makePoolEmitters,
      deviceStateChangedPayload,
      deviceAllocatedPayload,
      deviceReleasedPayload,
      deviceHealthFailedPayload,
    } from './events.js';
    export type { PoolRegistry, PoolEmitters, PoolEventName } from './events.js';

    // Queue surface (QUEUE-06 — Plan 20-03).
    export {
      DEVICE_REAP_QUEUE_NAME,
      DEVICE_BOOT_QUEUE_NAME,
      REAP_CRON,
      registerPoolQueues,
    } from './queue.js';
    export type {
      RegisterPoolQueuesDeps,
      PoolQueueRegistration,
    } from './queue.js';

    // NOTE: Types `DeviceDriver` / `DeviceState` / `VALID_TRANSITIONS` / `Platform`
    // / `DeviceInfo` / `DeviceMetadata` live in `server/types/index.ts` (global types),
    // not in pool. Consumers import those from `../types/index.js` directly.
    ```

    **Structural invariants** (MOD-02 strict):
    - EXACTLY ONE line matches `from '\\./internal/` — inline type modifier keeps `createPoolModule + PoolModule + CreatePoolModuleDeps` on one statement.
    - NO `export *` — per ADR-002 / MOD-02, named exports only (internal helpers would leak otherwise).
    - plugin.ts's `registerPoolQueues` / `poolPlugin` helpers that are NOT public surface are intentionally NOT re-exported.

    **Do NOT re-export** `processTracker.reapOrphans`, `scanOrphans`, `killProcess`, `killAll` — these are methods on the ProcessTracker class; the class is re-exported but its methods are accessed via instance.

    **Do NOT re-export** driver implementations (`DeviceStreamAndroidDriver`, `DeviceStreamIosDriver`) — they're plugin-internal construction details.

    **Do NOT re-export** types from `server/types/index.ts` (`DeviceState`, `VALID_TRANSITIONS`, `Platform`, `DeviceInfo`, `DeviceMetadata`) — those live in the global types file, not the pool module. Consumers import those from `../types/index.js`.
  </action>
  <verify>
    <automated>test -f server/pool/index.ts && grep -c "from './internal/" server/pool/index.ts | awk '{ if ($1 != 1) { print "ERROR: MOD-02 requires EXACTLY ONE internal/ re-export line; got", $1; exit 1 } else { print "OK: 1 internal/ line" } }' && grep -q "export { createPoolModule, type PoolModule, type CreatePoolModuleDeps } from './internal/module.js'" server/pool/index.ts && grep -q "export { default as poolPlugin }" server/pool/index.ts && grep -q "export { PoolManager }" server/pool/index.ts && grep -q "export { HealthChecker }" server/pool/index.ts && grep -q "export { ProcessTracker }" server/pool/index.ts && grep -q "export { DeviceInfoCollector }" server/pool/index.ts && grep -q "export { Device, InvalidTransitionError }" server/pool/index.ts && grep -q "export { poolRegistry" server/pool/index.ts && grep -q "export { DEVICE_REAP_QUEUE_NAME" server/pool/index.ts && grep -q "deviceStateChangedPayload" server/pool/index.ts && grep -cE "^export \*" server/pool/index.ts | grep -q "^0$" && npx tsc --noEmit && npm run dep-check</automated>
  </verify>
  <acceptance_criteria>
    - `server/pool/index.ts` exists (replaces Plan 20-00 stub)
    - EXACTLY ONE line matches `from './internal/` (MOD-02 strict — grep count === 1)
    - That one line uses inline `type` modifier: `export { createPoolModule, type PoolModule, type CreatePoolModuleDeps } from './internal/module.js'` (grep exact match)
    - Exports `poolPlugin` (default re-exported — grep match)
    - Exports back-compat classes: `PoolManager`, `HealthChecker`, `ProcessTracker`, `DeviceInfoCollector`, `Device`, `InvalidTransitionError` (grep each)
    - Exports events surface: `poolRegistry`, `POOL_EVENT_NAMES`, `POOL_AGGREGATE_ID`, `makePoolEmitters`, 4 payload schemas, types
    - Exports queue surface: `DEVICE_REAP_QUEUE_NAME`, `DEVICE_BOOT_QUEUE_NAME`, `REAP_CRON`, `registerPoolQueues`, types
    - Exports schema surface: `deviceStateSchema`, `platformSchema`, `deviceMetadataSchema`, `deviceSummarySchema`, `deviceListSchema`, type `DeviceSummary`
    - Does NOT use `export *` (grep count = 0)
    - Does NOT re-export driver classes (DeviceStreamAndroidDriver / DeviceStreamIosDriver — plugin-internal)
    - `npx tsc --noEmit` exits 0
    - `npm run dep-check` exits 0 (MOD-02 rule + cross-module: no new violations introduced)
  </acceptance_criteria>
  <done>
    Barrel ships with MOD-02 strict 1-line internal/ re-export + full public surface via named exports. `export *` forbidden per ADR-002. dep-check clean.
  </done>
</task>

<task type="auto">
  <name>Task 5.3: Rename 3 remaining *.test.ts → *.spec.ts via git mv (MOD-04 close)</name>
  <files>server/pool/__tests__/process-tracker.spec.ts, server/pool/__tests__/zombie-detector.spec.ts, server/pool/__tests__/cleanup.spec.ts</files>
  <read_first>
    - server/pool/__tests__/process-tracker.test.ts (current — test content unchanged by this plan)
    - server/pool/__tests__/zombie-detector.test.ts (current — unchanged)
    - server/pool/__tests__/cleanup.test.ts (current — unchanged)
    - .planning/phases/19-reporting-migration-webhooks-dlq/19-06-PLAN.md Task 6.3 (precedent — git mv pattern)
    - .planning/phases/20-pool-module-devices/20-02-PLAN.md (Plan 20-02 already renamed device-state + allocation + health-checker)
  </read_first>
  <action>
    **Run the 3 renames via `git mv` (100% similarity — blame preserved):**

    ```bash
    git mv server/pool/__tests__/process-tracker.test.ts server/pool/__tests__/process-tracker.spec.ts
    git mv server/pool/__tests__/zombie-detector.test.ts server/pool/__tests__/zombie-detector.spec.ts
    git mv server/pool/__tests__/cleanup.test.ts server/pool/__tests__/cleanup.spec.ts
    ```

    **Do NOT modify the contents of any file.** Phase 30 (Test Migration Cleanup — MOD-04 final) may later align describe-tree to match MODULE.md Public API sections; this plan does FILE-NAMING only (same scope as Phase 18 Plan 18-04 + Phase 19 Plan 19-06 renames).

    **If a file was already renamed** (idempotent re-run), `test -f` on the new path returns 0 — treat as success; only run `git mv` for still-existing `.test.ts` files.

    **Verify blame preservation.** `git log --follow` on each renamed file should show commits pre-dating Phase 20 (the files existed in Phase 1 MVP and have been evolved throughout v2.0).

    **No test content adjustments.** The renamed files run verbatim. After rename:
    - `process-tracker.spec.ts` — existing tests for register/unregister/killProcess/scanOrphans/reapOrphans still pass. NOTE: Plan 20-03 removed `startReaper` method + `reaperInterval` field from process-tracker.ts. If any existing test in this file asserts on `startReaper` behaviour (grep to check), it will fail. If that's the case, the fix is in-scope for this task (scope-boundary Rule 3): simply DELETE those tests (migration to queue.ts reaper is NOT a regression — the equivalent test moved to queue.ts / module.spec.ts Plan 20-03).
    - `zombie-detector.spec.ts` — zero impact from Phase 20 (pure utility module, no emit wiring).
    - `cleanup.spec.ts` — tests `PoolManager.release()` cleanup path; Plan 20-02 added emit calls BUT the NOOP_POOL_EMIT default means existing tests without emit param still pass unchanged.
  </action>
  <verify>
    <automated>test -f server/pool/__tests__/process-tracker.spec.ts && ! test -f server/pool/__tests__/process-tracker.test.ts && test -f server/pool/__tests__/zombie-detector.spec.ts && ! test -f server/pool/__tests__/zombie-detector.test.ts && test -f server/pool/__tests__/cleanup.spec.ts && ! test -f server/pool/__tests__/cleanup.test.ts && find server/pool/__tests__ -name '*.test.ts' 2>&1 | awk '{ if (NF > 0) { print "ERROR: .test.ts files still present:", $0; exit 1 } else { print "OK: no .test.ts files in pool __tests__" } }' && npx vitest run server/pool/__tests__/process-tracker.spec.ts server/pool/__tests__/zombie-detector.spec.ts server/pool/__tests__/cleanup.spec.ts --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - `test -f server/pool/__tests__/process-tracker.spec.ts` exits 0
    - `test -f server/pool/__tests__/zombie-detector.spec.ts` exits 0
    - `test -f server/pool/__tests__/cleanup.spec.ts` exits 0
    - NONE of `process-tracker.test.ts`, `zombie-detector.test.ts`, `cleanup.test.ts` exist (`test -f` exits non-zero)
    - `find server/pool/__tests__ -name '*.test.ts' | wc -l` returns 0 (all pool test files now .spec.ts)
    - `git log --follow -1 --format=%H server/pool/__tests__/process-tracker.spec.ts` returns a commit hash matching prior history (blame preserved via git's rename detection)
    - `npx vitest run server/pool/__tests__/process-tracker.spec.ts server/pool/__tests__/zombie-detector.spec.ts server/pool/__tests__/cleanup.spec.ts` exits 0 (renamed files still pass — content unchanged OR startReaper-specific tests deleted scope-in per Rule 3)
    - If any test was deleted (Rule 3 — startReaper removal consequence), the deletion is documented in the task SUMMARY with ≤ ~5 line citations
  </acceptance_criteria>
  <done>
    All 3 remaining `.test.ts` files renamed to `.spec.ts`. MOD-04 file-naming complete for the pool module. No `*.test.ts` files remain in `server/pool/__tests__/`.
  </done>
</task>

</tasks>

<verification>
- MODULE.md: 9 fixed H2 sections + Runnable Example present (≈130 lines)
- index.ts: MOD-02 strict 1-line internal/ re-export + full public surface via named exports
- 3 .test.ts → .spec.ts renames (process-tracker, zombie-detector, cleanup)
- `find server/pool/__tests__ -name '*.test.ts'` returns 0 (all renamed)
- `npx tsc --noEmit` exit 0
- `npm run dep-check` exit 0 (MOD-02 rule holds; no deep-import violations introduced)
- `npx vitest run server/pool/__tests__/` runs all 11 spec files (existing + new from 20-00..20-04 + 3 renamed)
</verification>

<success_criteria>
- MOD-01 satisfied: MODULE.md ships the 9-section canonical contract with runnable example
- MOD-02 satisfied: index.ts barrel + strict 1-line internal/ re-export
- MOD-04 satisfied for pool module (all test files now .spec.ts)
- Plan 20-06 (phase close — full-suite run + lint + typecheck + Nyquist gate + STATE/ROADMAP update) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/20-pool-module-devices/20-05-SUMMARY.md`
</output>
