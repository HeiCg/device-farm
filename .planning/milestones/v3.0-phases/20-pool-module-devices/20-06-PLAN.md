---
phase: 20
plan: 06
type: execute
wave: 6
depends_on: [20-04, 20-05]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/20-pool-module-devices/deferred-items.md
  - server/__tests__/plugin-order.spec.ts
autonomous: true
requirements: [MOD-04, MOD-08]
estimated_minutes: 7
gap_closure: false
must_haves:
  truths:
    - "`npm test` full suite runs against the Phase 20 working tree (inheriting the Phase 17/18/19 pre-existing-failure exclusion set documented in STATE.md deferred-items). Net new failures from Phase 20 code = 0. Plan SUMMARY documents test runtime + the inherited-exclusion count."
    - "`npm run lint` exits 0 — no new ESLint violations introduced by Phase 20 files."
    - "`npm run dep-check` exits 0 — the 5th forbidden rule `no-deep-imports-into-pool-internal` (from Plan 20-00) holds; no new deep-import violations anywhere in `server/`."
    - "`npx tsc --noEmit` does not introduce NEW errors from Phase 20 files. Pre-existing Phase 15/17/18/19 typecheck errors (documented in 19-SUMMARY.md — 6 Phase 15 Map-vs-RequestContext + 2 working-tree artifacts/recording-service.ts edits) are stable; net Phase 20 delta = 0 new errors attributable to pool/* files."
    - "`npm run nyquist:capture && npm run nyquist:check` exits 0 — `coverage.lines` current vs Phase 15 baseline 48.29% → delta ≥ -2pp (Phase 19 close was +8.19pp; Phase 20 expected net 0 to +3pp). Baseline file `.planning/nyquist-baseline.json` NOT overwritten (diff verified)."
    - "`.planning/phases/20-pool-module-devices/deferred-items.md` exists documenting: (a) inherited Phase 17/18/19 exclusions (fastify-zod-openapi v5 `required` emission bug, pool-plugin substring-match bug in plugin-order.spec — same files per STATE.md); (b) Phase 20 intentional deferrals per MODULE.md Non-Goals (device.booted as first-class event → Phase 24; pool.shutdown() migration → Phase 23; hot-plug → future; device.* webhook fan-out → future; persistEnvelope consolidation → Phase 27+; existing jobs/maestro/hooks deep imports into pool/* → Phase 21/23/24 scope); (c) the `device.boot` queue NAME-only constant (Phase 23 owns consumer)."
    - "`server/__tests__/plugin-order.spec.ts` extended with 2-3 additive assertions proving: (a) pool-plugin's dependencies array is `['config', 'db', 'queue', 'event-bus']`; (b) pool-plugin registers AFTER event-bus + queue in plugin-order. Phase 17/18/19 assertions byte-identical — ADDITIVE ONLY per Phase 18/19 pattern. If the pre-existing fastify-zod-openapi substring-match bug prevents plugin-order.spec from passing at all, document in deferred-items + mark the additive assertion as pending."
    - "`.planning/STATE.md` updated: Phase 20 marked CLOSED in `stopped_at` / `last_activity` / `progress.completed_phases` (5 → 6) / `progress.completed_plans` (36 → 43) / Phase 20 added to Performance Metrics By Phase table. Phase 21 marked next."
    - "`.planning/ROADMAP.md` updated: Phase 20 marked `[x]` + `Complete 2026-04-21`; Phase 20 plan count updated to 7; Progress table row for Phase 20 updated with Plans Complete = 7/7 + Status = Complete + Completed date."
  artifacts:
    - path: ".planning/phases/20-pool-module-devices/deferred-items.md"
      provides: "Inherited exclusions + Phase 20 intentional deferrals catalog"
      contains: "Phase 21"
    - path: ".planning/STATE.md"
      provides: "Project state updated — Phase 20 CLOSED + Phase 21 next"
      contains: "Phase 20 CLOSED"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap updated — Phase 20 complete"
      contains: "Phase 20 Pool Module"
    - path: "server/__tests__/plugin-order.spec.ts"
      provides: "Additive pool-plugin dep-order assertion"
      contains: "pool-plugin"
  key_links:
    - from: ".planning/STATE.md progress"
      to: "phase 20 completion"
      via: "progress.completed_phases incremented from 5 to 6; completed_plans from 36 to 43 (+7 for Phase 20)"
      pattern: "completed_phases: 6"
    - from: "deferred-items.md"
      to: "inherited Phase 17/18/19 exclusions"
      via: "Lists fastify-zod-openapi v5 + pool-plugin substring bug as inherited"
      pattern: "fastify-zod-openapi"
    - from: "server/__tests__/plugin-order.spec.ts"
      to: "server/pool/plugin.ts dependencies array"
      via: "Additive assertion: pool-plugin deps include config, db, queue, event-bus"
      pattern: "dependencies.*config.*db.*queue.*event-bus"
---

<objective>
Close Phase 20. Run the full-suite sweep (npm test + lint + dep-check + typecheck), capture + gate Nyquist coverage, update STATE.md + ROADMAP.md, ship deferred-items.md catalog, and extend plugin-order.spec.ts with additive pool-plugin dep-order assertion. Mirror Phase 19 Plan 19-06's close-out (Nyquist portion inside Task 6.4) + Phase 18 Plan 18-04 close-out shape.

This plan assumes plans 20-00..20-05 all shipped green on their own runs. The phase-close responsibility is to verify the COMPOSITE tree (all 7 plans merged) passes end-to-end: test suite green minus the inherited Phase 17/18/19 exclusion set, lint clean, dep-check clean, typecheck stable, Nyquist delta within budget.

Pattern: mirrors Phase 19 Plan 19-06 phase-close structure. Key difference: Phase 19 did MODULE.md + barrel + renames + Nyquist all in one plan; Phase 20 already split MODULE.md + barrel + renames into Plan 20-05 — this plan focuses on the sweep + STATE/ROADMAP + deferred-items + plugin-order additive assertion.

Purpose: DEBT-03 (Nyquist validation runs in every v3.0 phase) + ROADMAP §Progress marks Phase 20 CLOSED. Without this plan, Phase 21 can still start, but the phase-completion accounting is stale and the composite-tree green status is unverified.

Output: deferred-items.md (~100 lines), plugin-order.spec additive block (~10 lines), STATE.md + ROADMAP.md patches, SUMMARY with runtime + coverage + exclusion metrics.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pool-module-devices/20-CONTEXT.md
@.planning/phases/20-pool-module-devices/20-RESEARCH.md
@.planning/phases/20-pool-module-devices/20-VALIDATION.md
@.planning/phases/20-pool-module-devices/20-00-PLAN.md
@.planning/phases/20-pool-module-devices/20-01-PLAN.md
@.planning/phases/20-pool-module-devices/20-02-PLAN.md
@.planning/phases/20-pool-module-devices/20-03-PLAN.md
@.planning/phases/20-pool-module-devices/20-04-PLAN.md
@.planning/phases/20-pool-module-devices/20-05-PLAN.md
@.planning/phases/19-reporting-migration-webhooks-dlq/19-06-PLAN.md
@.planning/phases/19-reporting-migration-webhooks-dlq/deferred-items.md
@.planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-04-PLAN.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/nyquist-baseline.json
@scripts/capture-nyquist.mjs
@scripts/check-nyquist.mjs
@server/__tests__/plugin-order.spec.ts
@server/pool/plugin.ts

<interfaces>
<!-- Scripts + baseline -->

Nyquist baseline: `.planning/nyquist-baseline.json` — `coverage.lines` = 48.29% (captured at Phase 15 Plan 15-09 commit 55ff8ac; MUST NEVER be overwritten).

Nyquist delta rule per DEBT-03: `npm run nyquist:check` fails if current `coverage.lines` is below baseline by more than 2pp.

Phase 19 close: 56.48% lines coverage (+8.19pp above baseline).

Phase 20 expected delta: +0 to +3pp (per RESEARCH §Nyquist + dep-cruiser — new events.ts + queue.ts + internal/module.ts + 3 new spec files add lines; process-tracker.ts loses 12 lines; net small positive).

Inherited exclusion set (per STATE.md + Phase 19-06 SUMMARY):
1. `server/api/__tests__/routes.test.ts` — fastify-zod-openapi v5 `required` emission bug
2. `server/api/__tests__/artifact-routes.test.ts` — same bug
3. `server/auth/__tests__/auth-plugin.test.ts` — same bug
4. `server/__tests__/plugin-order.spec.ts` — pool-plugin substring-match bug ("fastify-websocket" contains "websocket" at position 424 vs pool-plugin declared as substring elsewhere)

Phase 19 exclusion invocation (from 19-06-PLAN Task 6.4):
```bash
npx vitest run --exclude 'server/api/__tests__/routes.test.ts' --exclude 'server/api/__tests__/artifact-routes.test.ts' --exclude 'server/auth/__tests__/auth-plugin.test.ts' --exclude 'server/__tests__/plugin-order.spec.ts' --coverage --reporter=default
```

Current plugin-order.spec.ts assertions (Phase 17 + 18 + 19 additions):
- Phase 17: websocket declares pool; lifecycle/api declare correct deps
- Phase 18: lifecycle-plugin dependencies = ['config','db','queue','event-bus']
- Phase 19: reporting dependencies + queue→reporting + event-bus→reporting + db→reporting + reporting→job-plugin dep-order

Phase 20 additive assertion: pool-plugin dependencies = ['config','db','queue','event-bus']. Phase 20 extends from ['config'] (pre-20) to the full list (post-20-03).

STATE.md progress block (current — after Phase 19 close):
```yaml
progress:
  total_phases: 16
  completed_phases: 5
  total_plans: 36
  completed_plans: 36
  percent: 100
```

Phase 20 close target:
```yaml
progress:
  total_phases: 16
  completed_phases: 6
  total_plans: 43
  completed_plans: 43
  percent: 100   # plans-in-completed-phases over total-plans-in-completed-phases
```

ROADMAP.md Phase 20 entry (current):
```
- [ ] **Phase 20: Pool Module (Devices)** — device.* events, health/reaper ownership move, websocket dep declared
```
→ Change to `[x]` + completion date.

Progress table row (current):
```
| 20. Pool Module | v3.0 | 0/TBD | Not started | - |
```
→ Update to `7/7 | Complete | 2026-04-21`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Create deferred-items.md catalog (inherited exclusions + Phase 20 deferrals)</name>
  <files>.planning/phases/20-pool-module-devices/deferred-items.md</files>
  <read_first>
    - .planning/phases/19-reporting-migration-webhooks-dlq/deferred-items.md (Phase 19 canonical — inheritance + new exclusions template)
    - .planning/STATE.md (Accumulated Context: Decisions — verifies inherited Phase 17/18/19 exclusion items)
    - .planning/phases/20-pool-module-devices/20-CONTEXT.md §Deferred (explicit Phase 20 deferrals)
    - server/pool/MODULE.md §Non-Goals (authoritative list of Phase 20 deferrals — must match this file)
  </read_first>
  <action>
    **Create `.planning/phases/20-pool-module-devices/deferred-items.md`**. Mirror Phase 19 deferred-items.md structure: (1) inherited exclusions with citation; (2) Phase 20 intentional deferrals with phase-target annotation.

    ```markdown
    # Phase 20 — Deferred Items

    Catalog of items explicitly NOT delivered by Phase 20. Two categories:
    (A) Inherited exclusions from Phase 17/18/19 that continue to apply.
    (B) Phase 20 intentional deferrals (scope-boundary decisions).

    ---

    ## A) Inherited Exclusions

    These test files have pre-existing failures documented in Phase 18 Plan 18-04 SUMMARY.md + Phase 19 Plan 19-06 SUMMARY.md. Phase 20 inherits the same exclusion set — full-suite run uses `npx vitest run --exclude <file>` flags.

    ### A.1 — fastify-zod-openapi v5 `required` emission bug

    Files affected:
    - `server/api/__tests__/routes.test.ts`
    - `server/api/__tests__/artifact-routes.test.ts`
    - `server/auth/__tests__/auth-plugin.test.ts`

    Symptom: `FST_ERR_SCH_VALIDATION_BUILD` on Fastify boot when multiple Zod schemas register across plugins; fastify-zod-openapi v5 incorrectly emits `required: []` for optional-only schemas.

    Origin: Phase 17 Plan 17-01 upgrade to fastify-zod-openapi v5. Phase 17 Plan 17-01 left this pending.

    Phase 20 action: none. Test exclusion continues per Phase 19 pattern.

    Fix path: upstream library bug; hotfix options listed in Phase 19 deferred-items.md. Out of scope for Phase 20.

    ### A.2 — plugin-order.spec pool-plugin substring-match bug

    File affected: `server/__tests__/plugin-order.spec.ts` (single pre-existing failing it-block).

    Symptom: the spec iterates `app.printPlugins()` text output and uses `.indexOf('pool-plugin')` substring match. `'fastify-websocket'` appears at position 424 and contains the substring `'websocket'`; at the same time `pool-plugin` appears at position 1016. The substring compare `A.indexOf('pool-plugin') < B.indexOf('websocket-plugin')` flips because 'websocket' is discovered at the earlier position inside 'fastify-websocket', not inside 'websocket-plugin'.

    Origin: Phase 17 Plan 17-07 added websocket→pool dep declaration but the assertion helper uses substring matching instead of whole-token matching.

    Phase 20 action: the additive plugin-order.spec assertion (Task 6.3) uses NEW test setup (registers the Fastify app and reads the decorator's `__fastify.PluginMetadata?.dependencies` property instead of substring-matching the log output — proposed fix in Task 6.3). If the pre-existing failing it-block continues to fail under Phase 20, it remains in the exclusion set.

    Fix path: rewrite the existing substring-based it-block to use decorator-metadata inspection. Deferred; Phase 27+ cleanup.

    ---

    ## B) Phase 20 Intentional Deferrals

    These were actively considered during planning/research and consciously scoped OUT. Each cites the specific phase that OWNS the follow-up.

    ### B.1 — `device.booted` as a first-class bus event → Phase 24

    Pool emits `device.state.changed {from: 'booting', to: 'idle'}` but NOT a dedicated `device.booted` event. Consumers wanting boot semantics filter on `payload.from === 'booting' && payload.to === 'idle'`.

    Rationale: CONTEXT.md §Specifics + RESEARCH §Pitfall 4 both defer to Phase 24. If Phase 24 Maestro finds the filter pattern painful in hierarchy/device-info collection, it may add `device.booted` as a convenience alias — Phase 20 deliberately does not pre-empt.

    Owner phase: 24 (Maestro Module).

    ### B.2 — `pool.shutdown()` migration into `module.shutdown` → Phase 23

    Current `server/index.ts` graceful-shutdown flow calls `app.pool.shutdown()` imperatively AFTER `jobService.shutdown()` (waits for running jobs) but BEFORE `app.close()` (plugin onClose hooks). Moving `pool.shutdown()` into `module.shutdown()` would change the ordering — it would fire AFTER `jobService.shutdown` via reverse-plugin-registration order, which MAY be safe but is a timing-sensitive behaviour change.

    Phase 20 action: `server/pool/internal/module.ts` `shutdown()` stops healthChecker + offWorks reaper; pool.shutdown() STAYS imperative in server/index.ts with a `TODO(Phase 23 jobs keystone)` comment.

    Rationale: RESEARCH §Open Question 1 documents the timing analysis. Phase 23 Jobs Keystone rewrites the shutdown drain procedure + saga, making it the natural place to revisit.

    Owner phase: 23 (Jobs Module Keystone).

    ### B.3 — Hot-plug device detection → Future phase (not yet chartered)

    `PoolManager.detectPhysicalDevices()` is a one-shot scan called inside `initPool()`. Adding a device AFTER initPool (user plugs in during session) is NOT detected.

    Rationale: RESEARCH §Pitfall 7 notes this is OUT OF SCOPE for Phase 20 — existing behaviour preserved. Adding `device.detected` event + adb-watch loop could fit a future phase.

    Owner phase: unchartered.

    ### B.4 — `device.*` webhook fan-out → Future

    Reporting module (Phase 19) subscribes ONLY to `job.completed` and enqueues webhook delivery. `device.*` events (state.changed, allocated, released, health.failed) are not fanned out to webhooks.

    Rationale: scope — Phase 19 established the webhook pipeline for job events; device-event webhooks are a product-scope decision. Deliberately not included in v3.0 milestone.

    Owner phase: v4.0 or later.

    ### B.5 — `persistEnvelope` middleware consolidation → Phase 27+

    Phase 20 is the 4th sample point of the 10-line `persistEnvelope` duplicate (hooks + lifecycle + reporting + pool). Phase 15/19 RESEARCH Open Question #1 flagged this as a consolidation trigger AT 3+ samples. Phase 20 deliberately does NOT consolidate — keeping the duplicate lets the consolidation PR (which will refactor 4 module factories atomically) catch all 4 call sites together without diff entanglement from Phase 20's feature work.

    Owner phase: 27+ (API Aggregator & Events API) — bundled with the trace-tree endpoint that consumes persisted events.

    ### B.6 — Rewiring existing deep imports (`jobs`, `maestro`, `hooks` → `pool/*` internals) → Phase 21/23/24

    Phase 20 establishes the `server/pool/index.ts` barrel + `no-deep-imports-into-pool-internal` dep-cruiser rule. EXISTING consumers that deep-import (e.g. `server/jobs/plugin.ts:86`, `server/maestro/plugin.ts:22`, `server/hooks/plugin.ts:163`) continue to work — the dep-cruiser rule scopes to `internal/*` specifically, NOT all of `server/pool/*`. Converting those imports to barrel imports is scope-bounded to the phase that owns each consumer module.

    Rationale: CONTEXT.md §Deferred Ideas + RESEARCH §Summary both explicitly defer. Phase 20 would risk cascading regressions across 4 consumer modules.

    Owner phases: 21 (artifacts consumer rewiring), 23 (jobs keystone consumer rewiring), 24 (maestro consumer rewiring).

    ### B.7 — `device.boot` pg-boss queue registration → Phase 23

    `QUEUE_NAMES.DEVICE_BOOT = 'device.boot'` is exported by Phase 20 (Plan 20-00), but the queue is NOT registered via `createQueue` in Phase 20. Registering a queue without a consumer would trap `boss.send(DEVICE_BOOT, ...)` calls.

    Rationale: RESEARCH §Queue Semantics decision matrix — forward-compat name constant only, Phase 23 jobs keystone owns the on-demand-boot consumer.

    Owner phase: 23 (Jobs Module Keystone).

    ### B.8 — Android/iOS driver internal refactor

    Pool's android/ + ios/ subdirectories contain driver implementations with hand-written kebab-case file naming + minimal Zod coverage. Not converted to MOD-04 convention in Phase 20.

    Rationale: CONTEXT.md §Deferred Ideas — drivers stay as-is unless incidental to events wiring. Phase 20 deliberately keeps the driver internals untouched.

    Owner phase: unchartered (driver-layer cleanup could fit Phase 30 test migration or a dedicated driver-quality phase).

    ---

    *Deferred-items.md created 2026-04-21 at Phase 20 close. Inherits Phase 19 exclusion set verbatim; Phase 20 adds 8 intentional deferrals matching MODULE.md §Non-Goals.*
    ```

    **Cross-check:** each Phase 20 deferral below also appears in `server/pool/MODULE.md §Non-Goals` (Plan 20-05). If there's a mismatch, MODULE.md is authoritative — update the other to match.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "## A) Inherited Exclusions" .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "fastify-zod-openapi" .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "plugin-order.spec" .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "## B) Phase 20 Intentional Deferrals" .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "device.booted" .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "pool.shutdown()" .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "persistEnvelope" .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "Phase 23" .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "Phase 24" .planning/phases/20-pool-module-devices/deferred-items.md && grep -q "device.boot" .planning/phases/20-pool-module-devices/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/20-pool-module-devices/deferred-items.md` exists
    - Section A (Inherited) + Section B (Phase 20 Intentional) both present
    - Section A lists the 4 inherited failing test files (routes.test, artifact-routes.test, auth-plugin.test, plugin-order.spec)
    - Section B lists 8 intentional deferrals matching MODULE.md §Non-Goals (device.booted, pool.shutdown migration, hot-plug, device.* webhooks, persistEnvelope consolidation, deep-import rewiring, device.boot queue, android/ios driver refactor)
    - Each Section B item cites the owner phase (Phase 21/23/24/27+/unchartered)
    - Cross-check passes: MODULE.md §Non-Goals items all represented in Section B (no deferred mentioned in MODULE.md but missing here)
  </acceptance_criteria>
  <done>
    Deferred-items.md catalog ships. Section A inherits; Section B documents 8 Phase 20 intentional deferrals matching MODULE.md §Non-Goals. Phase 21 planner + reviewer have a single file to check for scope decisions.
  </done>
</task>

<task type="auto">
  <name>Task 6.2: Run full-suite sweep + Nyquist gate (reports captured in SUMMARY, no file changes here)</name>
  <files></files>
  <read_first>
    - .planning/phases/19-reporting-migration-webhooks-dlq/19-06-PLAN.md Task 6.4 (Nyquist exclusion invocation pattern)
    - .planning/phases/18-lifecycle-migration-node-cron-pg-boss/18-04-PLAN.md Task 4.4 (precedent — same exclusion set)
    - scripts/capture-nyquist.mjs (Phase 15 Plan 15-09 script)
    - scripts/check-nyquist.mjs (Phase 15 Plan 15-09 script)
    - .planning/nyquist-baseline.json (48.29% lines — NEVER overwrite)
  </read_first>
  <action>
    **This task runs shell commands; output captured in the plan SUMMARY (which the orchestrator writes per execution workflow). NO files edited by this task itself — the SUMMARY records the run data.**

    **Step 1 — Full test suite with inherited exclusions:**
    ```bash
    npx vitest run \
      --exclude 'server/api/__tests__/routes.test.ts' \
      --exclude 'server/api/__tests__/artifact-routes.test.ts' \
      --exclude 'server/auth/__tests__/auth-plugin.test.ts' \
      --exclude 'server/__tests__/plugin-order.spec.ts' \
      --reporter=default 2>&1 | tee /tmp/phase20-test-output.log
    ```
    Capture: pass/fail counts, runtime, per-module breakdown.

    **Step 2 — Lint + dep-check + typecheck sweep:**
    ```bash
    npm run lint 2>&1 | tee /tmp/phase20-lint.log
    npm run dep-check 2>&1 | tee /tmp/phase20-depcheck.log
    npx tsc --noEmit 2>&1 | tee /tmp/phase20-typecheck.log
    ```
    Record each exit code. Lint MUST be 0. dep-check MUST be 0. Typecheck delta vs HEAD~<plan-count> MUST be 0 (new errors attributable to Phase 20 files — existing Phase 15 Map-vs-RequestContext + artifacts/recording-service.ts errors stable).

    **Step 3 — Nyquist capture + check:**
    ```bash
    npm run nyquist:capture 2>&1 | tee /tmp/phase20-nyquist-capture.log
    npm run nyquist:check 2>&1 | tee /tmp/phase20-nyquist-check.log
    ```
    `nyquist:capture` writes `.planning/nyquist-current.json` (NOT overwrites `.planning/nyquist-baseline.json`). `nyquist:check` compares and exits 0 if `coverage.lines` delta ≥ -2pp.

    Capture: baseline value (48.29%), current value, delta in pp, pass/fail.

    **Step 4 — Verify baseline NOT overwritten:**
    ```bash
    git diff .planning/nyquist-baseline.json 2>&1
    ```
    MUST return empty diff.

    **If any of these fail:**
    - Test failures in Phase 20 files (not in inherited-exclusion list) → document in SUMMARY + investigate ROOT CAUSE before marking phase CLOSED. Likely a bug in plans 20-01/02/03/04/05 tasks that escaped the per-task verify. Auto-fix scope-in per Rule 3 if fix is ≤15 lines total.
    - Lint failure → fix the lint issue (scope-in per Rule 3 — lint is gating).
    - dep-check failure → likely a legitimate deep-import violation in new pool code. Fix the import (use barrel).
    - Typecheck failure attributable to Phase 20 file → fix; pre-existing errors (documented in STATE.md deferred-items) acceptable.
    - Nyquist delta worse than -2pp → investigate; may indicate process-tracker.ts trimming removed more tested lines than expected. Add tests scope-in per Rule 3 until delta recovers.

    **Expected outcome:**
    - Test suite: ≥1000 tests pass; ~4 skipped from exclusion set; 0 new failures.
    - Lint: exit 0.
    - dep-check: exit 0 (5 forbidden rules + 1 new pool rule all hold).
    - Typecheck: 8 pre-existing errors (stable per STATE.md), 0 new.
    - Nyquist: +0 to +3pp delta (net positive due to new spec files + events/queue/module files).
  </action>
  <verify>
    <automated>set -e; npx vitest run --exclude 'server/api/__tests__/routes.test.ts' --exclude 'server/api/__tests__/artifact-routes.test.ts' --exclude 'server/auth/__tests__/auth-plugin.test.ts' --exclude 'server/__tests__/plugin-order.spec.ts' --reporter=default > /tmp/phase20-t.log 2>&1; TEST_EXIT=$?; echo "test exit: $TEST_EXIT"; npm run lint > /tmp/phase20-l.log 2>&1; LINT_EXIT=$?; echo "lint exit: $LINT_EXIT"; npm run dep-check > /tmp/phase20-d.log 2>&1; DEP_EXIT=$?; echo "dep-check exit: $DEP_EXIT"; npx tsc --noEmit > /tmp/phase20-tc.log 2>&1; TC_EXIT=$?; echo "typecheck exit: $TC_EXIT"; npm run nyquist:capture > /tmp/phase20-nc.log 2>&1; NC_EXIT=$?; echo "nyquist:capture exit: $NC_EXIT"; npm run nyquist:check > /tmp/phase20-nk.log 2>&1; NK_EXIT=$?; echo "nyquist:check exit: $NK_EXIT"; git diff --exit-code .planning/nyquist-baseline.json && echo "baseline unchanged OK"; [ "$TEST_EXIT" -eq 0 ] && [ "$LINT_EXIT" -eq 0 ] && [ "$DEP_EXIT" -eq 0 ] && [ "$NK_EXIT" -eq 0 ] && echo "ALL GATES PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --exclude <4 inherited files>` exits 0 (all non-excluded tests pass)
    - `npm run lint` exits 0
    - `npm run dep-check` exits 0 — both the 5 forbidden rules AND the existing codebase pass
    - `npx tsc --noEmit` exit code stable vs HEAD~6 (no NEW errors attributable to Phase 20 files; pre-existing 8 errors per STATE.md still present, that's fine)
    - `npm run nyquist:check` exits 0 (delta ≥ −2pp vs baseline 48.29%)
    - `git diff .planning/nyquist-baseline.json` returns empty (baseline NOT overwritten)
    - SUMMARY of this plan records: test pass/fail counts, lint/dep-check/tc exit codes, nyquist current + delta values
  </acceptance_criteria>
  <done>
    Full-suite composite-tree sweep green. Nyquist gate held. Baseline untouched. Ready to update STATE + ROADMAP.
  </done>
</task>

<task type="auto">
  <name>Task 6.3: Extend plugin-order.spec.ts with additive pool-plugin dep-order assertion</name>
  <files>server/__tests__/plugin-order.spec.ts</files>
  <read_first>
    - server/__tests__/plugin-order.spec.ts (current — Phase 17/18/19 assertions intact; has 1 pre-existing failing it-block from the substring-match bug)
    - server/pool/plugin.ts (Plan 20-03 — dependencies array: ['config','db','queue','event-bus'])
    - .planning/phases/19-reporting-migration-webhooks-dlq/19-04-PLAN.md (Phase 19 additive-pattern precedent — assertions added INSIDE existing it-block without duplicating app boot)
  </read_first>
  <action>
    **Extend `server/__tests__/plugin-order.spec.ts`** with additive pool-plugin dep assertions. Mirror Phase 18/19 pattern — add assertions INSIDE the existing plugin-order it-block (reuse the app-boot setup); do NOT add a new it-block if the existing one already boots the full app.

    The exact shape depends on the current plugin-order.spec structure. Read first, then choose the minimal additive change:

    **Option A (preferred if existing it-block boots full app + iterates plugins):**
    Add 2-3 `expect` statements inside the existing it-block that read pool-plugin's metadata:

    ```typescript
    // [Phase 20-06] Additive: pool-plugin dependencies extended from ['config']
    // → ['config', 'db', 'queue', 'event-bus'] per Plan 20-03.
    const poolPluginEntry = plugins.find(p => p.name === 'pool-plugin');
    expect(poolPluginEntry).toBeDefined();
    expect(poolPluginEntry!.dependencies).toEqual(expect.arrayContaining(['config', 'db', 'queue', 'event-bus']));
    expect(poolPluginEntry!.dependencies).toHaveLength(4);
    ```

    Replace `plugins` with whatever the existing spec already iterates (printPlugins output parsed, or `app[Symbol.for('fastify.plugin-meta')]`, depending on inspection approach).

    **Option B (if the existing it-block is the one with the substring-match bug — i.e. inherited-exclusion file):**
    The file remains in the Task 6.2 exclusion set; adding an assertion is a no-op for CI (file is excluded from vitest run). Still add the assertion — marks structural intent + becomes valid when Phase 27+ cleans up the substring-match bug.

    Decision: **ship the additive assertion regardless**. The file is in the inherited exclusion set; CI skips it. The assertion is structural intent for the future fix. Document in SUMMARY: "additive assertion committed; reads green once Phase 27+ substring-match fix lands."

    **Do NOT** modify any Phase 17/18/19 assertion — byte-identical preservation. ADDITIVE ONLY.
  </action>
  <verify>
    <automated>grep -q "pool-plugin" server/__tests__/plugin-order.spec.ts && grep -qE "expect.*'config'.*'db'.*'queue'.*'event-bus'" server/__tests__/plugin-order.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/__tests__/plugin-order.spec.ts` contains an assertion referencing `'pool-plugin'`
    - Assertion validates the dependencies array contains all 4 strings: `'config'`, `'db'`, `'queue'`, `'event-bus'`
    - Phase 17/18/19 assertions byte-identical (no diff outside the additive block)
    - The file IS in the inherited-exclusion list per Task 6.1 deferred-items.md — CI skips it via the vitest --exclude flag
  </acceptance_criteria>
  <done>
    Additive plugin-order assertion committed. Will read green once the substring-match bug is fixed in Phase 27+. Phase 17/18/19 assertions preserved verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 6.4: Update STATE.md + ROADMAP.md — Phase 20 CLOSED</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (current — after Phase 19 close: completed_phases=5, completed_plans=36)
    - .planning/ROADMAP.md (current — Phase 20 line 60 unchecked, Phase 20 Progress row "0/TBD | Not started")
    - .planning/phases/19-reporting-migration-webhooks-dlq/19-06-SUMMARY.md (for the STATE.md close-pattern template)
  </read_first>
  <action>
    **Step 1 — Update `.planning/STATE.md`.**

    Frontmatter:
    ```yaml
    stopped_at: Completed 20-06-PLAN.md — Phase 20 CLOSED
    last_updated: "2026-04-21T<timestamp>Z"
    last_activity: 2026-04-21 — Plan 20-06 complete — Phase 20 CLOSED. Pool module migrated to MOD-01..09 conventions (MODULE.md + barrel + events.ts + queue.ts + factory + tests-as-spec); device.* events fire from state machine; healthChecker + reaper ownership moved to pool plugin. Nyquist delta <N>pp (Phase 19 close was +8.19pp).
    progress:
      total_phases: 16
      completed_phases: 6
      total_plans: 43
      completed_plans: 43
      percent: 100
    ```

    Body "Current Position" section:
    ```
    Phase: 20 of 30 CLOSED (Pool Module — device.* events + healthChecker/reaper ownership) — next: Phase 21 (Artifacts Module)
    Plan: 7 of 7 in phase directory complete (20-00..20-06)
    Status: Plan 20-06 complete — Phase 20 CLOSED. [Brief detail: MODULE.md line count, index.ts export count, test count, Nyquist delta.]
    ```

    Body "Performance Metrics" By Phase table — ADD row:
    ```
    | Phase 20 | 7 | <N>min | <N/7>min |
    ```

    Body "Recent Trend" block — prepend:
    ```
    - Phase 20: 20-00 (<N>min) Wave 0 substrate → 20-01 (<N>min) events body → 20-02 (<N>min) emission sites wiring → 20-03 (<N>min) factory + plugin rewire + healthChecker/reaper migration → 20-04 (<N>min) DB-gated subscriber + correlation → 20-05 (<N>min) MODULE.md + barrel + renames → 20-06 (<N>min) phase close
    ```

    Body per-plan table — append 7 rows:
    ```
    | Phase 20 P00 | Xmin | 4 tasks | 9 files |
    | Phase 20 P01 | Xmin | 2 tasks | 2 files |
    | Phase 20 P02 | Xmin | 3 tasks | 6 files |
    | Phase 20 P03 | Xmin | 4 tasks | 7 files |
    | Phase 20 P04 | Xmin | 2 tasks | 2 files |
    | Phase 20 P05 | Xmin | 3 tasks | 5 files |
    | Phase 20 P06 | Xmin | 4 tasks | 4 files |
    ```

    (Exact runtime + file counts come from each plan's SUMMARY — orchestrator fills in during execution.)

    **Step 2 — Update `.planning/ROADMAP.md`.**

    Line 60 — change checkbox + annotate:
    ```
    - [x] **Phase 20: Pool Module (Devices)** — device.* events, health/reaper ownership move, websocket dep declared (completed 2026-04-21)
    ```

    Under "### Phase 20: Pool Module (Devices)" heading (line ~168), update Plans line from `**Plans**: TBD` to:
    ```
    **Plans** (7 plans across 7 waves):
    - [x] 20-00-PLAN.md — Wave 0 substrate: QUEUE_NAMES + events.ts/queue.ts/internal-module.ts/MODULE.md/index.ts/events.spec stubs + dep-cruiser rule + fixture + spec extension ✅ 2026-04-21
    - [x] 20-01-PLAN.md — Wave 1 events: poolRegistry + 4 payload schemas + makePoolEmitters + POOL_AGGREGATE_ID v5 + events.spec extension (MOD-03) ✅ 2026-04-21
    - [x] 20-02-PLAN.md — Wave 2 emission wiring: Device.transition return {from,to} + PoolManager/HealthChecker 4th-param emit + 3 spec renames + emit-envelope assertions (SC1) ✅ 2026-04-21
    - [x] 20-03-PLAN.md — Wave 3 factory + plugin rewire + healthChecker/reaper migration: queue.ts + createPoolModule + plugin.ts thin wirer + server/index.ts deletions + process-tracker.startReaper removal (SC2 + SC3 + MOD-06 + QUEUE-06) ✅ 2026-04-21
    - [x] 20-04-PLAN.md — Wave 4 DB-gated proofs: subscriber.spec + correlation.spec (SC1 + SC4) ✅ 2026-04-21
    - [x] 20-05-PLAN.md — Wave 5 close-out artifacts: MODULE.md + index.ts barrel + 3 more .test→.spec renames (MOD-01/02/04) ✅ 2026-04-21
    - [x] 20-06-PLAN.md — Wave 6 phase close: deferred-items + plugin-order additive + STATE/ROADMAP + Nyquist gate + full-suite sweep ✅ 2026-04-21
    ```

    "## Progress" table row 20 — update:
    ```
    | 20. Pool Module | v3.0 | 7/7            | Complete    | 2026-04-21 |
    ```

    **Do NOT** touch any other phase's checkbox or metrics. Additive ONLY.
  </action>
  <verify>
    <automated>grep -q "completed_phases: 6" .planning/STATE.md && grep -q "completed_plans: 43" .planning/STATE.md && grep -q "Phase 20 CLOSED" .planning/STATE.md && grep -q "Phase 20 of 30 CLOSED" .planning/STATE.md && grep -qE "^\| Phase 20 \| 7 \|" .planning/STATE.md && grep -qE "^- \[x\] \*\*Phase 20:" .planning/ROADMAP.md && grep -qE "20-00-PLAN\.md.*✅" .planning/ROADMAP.md && grep -qE "20-06-PLAN\.md.*✅" .planning/ROADMAP.md && grep -qE "^\| 20\. Pool Module \| v3\.0 \| 7/7" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` progress.completed_phases = 6 (was 5)
    - `.planning/STATE.md` progress.completed_plans = 43 (was 36; +7 for Phase 20)
    - `.planning/STATE.md` stopped_at + last_activity both reference "Phase 20 CLOSED"
    - `.planning/STATE.md` Performance Metrics has a "Phase 20" row in the By Phase table
    - `.planning/STATE.md` per-plan table has 7 new rows (P00 through P06)
    - `.planning/ROADMAP.md` line 60 (Phase 20 entry) changes from `[ ]` to `[x]`
    - `.planning/ROADMAP.md` "### Phase 20" section lists all 7 plans with `[x]` + completion date + brief objective
    - `.planning/ROADMAP.md` Progress table row 20 updated: `7/7 | Complete | 2026-04-21`
    - No other phases or progress entries modified (Phase 17/18/19 rows byte-identical)
  </acceptance_criteria>
  <done>
    STATE.md + ROADMAP.md reflect Phase 20 CLOSED. Phase 21 is the next queued phase. Progress: 43/43 plans complete across Phases 15-20.
  </done>
</task>

</tasks>

<verification>
- Full suite: npm test (with inherited exclusions) exit 0
- Lint + dep-check + typecheck: all stable; no new Phase 20 errors
- Nyquist: delta ≥ -2pp (expect +0 to +3pp net positive); baseline untouched
- deferred-items.md catalog: 2 sections (inherited + Phase 20) with 8 Phase 20 intentional deferrals
- plugin-order.spec.ts: additive pool-plugin dep assertion (inherited-exclusion file; will read green once substring bug fixed)
- STATE.md: completed_phases 5→6, completed_plans 36→43, Phase 20 CLOSED references + metrics rows
- ROADMAP.md: Phase 20 [x] + 7 plans listed + progress table row updated

Final state: Phase 20 CLOSED. Phase 21 (Artifacts Module) is next.
</verification>

<success_criteria>
- ROADMAP §Phase 20 SC1 (device.* events fire on state machine): PROVEN end-to-end (Plans 20-01/02 code + Plans 20-03/04 runtime proofs)
- ROADMAP §Phase 20 SC2 (server/index.ts no longer starts health checker or reaper): PROVEN via grep-guard in lifecycle-ownership.spec (Plan 20-03) + confirmed by full-suite sweep
- ROADMAP §Phase 20 SC3 (pool fully migrated — MODULE.md/barrel/events.ts/queue.ts/tests-as-spec): COMPLETE (Plans 20-01/02/03/05)
- ROADMAP §Phase 20 SC4 (Nyquist passes + downstream via bus): PROVEN (Plan 20-04 subscriber.spec + correlation.spec + this plan's Nyquist gate)
- MOD-01..06 + MOD-08 satisfied for pool module
- QUEUE-06 satisfied (server/pool/queue.ts colocated with factory body)
- DEBT-03 satisfied (Nyquist gate held in every v3.0 phase close)
- Phase 21 Artifacts Module unblocked — has device.* events to subscribe to via bus
</success_criteria>

<output>
After completion, create `.planning/phases/20-pool-module-devices/20-06-SUMMARY.md` capturing: full-suite pass/fail counts, lint/dep-check/typecheck exit codes, Nyquist current + delta values, baseline-diff-empty confirmation, files modified count + total Phase 20 runtime + plan-by-plan timings.
</output>
