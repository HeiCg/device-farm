---
phase: 34
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - server/sessions/events.ts
  - server/sessions/internal/module.ts
  - server/sessions/internal/protocol.ts
  - server/sessions/MODULE.md
  - server/sessions/index.ts
  - server/sessions/schemas.ts
  - server/sessions/__tests__/events.spec.ts
  - server/sessions/__tests__/routes.spec.ts
  - server/sessions/__tests__/protocol.spec.ts
  - server/sessions/__tests__/ws.spec.ts
  - server/sessions/__tests__/dispatch.spec.ts
  - server/sessions/__tests__/resolver-maestro.spec.ts
  - server/sessions/__tests__/resolver-claude.spec.ts
  - server/sessions/__tests__/auth-rate-sweeper.spec.ts
  - server/sessions/__tests__/__fixtures__/android-hierarchy.xml
  - server/sessions/__tests__/__fixtures__/screenshot.png
  - server/db/schema.ts
  - server/db/migrations/0009_sessions.sql
  - server/queue/names.ts
  - .dependency-cruiser.cjs
  - __fixtures__/dep-cruiser/bad-sessions-deep-import.ts
  - server/hooks/__tests__/dep-cruiser.spec.ts
  - mcp/package.json
  - mcp/tsconfig.json
  - mcp/vitest.config.ts
  - mcp/src/index.ts
  - mcp/__tests__/index.spec.ts
  - mcp/__tests__/tools.spec.ts
  - mcp/README.md
  - package.json
  - cli/cmd/session.go
  - cli/cmd/session_test.go
  - web/src/routes/sessions/+page.svelte
  - web/src/routes/sessions/[id]/+page.svelte
  - web/src/lib/sessions/__tests__/sessions-list.spec.ts
autonomous: true
requirements: [SESS-LEASE, SESS-WS, SESS-DISPATCH, SESS-NL-MAESTRO, SESS-NL-CLAUDE, SESS-AUTH, SESS-MCP, SESS-CLI, SESS-WEB, MOD-01, MOD-02, MOD-03, MOD-05]
estimated_minutes: 50
gap_closure: false
must_haves:
  truths:
    - "`server/sessions/events.ts` stub exists exporting `SESSION_EVENT_NAMES` constant with 4 keys (LEASED='session.leased', RELEASED='session.released', EXPIRED='session.expired', DEVICE_LOST='session.device.lost'), `SESSIONS_AGGREGATE_TYPE='session'` const, `SessionEventName` type, and stub `sessionsRegistry = {} as const satisfies EventRegistry` (full body lands in 34-01). NO emit helpers yet."
    - "`server/sessions/internal/module.ts` is an 8-line throw-stub exporting `createSessionsModule` with body `throw new Error('Plan 34-01 not yet executed');` — repeat of empirical Phase 18-26 pattern (depcruise silently drops unresolvable imports so the throw-stub satisfies the rule fixture without needing real implementation)."
    - "`server/sessions/internal/protocol.ts` placeholder ships baseEnvelope (`z.object({id: z.string().uuid()})`) plus stub `clientEnvelope = z.discriminatedUnion('type', [baseEnvelope.extend({type: z.literal('ping')})])` and stub `serverEnvelope = z.discriminatedUnion('type', [z.object({type: z.literal('pong'), forMsgId: z.string().uuid()})])` — full envelope union body lands in 34-02. This file lives at substrate because protocol.spec.ts stub imports it."
    - "`server/sessions/MODULE.md` placeholder with `## Purpose` section only (MOD-01 file-existence passes; full 9-section body lands in 34-07)."
    - "`server/sessions/index.ts` MOD-02 strict 1-line internal/ re-export barrel: `export { createSessionsModule, type SessionsModule } from './internal/module.js';` (full surface in 34-07)."
    - "`server/sessions/schemas.ts` ships REST Zod schemas at substrate: `leaseRequestSchema`, `leaseResponseSchema`, `releaseParamsSchema`, `releaseResponseSchema`, `listResponseSchema` each with `.meta({id: 'Session...'})` — full bodies match RESEARCH §REST Surface. Routes consumer in 34-01 imports these unchanged."
    - "All 8 `__tests__/*.spec.ts` files exist with skip-stubs (`it.skip(...)`) plus 1 always-passing shape assertion each (file existence pass for Validation §Wave 0 map). No DB writes, no live HTTP. Files: events.spec.ts, routes.spec.ts, protocol.spec.ts, ws.spec.ts, dispatch.spec.ts, resolver-maestro.spec.ts, resolver-claude.spec.ts, auth-rate-sweeper.spec.ts. `protocol.spec.ts` ships two `it.todo` markers: `it.todo('WS envelope discriminated union')` and `it.todo('ack id echo')` — full bodies land in Plan 34-02."
    - "`server/sessions/__tests__/__fixtures__/android-hierarchy.xml` ships a real Android `uiautomator dump`-shaped XML with `<node text='Sign In' clickable='true' bounds='[120,800][600,920]' ...>` AND a `<node content-desc='Settings' clickable='true' ...>` element so 34-03 resolver heuristic spec has determinist input."
    - "`server/sessions/__tests__/__fixtures__/screenshot.png` is a ~50KB 1080x1920 PNG (solid color is fine) used by resolver-claude.spec.ts cache test (Wave-3 plan)."
    - "`server/db/schema.ts` extended with: (a) `sessionStatusEnum = pgEnum('session_status', ['active','released','expired'])`, (b) `sessions` pgTable matching RESEARCH §Schema verbatim including `uniqueIndex('sessions_device_active_idx').on(deviceId).where(sql\\`status = 'active'\\`)` partial unique, (c) three secondary indexes (`leaseUntil`, `status`, `ownerApiKeyId`)."
    - "`server/db/migrations/0009_sessions.sql` exists with: (1) `CREATE TYPE session_status AS ENUM (...)`; (2) `CREATE TABLE sessions (...)`; (3) `CREATE UNIQUE INDEX sessions_device_active_idx ON sessions(device_id) WHERE status = 'active'`; (4) the three secondary indexes. Generated via `npx drizzle-kit generate` (rename file to `0009_sessions.sql` if drizzle-kit emits a different slug)."
    - "`server/queue/names.ts` `QUEUE_NAMES` extended with `SESSION_SWEEP: 'session.sweep'` literal — Plan 34-04 sweeper consumes this."
    - "`.dependency-cruiser.cjs` gains an ELEVENTH forbidden rule `no-deep-imports-into-sessions-internal` mirroring rules 5-10 verbatim. Header comment-block extended with rule-11 paragraph following the pattern of rules 5-10."
    - "`__fixtures__/dep-cruiser/bad-sessions-deep-import.ts` fixture imports from `server/sessions/internal/module.js` with `@ts-expect-error` — fires the new rule when invoked from spec."
    - "`server/hooks/__tests__/dep-cruiser.spec.ts` extended with `[MOD-02 sessions extension]` it-block (two-pass err+json pattern matching Phase 20-26)."
    - "`mcp/` workspace skeleton exists: `package.json` with `name: '@device-stream/mcp'`, `bin: { 'device-stream-mcp': './dist/index.js' }`, deps `@modelcontextprotocol/sdk` + `zod` + `ws`, devDeps `vitest` + `typescript`; `tsconfig.json` extends root config; `vitest.config.ts` points at `__tests__/`; `src/index.ts` is a 5-line shebang+console.log placeholder; `__tests__/index.spec.ts` and `__tests__/tools.spec.ts` are skip-stubs; `README.md` documents one-line install (full body in 34-05). Root `package.json` `workspaces` array gains `\"mcp\"` entry."
    - "`cli/cmd/session.go` ships a Cobra command skeleton with `Use: 'session'`, `Short: 'Interactive device sessions'`, RunE that prints `Phase 34-06 not yet executed`, and registers under `rootCmd` via `init()`. `cli/cmd/session_test.go` is a `TestSessionCommandExists` table-stub that compiles + passes."
    - "`web/src/routes/sessions/+page.svelte` is a 10-line stub with `<h1>Sessions</h1><p>Phase 34-06 placeholder</p>` and `web/src/routes/sessions/[id]/+page.svelte` is the same shape. `web/src/lib/sessions/__tests__/sessions-list.spec.ts` is a vitest skip-stub asserting the route file exists via `existsSync`."
    - "Wave 0 INTENTIONAL non-actions: NO `npm install` for mcp deps (left for 34-05 when MCP body lands); NO migration RUN against dev DB (drizzle-kit generate only emits the file); NO POST/DELETE route registration (lands in 34-01); NO WS handler (lands in 34-02); NO resolver implementation (lands in 34-03); NO sweeper schedule (lands in 34-04); NO `tap_by_description` end-to-end wiring."
  artifacts:
    - path: "server/sessions/events.ts"
      provides: "Events stub — 4 placeholder names + empty registry; full body in 34-01"
      contains: "SESSION_EVENT_NAMES"
    - path: "server/sessions/internal/module.ts"
      provides: "Throw-stub for dep-cruiser resolvable target"
      contains: "createSessionsModule"
    - path: "server/sessions/internal/protocol.ts"
      provides: "Protocol stub (ping/pong only); full discriminatedUnion in 34-02"
      contains: "clientEnvelope"
    - path: "server/sessions/MODULE.md"
      provides: "Placeholder Purpose-only section; full body in 34-07"
      contains: "## Purpose"
    - path: "server/sessions/index.ts"
      provides: "MOD-02 1-line strict internal re-export"
      contains: "from './internal/module"
    - path: "server/sessions/schemas.ts"
      provides: "5 REST Zod schemas with .meta() OpenAPI ids; consumed verbatim by 34-01"
      contains: "leaseRequestSchema"
    - path: "server/sessions/__tests__/events.spec.ts"
      provides: "Skip-stub + 1-test shape assertion; extends in 34-01"
      contains: "SESSION_EVENT_NAMES"
    - path: "server/sessions/__tests__/routes.spec.ts"
      provides: "Skip-stub; full body in 34-01"
      contains: "lease"
    - path: "server/sessions/__tests__/protocol.spec.ts"
      provides: "Skip-stub with two it.todo markers (WS envelope discriminated union; ack id echo); full body in 34-02"
      contains: "it.todo"
    - path: "server/sessions/__tests__/ws.spec.ts"
      provides: "Skip-stub; full body in 34-02"
      contains: "ws"
    - path: "server/sessions/__tests__/dispatch.spec.ts"
      provides: "Skip-stub; full body in 34-02"
      contains: "dispatch"
    - path: "server/sessions/__tests__/resolver-maestro.spec.ts"
      provides: "Skip-stub; full body in 34-03"
      contains: "MaestroAiResolver"
    - path: "server/sessions/__tests__/resolver-claude.spec.ts"
      provides: "Skip-stub; full body in 34-03"
      contains: "ClaudeVisionResolver"
    - path: "server/sessions/__tests__/auth-rate-sweeper.spec.ts"
      provides: "Skip-stub; full body in 34-04"
      contains: "rate"
    - path: "server/sessions/__tests__/__fixtures__/android-hierarchy.xml"
      provides: "Real-shaped uiautomator XML for 34-03 heuristic resolver spec"
      contains: "Sign In"
    - path: "server/sessions/__tests__/__fixtures__/screenshot.png"
      provides: "Fixture PNG for 34-03 claude-vision cache test"
    - path: "server/db/schema.ts"
      provides: "Adds sessionStatusEnum + sessions pgTable + partial unique index"
      contains: "sessions = pgTable"
    - path: "server/db/migrations/0009_sessions.sql"
      provides: "Drizzle migration: enum + table + partial unique + 3 indexes"
      contains: "CREATE TABLE sessions"
    - path: "server/queue/names.ts"
      provides: "Adds SESSION_SWEEP queue name literal"
      contains: "SESSION_SWEEP"
    - path: ".dependency-cruiser.cjs"
      provides: "11th forbidden rule (no-deep-imports-into-sessions-internal)"
      contains: "no-deep-imports-into-sessions-internal"
    - path: "__fixtures__/dep-cruiser/bad-sessions-deep-import.ts"
      provides: "Fixture firing rule 11"
      contains: "server/sessions/internal"
    - path: "server/hooks/__tests__/dep-cruiser.spec.ts"
      provides: "11th it-block proves sessions rule fires structurally"
      contains: "[MOD-02 sessions extension]"
    - path: "mcp/package.json"
      provides: "New workspace package skeleton with @modelcontextprotocol/sdk dep"
      contains: "@device-stream/mcp"
    - path: "mcp/src/index.ts"
      provides: "Shebang placeholder; full server in 34-05"
      contains: "device-stream-mcp"
    - path: "mcp/__tests__/index.spec.ts"
      provides: "Skip-stub; full smoke in 34-05"
      contains: "initialize"
    - path: "mcp/__tests__/tools.spec.ts"
      provides: "Skip-stub; full tool-routing in 34-05"
      contains: "tools"
    - path: "package.json"
      provides: "Root workspaces array gains 'mcp' entry"
      contains: "mcp"
    - path: "cli/cmd/session.go"
      provides: "Cobra session command skeleton (no impl)"
      contains: "session"
    - path: "cli/cmd/session_test.go"
      provides: "Compile-only test ensuring command registered"
      contains: "TestSessionCommandExists"
    - path: "web/src/routes/sessions/+page.svelte"
      provides: "Route stub; full list UI in 34-06"
      contains: "Sessions"
    - path: "web/src/routes/sessions/[id]/+page.svelte"
      provides: "Detail route stub; full live preview in 34-06"
      contains: "Sessions"
    - path: "web/src/lib/sessions/__tests__/sessions-list.spec.ts"
      provides: "Skip-stub asserting route files exist"
      contains: "existsSync"
  key_links:
    - from: "server/sessions/internal/** (future plans 34-01..04)"
      to: ".dependency-cruiser.cjs rule 11"
      via: "Rule denies import into sessions/internal/ from outside server/sessions/"
      pattern: "no-deep-imports-into-sessions-internal"
    - from: "server/db/schema.ts sessions partial unique index"
      to: "POST /api/sessions race protection (34-01)"
      via: "INSERT throws 23505 on duplicate active session per device — surfaced as 409"
      pattern: "sessions_device_active_idx"
    - from: "server/sessions/schemas.ts leaseRequestSchema"
      to: "future Plan 34-01 routes.ts POST handler"
      via: "Schema consumed via fastify-zod-openapi withTypeProvider for parse + OpenAPI emit"
      pattern: "leaseRequestSchema"
    - from: "mcp/ workspace"
      to: "root package.json workspaces array"
      via: "npm workspaces resolution"
      pattern: "\"mcp\""
    - from: "server/queue/names.ts SESSION_SWEEP"
      to: "future Plan 34-04 sweeper boss.schedule + boss.work"
      via: "Single source of truth for queue name literal"
      pattern: "session.sweep"
---

<objective>
Wave 0 substrate for Phase 34 Session API + MCP Server. Ships scaffolding that plans 34-01..34-07 depend on WITHOUT touching runtime auth/jobs/pool/streaming code.

Steps:
1. Creates `server/sessions/events.ts` stub (4 placeholder event names + empty registry; full body in 34-01).
2. Creates `server/sessions/internal/module.ts` throw-stub for dep-cruiser resolvable target (repeat of Phase 18-26 pattern).
3. Creates `server/sessions/internal/protocol.ts` ping/pong-only stub (full discriminatedUnion body lands in 34-02).
4. Creates `server/sessions/schemas.ts` with FULL REST Zod bodies (leaseRequestSchema, leaseResponseSchema, releaseParamsSchema, releaseResponseSchema, listResponseSchema) so 34-01 imports unchanged.
5. Creates `server/sessions/MODULE.md` Purpose-only placeholder + `server/sessions/index.ts` 1-line strict barrel.
6. Creates 7 `__tests__/*.spec.ts` skip-stubs + 2 fixture files (XML hierarchy + PNG screenshot).
7. EXTENDS `server/db/schema.ts` with sessionStatusEnum + sessions pgTable + partial unique index + secondary indexes.
8. Generates `server/db/migrations/0009_sessions.sql` via drizzle-kit (rename if slug differs).
9. Extends `server/queue/names.ts` QUEUE_NAMES with `SESSION_SWEEP` literal.
10. Adds ELEVENTH forbidden rule `no-deep-imports-into-sessions-internal` to `.dependency-cruiser.cjs`; ships fixture + 11th it-block in dep-cruiser.spec.
11. Scaffolds `mcp/` workspace (package.json + tsconfig.json + vitest.config.ts + src/index.ts placeholder + 2 skip-stubs + README); adds 'mcp' to root package.json workspaces array. NO `npm install` runs.
12. Creates `cli/cmd/session.go` Cobra skeleton + `cli/cmd/session_test.go` compile-only test.
13. Creates `web/src/routes/sessions/+page.svelte` + `web/src/routes/sessions/[id]/+page.svelte` placeholder stubs + `web/src/lib/sessions/__tests__/sessions-list.spec.ts` skip-stub.

**What Wave 0 INTENTIONALLY does NOT do:**
- NO REST route registration (POST/DELETE/GET /api/sessions lands in 34-01).
- NO WS handler (lands in 34-02).
- NO action dispatch / resolver implementation (lands in 34-03).
- NO auth chain / rate-limit / sweeper (lands in 34-04).
- NO MCP server body / `@modelcontextprotocol/sdk` install (lands in 34-05; only package.json declaration).
- NO CLI subcommand impl (lands in 34-06).
- NO web live preview / overlay (lands in 34-06).
- NO docs (lands in 34-07).
- NO server/index.ts plugin registration (lands in 34-01 alongside routes).

Mirrors Phase 26 Plan 26-00 sequencing with `auth → sessions` substitution + MCP workspace scaffold + CLI/web stub addition.

Output: `server/sessions/{events.ts, internal/module.ts, internal/protocol.ts, schemas.ts, MODULE.md, index.ts, __tests__/}` stubs exist; `server/db/schema.ts` sessions pgTable; migration `0009_sessions.sql`; QUEUE_NAMES.SESSION_SWEEP; `.dependency-cruiser.cjs` has 11 forbidden module rules; `mcp/` workspace skeleton; CLI + web route stubs.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/34-session-api-mcp/34-CONTEXT.md
@.planning/phases/34-session-api-mcp/34-BRIEF.md
@.planning/phases/34-session-api-mcp/34-RESEARCH.md
@.planning/phases/34-session-api-mcp/34-VALIDATION.md
@.planning/phases/26-auth-module/26-00-SUMMARY.md
@server/auth/MODULE.md
@server/auth/events.ts
@server/auth/internal/module.ts
@server/auth/index.ts
@server/db/schema.ts
@server/queue/names.ts
@.dependency-cruiser.cjs
@package.json

<interfaces>
<!-- Existing patterns to copy verbatim with `auth → sessions` substitution -->

server/auth/events.ts exports SHAPE (for sessions/events.ts stub):
```typescript
export const AUTH_EVENT_NAMES = {
  KEY_CREATED: 'auth.key.created',
  KEY_REVOKED: 'auth.key.revoked',
} as const;
export const AUTH_AGGREGATE_TYPE = 'auth' as const;
export type AuthEventName = (typeof AUTH_EVENT_NAMES)[keyof typeof AUTH_EVENT_NAMES];
export const authRegistry = {} as const satisfies EventRegistry;
```

server/auth/internal/module.ts stub shape (clone with createSessionsModule):
```typescript
import type { FastifyInstance } from 'fastify';
export interface CreateSessionsModuleDeps { fastify: FastifyInstance; /* ... */ }
export interface SessionsModule { /* ... */ }
export function createSessionsModule(_deps: CreateSessionsModuleDeps): SessionsModule {
  throw new Error('Plan 34-01 not yet executed');
}
```

server/auth/index.ts barrel shape:
```typescript
export { createSessionsModule, type SessionsModule } from './internal/module.js';
```

QUEUE_NAMES extension pattern (from server/queue/names.ts):
```typescript
export const QUEUE_NAMES = {
  HOOK_RUN: 'hook-run',
  // ... existing ...
  SESSION_SWEEP: 'session.sweep',  // ADD
} as const;
```

.dependency-cruiser.cjs rule pattern (from rule 10 — copy verbatim, substitute auth → sessions):
```javascript
{
  name: 'no-deep-imports-into-sessions-internal',
  severity: 'error',
  comment: 'External modules MUST import from server/sessions/index.ts (the barrel) — not from server/sessions/internal/*. MOD-02 enforcement for Phase 34.',
  from: { pathNot: '^server/sessions(/|$)' },
  to: { path: '^server/sessions/internal/' },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0.1: Create sessions module scaffolds + Drizzle migration + queue name + dep-cruiser rule</name>
  <files>
    server/sessions/events.ts,
    server/sessions/internal/module.ts,
    server/sessions/internal/protocol.ts,
    server/sessions/MODULE.md,
    server/sessions/index.ts,
    server/sessions/schemas.ts,
    server/db/schema.ts,
    server/db/migrations/0009_sessions.sql,
    server/queue/names.ts,
    .dependency-cruiser.cjs,
    __fixtures__/dep-cruiser/bad-sessions-deep-import.ts
  </files>
  <behavior>
    - Test: `npx vitest run server/sessions/__tests__/events.spec.ts` passes (Task 0.2 will create the spec; this task creates the source it imports).
    - Test: `npx tsc --noEmit` passes after substrate lands.
    - Test: `npm run dep-check` runs without errors AND, when manually pointed at the bad-sessions-deep-import fixture, returns exit code 1 with rule `no-deep-imports-into-sessions-internal` flagged.
    - Test: `npx drizzle-kit check` reports no schema drift (migration matches schema).
    - Test: importing `SESSION_SWEEP` from `server/queue/names.ts` is type-narrowed to `'session.sweep'`.
  </behavior>
  <action>
1. Create `server/sessions/events.ts` with constants only (no registry body):
   ```typescript
   import type { EventRegistry } from '../bus/registry.js';
   export const SESSION_EVENT_NAMES = {
     LEASED: 'session.leased',
     RELEASED: 'session.released',
     EXPIRED: 'session.expired',
     DEVICE_LOST: 'session.device.lost',
   } as const;
   export const SESSIONS_AGGREGATE_TYPE = 'session' as const;
   export type SessionEventName = (typeof SESSION_EVENT_NAMES)[keyof typeof SESSION_EVENT_NAMES];
   export const sessionsRegistry = {} as const satisfies EventRegistry;
   ```

2. Create `server/sessions/internal/module.ts` throw-stub (mirror server/auth/internal/module.ts shape):
   ```typescript
   import type { FastifyInstance } from 'fastify';
   export interface CreateSessionsModuleDeps { fastify: FastifyInstance; }
   export interface SessionsModule { shutdown(): Promise<void>; }
   export function createSessionsModule(_deps: CreateSessionsModuleDeps): SessionsModule {
     throw new Error('Plan 34-01 not yet executed');
   }
   ```

3. Create `server/sessions/internal/protocol.ts` ping/pong-only stub:
   ```typescript
   import { z } from 'zod';
   const baseEnvelope = z.object({ id: z.string().uuid() });
   export const clientEnvelope = z.discriminatedUnion('type', [
     baseEnvelope.extend({ type: z.literal('ping') }),
   ]);
   export const serverEnvelope = z.discriminatedUnion('type', [
     z.object({ type: z.literal('pong'), forMsgId: z.string().uuid() }),
   ]);
   export type ClientEnvelope = z.infer<typeof clientEnvelope>;
   export type ServerEnvelope = z.infer<typeof serverEnvelope>;
   ```

4. Create `server/sessions/schemas.ts` with FULL bodies (verbatim from RESEARCH §REST Surface — `leaseRequestSchema`, `leaseResponseSchema`, `releaseParamsSchema`, `releaseResponseSchema`, `listResponseSchema`). Each must call `.meta({ id: 'SessionLeaseRequest' | 'SessionLeaseResponse' | 'SessionReleaseResponse' | 'SessionListResponse' })` for OpenAPI emit per Phase 26 pattern.

5. Create `server/sessions/MODULE.md` with single `## Purpose` H2 section (~3 sentences referencing brief T-34.1..T-34.10).

6. Create `server/sessions/index.ts`:
   ```typescript
   export { createSessionsModule, type SessionsModule } from './internal/module.js';
   ```

7. Edit `server/db/schema.ts`: append `sessionStatusEnum` pgEnum and `sessions` pgTable using the exact shape from `34-RESEARCH.md §Schema` (lines ~110-145). Include partial unique index `WHERE status = 'active'` and three secondary indexes (`leaseUntil`, `status`, `ownerApiKeyId`).

8. Run `npx drizzle-kit generate` to emit migration; if drizzle-kit picks a different filename slug, `git mv` the file to `server/db/migrations/0009_sessions.sql`. Verify the SQL contains CREATE TYPE + CREATE TABLE + CREATE UNIQUE INDEX with the WHERE clause.

9. Edit `server/queue/names.ts`: extend `QUEUE_NAMES` with `SESSION_SWEEP: 'session.sweep'`.

10. Edit `.dependency-cruiser.cjs`: copy rule 10 (`no-deep-imports-into-auth-internal`) verbatim, rename to `no-deep-imports-into-sessions-internal`, substitute `auth` → `sessions` in paths. Extend the header comment-block with a rule-11 paragraph following the pattern.

11. Create `__fixtures__/dep-cruiser/bad-sessions-deep-import.ts`:
    ```typescript
    // @ts-expect-error fixture proves rule fires
    import { createSessionsModule } from '../../server/sessions/internal/module.js';
    void createSessionsModule;
    ```

Anti-action: Do NOT modify server/auth/, server/jobs/, server/pool/, server/streaming/. Do NOT register the sessions plugin in server/index.ts (lands in 34-01). Do NOT add emit helpers (`makeSessionsEmitters` lands in 34-01). Do NOT extend `clientEnvelope` beyond `ping`/`pong` (lands in 34-02).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run dep-check && cat server/db/migrations/0009_sessions.sql | grep -q "sessions_device_active_idx"</automated>
  </verify>
  <done>
    - `server/sessions/{events.ts,internal/module.ts,internal/protocol.ts,schemas.ts,MODULE.md,index.ts}` exist.
    - `server/db/schema.ts` contains `sessions = pgTable` and `sessionStatusEnum`.
    - `server/db/migrations/0009_sessions.sql` exists with `CREATE UNIQUE INDEX ... WHERE status = 'active'`.
    - `server/queue/names.ts` `QUEUE_NAMES` exports `SESSION_SWEEP: 'session.sweep'`.
    - `.dependency-cruiser.cjs` has 11 forbidden module rules; bad fixture exists.
    - `npx tsc --noEmit` exit 0; `npm run dep-check` exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 0.2: Create 8 server vitest skip-stubs + 2 fixtures + sessions-list web stub + dep-cruiser spec extension</name>
  <files>
    server/sessions/__tests__/events.spec.ts,
    server/sessions/__tests__/routes.spec.ts,
    server/sessions/__tests__/protocol.spec.ts,
    server/sessions/__tests__/ws.spec.ts,
    server/sessions/__tests__/dispatch.spec.ts,
    server/sessions/__tests__/resolver-maestro.spec.ts,
    server/sessions/__tests__/resolver-claude.spec.ts,
    server/sessions/__tests__/auth-rate-sweeper.spec.ts,
    server/sessions/__tests__/__fixtures__/android-hierarchy.xml,
    server/sessions/__tests__/__fixtures__/screenshot.png,
    server/hooks/__tests__/dep-cruiser.spec.ts,
    web/src/routes/sessions/+page.svelte,
    web/src/routes/sessions/[id]/+page.svelte,
    web/src/lib/sessions/__tests__/sessions-list.spec.ts
  </files>
  <behavior>
    - Test: every new `.spec.ts` file is `vitest`-discoverable (`npx vitest list` includes it) AND each has at least one passing or skipped test.
    - Test: `events.spec.ts` shape-asserts `SESSION_EVENT_NAMES` keys are 4 dotted past-tense strings — proves EVENTS-03 invariant survives the stub.
    - Test: `dep-cruiser.spec.ts [MOD-02 sessions extension]` it-block proves rule 11 fires structurally on the bad fixture.
    - Test: fixture files load (`readFileSync('android-hierarchy.xml')` succeeds; PNG file is binary and ≥ 1KB).
    - Test: web stubs render Svelte 5 without runtime error.
  </behavior>
  <action>
1. Create `server/sessions/__tests__/events.spec.ts`:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import { SESSION_EVENT_NAMES } from '../events.js';
   describe('Sessions events stub (Plan 34-00)', () => {
     it('SESSION_EVENT_NAMES has 4 dotted past-tense entries', () => {
       const values = Object.values(SESSION_EVENT_NAMES);
       expect(values).toHaveLength(4);
       for (const v of values) {
         expect(v).toMatch(/^session\.[a-z]+(\.[a-z]+)*$/);
       }
       expect(new Set(values).size).toBe(values.length); // uniqueness
     });
     it.skip('full registry shape (Plan 34-01)', () => {});
   });
   ```

2. Create the remaining 7 spec files. Each has the shape:
   ```typescript
   import { describe, it } from 'vitest';
   describe('<scope> (Plan 34-<NN>)', () => {
     it('placeholder pass', () => { /* stub */ });
     it.skip('full behavior lands in 34-NN', () => {});
   });
   ```
   - `routes.spec.ts` — describes "POST/DELETE /api/sessions"
   - `protocol.spec.ts` — describes "WS envelope discriminated union"; ships two `it.todo` markers: `it.todo('WS envelope discriminated union')` and `it.todo('ack id echo')` (full body lands in 34-02)
   - `ws.spec.ts` — describes "WS envelope dispatch"
   - `dispatch.spec.ts` — describes "action → primitive routing"
   - `resolver-maestro.spec.ts` — describes "MaestroAiResolver XML heuristic"
   - `resolver-claude.spec.ts` — describes "ClaudeVisionResolver mocked Anthropic"
   - `auth-rate-sweeper.spec.ts` — describes "auth + rate limit + sweeper"

3. Create `server/sessions/__tests__/__fixtures__/android-hierarchy.xml` with a real `uiautomator dump`-shaped document (200 LoC ok). MUST contain `<node text="Sign In" clickable="true" bounds="[120,800][600,920]" class="android.widget.Button" .../>` AND `<node content-desc="Settings" clickable="true" bounds="[800,100][1000,200]" class="android.widget.ImageButton" .../>`. The tree may have a hierarchy root + a few intermediate `FrameLayout`/`LinearLayout` siblings — doesn't matter as long as the two leaves above exist verbatim.

4. Create `server/sessions/__tests__/__fixtures__/screenshot.png` — write a 1080x1920 solid-color (e.g., gray) PNG via a one-shot script in this task, OR use a pre-existing test PNG from another phase (Phase 21 artifacts has one). Final file ≥ 1KB.

5. Extend `server/hooks/__tests__/dep-cruiser.spec.ts` with an `[MOD-02 sessions extension]` it-block at the end of the existing describe — copy the auth extension it-block verbatim, substitute `auth → sessions` in fixture path + rule name. Two-pass pattern: invoke `depcruise` programmatically against the bad fixture, then re-invoke against a known-good import, assert rule fires only on bad.

6. Create `web/src/routes/sessions/+page.svelte`:
   ```svelte
   <script lang="ts">
     // Phase 34-00 placeholder — full list UI in Plan 34-06
   </script>
   <h1>Sessions</h1>
   <p>Placeholder. Live session list lands in Plan 34-06.</p>
   ```

7. Create `web/src/routes/sessions/[id]/+page.svelte`:
   ```svelte
   <script lang="ts">
     import { page } from '$app/stores';
   </script>
   <h1>Session {$page.params.id}</h1>
   <p>Placeholder. Live preview + click-to-tap overlay lands in Plan 34-06.</p>
   ```

8. Create `web/src/lib/sessions/__tests__/sessions-list.spec.ts`:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import { existsSync } from 'node:fs';
   import { resolve } from 'node:path';
   describe('Sessions web route stubs (Plan 34-00)', () => {
     it('list route file exists', () => {
       expect(existsSync(resolve('src/routes/sessions/+page.svelte'))).toBe(true);
     });
     it('detail route file exists', () => {
       expect(existsSync(resolve('src/routes/sessions/[id]/+page.svelte'))).toBe(true);
     });
     it.skip('renders active sessions table (Plan 34-06)', () => {});
   });
   ```

Anti-action: Do NOT write any real DB code. Do NOT call `fastify.ready()`. Do NOT add live WebSocket handlers in the web stub.
  </action>
  <verify>
    <automated>npx vitest run server/sessions/__tests__/events.spec.ts server/hooks/__tests__/dep-cruiser.spec.ts && test -f server/sessions/__tests__/__fixtures__/android-hierarchy.xml && grep -q "Sign In" server/sessions/__tests__/__fixtures__/android-hierarchy.xml</automated>
  </verify>
  <done>
    - 7 spec files in `server/sessions/__tests__/` discoverable by vitest.
    - 2 fixture files exist (XML with `Sign In` element; PNG ≥ 1KB).
    - `dep-cruiser.spec.ts` extended with sessions it-block; passes.
    - Web stubs exist; web sessions-list.spec passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 0.3: Scaffold mcp/ workspace + CLI Cobra stub + register workspace</name>
  <files>
    mcp/package.json,
    mcp/tsconfig.json,
    mcp/vitest.config.ts,
    mcp/src/index.ts,
    mcp/__tests__/index.spec.ts,
    mcp/__tests__/tools.spec.ts,
    mcp/README.md,
    package.json,
    cli/cmd/session.go,
    cli/cmd/session_test.go
  </files>
  <behavior>
    - Test: `mcp/package.json` parses as JSON; declares `name: '@device-stream/mcp'`, `bin: { 'device-stream-mcp': './dist/index.js' }`, `private: true`, `type: 'module'`.
    - Test: root `package.json` `workspaces` array contains the literal string `"mcp"` (verifiable via `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json')).workspaces)"`).
    - Test: `cd mcp && npx vitest list` finds both spec files (config wired correctly).
    - Test: `cd cli && go build ./...` exits 0; `go test ./cmd -run TestSessionCommandExists` passes.
    - Test: NO `npm install` runs as part of this task — Plan 34-05 owns the install (dependency body lands then).
  </behavior>
  <action>
1. Create `mcp/package.json`:
   ```json
   {
     "name": "@device-stream/mcp",
     "version": "0.0.1",
     "private": true,
     "type": "module",
     "bin": { "device-stream-mcp": "./dist/index.js" },
     "scripts": {
       "build": "tsc",
       "test": "vitest run"
     },
     "dependencies": {
       "@modelcontextprotocol/sdk": "^1.0.0",
       "ws": "^8.18.0",
       "zod": "^3.23.0"
     },
     "devDependencies": {
       "@types/ws": "^8.5.0",
       "typescript": "^5.5.0",
       "vitest": "^1.6.0"
     }
   }
   ```

2. Create `mcp/tsconfig.json`:
   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "module": "NodeNext",
       "moduleResolution": "NodeNext",
       "outDir": "dist",
       "rootDir": "src",
       "strict": true,
       "esModuleInterop": true,
       "skipLibCheck": true
     },
     "include": ["src/**/*"]
   }
   ```

3. Create `mcp/vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config';
   export default defineConfig({
     test: { include: ['__tests__/**/*.spec.ts'] },
   });
   ```

4. Create `mcp/src/index.ts`:
   ```typescript
   #!/usr/bin/env node
   // Phase 34-00 placeholder — full MCP server lands in Plan 34-05.
   console.error('device-stream-mcp: not yet implemented (Plan 34-05)');
   process.exit(1);
   ```

5. Create `mcp/__tests__/index.spec.ts` and `mcp/__tests__/tools.spec.ts` as skip-stubs (one passing `it('placeholder')` + one `it.skip(...)` each).

6. Create `mcp/README.md` with a 5-line stub: "Phase 34 MCP server for device-farm. See 34-05-SUMMARY.md once shipped. One-line install: `claude mcp add device-stream npx @device-stream/mcp`."

7. Edit root `package.json`: append `"mcp"` to the `workspaces` array (keep relative order; `mcp` after `device-stream/web-sdk`).

8. Create `cli/cmd/session.go`:
   ```go
   package cmd

   import (
     "fmt"
     "github.com/spf13/cobra"
   )

   var sessionCmd = &cobra.Command{
     Use:   "session",
     Short: "Interactive device sessions (lease, tap, type, screenshot, release)",
     RunE: func(cmd *cobra.Command, args []string) error {
       return fmt.Errorf("device-farm session: not yet implemented (Plan 34-06)")
     },
   }

   func init() {
     rootCmd.AddCommand(sessionCmd)
   }
   ```

9. Create `cli/cmd/session_test.go`:
   ```go
   package cmd

   import "testing"

   func TestSessionCommandExists(t *testing.T) {
     if sessionCmd == nil {
       t.Fatal("sessionCmd not initialized")
     }
     if sessionCmd.Use != "session" {
       t.Fatalf("expected Use=session, got %s", sessionCmd.Use)
     }
   }
   ```

Anti-action: Do NOT run `npm install` (the mcp deps land via 34-05 wrapper). Do NOT call `vitest` from the root suite against mcp/ yet — Plan 34-05 wires the root npm test composite. Do NOT add subcommands (`session lease`, `session tap`) to the Cobra tree.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.workspaces.includes('mcp')) process.exit(1)" && cd cli && go build ./... && go test ./cmd -run TestSessionCommandExists</automated>
  </verify>
  <done>
    - `mcp/` workspace files exist; `mcp/package.json` declares `@device-stream/mcp` + bin.
    - Root `package.json` `workspaces` array contains `"mcp"`.
    - `cli/cmd/session.go` compiles; `go test ./cmd -run TestSessionCommandExists` passes.
    - mcp vitest config discoverable; `cd mcp && npx vitest list` lists 2 specs.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (TypeScript compiles).
- `npm run dep-check` exit 0; 11 forbidden module rules active.
- `npx vitest run server/sessions/__tests__/events.spec.ts` passes.
- `cd cli && go build ./... && go test ./cmd -run TestSessionCommandExists` passes.
- `cat server/db/migrations/0009_sessions.sql | grep -q "CREATE UNIQUE INDEX sessions_device_active_idx"` passes.
- `node -e "process.exit(JSON.parse(require('fs').readFileSync('package.json')).workspaces.includes('mcp')?0:1)"` exit 0.
- `grep -q "SESSION_SWEEP" server/queue/names.ts` passes.
- `grep -r "Phase 34-00 placeholder" mcp/src/index.ts` passes.
</verification>

<success_criteria>
- All 33 files listed in `files_modified` exist on disk with expected content.
- `npx tsc --noEmit` exit 0.
- `npm run dep-check` exit 0; rule 11 fires when invoked against bad fixture (validated by extended dep-cruiser.spec).
- `npx vitest run server/sessions/__tests__/` exit 0 (skip-stubs + events shape pass).
- `cd cli && go test ./cmd -run TestSessionCommandExists` exit 0.
- Plans 34-01..34-07 can import from `server/sessions/{events,schemas,internal/module,internal/protocol}` without resolution errors.
</success_criteria>

<output>
After completion, create `.planning/phases/34-session-api-mcp/34-00-SUMMARY.md` recording:
- Migration filename emitted by drizzle-kit (if rename was required, note original)
- Any deviations from Phase 26-00 substrate pattern
- Workspace install deferral (mcp deps NOT installed yet — Plan 34-05 owns)
- Sub-minute cron Open Question #1 status (verified during Wave 0 or carried to 34-04)
</output>
