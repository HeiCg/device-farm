---
phase: 34
plan: 08
type: execute
wave: 6
depends_on: ["34-05", "34-06", "34-07"]
files_modified:
  - server/sessions/MODULE.md
  - server/sessions/index.ts
  - docs/runbooks/session-api.md
  - docs/runbooks/mcp.md
  - docs/runbooks/session-resolver-costs.md
  - examples/agents/pr-bot.md
  - examples/agents/exploration.md
  - server/hooks/__tests__/plugin-order.spec.ts
  - .planning/phases/34-session-api-mcp/deferred-items.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SESS-DOCS, MOD-01]
estimated_minutes: 35
gap_closure: false
must_haves:
  truths:
    - "`server/sessions/MODULE.md` replaces the Purpose-only placeholder from 34-00 with the full 9 H2 sections per MOD-01 / Phase 26 template: `## Purpose`, `## Public API`, `## Events Emitted`, `## Events Consumed`, `## Queue Produced`, `## Queue Consumed`, `## Invariants`, `## Non-Goals`, `## Dependencies`, plus a `### Runnable Example` block. Cites SESS-* requirement IDs and references RESEARCH pitfalls inline (e.g., Pitfall §Open Q #1 sweeper strategy; §Open Q #8 TLS guard)."
    - "MODULE.md ## Public API lists every export from `server/sessions/index.ts`: `createSessionsModule`, `SessionsModule` type, `sessionsPlugin` default, `SESSION_EVENT_NAMES`, `SESSIONS_AGGREGATE_TYPE`, `sessionsRegistry`, `makeSessionsEmitters`, all 4 payload types, the 5 REST Zod schemas (leaseRequestSchema, leaseResponseSchema, releaseParamsSchema, releaseResponseSchema, listResponseSchema), `clientEnvelope` + `serverEnvelope` types, `createResolver` factory, `TargetResolver`/`ResolveTargetRequest`/`ResolveTargetResult` types, `ResolverError`. Fastify decorators: `fastify.sessionsModule: SessionsModule`, `fastify.sessionsResolver: TargetResolver`."
    - "MODULE.md ## Events Emitted lists 4 events (all `persisted: true`, `aggregateType: 'session'`): `session.leased`, `session.released`, `session.expired`, `session.device.lost`. Payload columns include `ownerActor` / `releasedBy` / `reason` per RESEARCH §Schema."
    - "MODULE.md ## Events Consumed lists `device.health.failed` (from pool) — the subscriber broadcasts device-lost + expires the session. NOT consuming `job.*` events (sessions are independent of job lifecycle)."
    - "MODULE.md ## Queue Produced lists `session.sweep` (pg-boss schedule) with policy + retry contract; Open Q #1 strategy (cron-30s vs cron-1m+interval) is documented inline."
    - "MODULE.md ## Invariants section lists 6 invariants with corresponding spec files: (1) partial unique index `WHERE status='active'` → 1 session/device; (2) WS upgrade requires owner match (`ownerApiKeyId === apiKey.id`); (3) Rate limit 30/10s sliding window per session; (4) Sweeper releases expired within 30s of TTL; (5) Resolver `confidence < 0.5` → `resolver_failed` error envelope; (6) TLS-first WS scheme guard (force-upgrade non-loopback plaintext)."
    - "MODULE.md ## Non-Goals lists deferred items: multi-session-per-device (v3.1), OmniParser (deferred), session replay (Phase 35), live list updates in web UI (Phase 36 CommandPalette), full iOS hierarchy walker for resolver (Phase 36/37)."
    - "MODULE.md ## Dependencies lists plugin dependencies array verbatim: `['config', 'db', 'queue', 'event-bus', 'pool-plugin', 'auth', 'websocket']`. Cross-module consumers noted: web UI consumes via REST + WS (not via decorator); MCP package consumes via REST + WS as external client."
    - "MODULE.md ### Runnable Example shows: (1) CURL POST /api/sessions to lease; (2) ws CLI command to dial WS and send tap; (3) inspecting events table for persisted `session.leased` row with actor populated. Matches the Phase 26 + Phase 23 runnable example precedent."
    - "`server/sessions/index.ts` MOD-02 strict barrel finalized: every public symbol re-exported via named exports (NO `export *`). Mirrors `server/auth/index.ts` final-form from Phase 26 Plan 26-05."
    - "`docs/runbooks/session-api.md` ships full operator runbook: lease/release REST shapes; WS envelope contract; TLS-first scheme guard (RESEARCH §Open Q #8); rate-limit + sweeper config; troubleshooting (no devices available → check pool; 409 device_already_leased → wait for release; resolver_failed → set SESSION_RESOLVER=claude-vision)."
    - "`docs/runbooks/mcp.md` ships: install via `claude mcp add device-stream npx @device-stream/mcp`; required env vars (`DEVICE_FARM_URL`, `DEVICE_FARM_TOKEN`, optional `SESSION_RESOLVER_MODEL`); 12-tool catalog with example agent prompts; resource `device-farm://devices` semantics; troubleshooting (token unauthorized → check API key scope; stdio handshake timeout → confirm Node 22+; tool not appearing in Claude Code → restart client)."
    - "`docs/runbooks/session-resolver-costs.md` ships: per-resolve cost ceiling (Sonnet 4.5 ~$0.005-0.01); cache hit rate considerations (5min TTL, 100 entries); recommended SESSION_RESOLVER setting per use case (CI=maestro-ai only; interactive dev=claude-vision OK); cost-cap implementation hooks (DEFERRED — Phase 37 may add per-session resolver call limit)."
    - "`examples/agents/pr-bot.md` ships SKELETON (Phase 34 stub): 'PR review bot' agent prompt + tool sequence example using device_lease → device_install → device_launch → device_screenshot → device_tap_by_description loop → device_release. Full implementation lands in Phase 37 PR Review Bot work; this file is the breadcrumb."
    - "`examples/agents/exploration.md` ships SKELETON: 'app explorer' agent prompt + BFS tool sequence using device_lease → device_tap_by_description → device_screenshot loop. Full implementation lands in Phase 35 (App Explorer + Atlas Graph)."
    - "`server/hooks/__tests__/plugin-order.spec.ts` extended with 3+ additive Phase 34 assertions: (1) `sessions` plugin appears AFTER `auth`, `pool-plugin`, `event-bus`, `queue`, and `websocket` in the plugin registration order; (2) `sessions` plugin appears BEFORE `static`; (3) `sessions` plugin declares ALL of `['config', 'db', 'queue', 'event-bus', 'pool-plugin', 'auth', 'websocket']` in its dependencies array (literal-grep friendly assertion)."
    - "`.planning/phases/34-session-api-mcp/deferred-items.md` catalogs Phase 34 DEFERRED items (mirrors Phase 26 deferred-items.md format): DEFERRED-34-A: persistEnvelope 11TH SAMPLE POINT consolidation (continues DEFERRED-26-B chain — Phase 27 owns); DEFERRED-34-B: multi-session-per-device (v3.1); DEFERRED-34-C: full iOS hierarchy walker (Phase 36/37); DEFERRED-34-D: per-session resolver cost cap (Phase 37); DEFERRED-34-E: MCP resource expansion beyond device-farm://devices (Phase 35); DEFERRED-34-F: live sessions list updates via WS (Phase 36 CommandPalette); DEFERRED-34-G: Maestro `--ai-prompt` fallback shell-out for unmatched targets (cleared as never-needed if heuristic + Claude Vision suffice — verify after 30-day prod usage)."
    - "`.planning/STATE.md` updated to mark Phase 34 complete with timestamp, plan count (9 plans across 7 waves: 0,1,2,3,4,5,5,5,6), Nyquist delta gate result, and a brief 'what shipped' note (REST + WS + 12 MCP tools + CLI subcmds + web UI + 4 events + sweeper + resolver fallback chain)."
    - "`.planning/ROADMAP.md` updated: Phase 34 status `Not started` → `Complete` with date; plan count 0/10 → 9/9; Phase 35 dependency note carried forward."
    - "Phase close requires user verification: this plan is `autonomous: false` because the final task is a `checkpoint:human-verify` reviewing the full acceptance criteria from BRIEF lines 235-244 (8 manual smoke tests). Nyquist `npm run nyquist:check` exit 0 (coverage delta ≥ -2pp) MUST pass before the checkpoint resumes."
  artifacts:
    - path: "server/sessions/MODULE.md"
      provides: "Full 9-section LLM-first module doc + runnable example"
      contains: "## Invariants"
    - path: "server/sessions/index.ts"
      provides: "Finalized strict barrel (named exports only, no export *)"
      contains: "createResolver"
    - path: "docs/runbooks/session-api.md"
      provides: "Operator runbook for lease/release REST + WS + sweeper config"
      contains: "TLS"
    - path: "docs/runbooks/mcp.md"
      provides: "MCP install + env vars + tool catalog + troubleshooting"
      contains: "claude mcp add"
    - path: "docs/runbooks/session-resolver-costs.md"
      provides: "Cost ceiling + cache strategy + per-use-case resolver recommendation"
      contains: "Sonnet"
    - path: "examples/agents/pr-bot.md"
      provides: "Phase 34 stub for Phase 37 PR review bot agent"
      contains: "device_tap_by_description"
    - path: "examples/agents/exploration.md"
      provides: "Phase 34 stub for Phase 35 app explorer agent"
      contains: "BFS"
    - path: "server/hooks/__tests__/plugin-order.spec.ts"
      provides: "3+ additive Phase 34 plugin-order assertions"
      contains: "sessions"
    - path: ".planning/phases/34-session-api-mcp/deferred-items.md"
      provides: "Catalog of Phase 34 DEFERRED items (DEFERRED-34-A..G)"
      contains: "DEFERRED-34-A"
    - path: ".planning/STATE.md"
      provides: "Phase 34 marked complete with timestamp + plan count + Nyquist gate"
      contains: "Phase 34"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 34 status updated to Complete"
      contains: "Phase 34"
  key_links:
    - from: "MODULE.md ## Invariants section"
      to: "spec files in server/sessions/__tests__/"
      via: "Each invariant cross-references its proving spec — MOD-08 enforcement"
      pattern: "spec"
    - from: "deferred-items.md DEFERRED-34-A"
      to: "Phase 27 API Aggregator consolidation"
      via: "persistEnvelope 11TH SAMPLE continues DEFERRED-26-B chain"
      pattern: "DEFERRED-26-B"
    - from: "ROADMAP.md Phase 34 entry"
      to: "STATE.md current_phase pointer"
      via: "ROADMAP marks Complete; STATE advances to Phase 35"
      pattern: "Complete"
    - from: "plugin-order.spec.ts Phase 34 assertions"
      to: "server/index.ts plugin registration order"
      via: "Spec asserts sessions placement after auth/pool/ws + dependencies declaration"
      pattern: "sessions"
---

<objective>
T-34.10 — Docs + example agents + phase close.

After this plan:
1. `server/sessions/MODULE.md` ships the full 9-section LLM-first body matching the Phase 26 + Phase 23 pattern.
2. Three operator runbooks (`session-api.md`, `mcp.md`, `session-resolver-costs.md`) live under `docs/runbooks/`.
3. Two agent skeletons (`pr-bot.md`, `exploration.md`) live under `examples/agents/` as breadcrumbs for Phase 35 + Phase 37.
4. Plugin-order spec extension proves Phase 34 plugin placement structurally.
5. `deferred-items.md`, `STATE.md`, `ROADMAP.md` updated; Phase 34 marked Complete.
6. Nyquist gate passes.
7. User-driven UAT checkpoint confirms all 8 BRIEF acceptance criteria.

Plan is `autonomous: false` because the final checkpoint requires manual UAT.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-session-api-mcp/34-05-SUMMARY.md
@.planning/phases/34-session-api-mcp/34-07-SUMMARY.md
@.planning/phases/34-session-api-mcp/34-RESEARCH.md
@.planning/phases/34-session-api-mcp/34-BRIEF.md
@server/auth/MODULE.md
@server/jobs/MODULE.md
@.planning/phases/26-auth-module/deferred-items.md
@server/hooks/__tests__/plugin-order.spec.ts
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- Phase 26 deferred-items.md format (mirror exactly) -->
```markdown
# Phase 34 — Deferred Items

| ID | Description | Resolution Path | Status |
|----|-------------|-----------------|--------|
| DEFERRED-34-A | persistEnvelope 11TH SAMPLE POINT consolidation | Phase 27 API Aggregator | open |
| DEFERRED-34-B | Multi-session-per-device | v3.1 | open |
...
```

<!-- Phase 26 plugin-order.spec extension pattern (additive — never break existing assertions) -->
```typescript
it('[Phase 34 sessions] sessions plugin registers after auth/pool/ws and before static', async () => {
  const order = readPluginOrder('server/index.ts');
  const sessionsIdx = order.indexOf('sessions');
  expect(sessionsIdx).toBeGreaterThan(order.indexOf('auth-plugin'));
  expect(sessionsIdx).toBeGreaterThan(order.indexOf('pool-plugin'));
  expect(sessionsIdx).toBeGreaterThan(order.indexOf('websocket-plugin'));
  expect(sessionsIdx).toBeLessThan(order.indexOf('static-plugin'));
});

it('[Phase 34 sessions] dependencies array literal', () => {
  const src = readFileSync('server/sessions/plugin.ts', 'utf8');
  expect(src).toMatch(/dependencies:\s*\[\s*'config',\s*'db',\s*'queue',\s*'event-bus',\s*'pool-plugin',\s*'auth',\s*'websocket'\s*\]/);
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 8.1: Write full MODULE.md + finalize index.ts barrel</name>
  <files>
    server/sessions/MODULE.md,
    server/sessions/index.ts
  </files>
  <action>
1. Replace placeholder `server/sessions/MODULE.md` with the full 9-section body. Use `server/auth/MODULE.md` and `server/jobs/MODULE.md` as templates. Each section is required:
   - **## Purpose** — 1-2 paragraphs citing the SESS-* requirements, brief T-34.x tasks, and the "session-aware vs batch-only" framing.
   - **## Public API** — table of every export from `server/sessions/index.ts` + Fastify decorators table (`sessionsModule`, `sessionsResolver`) + HTTP routes table (POST/DELETE/GET `/api/sessions` + WS `/ws/sessions/:id`).
   - **## Events Emitted** — 4 events: `session.leased`, `session.released`, `session.expired`, `session.device.lost` — all persisted, all aggregateType=`'session'`. Payload column lists key fields per RESEARCH.
   - **## Events Consumed** — `device.health.failed` (from pool) handler description.
   - **## Queue Produced** — `session.sweep` schedule + Open Q #1 strategy + retry policy.
   - **## Queue Consumed** — None.
   - **## Invariants** — 6 invariants with spec file cross-references (per truths spec list above).
   - **## Non-Goals** — multi-session-per-device, OmniParser, replay, live list, full iOS walker.
   - **## Dependencies** — plugin name `'sessions'`, dependencies array literal, module deps consumed via decorators, cross-module consumers list.
   - **### Runnable Example** — curl POST + ws CLI + psql events query (3 fenced blocks).

2. Finalize `server/sessions/index.ts` to the canonical MOD-02 strict barrel. Every export named explicitly (NO `export *`). Mirror `server/auth/index.ts` final shape from Phase 26 Plan 26-05.

Anti-action: Do NOT use `export *` (MOD-02 violation). Do NOT add invented exports — barrel content matches what was actually shipped through 34-01..34-04.
  </action>
  <verify>
    <automated>test -f server/sessions/MODULE.md && grep -c "^## " server/sessions/MODULE.md | awk '$1 >= 9 {exit 0} {exit 1}' && grep -q "Runnable Example" server/sessions/MODULE.md</automated>
  </verify>
  <done>
    - MODULE.md has 9+ H2 sections + runnable example.
    - index.ts barrel finalized; no `export *`.
  </done>
</task>

<task type="auto">
  <name>Task 8.2: Write 3 operator runbooks + 2 example agent skeletons</name>
  <files>
    docs/runbooks/session-api.md,
    docs/runbooks/mcp.md,
    docs/runbooks/session-resolver-costs.md,
    examples/agents/pr-bot.md,
    examples/agents/exploration.md
  </files>
  <action>
1. Create `docs/runbooks/session-api.md` (~120 LoC):
   - Quickstart: lease/release curl examples.
   - Auth: Bearer token requirement, owner check on release.
   - TLS-first scheme guard (cite RESEARCH §Open Q #8): production deployments terminate TLS; plaintext only on loopback dev.
   - Rate limit: 30/10s default; what error envelope looks like; how to opt out (configurable in env — TBD or deferred to Phase 37).
   - Sweeper: 30s tick (cron-30s) or 1-minute schedule + 30s interval fallback (Open Q #1 documented).
   - Troubleshooting: 409 device_already_leased; 401 unauthorized; resolver_failed (env switch to claude-vision).

2. Create `docs/runbooks/mcp.md` (~80 LoC):
   - One-line install: `claude mcp add device-stream npx @device-stream/mcp`
   - Env vars: `DEVICE_FARM_URL`, `DEVICE_FARM_TOKEN` (required), `SESSION_RESOLVER_MODEL` (optional).
   - Tool catalog (12 tools): name + brief description + example input.
   - Resources: `device-farm://devices` semantics.
   - Troubleshooting: token unauthorized; stdio handshake timeout (Node version); tool absent in Claude Code (client restart).

3. Create `docs/runbooks/session-resolver-costs.md` (~50 LoC):
   - Cost model: Sonnet 4.5 ~$3/Mtok in, ~$15/Mtok out; PNG ~1500 tokens; per-resolve $0.005-0.01.
   - Cache: LRU 100 / 5 min TTL.
   - Recommended settings: CI=maestro-ai (zero cost); interactive dev=claude-vision OK; production agents=fallback chain.
   - Cost cap hooks deferred (DEFERRED-34-D).

4. Create `examples/agents/pr-bot.md` (~40 LoC stub):
   - Agent prompt skeleton: "You are a PR review bot. Given a build artifact and a screen list, install the build, navigate the screen list, capture screenshots, and report any visual regressions."
   - Tool sequence: device_lease → device_install → device_launch → loop(device_tap_by_description, device_screenshot) → device_release.
   - Note: full implementation lands in Phase 37.

5. Create `examples/agents/exploration.md` (~40 LoC stub):
   - Agent prompt: "Map every screen of this app by tapping interactive elements and recording transitions."
   - Tool sequence: device_lease → device_launch → BFS(device_tap_by_description on unique elements, device_screenshot) → device_release.
   - Note: full implementation lands in Phase 35 (App Explorer + Atlas Graph).

Anti-action: Do NOT bloat runbooks with full SDK docs (link to Anthropic + MCP docs instead). Do NOT write production-grade agent code in skeletons — they're breadcrumbs.
  </action>
  <verify>
    <automated>for f in docs/runbooks/session-api.md docs/runbooks/mcp.md docs/runbooks/session-resolver-costs.md examples/agents/pr-bot.md examples/agents/exploration.md; do test -f "$f" || exit 1; done</automated>
  </verify>
  <done>
    - 5 docs files exist.
    - session-api.md cites TLS-first guard.
    - mcp.md documents 1-line install + env vars.
  </done>
</task>

<task type="auto">
  <name>Task 8.3: Extend plugin-order.spec + write deferred-items.md + update STATE/ROADMAP</name>
  <files>
    server/hooks/__tests__/plugin-order.spec.ts,
    .planning/phases/34-session-api-mcp/deferred-items.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
1. Extend `server/hooks/__tests__/plugin-order.spec.ts` with 3+ additive Phase 34 assertions (per §interfaces pattern):
   - sessions registers after auth-plugin AND pool-plugin AND websocket-plugin.
   - sessions registers before static.
   - dependencies array literal grep for the 7-entry list.

2. Create `.planning/phases/34-session-api-mcp/deferred-items.md` with 7 DEFERRED entries (DEFERRED-34-A through G) per truths spec list above. Mirror the table format from `.planning/phases/26-auth-module/deferred-items.md`.

3. Edit `.planning/STATE.md`:
   - Mark Phase 34 complete (status, timestamp = today).
   - Update plan count: `9/9` (counts plans 00, 01, 02, 03, 04, 05, 06, 07, 08).
   - Add "Phase 34 SHIPPED" entry to recent-completions log with brief summary.
   - Advance current_phase pointer to Phase 33 if not yet started OR Phase 35 (depending on STATE.md cadence).

4. Edit `.planning/ROADMAP.md`:
   - Find Phase 34 entry (~line 343).
   - Change status from `Not started` to `Complete` with date.
   - Update Plans table: `0/10` → `9/9` (note: original plan was 10 tasks → consolidated into 8 plans).
   - Tick all 9 plan checkboxes.

Anti-action: Do NOT mark Phase 35 / 36 / 37 anything other than `Not started` — those are downstream phases not touched here.
  </action>
  <verify>
    <automated>npx vitest run server/hooks/__tests__/plugin-order.spec.ts && test -f .planning/phases/34-session-api-mcp/deferred-items.md && grep -q "Phase 34.*Complete" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - plugin-order.spec passes with new Phase 34 assertions.
    - deferred-items.md catalogs 7 items.
    - STATE.md + ROADMAP.md reflect Phase 34 complete.
  </done>
</task>

<task type="auto">
  <name>Task 8.4: Nyquist gate + commit</name>
  <files></files>
  <action>
1. Run full test suite: `npm test && cd cli && go test ./... && cd ../mcp && npm test && cd ../web && npx vitest run`.

2. Run coverage: `npm run test:coverage` then `npm run nyquist:check` — MUST exit 0 (coverage delta ≥ -2pp from baseline).

3. If Nyquist fails: investigate uncovered surface, add targeted tests, re-run. Do NOT bypass the gate.

4. Commit all Phase 34 changes via gsd-tools:
   ```bash
   node "/Users/heicg/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(34): ship Session API + @device-stream/mcp + CLI/web sessions" --files server/sessions/ mcp/ cli/cmd/session*.go cli/internal/session/ web/src/routes/sessions/ web/src/lib/sessions/ docs/runbooks/session-*.md docs/runbooks/mcp.md examples/agents/ .planning/phases/34-session-api-mcp/ .planning/STATE.md .planning/ROADMAP.md
   ```

Anti-action: Do NOT amend prior commits. Do NOT skip the Nyquist gate. Do NOT batch-commit files unrelated to Phase 34.
  </action>
  <verify>
    <automated>npm run nyquist:check && git log -1 --pretty=%s | grep -q "feat(34)"</automated>
  </verify>
  <done>
    - All test suites green.
    - Nyquist exit 0.
    - Single Phase 34 close commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: human UAT</name>
  <action>Pause execution for user-driven UAT. Resume per resume-signal below.</action>
  <what-built>
    Phase 34 complete: Session API + MCP + CLI + Web shipped.

    Concretely:
    - REST POST/DELETE/GET `/api/sessions` with Zod schemas and OpenAPI emission
    - WS `/ws/sessions/:id?token=` with 11 client + 4 server envelope types
    - Action dispatch into `@device-stream/android` + iOS `simctl` shell-out
    - NL resolver chain (MaestroAi default + ClaudeVision opt-in via env)
    - Rate limit (30/10s) + pg-boss sweeper (30s tick)
    - `@device-stream/mcp` npm workspace with 12 tools + `device-farm://devices` resource
    - `device-farm session [lease|tap|type|swipe|key|screenshot|release]` Cobra subcommands with `~/.device-farm/session.json` persist
    - Web UI `/sessions` list + `/sessions/[id]` detail with click-to-tap overlay
    - TLS-first WS scheme guard (force-upgrade non-loopback plaintext)
    - 3 operator runbooks + 2 agent skeletons
    - MODULE.md (9 sections) + plugin-order.spec extension + deferred-items.md
  </what-built>
  <how-to-verify>
    Walk through the 8 BRIEF acceptance criteria (lines 235-244):

    1. `POST /api/sessions` returns 200 with a WS URL; `DELETE` releases.
       - `device-farm session lease --platform android --ttl 600` (CLI smoke).

    2. E2E: Python (or bash) script leases an Android sim, taps, types text, screenshots, releases — without a Maestro flow file.
       ```bash
       SESSION=$(curl -sX POST http://localhost:3000/api/sessions -H "Authorization: Bearer $DF_TOKEN" -d '{"platform":"android"}' | jq -r .sessionId)
       # ... use websocat or curl --include for WS
       curl -X DELETE "http://localhost:3000/api/sessions/$SESSION" -H "Authorization: Bearer $DF_TOKEN"
       ```

    3. Auto-release fires within 30s of TTL expiry.
       - Lease with `--ttl 30`; wait 60s; query GET /api/sessions/:id → status='expired'.

    4. Rate limit kicks in on stress test (verified by `429`-equivalent envelope).
       - Spam 31 actions over WS in <1s; 31st returns `{type:'error', code:'rate_limited'}`.

    5. `npx @device-stream/mcp` registered in `~/.claude.json` allows Claude Code to drive a device.
       - `claude mcp add device-stream node $(pwd)/mcp/dist/index.js`; restart Claude Code; ask Claude to lease a device and tap somewhere.

    6. `device-farm session tap` works in a fresh shell.
       - Open new terminal after lease; `device-farm session tap --x 540 --y 960`.

    7. All sessions visible at `/sessions`; clicking a stream sends a real tap.
       - http://localhost:5173/sessions; navigate to detail; click in canvas.

    8. Concurrency: 4 sessions on 4 devices simultaneously — no cross-talk.
       - Boot 4 emulators; lease 4 sessions; tap each via separate WS clients; verify no cross-device drift in adb logcat.

    EXPECTED: All 8 succeed. If any fail, capture details + open a gap-closure plan via `/gsd:plan-phase --gaps`.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 34 complete or describe failures (which become gap-closure tasks).</resume-signal>
</task>

</tasks>

<verification>
- MODULE.md 9 sections + runnable example.
- 5 docs files exist.
- plugin-order.spec passes with Phase 34 additive assertions.
- STATE.md + ROADMAP.md updated.
- Full test suite green; Nyquist exit 0.
- Phase close commit landed.
- Checkpoint approved.
</verification>

<success_criteria>
- 11 files modified per `files_modified`.
- Phase 34 closed in ROADMAP + STATE.
- All 8 BRIEF acceptance criteria validated via checkpoint.
- 7 DEFERRED items tracked in deferred-items.md.
- Nyquist gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/34-session-api-mcp/34-08-SUMMARY.md` and a phase-level `.planning/phases/34-session-api-mcp/34-PHASE-COMPLETE.md` recording:
- Acceptance criteria pass/fail matrix (all 8 BRIEF lines 235-244)
- Final Nyquist delta (start vs end of phase)
- Test counts per suite (server vitest / cli go / mcp vitest / web vitest)
- Gap-closure plans needed (none expected, but enumerate if any)
- Phase duration (planning start → checkpoint approval)
</output>
