---
phase: 17-contracts-pipeline-ops-hygiene
verified: 2026-04-20T20:00:00Z
status: passed
score: 5/5 success criteria verified
re_verification:
  previous_status: gaps_found
  previous_score: 2/5
  gaps_closed:
    - "contracts/ws-messages.json generated and valid JSON (7 message schemas + __shared)"
    - "cli/internal/types/generated.go generated by go-jsonschema; unions.go hand-rolled discriminator per ADR-003; generated_test.go round-trip harness; cli/Makefile has types + types-check targets"
    - "web/src/lib/api/generated-types.ts generated by openapi-typescript (2164 lines); smoke test wired at web/src/lib/api/__tests__/generated-types.test.ts"
    - "@device-stream/* no longer resolves via file:../device-stream; now file:./vendor/device-stream/*.tgz; tarballs exist in vendor/device-stream/"
    - "server/streaming/websocket-plugin.ts declares pool-plugin dep; server/api/plugin.ts declares lifecycle-plugin dep"
    - "scripts/check-generated.sh exists and is executable (chmod +x confirmed)"
  gaps_remaining: []
  regressions: []
---

# Phase 17: Contracts Pipeline & Ops Hygiene — Verification Report

**Phase Goal:** Lock the source-of-truth contract pipeline (Zod -> OpenAPI 3.1 -> Go + TS codegen) and clear v2.0 operational tech debt that would otherwise block later phases.
**Verified:** 2026-04-20T20:00:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plans 17-03 through 17-08)

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | openapi:generate emits server/openapi.json AND contracts/ws-messages.json; make types regenerates cli/internal/types/generated.go; npm run web:types regenerates generated-types.ts; scripts/check-generated.sh enforces CI drift | VERIFIED | contracts/ws-messages.json exists (8 schema keys: 7 variants + __shared). cli/internal/types/generated.go exists (go-jsonschema header line 1). web/src/lib/api/generated-types.ts exists (2164 lines). scripts/check-generated.sh exists and is executable. cli/Makefile .PHONY line 5 includes both `types` and `types-check` targets. |
| 2 | fastify-zod-openapi wired; >= 1 route per module with Zod request+response schemas; components.schemas named; consumed by CLI + web codegen | VERIFIED | (carried from initial verification — unchanged) server/openapi.json 1762 lines, OpenAPI 3.1.0, 14+ named schemas. web/src/lib/api/index.ts re-exports components, paths, operations from generated-types.js. |
| 3 | cli/internal/types/unions.go with discriminated-union mapping; round-trip test decodes WS fixtures | VERIFIED | unions.go exists (91 lines): JobMessage struct with Log/Step/Status pointers; UnmarshalJSON + MarshalJSON using discriminator-peek pattern per ADR-003. generated_test.go exists (144 lines): TestJobMessageRoundTrip covers log/step/status variants; TestDevicePreviewDecode; TestUnknownDiscriminator asserts hard error on unknown type. |
| 4 | @device-stream/* no longer uses file:../device-stream; fresh-clone + npm install works standalone | VERIFIED | package.json lines 25-27 now read file:./vendor/device-stream/android-1.1.0.tgz, file:./vendor/device-stream/core-1.1.0.tgz, file:./vendor/device-stream/ios-simulator-1.1.0.tgz. All three tarballs exist in vendor/device-stream/. No ../device-stream path references remain. |
| 5 | Plugin name normalization: pipelines->jobs ref fixed; websocket declares pool dep; lifecycle/api dep graph aligned | VERIFIED | server/streaming/websocket-plugin.ts line 174: dependencies: ['config', 'auth', 'pool-plugin']. server/api/plugin.ts line 51: dependencies includes 'lifecycle-plugin'. |

**Score:** 5/5 success criteria verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `contracts/ws-messages.json` | Generated WS JSON Schema, 7 variants | VERIFIED | Exists; valid JSON; schemas object contains JobLogMessage, JobStepMessage, JobStatusMessage, DevicePreviewMessage, DeviceStateMessage, ArtifactCreatedMessage, WsEnvelope, __shared (8 keys) |
| `cli/internal/types/generated.go` | Go structs from go-jsonschema, package types | VERIFIED | Exists; line 1: "// Code generated by github.com/atombender/go-jsonschema, DO NOT EDIT."; package types; imports encoding/json, fmt, mapstructure, reflect, regexp, strings |
| `cli/internal/types/unions.go` | Hand-rolled discriminated-union UnmarshalJSON per ADR-003 | VERIFIED | Exists (91 lines); JobMessage struct; unionPeek type; UnmarshalJSON dispatches on "log"/"step"/"status"; MarshalJSON round-trip; hard error on unknown discriminator |
| `cli/internal/types/generated_test.go` | Go round-trip test, reads ws-fixtures, >= 3 test functions | VERIFIED | Exists (144 lines); TestJobMessageRoundTrip (3 sub-cases), TestDevicePreviewDecode, TestUnknownDiscriminator; fixturesDir wired to contracts/ws-fixtures relative path |
| `cli/Makefile` (types + types-check targets) | make types runs go-jsonschema; types-check exits non-zero on drift | VERIFIED | .PHONY line 5 lists types and types-check; types target runs extract-ws-schemas.mjs then go-jsonschema then gofmt then go mod tidy; types-check depends on types and runs git diff --quiet check |
| `web/src/lib/api/generated-types.ts` | Generated TS types from openapi.json, > 100 lines | VERIFIED | 2164 lines; auto-generated header; exports paths, components interfaces; Hook, JobSummary, DeviceList, ArtifactList, Pipeline, ApiKey, Webhook all present in components.schemas |
| `web/src/lib/api/__tests__/generated-types.test.ts` | Smoke test asserting Hook, JobSummary, DeviceList shapes | VERIFIED | Exists (74 lines); 4 tests: Hook satisfies check, JobSummary type-level + runtime, DeviceList mutual assignability, paths['/api/hooks']['post'] compile-time check |
| `web/src/lib/api/index.ts` | Re-exports generated-types.ts | VERIFIED | Line 12: export type { components, paths, operations } from './generated-types.js' |
| `vendor/device-stream/android-1.1.0.tgz` | Vendored tarball | VERIFIED | File exists in vendor/device-stream/ |
| `vendor/device-stream/core-1.1.0.tgz` | Vendored tarball | VERIFIED | File exists in vendor/device-stream/ |
| `vendor/device-stream/ios-simulator-1.1.0.tgz` | Vendored tarball | VERIFIED | File exists in vendor/device-stream/ |
| `scripts/check-generated.sh` | CI drift detection, executable | VERIFIED | Exists; -rwxr-xr-x permissions confirmed; regenerates all 4 files then git diff --quiet loop; exits 1 with named drift list on mismatch |
| `server/streaming/websocket-plugin.ts` | dependencies includes pool-plugin | VERIFIED | Line 174: dependencies: ['config', 'auth', 'pool-plugin'] |
| `server/api/plugin.ts` | dependencies includes lifecycle-plugin | VERIFIED | Line 51: dependencies array contains 'lifecycle-plugin' |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `package.json` | `vendor/device-stream/*.tgz` | file: protocol | WIRED | Lines 25-27 reference file:./vendor/device-stream/; tarballs exist |
| `cli/Makefile types` | `cli/internal/types/generated.go` | go-jsonschema CLI | WIRED | Target defined; references internal/types/generated.go output path |
| `cli/Makefile types` | `scripts/extract-ws-schemas.mjs` | node scripts/extract-ws-schemas.mjs | WIRED | Target runs node at repo root for schema extraction step |
| `cli/internal/types/generated_test.go` | `contracts/ws-fixtures/*.sample.json` | fixturesDir relative path | WIRED | fixturesDir = filepath.Join("..", "..", "..", "contracts", "ws-fixtures") |
| `web/src/lib/api/__tests__/generated-types.test.ts` | `web/src/lib/api/generated-types.ts` | import type { components, paths } | WIRED | Line 13: import type { components, paths } from '../generated-types.js' |
| `web/src/lib/api/index.ts` | `web/src/lib/api/generated-types.ts` | re-export | WIRED | Line 12: export type { components, paths, operations } from './generated-types.js' |
| `scripts/check-generated.sh` | all 4 generated files | npm run + make -C cli | WIRED | Enforces server/openapi.json, contracts/ws-messages.json, cli/internal/types/generated.go, web/src/lib/api/generated-types.ts |
| `server/streaming/websocket-plugin.ts` | `pool-plugin` | dependencies declaration | WIRED | Line 174 deps array includes pool-plugin |
| `server/api/plugin.ts` | `lifecycle-plugin` | dependencies declaration | WIRED | Line 51 deps array includes lifecycle-plugin |

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|---------|
| SPEC-06 | fastify-zod-openapi generating OpenAPI 3.1 spec from routes; spec committed in server/openapi.json | SATISFIED | server/openapi.json 1762 lines, OpenAPI 3.1.0, 14+ named schemas; REQUIREMENTS.md status: Complete |
| SPEC-07 | JSON Schema for WebSocket protocol exported in build (consumed by CLI Go + web) | SATISFIED | contracts/ws-messages.json generated (7 variants + __shared); go-jsonschema consumes it via make types; REQUIREMENTS.md status: Complete |
| CLI-01 | Build pipeline: Zod -> OpenAPI 3.1 -> go-jsonschema -> cli/internal/types/generated.go committed | SATISFIED | generated.go exists with go-jsonschema header; Makefile types target documented; REQUIREMENTS.md status: Complete |
| CLI-02 | make types regenerates and fails on diff; CI enforce via types-check | SATISFIED | Makefile types-check target: runs types then git diff --quiet with exit 1 on drift; scripts/check-generated.sh also runs make -C cli types; REQUIREMENTS.md status: Complete |
| CLI-03 | Discriminated unions in cli/internal/types/unions.go; hand-rolled UnmarshalJSON | SATISFIED | unions.go exists (91 lines); JobMessage with Log/Step/Status pointers; peek-then-dispatch UnmarshalJSON; REQUIREMENTS.md status: Complete |
| WEB-01 | openapi-typescript generates web/src/lib/api/generated-types.ts; committed; CI enforce | SATISFIED | generated-types.ts exists (2164 lines); web:types script in package.json; scripts/check-generated.sh enforces drift; REQUIREMENTS.md status: Complete |
| DEBT-01 | @device-stream/* published/vendored (file:../device-stream removed); installSimCapture works standalone | SATISFIED | file:../device-stream removed; file:./vendor/device-stream/*.tgz references; all 3 tarballs present in vendor/; REQUIREMENTS.md status: Complete |

**Orphaned requirements:** None. All 7 Phase 17 requirements are accounted for and satisfied.

### Anti-Patterns Found

None. No TODO/FIXME/placeholder blockers detected in the gap-closure files. The WS emit block that was previously commented out with TODO(17-02) has been activated (contracts/ws-messages.json now exists). The file:../device-stream references have been fully replaced with in-repo vendored paths.

### Human Verification Required

The following items cannot be verified by file inspection alone and require human confirmation before declaring the pipeline operationally proven end-to-end:

#### 1. Go round-trip test execution

**Test:** Run `make test` in cli/ directory (or `go test ./internal/types/...`)
**Expected:** generated_test.go passes — TestJobMessageRoundTrip decodes all 3 job variants without field loss; TestDevicePreviewDecode preserves correlationId through marshal cycle; TestUnknownDiscriminator returns an error containing "unknown discriminator"
**Why human:** Requires Go toolchain with go-jsonschema dependencies (mapstructure v2) installed and go.mod synced. Cannot confirm go.mod includes the new mapstructure import.

#### 2. Fresh-clone npm install validation

**Test:** In a directory without a sibling device-stream repo, run `npm install` from repo root
**Expected:** npm installs all three @device-stream/* packages from the vendored tarballs in vendor/device-stream/ without an ENOENT or file-not-found error
**Why human:** Verifying npm resolution from local tarballs requires running npm install; tarball integrity and package.json `name` field matching cannot be confirmed by file inspection alone.

#### 3. check-generated.sh end-to-end pass

**Test:** Run `DATABASE_URL=<dev-db> bash scripts/check-generated.sh`
**Expected:** All 3 regeneration steps complete without error; git diff --quiet passes for all 4 files; script exits 0
**Why human:** Requires live DATABASE_URL, go-jsonschema on PATH, and matching Node/Go toolchain versions. The script's correctness is verified by inspection but its runtime behavior requires execution.

### Re-verification Summary

All 4 gaps identified in the initial 2026-04-20 verification have been closed by plans 17-03 through 17-08:

1. **contracts/ws-messages.json** — Generated and committed. Contains all 7 WS variant schemas as JSON Schema draft-2020-12. The previously-commented WS emit block in build-openapi.ts has been activated.

2. **Go codegen pipeline** — cli/internal/types/ now contains generated.go (go-jsonschema output), unions.go (hand-rolled discriminator per ADR-003), and generated_test.go (round-trip harness against ws-fixtures). cli/Makefile has both `types` and `types-check` targets.

3. **@device-stream vendoring** — All three file:../device-stream references replaced with file:./vendor/device-stream/*.tgz. Tarballs (android-1.1.0.tgz, core-1.1.0.tgz, ios-simulator-1.1.0.tgz) are present in vendor/device-stream/.

4. **Plugin dep graph** — websocket-plugin.ts now declares pool-plugin as a dependency. api/plugin.ts now declares lifecycle-plugin as a dependency.

5. **scripts/check-generated.sh** — Created and executable. Enforces drift on all 4 generated files by regenerating them and running git diff --quiet.

The phase goal "lock the source-of-truth contract pipeline (Zod -> OpenAPI 3.1 -> Go + TS codegen) and clear v2.0 operational tech debt" is fully achieved. The pipeline is bidirectional: server Zod schemas emit OpenAPI 3.1 (SPEC-06), which drives both Go types (CLI-01/02/03) and web TS types (WEB-01), with WS JSON Schema as the intermediate (SPEC-07). DEBT-01 is resolved with vendored tarballs replacing the sibling-repo dependency.

---

_Verified: 2026-04-20T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
