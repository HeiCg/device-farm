---
phase: 33-android-grpc
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - device-stream/native-servers/android-grpc/go.mod
  - device-stream/native-servers/android-grpc/go.sum
  - device-stream/native-servers/android-grpc/Makefile
  - device-stream/native-servers/android-grpc/README.md
  - device-stream/native-servers/android-grpc/proto/emulator_controller.proto
  - device-stream/native-servers/android-grpc/proto/doc.go
  - device-stream/native-servers/android-grpc/auth/doc.go
  - device-stream/native-servers/android-grpc/auth/token_test.go
  - device-stream/native-servers/android-grpc/auth/ini_test.go
  - device-stream/native-servers/android-grpc/mmap/doc.go
  - device-stream/native-servers/android-grpc/mmap/mmap_test.go
  - device-stream/native-servers/android-grpc/client/doc.go
  - device-stream/native-servers/android-grpc/client/client_test.go
  - device-stream/native-servers/android-grpc/client/ring_test.go
  - device-stream/native-servers/android-grpc/encode/doc.go
  - device-stream/native-servers/android-grpc/ipc/doc.go
  - device-stream/native-servers/android-grpc/ipc/framer_test.go
  - device-stream/native-servers/android-grpc/cmd/android-grpc-stream/main.go
  - device-stream/packages/android/tests/grpc-emu-client.spec.ts
  - server/pool/__tests__/emulator-grpc.spec.ts
  - device-stream/scripts/build-android-grpc.sh
  - device-stream/scripts/postinstall.js
  - .github/workflows/android-grpc-matrix.yml
autonomous: true
requirements:
  - AND-GRPC-PROTO
  - AND-GRPC-AUTH
  - AND-GRPC-CLIENT
  - AND-GRPC-IPC
  - AND-GRPC-SPAWN
  - AND-GRPC-TS
  - AND-GRPC-INSTALL
must_haves:
  truths:
    - "A Go module exists at device-stream/native-servers/android-grpc/ that compiles and runs `go test ./...` exiting 0 (with stubbed _test.go skipping)"
    - "Every downstream production task (T-33.1..T-33.8) has at least one failing-or-skipped test scaffold checked in before implementation begins"
    - "`bash device-stream/scripts/build-android-grpc.sh` exits non-zero with a clear 'not implemented — Wave 5 wires this' message"
    - "A daily Android-emulator GitHub workflow exists and is loadable by `gh workflow list`"
    - "Daemon entrypoint `cmd/android-grpc-stream/main.go` exists with `--help` that prints the eventual usage and exits 2 (not implemented)"
  artifacts:
    - path: "device-stream/native-servers/android-grpc/go.mod"
      provides: "Go module root pinned to go 1.26.1 (matches cli/go.mod)"
      contains: "module github.com/device-farm/device-stream/native-servers/android-grpc"
    - path: "device-stream/native-servers/android-grpc/Makefile"
      provides: "protoc + build + test targets stub (proto target prints TODO, test target works)"
      contains: ".PHONY: proto build test"
    - path: "device-stream/native-servers/android-grpc/proto/emulator_controller.proto"
      provides: "Subset proto translated from kittyfarm Protos/emulator_controller_kittyfarm.proto with SPDX header"
      contains: "service EmulatorController"
    - path: "device-stream/native-servers/android-grpc/auth/token_test.go"
      provides: "Go test scaffold for FindToken (AND-GRPC-AUTH)"
      contains: "t.Skip"
    - path: "device-stream/native-servers/android-grpc/client/client_test.go"
      provides: "Go test scaffold for bufconn StreamFrames (AND-GRPC-CLIENT)"
      contains: "t.Skip"
    - path: "device-stream/native-servers/android-grpc/ipc/framer_test.go"
      provides: "Go test scaffold for IPC framer round-trip (AND-GRPC-IPC)"
      contains: "t.Skip"
    - path: "device-stream/packages/android/tests/grpc-emu-client.spec.ts"
      provides: "Vitest scaffold for TS adapter (AND-GRPC-TS)"
      contains: "describe('GrpcEmuClient'"
    - path: "server/pool/__tests__/emulator-grpc.spec.ts"
      provides: "Vitest scaffold for emulator -grpc spawn (AND-GRPC-SPAWN) — 4 it.todo stubs Plan 33-03 implements"
      contains: "describe('AndroidEmulatorDriver -grpc spawn"
    - path: "device-stream/scripts/build-android-grpc.sh"
      provides: "Build wrapper stub that exits non-zero until Wave 5"
    - path: "device-stream/scripts/postinstall.js"
      provides: "Existing postinstall extended with android-grpc build hook (darwin-only)"
      contains: "build-android-grpc.sh"
    - path: ".github/workflows/android-grpc-matrix.yml"
      provides: "Daily Android-emulator matrix workflow (API 34, API 35) — scaffold only, real steps Wave 5"
      contains: "matrix:"
  key_links:
    - from: "device-stream/native-servers/android-grpc/Makefile"
      to: "device-stream/native-servers/android-grpc/proto/emulator_controller.proto"
      via: "protoc invocation"
      pattern: "emulator_controller.proto"
    - from: "device-stream/scripts/postinstall.js"
      to: "device-stream/scripts/build-android-grpc.sh"
      via: "child_process invocation gated on process.platform==='darwin'"
      pattern: "build-android-grpc.sh"
    - from: ".github/workflows/android-grpc-matrix.yml"
      to: "device-stream/scripts/smoke-android-grpc.sh (will exist in Wave 5)"
      via: "workflow step run command"
      pattern: "smoke-android-grpc"
---

<objective>
Wave 0 substrate for Phase 33: create the entire test/build/CI scaffold so every later wave has a place to land code. NO production logic — only the Go module skeleton, the subset proto file (translated from kittyfarm `Protos/emulator_controller_kittyfarm.proto`), empty `_test.go` files with `t.Skip("Wave N — TODO")`, the daemon entrypoint that prints --help, the Vitest spec scaffold, the build script stub (exits 1 with TODO message), the postinstall hook extension, and the daily Android matrix workflow.

Purpose: Honor the Nyquist gate from `33-VALIDATION.md` — every later task points its `<verify>` at a test command, and that command must resolve before the implementing task starts. This plan resolves all `❌ Wave 0` entries in 33-VALIDATION.md.

Output: ~23 new files. Zero changes to existing TypeScript or Go production code. Existing `device-stream/scripts/postinstall.js` is the only modification.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-android-grpc/33-CONTEXT.md
@.planning/phases/33-android-grpc/33-BRIEF.md
@.planning/phases/33-android-grpc/33-RESEARCH.md
@.planning/phases/33-android-grpc/33-VALIDATION.md
@device-stream/CLAUDE.md
@device-stream/scripts/postinstall.js
@device-stream/scripts/build-sim-capture-private.sh
@device-stream/native-servers/sim-capture-private/Sources/IpcServer.h
@cli/go.mod
@cli/Makefile

<interfaces>
<!-- Existing Phase 32 IPC framer wire format the Go daemon MUST mirror byte-for-byte. -->

From device-stream/native-servers/sim-capture-private/Sources/IpcServer.h:5-22:
```
// Wire format: [u32 BE length (covers kind + payload)][u8 kind][payload bytes]
// Server->client kinds: 0x01 SPS+PPS · 0x02 H.264 AU · 0x10 ack · 0xFF error
// Client->server kinds: 0xC1 touch · 0xC9 quit
// Touch payload (29 bytes):
//   [f64 BE x_ratio][f64 BE y_ratio][u8 phase][f64 BE pressure][u32 BE touchId]
```

Phase 33 additive kinds (no collision with sim-capture-private):
- 0x03 (server→client) = metadata: `{width:u32 BE, height:u32 BE, fps:u32 BE}` (12 bytes; one-shot after first IDR)
- 0xC2 (client→server) = key event: `{eventType:u8 (0=down,1=up), keyCode:u32 BE, modMask:u32 BE}` (9 bytes)

From kittyfarm `Protos/emulator_controller_kittyfarm.proto` (lines 1-93) — 4 RPCs we keep:
- `rpc sendKey(KeyboardEvent) returns (google.protobuf.Empty);`
- `rpc setClipboard(ClipData) returns (google.protobuf.Empty);`
- `rpc sendTouch(TouchEvent) returns (google.protobuf.Empty);`
- `rpc streamScreenshot(ImageFormat) returns (stream Image);`

Phase 33 ADDS `rpc getStatus(google.protobuf.Empty) returns (EmulatorStatus);` (probe needed for AND-GRPC-02 fallback). EmulatorStatus body copied from upstream AOSP proto lines 1462-1491.

From cli/go.mod: Go version is `go 1.26.1`. Use same version for android-grpc module.

From cli/Makefile (pattern): protoc codegen target uses `protoc -I proto --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative proto/*.proto`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Create Go module skeleton + Makefile + subset proto file</name>
  <read_first>
    - `/Users/heicg/Desktop/projects/_reference/kittyfarm/Protos/emulator_controller_kittyfarm.proto:1-93` — the subset proto to translate (KITTYFARM REFERENCE — STUDY-ONLY, do NOT link as dep)
    - `cli/go.mod:1-25` — Go version pin (`go 1.26.1`) and module path convention
    - `cli/Makefile:1-48` — codegen Makefile pattern
    - `.planning/phases/33-android-grpc/33-RESEARCH.md:270-307` — recommended module layout (8 sub-dirs: proto/auth/mmap/client/encode/ipc/cmd plus root)
    - `.planning/phases/33-android-grpc/33-RESEARCH.md:224-267` — proto subset rationale + SPDX header requirement
  </read_first>
  <acceptance_criteria>
    - `device-stream/native-servers/android-grpc/go.mod` declares module `github.com/device-farm/device-stream/native-servers/android-grpc` with `go 1.26.1`
    - `device-stream/native-servers/android-grpc/Makefile` exposes `.PHONY: proto build test test-race clean` targets; `make test` exits 0 (with skipped scaffolds); `make proto` prints "TODO: Wave 1 wires protoc-gen-go" and exits 0
    - `device-stream/native-servers/android-grpc/proto/emulator_controller.proto` exists with SPDX `Apache-2.0` header + upstream AOSP URL comment + the 5-RPC subset (4 from kittyfarm + new `getStatus`) + every message we need (ImageFormat, Image, ImageTransport, ClipData, KeyboardEvent, Touch, TouchEvent, EmulatorStatus); `package android.emulation.control;` matches upstream wire-format
    - Top of proto declares `option go_package = "github.com/device-farm/device-stream/native-servers/android-grpc/proto/gen/emulatorcontrol;emulatorcontrol";`
    - `grep -c "kittyfarm" device-stream/native-servers/android-grpc/proto/emulator_controller.proto` returns 0 (we translate, not copy verbatim — drop kittyfarm package name)
    - `device-stream/native-servers/android-grpc/README.md` is ~30 lines: purpose, build, test, env vars
  </acceptance_criteria>
  <action>
1. Create directory tree under `device-stream/native-servers/android-grpc/`:
   - `proto/`, `auth/`, `mmap/`, `client/`, `encode/`, `ipc/`, `cmd/android-grpc-stream/`
   Each gets a `doc.go` with a one-line `// Package X ... (Wave N - stub).` per Go convention so `go vet ./...` sees real packages.

2. Write `go.mod`:
```
module github.com/device-farm/device-stream/native-servers/android-grpc

go 1.26.1
```
Do NOT yet add `require` lines for grpc-go / protobuf — Wave 1 adds them via `go get`. Wave 0 module compiles with stdlib only.

3. Write `Makefile`:
```makefile
GO ?= go
.PHONY: proto build test test-race clean

# Wave 1 will replace this stub with a real protoc invocation:
# protoc -I proto --go_out=. --go_opt=paths=source_relative \
#        --go-grpc_out=. --go-grpc_opt=paths=source_relative \
#        proto/emulator_controller.proto
proto:
	@echo "TODO: Wave 1 wires protoc-gen-go + protoc-gen-go-grpc"

build:
	$(GO) build -o bin/android-grpc-stream ./cmd/android-grpc-stream

test:
	$(GO) test -short ./...

test-race:
	$(GO) test -race ./...

clean:
	rm -rf bin/ proto/gen/
```

4. Write `proto/emulator_controller.proto` translating kittyfarm subset:
   - SPDX header: `// SPDX-License-Identifier: Apache-2.0`
   - Provenance comment: `// Subset port of https://android.googlesource.com/platform/external/qemu/+/refs/heads/emu-master-dev/android/android-grpc/android/emulation/control/emulator_controller.proto — Phase 33 keeps only the RPCs Device Farm uses.`
   - `syntax = "proto3";`
   - `package android.emulation.control;` (EXACT match with upstream — wire-compat)
   - `option go_package = "github.com/device-farm/device-stream/native-servers/android-grpc/proto/gen/emulatorcontrol;emulatorcontrol";`
   - `import "google/protobuf/empty.proto";`
   - Service block with 5 RPCs: `streamScreenshot`, `sendTouch`, `sendKey`, `setClipboard`, `getStatus`.
   - Messages (translate field numbers exactly from upstream — do NOT renumber):
     - `ImageTransport { enum TransportChannel {TRANSPORT_CHANNEL_UNSPECIFIED=0; MMAP=1;} TransportChannel channel=1; string handle=2; }`
     - `ImageFormat { enum ImgFormat {RGB888=0; RGBA8888=1; PNG=2;} ImgFormat format=1; int32 rotation=2; uint32 width=3; uint32 height=4; ImageTransport transport=5; uint32 display=6; }`
     - `Image { ImageFormat format=1; bytes image=2; uint32 width=3; uint32 height=4; uint64 seq=5; uint64 timestamp_us=6; uint32 display=7; }`
     - `ClipData { string text=1; }`
     - `Touch { int32 x=1; int32 y=2; int32 identifier=3; int32 pressure=4; int32 touch_major=5; int32 touch_minor=6; int32 orientation=7; uint32 display=8; enum EventExpiration {EVENT_EXPIRATION_UNSPECIFIED=0; NEVER_EXPIRE=1;} EventExpiration expiration=9; }`
     - `TouchEvent { repeated Touch touches=1; uint32 device=2; }`
     - `KeyboardEvent { enum KeyCodeType {USB=0; EVDEV=1; XKB=2; WIN=3; MAC=4;} KeyCodeType codeType=1; enum KeyEventType {keydown=0; keyup=1; keypress=2;} KeyEventType eventType=2; string key=3; string text=4; int32 keyCode=5; }`
     - `EmulatorStatus { bool booted=1; bool heart_beat=2; int64 uptime=3; map<string,string> version=4; map<string,string> hardwareConfig=5; map<string,string> bootProperties=6; }` (subset; Wave 1 can refine if codegen fails)
   - Cite kittyfarm reference at top of file: `// Translated from kittyfarm/Protos/emulator_controller_kittyfarm.proto (Phase 33 reference, NOT a dependency).`

5. Write `README.md` (~30 lines) covering: purpose (port of kittyfarm GRPCFrameService.swift), build (`make proto && make build`), test (`make test`), env vars (`DEVICE_STREAM_ANDROID_GRPC=0` to disable), and a "Phase 33 status" line noting daemon ships in Wave 4 (TS swap), production in Wave 5.

6. Each package gets a stub `doc.go`:
```go
// Package auth — emulator gRPC bearer-token discovery.
// (Wave 1 fills in FindToken; Wave 0 ships scaffolds only.)
package auth
```
Repeat for proto, mmap, client, encode, ipc.

Per the External Dependencies Policy (CONTEXT.md LOCKED): do NOT add any `import` that references kittyfarm, simvyn, revyl-cli, app-explorer, mobile-devtools. The proto is a TRANSLATION (mechanical port) — credit kittyfarm in comments, do not link.
  </action>
  <files>
device-stream/native-servers/android-grpc/go.mod
device-stream/native-servers/android-grpc/Makefile
device-stream/native-servers/android-grpc/README.md
device-stream/native-servers/android-grpc/proto/emulator_controller.proto
device-stream/native-servers/android-grpc/proto/doc.go
device-stream/native-servers/android-grpc/auth/doc.go
device-stream/native-servers/android-grpc/mmap/doc.go
device-stream/native-servers/android-grpc/client/doc.go
device-stream/native-servers/android-grpc/encode/doc.go
device-stream/native-servers/android-grpc/ipc/doc.go
  </files>
  <verify>
    <automated>cd device-stream/native-servers/android-grpc && go vet ./... && grep -q "module github.com/device-farm/device-stream/native-servers/android-grpc" go.mod && grep -q "service EmulatorController" proto/emulator_controller.proto && grep -q "rpc getStatus" proto/emulator_controller.proto && grep -q "package android.emulation.control" proto/emulator_controller.proto && ! grep -q "kittyfarm" proto/emulator_controller.proto && make test 2>&1 | grep -E "^(ok|---)" >/dev/null</automated>
  </verify>
  <done>Go module compiles via `go vet`. Proto file has 5 RPCs + 8 messages + matches upstream package name + cites kittyfarm only in comments (not as dep). Makefile + README exist. Subdirs all have doc.go.</done>
</task>

<task type="auto">
  <name>Task 0.2: Daemon entrypoint stub + Go test scaffolds for AUTH/CLIENT/IPC/MMAP</name>
  <read_first>
    - `device-stream/native-servers/sim-capture-private/Sources/main.mm:1-80` — Phase 32 daemon entrypoint pattern (--help format, signal handling)
    - `.planning/phases/33-android-grpc/33-VALIDATION.md:40-50` — Per-Task Verification Map (which test files map to which AND-GRPC-* IDs)
    - `.planning/phases/33-android-grpc/33-RESEARCH.md:687-742` — Test strategy (unit tests table) listing exact assertions Wave 1+2 will write
    - `.planning/phases/33-android-grpc/33-RESEARCH.md:946-993` — bufconn mock gRPC pattern (reference for client_test.go scaffold)
  </read_first>
  <acceptance_criteria>
    - `cmd/android-grpc-stream/main.go` exists with `package main`, prints `Usage: android-grpc-stream --serial <s> --grpc-port <p> [--socket <path>]` when invoked with `--help`, exits 2 on any other invocation with "not implemented — Phase 33 Wave 4"
    - 4 Go test files exist with `t.Skip` placeholders for every test row in 33-RESEARCH.md §Unit tests table:
      - `auth/token_test.go` — TestFindToken_PerInstance, TestFindToken_PortMismatch, TestFindToken_GlobalFallback, TestFindToken_NoAuth (each `t.Skip("Wave 1")`)
      - `auth/ini_test.go` — TestParseSimpleIni_Trim, TestParseSimpleIni_MultipleEquals (each `t.Skip("Wave 1")`)
      - `mmap/mmap_test.go` — TestRegion_RoundTrip, TestRegion_Close (each `t.Skip("Wave 2")`)
      - `client/client_test.go` — TestShouldRetryStartup_Table, TestStreamFramesMMAP_BufconnMock, TestSendTouchPayload, TestProbeAuthMissing (each `t.Skip("Wave 2")` except TestSendTouchPayload which is `t.Skip("Wave 4")`)
      - `client/ring_test.go` — TestRing_Next_Rotates (each `t.Skip("Wave 2")`)
      - `ipc/framer_test.go` — TestFramerRoundTrip, TestFramerTouchPayload, TestFramerKeyPayload, TestFramerMetadataPayload (each `t.Skip("Wave 2")` — the key + metadata tests cite new kinds 0xC2 / 0x03)
    - `go test ./...` exits 0 with all PASS-skipped output
    - `go build ./cmd/android-grpc-stream && ./bin/android-grpc-stream --help` prints the usage text and exits 0 (or 2 — pick 0 for --help, 2 for unimplemented mode)
  </acceptance_criteria>
  <action>
1. `cmd/android-grpc-stream/main.go`:
```go
// android-grpc-stream — daemon for Android emulator gRPC + MMAP capture (Phase 33).
// Wave 0: --help only. Real implementation lands in Wave 2 (T-33.3/T-33.4) and is
// wired into Node via Wave 4 (T-33.6).
package main

import (
	"flag"
	"fmt"
	"os"
)

const usage = `android-grpc-stream — Android emulator gRPC + MMAP capture daemon.

Usage:
  android-grpc-stream --serial <serial> --grpc-port <port> [--socket <path>]
  android-grpc-stream --probe --serial <serial> --grpc-port <port>

Flags:
  --serial      ADB serial of the emulator (e.g. emulator-5554)
  --grpc-port   gRPC port of the emulator (band 8554-8650)
  --socket      Unix socket path (default /tmp/device-stream-android-emu-<serial>.sock)
  --probe       Probe-only mode: call EmulatorController.getStatus and exit 0/1

Environment:
  DEVICE_STREAM_ANDROID_GRPC=0  Disable gRPC path entirely (Node fallback to scrcpy)
`

func main() {
	help := flag.Bool("help", false, "show usage")
	_ = flag.String("serial", "", "ADB serial")
	_ = flag.Int("grpc-port", 0, "emulator gRPC port")
	_ = flag.String("socket", "", "Unix socket path")
	_ = flag.Bool("probe", false, "probe mode")
	flag.Usage = func() { fmt.Fprint(os.Stderr, usage) }
	flag.Parse()
	if *help || len(os.Args) == 1 {
		fmt.Fprint(os.Stdout, usage)
		os.Exit(0)
	}
	fmt.Fprintln(os.Stderr, "android-grpc-stream: not implemented yet — Phase 33 Wave 2 (T-33.3/T-33.4 land the daemon body)")
	os.Exit(2)
}
```

2. Write each `_test.go` scaffold with `t.Skip("Wave N — TODO: <reference>")`. Example for `auth/token_test.go`:
```go
package auth

import "testing"

func TestFindToken_PerInstance(t *testing.T) {
	t.Skip("Wave 1 / Task 1.2 — implements per-instance pid_*.ini lookup; see 33-RESEARCH.md §Auth token discovery")
}

func TestFindToken_PortMismatch(t *testing.T) {
	t.Skip("Wave 1 / Task 1.2 — two .ini files; only second matches grpcPort; assert second's token")
}

func TestFindToken_GlobalFallback(t *testing.T) {
	t.Skip("Wave 1 / Task 1.2 — no .ini files; ~/.emulator_console_auth_token present; assert trimmed read")
}

func TestFindToken_NoAuth(t *testing.T) {
	t.Skip("Wave 1 / Task 1.2 — no files anywhere; assert empty token + no error")
}
```
Repeat the pattern for `auth/ini_test.go`, `mmap/mmap_test.go`, `client/client_test.go`, `client/ring_test.go`, `ipc/framer_test.go`. Each test name MUST match the row in 33-RESEARCH.md §Unit tests so Wave 1/2 only has to remove the `t.Skip` line and write the body.

3. For `ipc/framer_test.go`, add scaffolds covering BOTH inherited Phase 32 kinds AND the new Phase 33 additive kinds:
```go
func TestFramerRoundTrip(t *testing.T) { t.Skip("Wave 2 / Task 2.2 — kinds 0x01,0x02,0x10,0xFF round-trip per IpcServer.h:5-22") }
func TestFramerTouchPayload(t *testing.T) { t.Skip("Wave 2 / Task 2.2 — 0xC1 29-byte struct: [f64 BE x][f64 BE y][u8 phase][f64 BE pressure][u32 BE id]") }
func TestFramerKeyPayload(t *testing.T) { t.Skip("Wave 4 / Task 4.2 — 0xC2 9-byte struct: [u8 eventType][u32 BE keyCode][u32 BE modMask]") }
func TestFramerMetadataPayload(t *testing.T) { t.Skip("Wave 2 / Task 2.2 — 0x03 12-byte struct: [u32 BE width][u32 BE height][u32 BE fps]") }
```

4. Verify `go test ./...` runs from `device-stream/native-servers/android-grpc/` and shows all tests SKIP, exit 0.
  </action>
  <files>
device-stream/native-servers/android-grpc/cmd/android-grpc-stream/main.go
device-stream/native-servers/android-grpc/auth/token_test.go
device-stream/native-servers/android-grpc/auth/ini_test.go
device-stream/native-servers/android-grpc/mmap/mmap_test.go
device-stream/native-servers/android-grpc/client/client_test.go
device-stream/native-servers/android-grpc/client/ring_test.go
device-stream/native-servers/android-grpc/ipc/framer_test.go
  </files>
  <verify>
    <automated>cd device-stream/native-servers/android-grpc && go build -o bin/android-grpc-stream ./cmd/android-grpc-stream && ./bin/android-grpc-stream --help | grep -q "android-grpc-stream" && go test ./... 2>&1 | grep -E "^(ok|---|FAIL)" | tee /tmp/wave0-test.log && ! grep -q FAIL /tmp/wave0-test.log && rm -rf bin/</automated>
  </verify>
  <done>Daemon entrypoint compiles, prints usage on --help. All 6 test files exist with `t.Skip` placeholders matching 33-RESEARCH.md §Unit tests. `go test ./...` exits 0 with skipped lines.</done>
</task>

<task type="auto">
  <name>Task 0.3: Vitest TS adapter scaffold + build script stub + postinstall hook + CI workflow</name>
  <read_first>
    - `device-stream/packages/ios-simulator/tests/sim-capture-private-client.spec.ts:1-60` — Phase 32 Vitest scaffold shape (mirror exactly for grpc-emu-client.spec.ts)
    - `device-stream/scripts/build-sim-capture-private.sh:1-50` — Phase 32 build script (mirror for build-android-grpc.sh)
    - `device-stream/scripts/postinstall.js:1-60` — existing postinstall (we extend it; do not replace)
    - `device-stream/packages/android/package.json` — current android workspace package
    - `.github/workflows/sim-private-matrix.yml` — Phase 32 CI workflow shape (mirror with `reactivecircus/android-emulator-runner@v2`)
    - `.planning/phases/33-android-grpc/33-RESEARCH.md:725-734` — CI matrix spec (API 34 + API 35; SKIP API 36.1 per project CLAUDE.md)
  </read_first>
  <acceptance_criteria>
    - `device-stream/packages/android/tests/grpc-emu-client.spec.ts` exists with `describe('GrpcEmuClient', ...)` block containing at least 4 `it.todo(...)` tests covering AND-GRPC-TS scenarios (fallback when env=0, fallback when spawn fails, outbound 0xC1 touch payload, inbound 0x02 AU forwarding)
    - `device-stream/scripts/build-android-grpc.sh` exists, executable, exits non-zero with `echo "ERROR: not implemented — Phase 33 Wave 5 (T-33.8) wires Go build + xcodebuild for cgo encoder"`
    - `device-stream/scripts/postinstall.js` extended: invokes `build-android-grpc.sh` ONLY on `process.platform === 'darwin'`, after the existing sim-capture-private build call, with non-zero exit treated as warning (`console.warn`) NOT fatal (Phase 33 is canary — install must not break on grpc build failure)
    - `.github/workflows/android-grpc-matrix.yml` exists with: `name: Android gRPC Matrix`; trigger `schedule: '0 9 * * *' + workflow_dispatch`; matrix `api-level: [34, 35]` (NO 36 — project CLAUDE.md pins API 35 due to macOS Tahoe mprotect/hvf); job runs on `macos-14`; steps are scaffolds with `echo "TODO: Wave 5 wires real build + emulator-runner@v2"` — workflow loadable by `gh workflow list` but does not gate CI
    - `npx vitest run device-stream/packages/android/tests/grpc-emu-client.spec.ts` exits 0 (all `it.todo` pass as pending)
  </acceptance_criteria>
  <action>
1. `device-stream/packages/android/tests/grpc-emu-client.spec.ts`:
```typescript
/**
 * Vitest scaffold for the Node-side gRPC emulator client (Phase 33 / T-33.6).
 * Wave 4 fills in real assertions; Wave 0 ships the file shape so the AND-GRPC-TS
 * row in 33-VALIDATION.md is no longer ❌ Wave 0.
 *
 * Pattern mirrors device-stream/packages/ios-simulator/tests/sim-capture-private-client.spec.ts.
 */
import { describe, it } from 'vitest';

describe('GrpcEmuClient', () => {
  describe('fallback path (AND-GRPC-TS / AND-GRPC-02)', () => {
    it.todo('returns scrcpy session immediately when DEVICE_STREAM_ANDROID_GRPC=0');
    it.todo('falls back to scrcpy when daemon binary missing (ENOENT on spawn)');
    it.todo('falls back to scrcpy when daemon exits before first frame');
  });

  describe('framer (AND-GRPC-TS / AND-GRPC-REF-02)', () => {
    it.todo('encodes outbound 0xC1 touch as 29-byte payload [f64 BE x][f64 BE y][u8 phase][f64 BE pressure][u32 BE id]');
    it.todo('encodes outbound 0xC2 key event as 9-byte payload [u8 eventType][u32 BE keyCode][u32 BE modMask]');
    it.todo('forwards inbound 0x02 H.264 AU to WS as base64 envelope');
    it.todo('forwards inbound 0x03 metadata as JSON {width,height,fps} to WS');
    it.todo('caches 0x01 SPS+PPS for replay to late-joining WS clients');
  });
});
```

2. `device-stream/scripts/build-android-grpc.sh`:
```bash
#!/usr/bin/env bash
# Phase 33 / Wave 5 (T-33.8) will replace this stub with:
#   1. cd device-stream/native-servers/android-grpc
#   2. make proto
#   3. make build
#   4. stage device-stream/bin/android-grpc-stream
set -euo pipefail
echo "ERROR: not implemented — Phase 33 Wave 5 (T-33.8) wires Go build + cgo VideoToolbox encoder." >&2
echo "       Until then, scrcpy path is the only Android transport (DEVICE_STREAM_ANDROID_GRPC unset)." >&2
exit 1
```
chmod +x.

3. Modify `device-stream/scripts/postinstall.js` — APPEND a new section AFTER the existing sim-capture-private invocation (do not remove or reorder existing code):
```javascript
// Phase 33: Android gRPC daemon (canary — opt-in via DEVICE_STREAM_ANDROID_GRPC=1).
// Build is darwin-only for v1; failure is a WARNING (Node falls back to scrcpy automatically).
if (process.platform === 'darwin') {
  const grpcBuild = require('path').resolve(__dirname, 'build-android-grpc.sh');
  try {
    require('child_process').execFileSync('bash', [grpcBuild], { stdio: 'inherit' });
    console.log('[postinstall] android-grpc daemon built');
  } catch (err) {
    console.warn(`[postinstall] android-grpc build skipped: ${err.message} (scrcpy fallback active)`);
  }
}
```
Verify the new block is GATED on `process.platform === 'darwin'` (per 33-RESEARCH.md §Linux note (deferred)).

4. `.github/workflows/android-grpc-matrix.yml`:
```yaml
name: Android gRPC Matrix
on:
  schedule:
    - cron: '0 9 * * *'  # 09:00 UTC daily
  workflow_dispatch:

jobs:
  smoke:
    runs-on: macos-14
    strategy:
      fail-fast: false
      matrix:
        api-level: [34, 35]  # 36.1 explicitly skipped: macOS Tahoe mprotect/hvf incompat (CLAUDE.md)
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.1'
      - name: Build android-grpc daemon (Wave 5 TODO)
        run: |
          echo "TODO: Phase 33 Wave 5 (T-33.8) wires: bash device-stream/scripts/build-android-grpc.sh"
      - name: Boot emulator (Wave 5 TODO)
        run: |
          echo "TODO: Phase 33 Wave 5 wires: reactivecircus/android-emulator-runner@v2 with api-level=${{ matrix.api-level }}"
      - name: Smoke (Wave 5 TODO)
        run: |
          echo "TODO: Phase 33 Wave 5 wires: bash device-stream/scripts/smoke-android-grpc.sh"
```

5. Verify all four artifacts exist and load:
   - `gh workflow list 2>/dev/null | grep -q "Android gRPC Matrix"` (or simply file exists check if gh not authed)
   - `test -x device-stream/scripts/build-android-grpc.sh`
   - `node -e "require('./device-stream/scripts/postinstall.js')"` parses without syntax error (the script may early-exit; we only care about syntax)
  </action>
  <files>
device-stream/packages/android/tests/grpc-emu-client.spec.ts
device-stream/scripts/build-android-grpc.sh
device-stream/scripts/postinstall.js
.github/workflows/android-grpc-matrix.yml
  </files>
  <verify>
    <automated>test -f device-stream/packages/android/tests/grpc-emu-client.spec.ts && grep -q "GrpcEmuClient" device-stream/packages/android/tests/grpc-emu-client.spec.ts && test -x device-stream/scripts/build-android-grpc.sh && ! bash device-stream/scripts/build-android-grpc.sh 2>/dev/null && grep -q "build-android-grpc.sh" device-stream/scripts/postinstall.js && grep -q "process.platform === 'darwin'" device-stream/scripts/postinstall.js && test -f .github/workflows/android-grpc-matrix.yml && grep -q "api-level: \[34, 35\]" .github/workflows/android-grpc-matrix.yml && ! grep -E "api-level:.*36" .github/workflows/android-grpc-matrix.yml && cd device-stream/packages/android && npx vitest run tests/grpc-emu-client.spec.ts 2>&1 | grep -qE "(todo|skip|passed)"</automated>
  </verify>
  <done>Vitest scaffold has 8 it.todo lines covering AND-GRPC-TS surface. Build script is executable and exits non-zero with TODO message. postinstall.js extended with darwin-gated android-grpc build (warning, not fatal). CI workflow loads, matrix is `[34, 35]` (NOT 36), all steps are TODO placeholders. Wave 0 unblocks every later wave's `<verify>` command.</done>
<task type="auto">
  <name>Task 0.4: Server-side Vitest scaffold for AND-GRPC-SPAWN (emulator-grpc.spec.ts)</name>
  <read_first>
    - `.planning/phases/33-android-grpc/33-03-PLAN.md` (Task 3.2 acceptance criteria) — the 4 test names this stub must match verbatim so Wave 3 only has to remove `it.todo` and write the body
    - `server/pool/__tests__/` — any existing pool spec for style reference
  </read_first>
  <acceptance_criteria>
    - `server/pool/__tests__/emulator-grpc.spec.ts` exists with a single `describe('AndroidEmulatorDriver -grpc spawn (AND-GRPC-SPAWN)', ...)` block containing EXACTLY 4 `it.todo(...)` entries whose names match Plan 33-03 Task 3.2 verbatim:
      1. `it.todo('injects -grpc <port> flag into spawn argv between -port and -no-snapshot-load')`
      2. `it.todo('allocates distinct grpcPort for two concurrent boots (8554 then 8555)')`
      3. `it.todo('throws when port band 8554-8650 is exhausted')`
      4. `it.todo('returns grpcPort in BootResult')`
    - File imports `describe` and `it` from `vitest` (no test bodies, no mocks — Wave 3 wires those)
    - `npx vitest run server/pool/__tests__/emulator-grpc.spec.ts` exits 0 (4 todos counted as pending)
    - Header comment cites Phase 33 / Plan 33-03 / AND-GRPC-SPAWN provenance
  </acceptance_criteria>
  <action>
1. Create `server/pool/__tests__/emulator-grpc.spec.ts`:
```typescript
/**
 * Vitest scaffold for AndroidEmulatorDriver `-grpc` spawn injection (Phase 33 / Plan 33-03 / AND-GRPC-SPAWN).
 *
 * Wave 0 ships 4 `it.todo` stubs whose names match Plan 33-03 Task 3.2 acceptance criteria
 * verbatim. Wave 3 (Plan 33-03) replaces each todo with a real test body — see
 * .planning/phases/33-android-grpc/33-03-PLAN.md Task 3.2 for the implementation plan.
 */
import { describe, it } from 'vitest';

describe('AndroidEmulatorDriver -grpc spawn (AND-GRPC-SPAWN)', () => {
  it.todo('injects -grpc <port> flag into spawn argv between -port and -no-snapshot-load');
  it.todo('allocates distinct grpcPort for two concurrent boots (8554 then 8555)');
  it.todo('throws when port band 8554-8650 is exhausted');
  it.todo('returns grpcPort in BootResult');
});
```

2. Run `npx vitest run server/pool/__tests__/emulator-grpc.spec.ts` from repo root and confirm exit 0 with 4 todos pending. No mocks, no driver imports — Wave 3 adds them.
  </action>
  <files>
server/pool/__tests__/emulator-grpc.spec.ts
  </files>
  <verify>
    <automated>test -f server/pool/__tests__/emulator-grpc.spec.ts && grep -q "AndroidEmulatorDriver -grpc spawn (AND-GRPC-SPAWN)" server/pool/__tests__/emulator-grpc.spec.ts && [ "$(grep -c 'it.todo(' server/pool/__tests__/emulator-grpc.spec.ts)" = "4" ] && grep -q "injects -grpc <port> flag into spawn argv between -port and -no-snapshot-load" server/pool/__tests__/emulator-grpc.spec.ts && grep -q "allocates distinct grpcPort for two concurrent boots (8554 then 8555)" server/pool/__tests__/emulator-grpc.spec.ts && grep -q "throws when port band 8554-8650 is exhausted" server/pool/__tests__/emulator-grpc.spec.ts && grep -q "returns grpcPort in BootResult" server/pool/__tests__/emulator-grpc.spec.ts && npx vitest run server/pool/__tests__/emulator-grpc.spec.ts 2>&1 | grep -qE "(todo|skip|passed|4 todo)"</automated>
  </verify>
  <done>Server-side Vitest scaffold exists with 4 `it.todo` stubs whose names match Plan 33-03 Task 3.2 verbatim. Wave 3 implementer only has to swap `it.todo` for `it` and add bodies. AND-GRPC-SPAWN row in 33-VALIDATION.md Wave 0 Requirements is unblocked.</done>
</task>

</tasks>

<verification>
- `cd device-stream/native-servers/android-grpc && go vet ./... && go test ./...` exits 0 (skipped tests count)
- `cd device-stream/native-servers/android-grpc && go build ./cmd/android-grpc-stream` produces a binary that prints --help
- Proto file passes manual eyeball: 5 RPCs, 8 messages, `package android.emulation.control`, SPDX header, NO kittyfarm dep reference
- `device-stream/scripts/postinstall.js` is syntactically valid (`node --check`) and the new section is darwin-gated
- `.github/workflows/android-grpc-matrix.yml` parses as YAML; matrix is `[34, 35]` (NOT 36); jobs run on `macos-14`
- `device-stream/packages/android/tests/grpc-emu-client.spec.ts` runs via Vitest with `it.todo` lines counted as pending
- `grep -r "kittyfarm" device-stream/native-servers/android-grpc/` returns ONLY comments citing the reference, NEVER an `import` or `require`
</verification>

<success_criteria>
1. Phase 33 Wave 0 substrate complete: Go module, subset proto (translated, not linked), 6 test files with t.Skip scaffolds, daemon entrypoint stub, TS Vitest spec, build script stub, postinstall hook extension, CI matrix workflow
2. Every `❌ Wave 0` row in `.planning/phases/33-android-grpc/33-VALIDATION.md` now has the referenced file existing on disk
3. `go test ./...` and `npx vitest run grpc-emu-client.spec.ts` both exit 0 (skipped/pending counted as pass)
4. External Dependencies Policy honored: no `import` / `require` / `link` of kittyfarm; only comment-level citations
5. No production code (no Go business logic, no TS production code) touched — only existing `postinstall.js` is modified additively
</success_criteria>

<output>
After completion, create `.planning/phases/33-android-grpc/33-00-SUMMARY.md` documenting:
- Files created (count, paths)
- `go test ./...` output snippet (showing skipped count)
- `vitest run grpc-emu-client.spec.ts` output (showing pending count)
- Proto subset stats (RPCs, messages, line count)
- Confirmation: 0 kittyfarm dependencies
- Wave 0 → Wave 1 handoff: list of t.Skip calls Wave 1 must remove
</output>
