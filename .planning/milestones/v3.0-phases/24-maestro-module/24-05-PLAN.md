---
phase: 24
plan: 05
type: execute
wave: 5
depends_on: [24-04]
files_modified:
  - server/maestro/MODULE.md
  - server/maestro/index.ts
  - server/maestro/__tests__/hierarchy-service.spec.ts
  - server/maestro/__tests__/appium-service.spec.ts
  - server/__tests__/plugin-order.spec.ts
  - .planning/phases/24-maestro-module/deferred-items.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MOD-01, MOD-02, MOD-04]
estimated_minutes: 35
gap_closure: false
must_haves:
  truths:
    - "`server/maestro/MODULE.md` REPLACES Plan 24-00 Purpose-only placeholder with full 9 H2 sections in canonical order: `## Purpose` / `## Public API` / `## Events Emitted` / `## Events Consumed` / `## Queue Produced` / `## Queue Consumed` / `## Invariants` / `## Non-Goals` / `## Dependencies`. Plus optional H3 Runnable Example section. Queue Produced + Queue Consumed sections explicitly say `None.` (no-queue module per CONTEXT lock). Every H2 heading exact-matches the regex `^## (Purpose|Public API|Events Emitted|Events Consumed|Queue Produced|Queue Consumed|Invariants|Non-Goals|Dependencies)$` for grep-validation in plugin-order.spec extension."
    - "MODULE.md §Purpose: maestro integration (hierarchy fetching, device-info collection, Appium driver lifecycle); subscribes to device.booted for boot-time metadata refresh. §Public API: maestroPlugin default; createMaestroModule + MaestroModule + CreateMaestroModuleDeps factory types; back-compat HierarchyService/AppiumService/DeviceInfoCollector classes (3 decorator surfaces preserved); events surface (maestroRegistry/MAESTRO_EVENT_NAMES/MAESTRO_AGGREGATE_ID/makeMaestroEmitters + 2 payload schemas). §Events Emitted: maestro.hierarchy.fetched (transient), maestro.device-info.collected (transient). §Events Consumed: device.booted (from poolModule.bus via fastify.poolModule.bus.on; deferred to onReady per Pitfall 2). §Queue Produced: None. §Queue Consumed: None. §Invariants: (1) cross-module subscription deferred to onReady; (2) device.booted handler swallows DeviceInfoCollector failures (no re-throw); (3) Device.metadata mutation happens BEFORE the maestro.device-info.collected emission; (4) shutdown is idempotent; (5) no producer outside the maestro module may directly call hierarchyService.getHierarchy/appiumService.createSession/deviceInfoCollector.collect — all calls via decorators. §Non-Goals: (a) test rewrite to tests-as-spec (DEFERRED-24-A — Phase 30); (b) persistEnvelope consolidation (DEFERRED-24-B — 8TH SAMPLE POINT consolidation trigger; Phase 27+); (c) Appium driver queue-managed lifecycle (DEFERRED-24-C — future phase); (d) hookExecutor.execute('device.booted', ...) loop in server/index.ts:167-191 RETAINED — different surface (hooks user-facing trigger system, not bus); future hooks→bus migration may consolidate. §Dependencies: config + db + event-bus + pool-plugin (4 entries; plugin name 'maestro-plugin' preserved for back-compat)."
    - "MODULE.md adds optional H3 Runnable Example block under §Public API or as a final section, showing a 5-10 line snippet that imports from server/maestro and demonstrates createMaestroModule construction or a sample emit. Mirrors Phase 22 streaming MODULE.md format. Snippet MUST typecheck against the actual exports (verified at Phase 27+ via per-module example test — for Phase 24 just include and document)."
    - "`server/maestro/index.ts` EXPANDS from Plan 24-00 1-line stub to full barrel surface (~40-60 lines). MOD-02 strict invariant: ONE `from './internal/module.js'` re-export line + back-compat re-exports of internal/ classes (allowed since they're at the module boundary, NOT crossing it). Exports: (a) maestroPlugin default from './plugin.js'; (b) createMaestroModule + MaestroModule + CreateMaestroModuleDeps from './internal/module.js' (THE single internal/module.js re-export line); (c) back-compat classes HierarchyService (from './internal/hierarchy-service.js') + AppiumService (from './internal/appium-service.js') + DeviceInfoCollector (from './internal/device-info-collector.js'); (d) events surface (maestroRegistry, MAESTRO_EVENT_NAMES, MAESTRO_AGGREGATE_ID, makeMaestroEmitters, MaestroRegistry, MaestroEmitters, MaestroEventName, maestroHierarchyFetchedPayload, maestroDeviceInfoCollectedPayload from './events.js')."
    - "Two `git mv` operations preserve blame: `git mv server/maestro/__tests__/hierarchy-service.test.ts server/maestro/__tests__/hierarchy-service.spec.ts` AND `git mv server/maestro/__tests__/appium-service.test.ts server/maestro/__tests__/appium-service.spec.ts`. Pure file renames (MOD-04); test BODY unchanged in this plan. Phase 30 owns the rewrite to tests-as-spec style (DEFERRED-24-A)."
    - "`server/__tests__/plugin-order.spec.ts` EXTENDED with additive Phase 24 assertions (mirrors Phase 22/23 extension pattern). Adds: (a) positional assert — maestro-plugin index in plugin order > pool-plugin index (maestro after pool); (b) positional assert — maestro-plugin index in plugin order < pipelines-plugin index (maestro before pipelines); (c) structural assert — readFileSync(server/maestro/plugin.ts) contains `dependencies: ['config', 'db', 'event-bus', 'pool-plugin']` exact literal (4-entry deps array); (d) MODULE.md 9-section assert — readFileSync(server/maestro/MODULE.md) split on `^## ` headings has length 10 (1 H1 + 9 H2 sections). Existing pre-Phase-24 assertions UNCHANGED."
    - "`.planning/phases/24-maestro-module/deferred-items.md` NEW — catalog of Phase 24 close-out deferrals: (1) DEFERRED-24-A: Maestro test rewrite to tests-as-spec (Phase 30); (2) DEFERRED-24-B: persistEnvelope 8TH SAMPLE POINT consolidation (Phase 27+); (3) DEFERRED-24-C: Appium driver queue-managed lifecycle (future phase); (4) inherited Phase 17 exclusions (3 files: routes.test, artifact-routes.test, auth-plugin.test — fastify-zod-openapi v5 required-emission bug); (5) DEFERRED-24-D (NEW if applicable): cross-module causationId thread on subscriber-side re-emit — needs envelope-aware emit helper; Phase 27+ scope. Each entry names the phase that will address."
    - "`.planning/STATE.md` UPDATED — Phase 24 entry transitions to `Complete YYYY-MM-DD` (today's date). Increment current_plan and completed_plans counts per Phase 15-23 format (Phase 23 closed at completed_plans=72 = 64 + 8; Phase 24 adds 6 plans so new total = 78; verify by reading STATE.md current Phase 23 close numbers and adding 6)."
    - "`.planning/ROADMAP.md` UPDATED — Phase 24 row in §Progress table changes to `Complete YYYY-MM-DD`. Phase 24 §Phase Details section adds `Plans (6 plans across 6 waves):` line and lists all 6 plans with ✅ marks + completion dates in same format as Phases 20-23."
    - "Nyquist delta gate: `npm run nyquist:check` exits 0 (coverage delta >= -2pp vs Phase 15 baseline). Phase 24 added ~550 lines of test code (3 DB-gated specs + module.spec) + ~250 lines of source (factory + plugin rewrite + routes extraction + events.ts body + 4 pool emit lines) and subtracted ~300 lines (354-line plugin.ts shrinks to ~80 lines + 1 line deletion in pool/index.ts). Net delta expected close to 0pp."
    - "`.planning/nyquist-baseline.json` UNCHANGED — `git diff .planning/nyquist-baseline.json` produces no output (locked since Phase 15 commit)."
    - "Final commit: `docs(phase-24): complete phase execution` (matches Phase 22/23 precedent format). Includes all 8 files_modified in this plan. After this commit, Phase 24 is CLOSED — Plan 25 (Pipelines Module) becomes next-in-queue."
  artifacts:
    - path: "server/maestro/MODULE.md"
      provides: "Full 9 H2 sections + optional H3 Runnable Example — MOD-01 canonical close-out"
      contains: "## Events Emitted"
    - path: "server/maestro/index.ts"
      provides: "Full barrel — factory + 3 back-compat classes + events surface (~40-60 lines)"
      contains: "createMaestroModule"
    - path: "server/maestro/__tests__/hierarchy-service.spec.ts"
      provides: "MOD-04 .spec.ts rename of hierarchy-service.test.ts via git mv (blame preserved)"
      contains: "describe"
    - path: "server/maestro/__tests__/appium-service.spec.ts"
      provides: "MOD-04 .spec.ts rename of appium-service.test.ts via git mv (blame preserved)"
      contains: "describe"
    - path: "server/__tests__/plugin-order.spec.ts"
      provides: "Extended with Phase 24 maestro positional + dependency-literal + MODULE.md 9-section assertions"
      contains: "maestro-plugin"
    - path: ".planning/phases/24-maestro-module/deferred-items.md"
      provides: "Catalog of Phase 24 deferrals + inherited Phase 17 exclusions"
      contains: "DEFERRED-24-"
    - path: ".planning/STATE.md"
      provides: "Phase 24 marked Complete with date; counts incremented"
      contains: "Phase 24"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 24 marked Complete; 6-plan list with dates"
      contains: "Phase 24"
  key_links:
    - from: "server/maestro/MODULE.md §Events Emitted"
      to: "server/maestro/events.ts maestroRegistry"
      via: "MODULE.md text matches the 2 entries (hierarchy.fetched + device-info.collected); plugin-order.spec extension grep-checks 9-section count"
      pattern: "maestro\\.(hierarchy|device-info)"
    - from: "server/maestro/index.ts back-compat re-exports"
      to: "server/maestro/internal/{hierarchy,appium,device-info-collector}.ts"
      via: "Re-exports stay inside server/maestro/ — dep-cruiser rule 8 only fires for OUTSIDE-the-module imports"
      pattern: "from './internal/(hierarchy|appium|device-info-collector)"
    - from: "server/__tests__/plugin-order.spec.ts (Phase 24 extension)"
      to: "server/maestro/plugin.ts dependencies array literal"
      via: "Spec asserts maestro-plugin position + dep-array exact match + MODULE.md 9-section count"
      pattern: "maestro-plugin"
    - from: ".planning/phases/24-maestro-module/deferred-items.md"
      to: "Phase 27 (persistEnvelope consolidation) + Phase 30 (test rewrite)"
      via: "Each DEFERRED-24-X entry names the phase that will address"
      pattern: "Phase (27|30)"
---

<objective>
Wave 5 phase-close plan landing the MOD-01 (MODULE.md full body) + MOD-02 (full barrel) + MOD-04 (.test→.spec renames) + plugin-order.spec extension + Nyquist gate + deferred catalog + STATE/ROADMAP updates. After this plan, Phase 24 is CLOSED.

Eight artifact updates land in one administrative + content close:

1. **MODULE.md full body** — replaces Purpose-only placeholder with 9 canonical H2 sections + optional Runnable Example. Documents Public API + 2 events + Invariants + Non-Goals (incl. all 4 deferreds) + Dependencies.

2. **index.ts full barrel** — expands 1-line stub to ~40-60 lines exposing factory + 3 back-compat classes + events surface. MOD-02 strict invariant: only ONE `internal/module.js` re-export; other internal/ re-exports allowed because they cross the module boundary INWARD-OUT.

3. **2 .test→.spec renames** — `hierarchy-service.test.ts` and `appium-service.test.ts` via `git mv` 100% similarity (blame preserved; body unchanged; rewrite Phase 30 scope).

4. **plugin-order.spec extension** — additive Phase 24 assertions (4 new): maestro-plugin position relative to pool/pipelines + 4-entry deps literal + MODULE.md 9-section count.

5. **deferred-items.md** — catalog of all Phase 24 deferrals (DEFERRED-24-A/B/C/D) + inherited Phase 17 exclusions.

6. **STATE.md update** — Phase 24 entry → Complete YYYY-MM-DD; completed_plans count += 6.

7. **ROADMAP.md update** — Phase 24 progress row → Complete; Phase 24 §Phase Details lists all 6 plans with ✅ + dates.

8. **Nyquist gate** — `npm run nyquist:check` exits 0 (delta ≥ -2pp). `.planning/nyquist-baseline.json` unchanged.

9. **Final commit** — `docs(phase-24): complete phase execution` matching Phase 22/23 close commit format.

Mirrors Phase 22 Plan 22-05 + 22-06 collapsed into one (Phase 24 doesn't need a separate Wave 6 admin plan because the close fits in 35 minutes — fewer artifacts to coordinate). Phase 23's Plan 23-07 used the same "all close in one" pattern.

Purpose: Phase 24 transitions from IN-PROGRESS to CLOSED state. Subsequent phases (Phase 25 Pipelines Module) reference Phase 24's completion as dependency. STATE.md + ROADMAP.md become source of truth that `/gsd:plan-phase 25` reads when bootstrapping.

Output: 8 file modifications. ~250 lines added MODULE.md (replacing 5-line placeholder); ~50 lines added index.ts (replacing 2-line stub); 2 file renames; ~20 lines added plugin-order.spec; ~80 lines new deferred-items.md; STATE.md + ROADMAP.md edits; 1 final commit.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-maestro-module/24-CONTEXT.md
@.planning/phases/24-maestro-module/24-RESEARCH.md
@.planning/phases/24-maestro-module/24-VALIDATION.md
@.planning/phases/24-maestro-module/24-03-PLAN.md
@.planning/phases/24-maestro-module/24-04-PLAN.md
@.planning/phases/22-streaming-module/22-05-PLAN.md
@.planning/phases/22-streaming-module/22-06-PLAN.md
@.planning/phases/23-jobs-module-keystone/23-07-PLAN.md
@.planning/phases/22-streaming-module/deferred-items.md
@.planning/phases/23-jobs-module-keystone/deferred-items.md
@server/streaming/MODULE.md
@server/jobs/MODULE.md
@server/streaming/index.ts
@server/maestro/MODULE.md
@server/maestro/index.ts
@server/__tests__/plugin-order.spec.ts
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/nyquist-baseline.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 5.1: Write full MODULE.md body (9 H2 sections + optional Runnable Example) + expand index.ts barrel</name>
  <files>server/maestro/MODULE.md, server/maestro/index.ts</files>
  <read_first>
    - server/maestro/MODULE.md (Plan 24-00 Purpose-only placeholder)
    - server/maestro/index.ts (Plan 24-00 1-line stub)
    - server/streaming/MODULE.md (Phase 22 canonical 9-section template)
    - server/jobs/MODULE.md (Phase 23 canonical 9-section template)
    - server/streaming/index.ts (Phase 22 full barrel template — multi-internal re-exports)
    - server/maestro/events.ts (final exports for events surface)
    - server/maestro/internal/module.ts (factory exports)
    - .planning/phases/24-maestro-module/24-CONTEXT.md §Decisions §Specific Ideas
  </read_first>
  <action>
    1. **OVERWRITE `server/maestro/MODULE.md`** with full 9-section body (~250 lines):

       ```markdown
       # Maestro Module

       ## Purpose

       Own Maestro integration: hierarchy fetching (Maestro CLI / device-stream / native / Appium), device metadata collection (`adb getprop` / `xcrun simctl`), and Appium driver lifecycle. Subscribes to `device.booted` from the pool's bus to trigger boot-time metadata refresh — replacing the imperative `onReady` loop that previously lived in `server/maestro/plugin.ts:55-72`.

       Maestro is a no-queue module (no pg-boss surface) — same shape as Phase 22 streaming. Two transient events emitted; one cross-module event consumed.

       ## Public API

       Exports from `server/maestro/index.ts` (the ONLY legitimate import surface outside this module — enforced by `dependency-cruiser` rule `no-deep-imports-into-maestro-internal` added in Phase 24 Plan 24-00):

       - **Plugin:** `maestroPlugin` (default, name 'maestro-plugin', deps `['config', 'db', 'event-bus', 'pool-plugin']`)
       - **Factory (canonical v3.0):** `createMaestroModule(deps)` + types `MaestroModule`, `CreateMaestroModuleDeps`
       - **Back-compat classes (preserved decorators on fastify):** `HierarchyService`, `AppiumService`, `DeviceInfoCollector`
       - **Events surface:** `maestroRegistry`, `MAESTRO_EVENT_NAMES`, `MAESTRO_AGGREGATE_ID`, `makeMaestroEmitters` + types `MaestroRegistry`, `MaestroEmitters`, `MaestroEventName`, payload schemas `maestroHierarchyFetchedPayload`, `maestroDeviceInfoCollectedPayload`

       Decorators: `fastify.maestroModule` (NEW canonical), `fastify.hierarchyService` / `fastify.appiumService` / `fastify.deviceInfoCollector` (3 back-compat — Phase 22/23 convention).

       ## Events Emitted

       | Name                              | Persisted | Aggregate     | When fired                                                          |
       |-----------------------------------|-----------|---------------|---------------------------------------------------------------------|
       | `maestro.hierarchy.fetched`       | NO        | maestro       | After HierarchyService.getHierarchy resolves (Plan 24-03 wiring opt) |
       | `maestro.device-info.collected`  | NO        | maestro       | After device.booted handler completes DeviceInfoCollector.collect    |

       Both transient per TRACE-08 (high-frequency, derivable from logs).

       ## Events Consumed

       | Name             | Source        | Action                                                                         |
       |------------------|---------------|--------------------------------------------------------------------------------|
       | `device.booted`  | `pool` module | Run DeviceInfoCollector.collect, mutate Device.metadata, emit device-info.collected |

       Subscription deferred to `fastify.addHook('onReady', ...)` per Pitfall 2 (Phase 23 inheritance).

       ## Queue Produced

       None.

       ## Queue Consumed

       None. Maestro is a no-queue module (matches Phase 22 streaming).

       ## Invariants

       1. Cross-module subscription is deferred to `onReady` — never registered in plugin body.
       2. `device.booted` handler swallows DeviceInfoCollector errors (logs warn; never re-throws). Subscriber failures must not propagate (Phase 21 invariant inheritance).
       3. `Device.metadata` mutation happens BEFORE the `maestro.device-info.collected` emission — subscribers see the mutated device state when they consume the new event.
       4. `shutdown` is idempotent (closes Appium sessions + unsubscribes; double-call no-throw).
       5. No producer outside the maestro module may directly call `hierarchyService.getHierarchy` / `appiumService.createSession` / `deviceInfoCollector.collect` — all calls go through decorators (`fastify.maestroModule.*` or back-compat shorthand).

       ## Non-Goals

       - **Test rewrite to tests-as-spec style** (DEFERRED-24-A). Phase 30 Test Migration Cleanup owns the rewrite of `hierarchy-service.spec.ts` + `appium-service.spec.ts` beyond the Plan 24-05 `.test → .spec` rename.
       - **persistEnvelope consolidation** (DEFERRED-24-B — 8TH SAMPLE POINT). The 10-line persistEnvelope middleware in `internal/module.ts` is the 8th verbatim copy across hooks/lifecycle/reporting/pool/artifacts/streaming/jobs/maestro. Phase 27+ extracts to `server/bus/persist-envelope.ts`. DO NOT consolidate here — scope creep.
       - **Appium driver queue-managed lifecycle** (DEFERRED-24-C). AppiumService spawns/tears down drivers on demand today. A future phase may move Appium sessions behind a pg-boss queue with explicit ownership transfer.
       - **hookExecutor.execute('device.booted', ...) loop in `server/index.ts:167-191`**. RETAINED — different surface (hooks-side user-facing trigger, not bus). Future hooks→bus migration may consolidate. Lifecycle-ownership.spec asserts the loop is still present (guard against accidental deletion).
       - **Cross-module causationId on subscriber-side re-emit** (DEFERRED-24-D, if applicable). The maestro re-emit on `device.booted` carries its own correlationId (read from ALS) but does NOT explicitly set `causationId = pool-envelope.id` because the subscriber receives PAYLOAD, not envelope. Phase 27+ may introduce envelope-aware emit helpers.

       ## Dependencies

       Plugin dependencies (declared in `server/maestro/plugin.ts`):
       - `config` — for AppiumService server URL + session timeout
       - `db` — for persistEnvelope middleware (currently short-circuits on persisted:false; kept for symmetry + future-proofing)
       - `event-bus` — substrate convention; createEventHelpers + ALS reads
       - `pool-plugin` — to read `fastify.poolModule.bus` in registerSubscribers

       Module dependencies (consumed via fastify decorators):
       - `fastify.pool` — read `getDevice(id)` and `getDeviceMap()` for metadata cache mutation
       - `fastify.poolModule.bus` — subscribe to `device.booted`
       - `fastify.config` — Appium server URL + session timeout
       - `fastify.db` — persistEnvelope writes (no-op for transient events)
       - `fastify.log` — child logger named `'maestro'` (MOD-07)

       ## Runnable Example

       ```typescript
       // Subscribe to maestro.device-info.collected from any module:
       fastify.addHook('onReady', async () => {
         const maestroBus = fastify.maestroModule.bus;
         maestroBus.on('maestro.device-info.collected', (payload) => {
           fastify.log.info(
             { deviceId: payload.deviceId, model: payload.model, os: payload.osVersion },
             'Device metadata collected',
           );
         });
       });
       ```
       ```

    2. **OVERWRITE `server/maestro/index.ts`** with full barrel (~50 lines):

       ```typescript
       /**
        * Phase 24 / Plan 24-05 — Maestro module public barrel (MOD-02).
        *
        * This is the ONLY import surface for consumers outside `server/maestro/`.
        * The dep-cruiser rule `no-deep-imports-into-maestro-internal` (added in
        * Phase 24 Plan 24-00 as the 8th forbidden rule) enforces structurally —
        * imports into `server/maestro/internal/**` from outside the module fail CI.
        *
        * Re-exports inside this barrel from `./internal/*` are at the module
        * BOUNDARY and do NOT violate the rule (the rule fires only for paths
        * matching `pathNot: '^server/maestro/'` — i.e. external).
        *
        * MOD-02 strict invariant: ONE re-export from `./internal/module.js`
        * (the factory entry point per MOD-06). Other internal/ re-exports
        * (back-compat classes) are permitted because they cross the boundary
        * inward-out (e.g. server/jobs/job-service.ts holds `import type { ... }`).
        */

       // Fastify plugin (default — Phase 15 substrate convention).
       export { default as maestroPlugin } from './plugin.js';

       // Factory surface (the v3.0 canonical way to instantiate the module). MOD-06.
       // ONE internal/module.js re-export line — MOD-02 structural invariant.
       export {
         createMaestroModule,
         type MaestroModule,
         type CreateMaestroModuleDeps,
       } from './internal/module.js';

       // Back-compat class surfaces (decorators on fastify still consume these
       // for routes and any pre-Phase-24 caller). Re-exporting from internal/
       // is allowed at the barrel — see file header.
       export { HierarchyService } from './internal/hierarchy-service.js';
       export type { HierarchySource } from './internal/hierarchy-service.js';
       export { AppiumService } from './internal/appium-service.js';
       export { DeviceInfoCollector } from './internal/device-info-collector.js';

       // Events surface (MOD-03 — Plan 24-01).
       export {
         maestroRegistry,
         MAESTRO_EVENT_NAMES,
         MAESTRO_AGGREGATE_ID,
         makeMaestroEmitters,
         maestroHierarchyFetchedPayload,
         maestroDeviceInfoCollectedPayload,
       } from './events.js';
       export type {
         MaestroRegistry,
         MaestroEmitters,
         MaestroEventName,
       } from './events.js';
       ```

       NOTE on `HierarchySource` type — if HierarchyService doesn't export this type, omit. Otherwise re-export for routes.ts consumption.
  </action>
  <verify>
    <automated>grep -cE "^## (Purpose|Public API|Events Emitted|Events Consumed|Queue Produced|Queue Consumed|Invariants|Non-Goals|Dependencies)$" server/maestro/MODULE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## (Purpose|Public API|Events Emitted|Events Consumed|Queue Produced|Queue Consumed|Invariants|Non-Goals|Dependencies)$" server/maestro/MODULE.md` returns exactly 9
    - `grep -c "^# Maestro Module" server/maestro/MODULE.md` returns exactly 1 (1 H1)
    - `grep -c "DEFERRED-24-A\\|DEFERRED-24-B\\|DEFERRED-24-C" server/maestro/MODULE.md` returns >= 3
    - `grep -c "createMaestroModule\\|maestroPlugin" server/maestro/index.ts` returns >= 2
    - `grep -c "from './internal/module.js'" server/maestro/index.ts` returns exactly 1 (MOD-02 strict factory invariant)
    - `grep -c "HierarchyService\\|AppiumService\\|DeviceInfoCollector" server/maestro/index.ts` returns >= 3 (back-compat re-exports)
    - `grep -c "maestroRegistry\\|MAESTRO_EVENT_NAMES" server/maestro/index.ts` returns >= 2 (events surface)
    - `npx tsc --noEmit 2>&1 | grep -c "server/maestro/index.ts"` returns 0
  </acceptance_criteria>
  <done>MODULE.md has 9 H2 sections; index.ts barrel exposes factory + 3 back-compat + events; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.2: Two .test→.spec git mv renames + plugin-order.spec extension</name>
  <files>server/maestro/__tests__/hierarchy-service.spec.ts, server/maestro/__tests__/appium-service.spec.ts, server/__tests__/plugin-order.spec.ts</files>
  <read_first>
    - server/maestro/__tests__/hierarchy-service.test.ts (verify exists pre-rename)
    - server/maestro/__tests__/appium-service.test.ts (verify exists pre-rename)
    - server/__tests__/plugin-order.spec.ts (full file — locate Phase 22/23 extension blocks for additive pattern)
    - .planning/phases/22-streaming-module/22-04-PLAN.md (Phase 22 plugin-order.spec extension template)
    - .planning/phases/23-jobs-module-keystone/23-07-PLAN.md (Phase 23 plugin-order.spec extension)
  </read_first>
  <action>
    1. **Two `git mv` renames** (100% similarity; preserve blame; do NOT edit body):
       ```bash
       git mv server/maestro/__tests__/hierarchy-service.test.ts server/maestro/__tests__/hierarchy-service.spec.ts
       git mv server/maestro/__tests__/appium-service.test.ts server/maestro/__tests__/appium-service.spec.ts
       ```
       After moves, both `.spec.ts` files exist with identical content. Test imports may need adjusting if they imported services from the OLD top-level path (`../hierarchy-service.js`) — Plan 24-03 git-mv'd those services to `../internal/hierarchy-service.js`. Update imports if the relocated paths break compilation:
       ```typescript
       // BEFORE (relative path from __tests__/ to top-level):
       // import { HierarchyService } from '../hierarchy-service.js';
       // AFTER (relative path from __tests__/ to internal/):
       import { HierarchyService } from '../internal/hierarchy-service.js';
       ```
       Same for `appium-service.spec.ts`.

    2. **Edit `server/__tests__/plugin-order.spec.ts`** — append Phase 24 additive block before the closing `})` of the outer describe:
       ```typescript
       describe('Phase 24 — maestro-plugin position + dependencies + MODULE.md', () => {
         const buildOrder = readBuildPluginOrder(); // existing helper that returns plugin names array in registration order

         it('maestro-plugin registers AFTER pool-plugin', () => {
           const poolIdx = buildOrder.indexOf('pool-plugin');
           const maestroIdx = buildOrder.indexOf('maestro-plugin');
           expect(poolIdx).toBeGreaterThanOrEqual(0);
           expect(maestroIdx).toBeGreaterThan(poolIdx);
         });

         it('maestro-plugin registers BEFORE pipelines-plugin', () => {
           const maestroIdx = buildOrder.indexOf('maestro-plugin');
           const pipelinesIdx = buildOrder.indexOf('pipelines-plugin');
           expect(maestroIdx).toBeGreaterThanOrEqual(0);
           // pipelines may not yet exist post-Phase-25; allow undefined
           if (pipelinesIdx >= 0) {
             expect(maestroIdx).toBeLessThan(pipelinesIdx);
           }
         });

         it('maestro-plugin declares 4-entry dependencies array (Phase 24 extension)', () => {
           const src = readFileSync('server/maestro/plugin.ts', 'utf8');
           expect(src).toContain("dependencies: ['config', 'db', 'event-bus', 'pool-plugin']");
         });

         it('maestro/MODULE.md has exactly 9 H2 sections (MOD-01 canonical)', () => {
           const src = readFileSync('server/maestro/MODULE.md', 'utf8');
           const h2Headings = src.match(/^## (Purpose|Public API|Events Emitted|Events Consumed|Queue Produced|Queue Consumed|Invariants|Non-Goals|Dependencies)$/gm) ?? [];
           expect(h2Headings).toHaveLength(9);
         });
       });
       ```

       Adapt to whatever helper exists in plugin-order.spec.ts (`readBuildPluginOrder`, `extractPluginNames`, etc.). Use the same readFileSync import path used by existing extensions.
  </action>
  <verify>
    <automated>test -f server/maestro/__tests__/hierarchy-service.spec.ts && test -f server/maestro/__tests__/appium-service.spec.ts && ! test -f server/maestro/__tests__/hierarchy-service.test.ts && ! test -f server/maestro/__tests__/appium-service.test.ts && npx vitest run server/__tests__/plugin-order.spec.ts  </verify>
  <acceptance_criteria>
    - `test -f server/maestro/__tests__/hierarchy-service.spec.ts` exits 0; old `.test.ts` file gone
    - `test -f server/maestro/__tests__/appium-service.spec.ts` exits 0; old `.test.ts` file gone
    - `git log --follow server/maestro/__tests__/hierarchy-service.spec.ts | head -3` shows commits PRE-DATING Plan 24-05 (blame preserved)
    - `git log --follow server/maestro/__tests__/appium-service.spec.ts | head -3` shows pre-Plan 24-05 commits
    - `grep -c "describe('Phase 24" server/__tests__/plugin-order.spec.ts` returns >= 1 (additive block landed)
    - `grep -c "dependencies: \\['config', 'db', 'event-bus', 'pool-plugin'\\]" server/__tests__/plugin-order.spec.ts` returns >= 1 (literal-match assertion present)
    - `grep -c "9 H2 sections" server/__tests__/plugin-order.spec.ts` returns >= 1 OR test name contains "9 H2 sections" / "MOD-01 canonical"
    - `npx vitest run server/__tests__/plugin-order.spec.ts` shows ALL prior tests + 4 new Phase 24 tests passing
    - `npx vitest run server/maestro/__tests__/hierarchy-service.spec.ts server/maestro/__tests__/appium-service.spec.ts` — both rename targets still pass
    - `npx tsc --noEmit 2>&1 | grep -E "server/maestro/__tests__/(hierarchy|appium)-service.spec.ts" | wc -l` returns 0
  </acceptance_criteria>
  <done>2 .test → .spec renames preserved blame; plugin-order.spec extension passes 4 new assertions; full Phase 24 test surface migrated to MOD-04 naming.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.3: Write deferred-items.md catalog + update STATE.md + ROADMAP.md + Nyquist gate + final commit</name>
  <files>.planning/phases/24-maestro-module/deferred-items.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/22-streaming-module/deferred-items.md (Phase 22 catalog template)
    - .planning/phases/23-jobs-module-keystone/deferred-items.md (Phase 23 catalog template)
    - .planning/STATE.md (find Phase 23 close entry; understand counter increment pattern)
    - .planning/ROADMAP.md (find Phase 24 row in §Progress + §Phase Details)
    - .planning/nyquist-baseline.json (verify still committed, unchanged)
  </read_first>
  <action>
    1. **Create `.planning/phases/24-maestro-module/deferred-items.md`** (~80 lines) cataloging all Phase 24 deferrals + inherited Phase 17 exclusions. Mirror Phase 22/23 format:

       ```markdown
       # Phase 24 — Deferred Items

       Closed 2026-05-08 (or today's date).

       ## Phase 24-Specific Deferrals

       ### DEFERRED-24-A: Maestro test rewrite to tests-as-spec style
       **Status:** Deferred to Phase 30 Test Migration Cleanup.
       **Why:** Phase 24 Plan 24-05 performs only `.test.ts → .spec.ts` rename via `git mv` (MOD-04 naming). Body rewrite to match MODULE.md Public API + Invariants sections is Phase 30 scope.
       **Files affected:** `server/maestro/__tests__/hierarchy-service.spec.ts`, `server/maestro/__tests__/appium-service.spec.ts`.

       ### DEFERRED-24-B: persistEnvelope 8TH SAMPLE POINT consolidation
       **Status:** Deferred to Phase 27+.
       **Why:** The 10-line `persistEnvelope` middleware in `server/maestro/internal/module.ts` is the 8th verbatim copy across hooks/lifecycle/reporting/pool/artifacts/streaming/jobs/maestro. Pattern is locked-in; consolidation requires touching 8 modules atomically + extracting to `server/bus/persist-envelope.ts`. Phase 27+ owns.
       **Files affected:** All 8 module factories in `server/*/internal/module.ts`.

       ### DEFERRED-24-C: Appium driver queue-managed lifecycle
       **Status:** Deferred to a future phase (no specific phase committed).
       **Why:** AppiumService currently spawns/tears down drivers on demand inside HTTP routes. A future phase may move Appium driver lifecycle behind a pg-boss queue with explicit ownership transfer + driver pooling. Phase 24 retains the on-demand surface verbatim.
       **Files affected:** `server/maestro/internal/appium-service.ts`.

       ### DEFERRED-24-D: Cross-module causationId on subscriber-side re-emit
       **Status:** Deferred to Phase 27+ envelope-aware emit helpers.
       **Why:** Maestro's `device.booted` subscriber receives a payload (not envelope). The re-emit on `maestro.device-info.collected` carries its own correlationId from ALS but does not explicitly set `causationId = pool-envelope.id` because the original envelope is opaque to the handler. Cross-module envelope-aware emit helpers (Phase 27 trace-tree scope) will close this gap.
       **Files affected:** `server/maestro/internal/subscribers.ts` (or `module.ts`), `server/bus/helpers.ts`.

       ### DEFERRED-24-E: hookExecutor 'device.booted' loop in server/index.ts
       **Status:** Retained per Phase 24 scope; future hooks→bus migration may consolidate.
       **Why:** The `app.hookExecutor.execute('device.booted', ...)` loop at `server/index.ts:167-191` fires user-defined shell hooks — a DIFFERENT surface from the bus-driven metadata refresh. RESEARCH §Anti-Patterns explicitly preserves this loop. Lifecycle-ownership.spec test (e) asserts the loop is still present (guard against accidental deletion).
       **Files affected:** `server/index.ts:167-191`, `server/hooks/schemas.ts:14`.

       ## Inherited from Phase 17 (Carry-Forward Exclusions)

       Three test files remain excluded due to fastify-zod-openapi v5 required-emission bug (DEFERRED-17-A inheritance):
       - `server/api/__tests__/routes.test.ts`
       - `server/artifacts/__tests__/artifact-routes.test.ts`
       - `server/auth/__tests__/auth-plugin.test.ts`

       Each excluded by `vitest.config.ts` excludes pattern; tracked in Phase 17 deferred-items.md.

       ## Inherited from Phase 15 (DEFERRED-15-A)

       6-10 pre-existing TypeScript errors related to `Map` vs `RequestContext` shape continue to be tolerated. `npx tsc --noEmit` baseline unchanged across Phases 16-24.
       ```

    2. **Edit `.planning/STATE.md`** — Phase 24 close entry:
       - Change Phase 24 row from `Not started` / `In progress` to `Complete YYYY-MM-DD` (today's date).
       - Increment `completed_plans` by 6 (Phase 23 closed at 72; Phase 24 ships 6 plans → 78).
       - Update `current_phase` to `25` (next-in-queue).
       - Match the exact format used by Phase 22/23 close entries (read those first; do NOT improvise structure).

    3. **Edit `.planning/ROADMAP.md`** — Phase 24 row updates:
       - In §Progress table: row `| 24. Maestro Module | v3.0 | 6/6 | Complete | 2026-05-08 |` (or today's date).
       - In §Phase Details Phase 24 section: append `Plans (6 plans across 6 waves):` line followed by 6-bullet list:
         ```
         - [x] 24-00-PLAN.md — Wave 0 substrate (events stub + queue.ts comment + module throw-stub + MODULE.md placeholder + index.ts barrel + 8th dep-cruiser rule + fixture + events.spec stub + pool BOOTED placeholder) ✅ YYYY-MM-DD
         - [x] 24-01-PLAN.md — Wave 1 events bodies (2 maestro events + payload schemas + makeMaestroEmitters; pool 5th event device.booted body) ✅ YYYY-MM-DD
         - [x] 24-02-PLAN.md — Wave 2 pool emission wiring (4 emit.booted sites in pool-manager.ts after Booting→Idle transitions; subscriber.spec extension) ✅ YYYY-MM-DD
         - [x] 24-03-PLAN.md — Wave 3 factory + 3 git-mv + plugin rewrite + routes extraction + onReady-loop deletion + module.spec ✅ YYYY-MM-DD
         - [x] 24-04-PLAN.md — Wave 4 DB-gated proofs (subscriber.spec + correlation.spec + lifecycle-ownership.spec) ✅ YYYY-MM-DD
         - [x] 24-05-PLAN.md — Wave 5 phase close (MODULE.md 9 sections + barrel expansion + 2 .test→.spec renames + plugin-order.spec extension + deferred-items + Nyquist + STATE/ROADMAP) ✅ YYYY-MM-DD
         ```
       - Phase 24 SC1/SC2/SC3 unchanged (already in §Phase Details).

    4. **Run Nyquist gate**: `npm run nyquist:check` MUST exit 0. `git diff .planning/nyquist-baseline.json` MUST produce no output. If delta < -2pp, HALT and review (do not commit Phase 24 close until gate passes).

    5. **Run full-suite green sweep**:
       ```bash
       npm test                    # exit 0 (with Phase 17 exclusions inherited)
       npm run lint                # exit 0
       npx tsc --noEmit            # baseline 6-10 pre-existing errors unchanged
       npm run dep-check           # exit 0 (8 forbidden module rules + 1 emit rule, no new violations)
       ```

    6. **Final commit**:
       ```bash
       git add server/maestro/ server/__tests__/plugin-order.spec.ts server/pool/events.ts server/pool/pool-manager.ts server/pool/index.ts server/pool/MODULE.md server/pool/__tests__/ server/index.ts .dependency-cruiser.cjs __fixtures__/dep-cruiser/bad-maestro-deep-import.ts server/hooks/__tests__/dep-cruiser.spec.ts .planning/phases/24-maestro-module/ .planning/STATE.md .planning/ROADMAP.md
       git commit -m "$(cat <<'EOF'
       docs(phase-24): complete phase execution

       Maestro module migration to canonical MOD-01..09 shape:
       - 3 services relocated under server/maestro/internal/ via git mv (HierarchyService, AppiumService, DeviceInfoCollector)
       - 2 transient events emitted (maestro.hierarchy.fetched, maestro.device-info.collected)
       - device.booted bus subscription replaces imperative onReady metadata-collection loop
       - Pool 5th event (device.booted) emitted from 4 PoolManager sites after Booting→Idle
       - 8th persistEnvelope sample point in createMaestroModule (DEFERRED-24-B)
       - 8th dep-cruiser rule no-deep-imports-into-maestro-internal
       - 6 plans across 6 waves; SC1/SC2/SC3 all proven

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```

       Commits ALL Phase 24 changes — including any earlier-plan files that weren't yet pushed. After this commit, Phase 24 is CLOSED.
  </action>
  <verify>
    <automated>test -f .planning/phases/24-maestro-module/deferred-items.md && grep -q "DEFERRED-24-A" .planning/phases/24-maestro-module/deferred-items.md && grep -q "Phase 24" .planning/STATE.md && grep -q "24. Maestro Module" .planning/ROADMAP.md && npm run nyquist:check && git diff --quiet .planning/nyquist-baseline.json</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/24-maestro-module/deferred-items.md` exits 0
    - `grep -c "DEFERRED-24-" .planning/phases/24-maestro-module/deferred-items.md` returns >= 4 (A, B, C, D — E optional)
    - `grep -c "Phase 17.*exclusions\\|DEFERRED-17-A" .planning/phases/24-maestro-module/deferred-items.md` returns >= 1 (inherited)
    - `grep -c "Phase 24" .planning/STATE.md` returns >= 1 (state entry present)
    - `grep -c "Complete.*2026" .planning/STATE.md` returns increment by 1 vs Phase 23 close (or matches today's-date format)
    - `grep -c "24. Maestro Module" .planning/ROADMAP.md` returns >= 1 (row present)
    - `grep -c "24-00-PLAN.md\\|24-05-PLAN.md" .planning/ROADMAP.md` returns >= 2 (all 6 plans listed in Phase Details)
    - `npm run nyquist:check` exits 0 (delta ≥ -2pp)
    - `git diff --quiet .planning/nyquist-baseline.json` exits 0 (file UNCHANGED)
    - `npm test` exits 0 (with Phase 17 exclusions inherited; ZERO new failures)
    - `npm run dep-check` exits 0 (8 module rules + 1 emit rule; no new violations)
    - `git log --oneline HEAD~1..HEAD` shows the close commit `docs(phase-24): complete phase execution`
  </acceptance_criteria>
  <done>Phase 24 CLOSED. deferred-items catalogued; STATE+ROADMAP updated; Nyquist gate green; full suite green; close commit shipped.</done>
</task>

</tasks>

<verification>
**Wave 5 Phase Close Verification:**

```bash
# 1. MODULE.md has 9 H2 sections
grep -cE "^## (Purpose|Public API|Events Emitted|Events Consumed|Queue Produced|Queue Consumed|Invariants|Non-Goals|Dependencies)$" server/maestro/MODULE.md  # expect 9

# 2. index.ts barrel — strict 1 internal/module.js + back-compat re-exports
grep -c "from './internal/module.js'" server/maestro/index.ts  # expect 1 (strict)
grep -c "from './internal/" server/maestro/index.ts            # expect ~4 (1 module + 3 services)
grep -c "from './events.js'" server/maestro/index.ts           # expect 1 (events surface in 1 statement)

# 3. .test → .spec renames preserved blame
test -f server/maestro/__tests__/hierarchy-service.spec.ts
test -f server/maestro/__tests__/appium-service.spec.ts
! test -f server/maestro/__tests__/hierarchy-service.test.ts
! test -f server/maestro/__tests__/appium-service.test.ts
git log --follow server/maestro/__tests__/hierarchy-service.spec.ts | head -2

# 4. plugin-order.spec extension
grep -c "describe('Phase 24" server/__tests__/plugin-order.spec.ts  # expect >= 1
npx vitest run server/__tests__/plugin-order.spec.ts

# 5. deferred-items + STATE + ROADMAP
test -f .planning/phases/24-maestro-module/deferred-items.md
grep -c "DEFERRED-24-" .planning/phases/24-maestro-module/deferred-items.md  # expect >= 4
grep -c "Phase 24" .planning/STATE.md  # expect >= 1 (CLOSED entry)
grep -c "24-05-PLAN.md" .planning/ROADMAP.md  # expect >= 1

# 6. Nyquist + full-suite green
npm run nyquist:check
git diff --quiet .planning/nyquist-baseline.json
npm test
npm run dep-check
npx tsc --noEmit 2>&1 | grep -c "Phase 24"  # baseline unchanged

# 7. Final commit
git log --oneline HEAD~1..HEAD | grep -c "phase-24.*complete"  # expect 1
```

**Phase 24 Close Gate:**
- SC1: lifecycle-ownership.spec proves services-behind-barrel + zero imperative loops
- SC2: subscriber.spec proves bus subscription drives metadata refresh
- SC3a: MODULE.md 9-section + barrel + factory + tests-as-spec rename — all green
- SC3b: events.spec proves 2 events emitted with correct shapes
- SC3c: Nyquist gate exits 0
- SC3d: dep-cruiser 8th rule fires on fixture
- SC3e: plugin-order.spec extension passes
- SC3f: correlation.spec proves correlationId thread
- SC3g: pool events.spec proves 5-entry registry
- SC3h: pool subscriber.spec proves 4-emit-site coverage
</verification>

<success_criteria>
- MODULE.md has 9 H2 sections + Runnable Example documenting Public API + Events Emitted (2) + Events Consumed (1) + Invariants (5) + Non-Goals (5 incl. all DEFERRED-24-*) + Dependencies (4 plugin deps + 5 module deps)
- index.ts full barrel: factory (strict 1-line internal/module re-export) + 3 back-compat classes + events surface
- 2 .test → .spec renames via `git mv` 100% similarity (blame preserved)
- plugin-order.spec extension: 4 new Phase 24 assertions (positional + dep-array literal + MODULE.md 9-section count)
- deferred-items.md: 4-5 DEFERRED-24-* entries + inherited Phase 17 + Phase 15 carry-forwards
- STATE.md + ROADMAP.md: Phase 24 marked Complete; 6 plans listed
- Nyquist baseline UNCHANGED; npm run nyquist:check exits 0
- Full test suite green (npm test); npm run lint green; npm run dep-check exits 0; npx tsc --noEmit baseline unchanged
- Final commit `docs(phase-24): complete phase execution` shipped
- Phase 25 (Pipelines Module) becomes next-in-queue
</success_criteria>

<output>
After completion, create `.planning/phases/24-maestro-module/24-05-SUMMARY.md` documenting:
- MODULE.md 9-section content map (Purpose / Public API / Events Emitted / Events Consumed / Queue Produced / Queue Consumed / Invariants / Non-Goals / Dependencies)
- index.ts final barrel surface enumeration
- 2 .test → .spec rename targets + blame preservation check
- plugin-order.spec 4 new Phase 24 assertions
- deferred-items.md 4-5 entries (A/B/C/D[/E])
- STATE.md + ROADMAP.md update lines (Phase 24 → Complete; counts incremented)
- Nyquist gate result
- Final close commit hash
- Phase 24 → Phase 25 transition signal (next-in-queue)
</output>
