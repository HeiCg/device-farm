---
phase: 26
plan: 05
type: execute
wave: 5
depends_on: ["26-04"]
files_modified:
  - server/auth/MODULE.md
  - server/auth/index.ts
  - server/auth/__tests__/auth-plugin.spec.ts
  - server/auth/__tests__/auth-service.spec.ts
  - server/__tests__/plugin-order.spec.ts
  - .planning/phases/26-auth-module/deferred-items.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TRACE-10, MOD-01, MOD-02, MOD-04, MOD-08]
estimated_minutes: 25
gap_closure: false
must_haves:
  truths:
    - "`server/auth/MODULE.md` overwritten with canonical 9 H2 sections + Runnable Example (mirrors Phase 22/23/24/25 close-out): ## Purpose, ## Public API, ## Events Emitted, ## Events Consumed, ## Queue Produced, ## Queue Consumed, ## Invariants, ## Non-Goals, ## Dependencies. Public API enumerates createAuthModule + AuthService + requireAdmin + actorSchema + 4 actor helpers. Events Emitted lists 2 events (auth.key.created + auth.key.revoked) with persistence flags both true. Events Consumed: NONE (Phase 26 is emit-only). Queue Produced: NONE. Queue Consumed: NONE. Invariants section has 5+ invariants each citing a spec file: (a) actorSchema regex rejects 'anonymous' (Pitfall 4 — actor.spec); (b) bearer-auth callback writes ALS actor before returning true (Pitfall 1 — als-actor.spec Test 5 concurrent isolation); (c) requireAdmin returns 403 RFC 7807 on missing claim (admin-claim.spec); (d) jsonb_set preserves other claim keys (admin-claim.spec Test 6); (e) auth.key.created+revoked persist with TRACE-08 actor populated from caller's ALS (subscriber.spec). Non-Goals lists DEFERRED-26-A through E. Dependencies lists 3-entry plugin deps array verbatim from plugin.ts ('config','db','event-bus'). Runnable Example shows the auth-key creation + revocation flow via curl + asserts the events table query."
    - "`server/auth/index.ts` expanded from Plan 26-03 2-line barrel to canonical full-surface barrel: re-exports createAuthModule + AuthModule + AuthService + MatchedApiKey + requireAdmin + events surface (authRegistry, makeAuthEmitters, AUTH_EVENT_NAMES, AuthEventName, AUTH_AGGREGATE_ID, AUTH_AGGREGATE_TYPE, authKeyCreatedPayloadSchema, authKeyRevokedPayloadSchema, AuthKeyCreatedPayload, AuthKeyRevokedPayload, AuthRegistry, AuthEmitters) + actor surface (actorSchema, Actor, asApiKeyActor, asUserActor, SYSTEM_ACTOR, CRON_ACTOR). MOD-02 strict invariant kept on the FIRST line: `export { createAuthModule, type AuthModule } from './internal/module.js';` followed by the additional re-exports. NO `export *`."
    - "2 .test.ts → .spec.ts renames in `server/auth/__tests__/` via `git mv` 100% similarity (preserves blame): auth-plugin.test.ts → auth-plugin.spec.ts (DEFERRED-17-A inheritance — see fastify-zod-openapi v6 carry-forward decision below); auth-service.test.ts → auth-service.spec.ts. After rename: `find server/auth/__tests__ -name '*.test.ts' | wc -l` returns 0 (MOD-04 closed for auth)."
    - "If `auth-plugin.test.ts → auth-plugin.spec.ts` rename surfaces DEFERRED-17-A failures (fastify-zod-openapi v5 `required`-emission bug per Pitfall 7), those failures are documented as DEFERRED-26-E carry-forward. Body NOT edited in this plan; DEFERRED-26-E entry in deferred-items.md retargets to Phase 27+ when API aggregator can swap libraries holistically. Per RESEARCH §Open Question 1 + §Pitfall 7: v6 of fastify-zod-openapi NOT released through 2026-05-08; carry-forward is the defensible choice."
    - "`auth-service.test.ts → auth-service.spec.ts` rename should pass cleanly (NOT inherited DEFERRED-17-A — pure unit tests). Imports updated post-rename if needed (the test file likely imports from `'../auth-service.js'` which after Plan 26-03 git mv is now `'../internal/auth-service.js'` — fix the import line; do NOT touch test bodies otherwise)."
    - "`server/__tests__/plugin-order.spec.ts` extended with Phase 26 additive block (mirrors Phase 24/25 plugin-order extension): 3 positional dep-order assertions (config→auth, db→auth, event-bus→auth) + 1 structural readFileSync regex-extract verifying canonical 3-entry deps literal `['config','db','event-bus']` from server/auth/plugin.ts + 1 MODULE.md 9-section count assertion (`grep -cE '^## (Purpose|Public API|Events Emitted|Events Consumed|Queue Produced|Queue Consumed|Invariants|Non-Goals|Dependencies)$' server/auth/MODULE.md` returns 9) + 1 positional assertion authIdx < jobs-pluginIdx (auth must come before jobs because jobs imports requireAdmin from auth barrel for /admin/drain gate) = 6 additive Phase 26 assertions. All previous Phase 17-25 assertions byte-for-byte preserved."
    - "`.planning/phases/26-auth-module/deferred-items.md` NEW: catalog of Phase 26 deferrals + carry-forwards. Phase 26-specific (5): DEFERRED-26-A bootstrap CLI for first admin claim → Phase 28 Go-side; DEFERRED-26-B persistEnvelope 10TH SAMPLE POINT consolidation → Phase 27+ (supersedes DEFERRED-25-A); DEFERRED-26-C OIDC/OAuth integration → v2 territory; DEFERRED-26-D audit log UI → Phase 29 web; DEFERRED-26-E fastify-zod-openapi v5 `required`-emission bug → Phase 27+ (v6 not released through 2026-05-08; supersedes DEFERRED-17-A scope for auth module specifically). Carry-forwards (2): DEFERRED-15-A Map-vs-RequestContext tsc inheritance; DEFERRED-17-A non-auth-module inheritance (api/routes.test, artifacts/artifact-routes.test still affected if not organically resolved by 26-03 route rewrite — check)."
    - "`.planning/STATE.md` updated with Phase 26 close roll-up paragraph mirroring Phase 25 close shape: SC1 closed (auth.key.* events emit + persist with apikey actor proven via subscriber.spec), SC2 closed (4 actor sources resolve correctly proven via als-actor.spec — entry point #1 cron may be it.todo with Phase 18 cross-ref), SC3 substrate complete (Phase 27 owns trace endpoint), SC4 closed (canonical Phase 16 conventions + Nyquist gate); 6-plan structure (26-00 substrate / 26-01 events body + actorSchema spec / 26-02 ALS actor stamp wiring + grep-guards / 26-03 factory + bearer-auth + Zod routes + drain gate / 26-04 DB-gated proofs / 26-05 phase close); deferrals listed (5+2); Phase 27 API Aggregator unblocked. Progress counter increment per phase 26."
    - "`.planning/ROADMAP.md` updated: Phase 26 entry checkbox flipped to ✅ + 'Plans (6 plans across 6 waves)' + 6 plan list items each with date stamp + completion summary; Progress table 'Phase 26. Auth Module' row status changed from 'Not started' to 'Complete'."
    - "Final phase-close sweep: `npm test` (excluding any newly-inherited DEFERRED-26-E files if needed — auth-plugin.spec MAY fail per Pitfall 7); `npm run lint` clean; `npx tsc --noEmit` 0 NEW errors; `npm run dep-check` baseline UNCHANGED (rule 10 contributes 0 on committed code); `npm run nyquist:check` exit 0 with delta ≥ -2pp; `find server/auth/__tests__ -name '*.test.ts' | wc -l` returns 0 (MOD-04 closed); `! grep -r \"'anonymous'\" server/ --include='*.ts' --exclude-dir=__tests__` succeeds (only in test bodies if any cataloged from 26-02 SUMMARY)."
  artifacts:
    - path: "server/auth/MODULE.md"
      provides: "Canonical 9 H2 sections + Runnable Example (MOD-01)"
      contains: "## Public API"
    - path: "server/auth/index.ts"
      provides: "Full barrel — factory + AuthService + requireAdmin + events surface + actor surface (MOD-02)"
      contains: "createAuthModule"
    - path: "server/auth/__tests__/auth-plugin.spec.ts"
      provides: "Renamed from .test.ts (MOD-04); DEFERRED-26-E inherited if v5 bug still bites"
      contains: ""
    - path: "server/auth/__tests__/auth-service.spec.ts"
      provides: "Renamed from .test.ts (MOD-04); imports updated post-mv"
      contains: ""
    - path: "server/__tests__/plugin-order.spec.ts"
      provides: "Phase 26 additive block — 6 new assertions"
      contains: "auth"
    - path: ".planning/phases/26-auth-module/deferred-items.md"
      provides: "5 Phase 26 deferrals + 2 carry-forwards catalog"
      contains: "DEFERRED-26-A"
    - path: ".planning/STATE.md"
      provides: "Phase 26 close roll-up paragraph; Phase 27 unblock"
      contains: "Phase 26"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 26 status flip to Complete"
      contains: "Phase 26"
  key_links:
    - from: "server/auth/MODULE.md §Invariants"
      to: "server/auth/__tests__/{subscriber,admin-claim,als-actor,actor,lifecycle-ownership}.spec.ts"
      via: "Each invariant cites the spec file that proves it"
      pattern: "Pitfall"
    - from: "server/auth/index.ts (full barrel)"
      to: "cross-module consumers (server/jobs/internal/routes.ts requireAdmin import)"
      via: "Re-exports preserve cross-module surface; jobs imports requireAdmin via barrel (MOD-02 compliant)"
      pattern: "requireAdmin"
    - from: "server/__tests__/plugin-order.spec.ts (Phase 26 additive block)"
      to: "server/auth/plugin.ts dependencies literal"
      via: "readFileSync regex-extract verifies 3-entry deps array literal"
      pattern: "config.*db.*event-bus"
---

<objective>
Wave 5 of Phase 26: phase-close. Ships MODULE.md canonical body + full barrel + 2 .test→.spec renames + plugin-order extension + deferred-items + Nyquist gate + STATE/ROADMAP updates. NO production code changes (the rewrites are all done in 26-00..26-04).

Mirrors Phase 25 Plan 25-05 verbatim with name + count substitutions (auth has 2 events vs pipelines' 5; auth has 0 queues vs pipelines' 1; auth has 5 deferrals vs pipelines' 8).

Output:
- MODULE.md 9 H2 sections + Runnable Example
- index.ts full barrel
- 2 .test.ts → .spec.ts renames via git mv
- plugin-order.spec extension (6 new assertions)
- deferred-items.md catalog (5 + 2)
- STATE.md + ROADMAP.md flipped to Complete
- Nyquist gate exit 0 (delta ≥ -2pp)
- Phase 27 API Aggregator and Events API explicitly unblocked
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-auth-module/26-CONTEXT.md
@.planning/phases/26-auth-module/26-RESEARCH.md
@.planning/phases/26-auth-module/26-VALIDATION.md
@.planning/phases/26-auth-module/26-00-PLAN.md
@.planning/phases/26-auth-module/26-01-PLAN.md
@.planning/phases/26-auth-module/26-02-PLAN.md
@.planning/phases/26-auth-module/26-03-PLAN.md
@.planning/phases/26-auth-module/26-04-PLAN.md
@.planning/phases/25-pipelines-module/25-05-PLAN.md
@.planning/phases/24-maestro-module/24-05-PLAN.md
@.planning/phases/25-pipelines-module/deferred-items.md
@server/maestro/MODULE.md
@server/pipelines/MODULE.md
@server/__tests__/plugin-order.spec.ts

<interfaces>
<!-- Existing precedent that this close-out plan extends -->

server/pipelines/MODULE.md (most recent canonical 9-section body — verbatim template).

server/__tests__/plugin-order.spec.ts current shape: contains additive Phase 17-25 blocks. Phase 26 appends Phase 26 block at the end.

deferred-items.md template (Phase 25):
- Phase-specific section
- Carry-forward section
- Each entry: ID, brief description, target phase, link to spec/code

.planning/STATE.md current paragraph (~lines 30-50): Phase 25 CLOSED roll-up. Plan 26-05 PREPENDS a Phase 26 CLOSED roll-up paragraph + updates progress counter.

.planning/ROADMAP.md row table: 'Phase 26. Auth Module | v3.0 | 0/TBD | Not started | -' becomes 'Phase 26. Auth Module | v3.0 | 6/6 | Complete | 2026-05-08'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 5.1: Write canonical MODULE.md (9 H2 sections + Runnable Example) + expand server/auth/index.ts to full surface barrel</name>
  <files>server/auth/MODULE.md, server/auth/index.ts</files>
  <read_first>
    - server/pipelines/MODULE.md (most recent canonical 9-section body)
    - server/maestro/MODULE.md (alternative reference)
    - server/auth/MODULE.md (Plan 26-00 placeholder)
    - server/auth/events.ts (post-Plan 26-01 — full body for Public API enumeration)
    - server/auth/internal/{module,auth-service,actor,require-admin,key-routes}.ts (post-Plan 26-03 — public exports for barrel)
    - server/auth/index.ts (post-Plan 26-03 — 2-line barrel; this plan extends to full surface)
  </read_first>
  <action>
    OVERWRITE `server/auth/MODULE.md` with canonical 9 H2 sections (this is markdown content, NOT a shell script):

    Sections to write IN ORDER:
    1. ## Purpose — 1 paragraph describing Bearer API-key auth + Zod routes + auth.key.* events + TRACE-10 actor stamping + DEFERRED-23-A admin gate.
    2. ## Public API — bullet list enumerating createAuthModule, AuthService, MatchedApiKey, requireAdmin, events surface (authRegistry/makeAuthEmitters/AUTH_EVENT_NAMES/AUTH_AGGREGATE_ID/AUTH_AGGREGATE_TYPE/2 payload schemas/AuthRegistry/AuthEmitters), actor surface (actorSchema/Actor/asApiKeyActor/asUserActor/SYSTEM_ACTOR/CRON_ACTOR). Then HTTP routes: POST/GET/DELETE /admin/keys + POST /api/keys/:id/claims/admin.
    3. ## Events Emitted — 2 events: auth.key.created (persisted: true) + auth.key.revoked (persisted: true). Both stamp aggregateType:'auth', aggregateId:apiKeyId, actor from ALS.
    4. ## Events Consumed — NONE. Phase 26 is emit-only.
    5. ## Queue Produced — NONE.
    6. ## Queue Consumed — NONE.
    7. ## Invariants — numbered list with 5+ invariants each citing a spec file:
       1. actorSchema regex rejects 'anonymous' — actor.spec
       2. bearer-auth callback writes ALS actor before returning true (no concurrent bleed) — als-actor.spec Test 5
       3. requireAdmin returns 403 RFC 7807 on missing claim — admin-claim.spec
       4. grantAdminClaim uses jsonb_set semantics (preserves other claim keys) — admin-claim.spec Test 6
       5. auth.key.created/revoked persist with TRACE-08 actor — subscriber.spec
       6. NO 'anonymous' literal in production code — lifecycle-ownership.spec Test 5
       7. No deep imports into server/auth/internal/ — dep-cruiser rule 10 + dep-cruiser.spec MOD-02 auth extension
    8. ## Non-Goals — list DEFERRED-26-A through E with one-line descriptions.
    9. ## Dependencies — `{ name: 'auth', dependencies: ['config', 'db', 'event-bus'] }`. Cross-module consumers via barrel: `server/jobs/internal/routes.ts` imports requireAdmin for /admin/drain gate.

    Then a final ## Runnable Example section with two code blocks:
    (a) SQL bootstrap (UPDATE api_keys SET claims = jsonb_set(...));
    (b) HTTP curl POST /admin/keys + DELETE /admin/keys/:id + psql events table inspect query;
    (c) TypeScript bus subscribe example (createAuthModule + bus.on('auth.key.created', ...)).

    Use the Phase 25 server/pipelines/MODULE.md as the structural template — copy the heading hierarchy + content density verbatim with auth-specific substitutions.

    OVERWRITE `server/auth/index.ts` with full surface barrel. PRESERVE the strict 1-line MOD-02 form on the FIRST line, then add named re-exports:

    ```typescript
    /**
     * Auth module — public barrel (MOD-02).
     *
     * Strict 1-line internal/module re-export FIRST, followed by the canonical
     * full surface for cross-module consumers + back-compat:
     *  - factory + AuthService + MatchedApiKey + requireAdmin
     *  - events surface (registry, emitters, schemas, types)
     *  - actor surface (regex + helpers)
     *
     * NO `export *`. Each export is named.
     */
    export { createAuthModule, type AuthModule } from './internal/module.js';
    export { AuthService, type MatchedApiKey } from './internal/auth-service.js';
    export { requireAdmin } from './internal/require-admin.js';
    export {
      authRegistry,
      makeAuthEmitters,
      AUTH_EVENT_NAMES,
      AUTH_AGGREGATE_TYPE,
      AUTH_AGGREGATE_ID,
      authKeyCreatedPayloadSchema,
      authKeyRevokedPayloadSchema,
      type AuthRegistry,
      type AuthEmitters,
      type AuthEventName,
      type AuthKeyCreatedPayload,
      type AuthKeyRevokedPayload,
    } from './events.js';
    export {
      actorSchema,
      asApiKeyActor,
      asUserActor,
      SYSTEM_ACTOR,
      CRON_ACTOR,
      type Actor,
    } from './internal/actor.js';
    ```

    Run `npx tsc --noEmit` — verify ZERO new errors. If any export name doesn't exist (e.g., `AuthEmitters` was named differently in events.ts), adjust the barrel to match the actual export.
  </action>
  <verify>
    <automated>grep -cE '^## (Purpose|Public API|Events Emitted|Events Consumed|Queue Produced|Queue Consumed|Invariants|Non-Goals|Dependencies)$' server/auth/MODULE.md && grep -c "createAuthModule" server/auth/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^## (Purpose|Public API|Events Emitted|Events Consumed|Queue Produced|Queue Consumed|Invariants|Non-Goals|Dependencies)$' server/auth/MODULE.md` returns 9
    - `grep -c "Runnable Example" server/auth/MODULE.md` >= 1 (H2 or H3 section title)
    - `grep -c "DEFERRED-26-A" server/auth/MODULE.md` >= 1 (Non-Goals enumeration)
    - `grep -c "createAuthModule" server/auth/index.ts` >= 1 (first line MOD-02 strict)
    - `grep -c "requireAdmin" server/auth/index.ts` >= 1
    - `grep -c "actorSchema" server/auth/index.ts` >= 1
    - `grep -c "AUTH_EVENT_NAMES" server/auth/index.ts` >= 1
    - `grep -c "export \*" server/auth/index.ts` returns 0 (no wildcard exports — Phase 16 anti-pattern)
    - `npx tsc --noEmit` ZERO new errors
    - First line of `server/auth/index.ts` matches `^export { createAuthModule, type AuthModule } from './internal/module.js';` (MOD-02 strict invariant)
  </acceptance_criteria>
  <done>
    MODULE.md 9 H2 sections + Runnable Example shipped; index.ts full surface barrel exposes factory + AuthService + requireAdmin + events surface + actor surface; tsc clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.2: 2 .test → .spec renames via git mv (MOD-04 closure for auth)</name>
  <files>server/auth/__tests__/auth-plugin.spec.ts, server/auth/__tests__/auth-service.spec.ts</files>
  <read_first>
    - server/auth/__tests__/auth-plugin.test.ts (current — verify still .test.ts; will become .spec.ts)
    - server/auth/__tests__/auth-service.test.ts (current — verify still .test.ts; will become .spec.ts)
  </read_first>
  <action>
    Run 2 `git mv` commands preserving blame (100% similarity):

    ```bash
    git mv server/auth/__tests__/auth-plugin.test.ts server/auth/__tests__/auth-plugin.spec.ts
    git mv server/auth/__tests__/auth-service.test.ts server/auth/__tests__/auth-service.spec.ts
    ```

    INSPECT the renamed files for import-path references that broke from Plan 26-03's git mv:
    - `auth-service.spec.ts` (post-rename) likely imports `from '../auth-service.js'` which is BROKEN after Plan 26-03 moved auth-service.ts to internal/. Update to `from '../internal/auth-service.js'`. Single-line edit per file.
    - `auth-plugin.spec.ts` may import `from '../auth-plugin.js'` (BROKEN) — change to `from '../plugin.js'` (post-rename).

    Run each renamed spec:

    ```bash
    npx vitest run server/auth/__tests__/auth-plugin.spec.ts
    npx vitest run server/auth/__tests__/auth-service.spec.ts
    ```

    EXPECTED behaviour:
    - `auth-service.spec.ts`: PASSES (pure unit tests; not subject to DEFERRED-17-A).
    - `auth-plugin.spec.ts`: MAY FAIL with the inherited DEFERRED-17-A `fastify-zod-openapi` v5 `required`-emission bug. If so:
      1. Document the failure mode in 26-05-SUMMARY.md.
      2. Carry forward as DEFERRED-26-E (Plan 26-05 Task 5.4 deferred-items.md).
      3. Do NOT touch the test bodies in this plan.
      4. Verify `npm test` configuration excludes this file (matching Phase 17/18/19 inheritance pattern). If excluded list lives in `vitest.config.ts`, ADD the new path so phase-close sweep doesn't break.

    If the rewrite under Plan 26-03 organically fixed DEFERRED-17-A for this file (route schemas were rewritten), `auth-plugin.spec.ts` MIGHT now pass — verify by running it. If passes: DEFERRED-26-E scope SHRINKS to 0 auth-module files; deferred-items.md still notes the v6 release status as informational.

    `find server/auth/__tests__ -name '*.test.ts' | wc -l` MUST return 0 (MOD-04 closed for auth).
  </action>
  <verify>
    <automated>find server/auth/__tests__ -name '*.test.ts' | wc -l && test -f server/auth/__tests__/auth-plugin.spec.ts && test -f server/auth/__tests__/auth-service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `find server/auth/__tests__ -name '*.test.ts' | wc -l` returns 0
    - `test -f server/auth/__tests__/auth-plugin.spec.ts` succeeds
    - `test -f server/auth/__tests__/auth-service.spec.ts` succeeds
    - `git log --follow server/auth/__tests__/auth-plugin.spec.ts` shows pre-rename history (blame preserved)
    - `git log --follow server/auth/__tests__/auth-service.spec.ts` shows pre-rename history (blame preserved)
    - Import paths inside renamed files updated to match Plan 26-03's git mv (`../internal/auth-service.js`, `../plugin.js`)
    - `npx vitest run server/auth/__tests__/auth-service.spec.ts` PASSES (pure unit; not DEFERRED-17-A blocked)
    - `npx vitest run server/auth/__tests__/auth-plugin.spec.ts` outcome documented (PASS = DEFERRED-26-E scope shrinks; FAIL = carry forward as DEFERRED-26-E)
    - `npx tsc --noEmit` ZERO new errors
  </acceptance_criteria>
  <done>
    MOD-04 closed for auth (zero .test.ts under server/auth/__tests__/); blame preserved; import-path fixups complete; auth-plugin.spec.ts outcome documented (pass or DEFERRED-26-E).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.3: Extend server/__tests__/plugin-order.spec.ts with Phase 26 additive block (6 new assertions)</name>
  <files>server/__tests__/plugin-order.spec.ts</files>
  <read_first>
    - server/__tests__/plugin-order.spec.ts (current — find the Phase 25 additive block; insert Phase 26 block after it)
    - .planning/phases/25-pipelines-module/25-05-PLAN.md Task 5.3 (precedent additive block)
    - server/auth/plugin.ts (post-Plan 26-03 — verify dependencies literal `['config', 'db', 'event-bus']`)
    - server/auth/MODULE.md (post-Task 5.1 — verify 9 H2 sections)
  </read_first>
  <action>
    EDIT `server/__tests__/plugin-order.spec.ts`. Locate the existing Phase 25 additive block. Insert AFTER it a new Phase 26 block with 6 it-blocks:

    1. `Phase 26: config registers before auth-plugin` — printed.indexOf('config') < wbIndex(printed, 'auth')
    2. `Phase 26: db-plugin registers before auth-plugin` — wbIndex(printed, 'db-plugin') < wbIndex(printed, 'auth')
    3. `Phase 26: event-bus registers before auth-plugin` — wbIndex(printed, 'event-bus') < wbIndex(printed, 'auth')
    4. `Phase 26: auth registers before jobs-plugin (jobs uses requireAdmin from auth barrel)` — wbIndex(printed, 'auth') < wbIndex(printed, 'jobs-plugin')
    5. `Phase 26: server/auth/plugin.ts deps literal === ['config','db','event-bus']` — readFileSync + regex extract `dependencies:\s*\[([^\]]+)\]`; split + trim; expect.toEqual(['config','db','event-bus'])
    6. `Phase 26: server/auth/MODULE.md has 9 H2 sections (MOD-01)` — readFileSync; match the 9-section heading regex; expect length === 9

    Adapt `wbIndex` and `readFileSync` imports to the existing helpers used in the plugin-order.spec file. Phase 25 block has the canonical pattern — copy verbatim.

    PRESERVE all existing assertions byte-for-byte. Only ADD the Phase 26 block.

    Run `npx vitest run server/__tests__/plugin-order.spec.ts -t "Phase 26"` — expect 6 new green.

    NOTE: There is a documented Phase 17 fastify-zod-openapi substring-match bug in plugin-order.spec (websocket-plugin substring-of-fastify-websocket — see Phase 23 STATE.md). If the test fails on a pre-existing assertion (NOT the new Phase 26 block), DO NOT fix in this plan — that's DEFERRED-17-A inheritance scope, not Phase 26.

    If the new Phase 26 assertions PASS but pre-existing assertions FAIL: scope-boundary upheld; phase-close sweep documents the inherited failure as Phase 17 carry-forward.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/plugin-order.spec.ts -t "Phase 26"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 26" server/__tests__/plugin-order.spec.ts` >= 6 (6 new it-blocks labeled)
    - `grep -c "auth" server/__tests__/plugin-order.spec.ts` >= 6 (additive references)
    - `npx vitest run server/__tests__/plugin-order.spec.ts -t "Phase 26"` 6/6 pass for ONLY the Phase 26 block (filter by test name)
    - Pre-existing Phase 17-25 assertions byte-for-byte preserved (verify via git diff: only ADDITIVE lines)
    - `npx tsc --noEmit` ZERO new errors
  </acceptance_criteria>
  <done>
    Phase 26 additive block lands; 6 new assertions green; pre-existing assertions preserved; jobs-plugin order constraint enforced (auth before jobs because /admin/drain imports requireAdmin from auth barrel).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.4: Create .planning/phases/26-auth-module/deferred-items.md catalog (5 + 2 entries)</name>
  <files>.planning/phases/26-auth-module/deferred-items.md</files>
  <read_first>
    - .planning/phases/25-pipelines-module/deferred-items.md (most recent template — 8 + 2 entries)
    - .planning/phases/26-auth-module/26-RESEARCH.md (Open Questions §1 — DEFERRED-26-E rationale)
    - .planning/phases/26-auth-module/26-CONTEXT.md (Deferred Ideas section — DEFERRED-26-A through D pre-defined)
  </read_first>
  <action>
    Create `.planning/phases/26-auth-module/deferred-items.md`. Markdown content with 2 sections:

    Section 1 - Phase 26-specific deferrals (5 entries):

    DEFERRED-26-A: Bootstrap CLI command for first admin claim. Phase 26 ships server-side route + SQL runbook; Phase 28 owns Go CLI subcommand.

    DEFERRED-26-B: persistEnvelope 10TH SAMPLE POINT consolidation. Inherited from DEFERRED-25-A. Phase 27 (API Aggregator) is the natural home — touches all module barrels and can refactor without scope creep. Phase 26 reaches the 10TH sample but explicitly does NOT consolidate.

    DEFERRED-26-C: OIDC / OAuth integration. v2 territory; out of scope for spec-driven refactor milestone. actorSchema already accepts user-prefix.

    DEFERRED-26-D: Audit log UI. Phase 27 ships the API endpoint; Phase 29 ships the Web Refactor UI consumer.

    DEFERRED-26-E: fastify-zod-openapi v5 required-emission bug. v5.6.1 emits required-array shape that the validator-compiler rejects on certain request bodies. v6 was NOT released through 2026-05-08. Phase 27+ holistic library upgrade is the natural home. Supersedes DEFERRED-17-A scope for `server/auth/__tests__/auth-plugin.spec.ts` only. Outcome of 26-05 rename: [document here — pass or fail post-rename].

    Section 2 - Carry-forward from prior phases (2 entries):

    DEFERRED-15-A: Map-vs-RequestContext tsc errors (~9 pre-existing in test files using legacy `new Map()` ALS shape). Phase 26 introduces NO new errors of this class.

    DEFERRED-17-A: fastify-zod-openapi v5 emission bug (non-auth scope) — affects api/routes.test.ts + artifacts/artifact-routes.test.ts. Phase 26 carved out auth-plugin.spec.ts into DEFERRED-26-E specifically; the broader carry-forward continues for other affected files.

    Each entry has: ID, "What" 1-line description, "Why deferred" rationale, "Target" target phase, optional "Substrate ready" or "Supersedes" cross-reference.

    No automated verification beyond file existence; the SUMMARY documents the catalog completeness.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-auth-module/deferred-items.md && grep -c "DEFERRED-26-" .planning/phases/26-auth-module/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/26-auth-module/deferred-items.md` succeeds
    - `grep -c "DEFERRED-26-A" .planning/phases/26-auth-module/deferred-items.md` >= 1
    - `grep -c "DEFERRED-26-B" .planning/phases/26-auth-module/deferred-items.md` >= 1
    - `grep -c "DEFERRED-26-C" .planning/phases/26-auth-module/deferred-items.md` >= 1
    - `grep -c "DEFERRED-26-D" .planning/phases/26-auth-module/deferred-items.md` >= 1
    - `grep -c "DEFERRED-26-E" .planning/phases/26-auth-module/deferred-items.md` >= 1
    - `grep -c "DEFERRED-15-A" .planning/phases/26-auth-module/deferred-items.md` >= 1 (carry-forward)
    - `grep -c "DEFERRED-17-A" .planning/phases/26-auth-module/deferred-items.md` >= 1 (carry-forward)
  </acceptance_criteria>
  <done>
    deferred-items.md catalog ships with 5 Phase-26 + 2 carry-forward entries; DEFERRED-26-E supersedes DEFERRED-17-A scope for auth-plugin.spec specifically.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.5: Phase-close sweep — Nyquist gate + STATE.md + ROADMAP.md updates</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (current Phase 25 close roll-up — verify position; this plan PREPENDS Phase 26 paragraph)
    - .planning/ROADMAP.md (current Phase 26 row in progress table; current Phase 26 plan listing — verify shape)
    - .planning/phases/25-pipelines-module/25-05-PLAN.md Task 5.5 (precedent template)
  </read_first>
  <action>
    1. Run `npm run nyquist:check`. EXPECTED: exit 0 with delta ≥ -2pp. If delta < -2pp:
       - Inspect the Nyquist baseline (`.planning/nyquist-baseline.json` — verify it's the unchanged Phase 15 commit).
       - The auth module adds events.ts + actor.ts + module.ts + plugin.ts + 5 spec files (MODULE numerator + denominator both grow).
       - If delta still fails: identify the tests skipped due to TEST_DATABASE_URL absence and consider whether they should be excluded from Nyquist denominator. Document fallback in 26-05-SUMMARY.md.

    2. Run the FULL phase-close sweep:
       ```bash
       npm run lint              # expect: clean
       npx tsc --noEmit          # expect: ZERO new errors (DEFERRED-15-A baseline preserved)
       npm run dep-check         # expect: baseline UNCHANGED (rule 10 zero new violations on committed code)
       npm test                  # expect: green (excluding any DEFERRED-26-E carry-forwards if needed)
       npm run nyquist:check     # expect: exit 0
       ```

    3. UPDATE `.planning/STATE.md`. PREPEND a Phase 26 close roll-up paragraph at the top of the "Current Position" / "Status" area, mirroring the Phase 25 paragraph shape. The paragraph must include:

    - Opening: "Plan 26-05 complete — Phase 26 CLOSED. Auth Module fully migrated to MOD-01..09 conventions:"
    - SC1 closed bullet — Zod on all 4 routes + meta() promotion + auth.key.* events + TRACE-08 both persisted + actor populated. Cite subscriber.spec.
    - SC2 closed bullet — 4 actor sources resolve correctly. Detail entry points #1-4 verbatim. Cite als-actor.spec + Pitfall 1 concurrent test.
    - SC3 substrate ready bullet — Phase 27 owns trace endpoint; Phase 26 lays substrate (events.actor TEXT column stable + populated).
    - SC4 closed bullet — MODULE.md 9 sections + Runnable Example + index.ts full barrel + .test→.spec renames (MOD-04) + plugin-order.spec extension (6 additive Phase 26 assertions) + Nyquist gate exit 0.
    - DEFERRED-23-A RESOLVED paragraph — requireAdmin gates /admin/drain + /admin/drain/resume; admin-claim.spec proves 403/2xx; apiKeys.claims migration 0026; first admin via SQL runbook; POST /api/keys/:id/claims/admin route.
    - 5 deferrals (DEFERRED-26-A through E) + 2 carry-forwards (DEFERRED-15-A + DEFERRED-17-A non-auth scope).
    - Phase 26 CLOSED roll-up: 6 plans across 6 waves. Sweep results numerically. Phase 27 unblocked.

    PRESERVE all prior Phase 15-25 paragraphs below the new Phase 26 roll-up.

    UPDATE the progress table row:
    - From: `| 26. Auth Module | v3.0 | 0/TBD | Not started | - |`
    - To:   `| 26. Auth Module | v3.0 | 6/6 | Complete    | 2026-05-08 |`

    Update progress totals frontmatter:
    - `total_plans: 77` → `total_plans: 83` (77 + 6 from Phase 26)
    - `completed_plans: 77` → `completed_plans: 83`
    - `current_plan: 6` (within Phase 26)
    - `stopped_at: 'Completed 26-05-PLAN.md (Phase 26 CLOSED)'`
    - `last_updated:` to current ISO timestamp
    - `last_activity:` to current date

    UPDATE the Status frontmatter line + "Current focus" line to reflect Phase 26 CLOSED, Phase 27 next.

    4. UPDATE `.planning/ROADMAP.md`:
    - Find the Phase 26 entry (`### Phase 26: Auth Module`).
    - Add `Plans (6 plans across 6 waves):` section listing 6 plans with completion checkmark + date stamp:
      ```
      Plans (6 plans across 6 waves):
      - [x] 26-00-PLAN.md — Wave 0 substrate (events+actor stubs + apiKeys.claims migration + 10th dep-cruiser rule + fixtures) ✅ 2026-05-08
      - [x] 26-01-PLAN.md — Wave 1 events body + actorSchema spec (TRACE-10 substrate validation) ✅ 2026-05-08
      - [x] 26-02-PLAN.md — Wave 2 ALS actor stamp wiring (helpers.ts default migration + onReady wrap + 5 grep-guards) ✅ 2026-05-08
      - [x] 26-03-PLAN.md — Wave 3 factory + bearer-auth + Zod routes + drain gate + admin-bootstrap runbook (10TH persistEnvelope sample point) ✅ 2026-05-08
      - [x] 26-04-PLAN.md — Wave 4 DB-gated proofs (subscriber + admin-claim + als-actor) ✅ 2026-05-08
      - [x] 26-05-PLAN.md — Wave 5 phase close (MODULE.md + barrel + .test→.spec renames + plugin-order extension + deferred-items + Nyquist) ✅ 2026-05-08
      ```
    - Update Phase 26 progress table row to `Complete`.
  </action>
  <verify>
    <automated>npm run nyquist:check && grep -c "Phase 26 CLOSED" .planning/STATE.md && grep -c "Plans (6 plans across 6 waves)" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run nyquist:check` exits 0 (delta ≥ -2pp; baseline UNCHANGED)
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0 OR produces ONLY pre-existing DEFERRED-15-A errors (NO new errors from Phase 26)
    - `npm run dep-check` baseline UNCHANGED (rule 10 contributes 0 new violations on committed code)
    - `npm test` GREEN (excluding any DEFERRED-26-E carry-forwards documented in 26-05-SUMMARY.md)
    - `find server/auth/__tests__ -name '*.test.ts' | wc -l` returns 0 (MOD-04 closed)
    - `! grep -r \"'anonymous'\" server/ --include='*.ts' --exclude-dir=__tests__` returns 0 production-code matches
    - `grep -c "Phase 26 CLOSED" .planning/STATE.md` >= 1 (close roll-up landed)
    - `grep -c "26-00-PLAN.md" .planning/ROADMAP.md` >= 1 (plan list landed)
    - `grep -c "Phase 26. Auth Module .* Complete" .planning/ROADMAP.md` >= 1 (progress table updated)
    - All 6 plans listed in ROADMAP with date stamp
  </acceptance_criteria>
  <done>
    Phase 26 CLOSED. Nyquist gate green; full sweep clean; STATE + ROADMAP updated; Phase 27 API Aggregator unblocked.
  </done>
</task>

</tasks>

<verification>
- `npm run nyquist:check` exit 0
- `npm run lint` clean
- `npx tsc --noEmit` zero NEW errors
- `npm run dep-check` baseline unchanged
- `npm test` green (excluding documented DEFERRED-26-E carry-forwards)
- 6/6 plans in ROADMAP marked complete with date stamp
- `find server/auth/__tests__ -name '*.test.ts' | wc -l` returns 0
- MODULE.md has 9 H2 sections + Runnable Example
- index.ts is full surface barrel with MOD-02 strict 1-line FIRST
- 5 deferred-items entries + 2 carry-forwards documented
- Phase 27 API Aggregator and Events API unblocked
</verification>

<success_criteria>
- ROADMAP §Phase 26 SC1, SC2, SC4 all CLOSED. SC3 substrate ready (Phase 27 owns endpoint).
- DEFERRED-23-A RESOLVED: requireAdmin gates /admin/drain + /admin/drain/resume + /api/keys/:id/claims/admin.
- TRACE-10 fully shipped: 4 actor sources resolve correctly on events.actor; actorSchema rejects 'anonymous'; bearer-auth callback stamps apikey:{id} in ALS; helpers default migrated 'anonymous'→'system'.
- 10TH persistEnvelope sample point reached (DEFERRED-26-B → Phase 27+).
- Phase 26 follows canonical Phase 16 conventions verbatim (MODULE.md / barrel / events.ts / internal/ / tests-as-spec / factory / dep-cruiser rule 10 / plugin-order additive block).
</success_criteria>

<output>
After completion, create `.planning/phases/26-auth-module/26-05-SUMMARY.md` documenting:
- MODULE.md final size + 9 H2 sections confirmed
- index.ts final size + named exports count
- 2 .test→.spec rename outcomes:
  - auth-service.spec.ts: PASS confirmed
  - auth-plugin.spec.ts: PASS or DEFERRED-26-E carry-forward (with reason)
- plugin-order.spec extension: 6 new assertions, all green
- deferred-items.md catalog: 5 + 2 entries
- Nyquist delta (current vs baseline)
- npm test sweep result + any excluded files
- Phase 26 final velocity: total minutes / 6 plans / avg per plan
- Phase 27 API Aggregator and Events API explicitly unblocked
- Cumulative v3.0 progress: 83/83 plans complete (Phases 15-26 all CLOSED)
</output>
