---
phase: 37-platform-extensions
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - server/analysis/MODULE.md
  - server/analysis/index.ts
  - server/analysis/plugin.ts
  - server/analysis/routes.ts
  - server/analysis/schemas.ts
  - server/analysis/events.ts
  - server/analysis/__tests__/routes.spec.ts
  - server/preflight/MODULE.md
  - server/preflight/index.ts
  - server/preflight/plugin.ts
  - server/preflight/routes.ts
  - server/preflight/schemas.ts
  - server/preflight/events.ts
  - server/preflight/rules/__data__/forbidden-symbols.json
  - server/preflight/__tests__/rules.spec.ts
  - server/preflight/__tests__/privacy-manifest.spec.ts
  - server/integrations/github/MODULE.md
  - server/integrations/github/index.ts
  - server/integrations/github/plugin.ts
  - server/integrations/github/routes.ts
  - server/integrations/github/__tests__/routes.spec.ts
  - server/integrations/github/__tests__/webhook-handler.spec.ts
  - server/integrations/github/__tests__/commenter.spec.ts
  - server/jobs/internal/input-broadcaster.ts
  - server/jobs/internal/build-once-deploy-n.ts
  - server/jobs/__tests__/input-broadcaster.spec.ts
  - server/jobs/__tests__/parallel-deploy.spec.ts
  - server/db/schema.ts
  - server/db/migrations/0009_phase37_analyses_preflight_github.sql
  - server/db/__tests__/migration-037.spec.ts
  - cli/internal/macho/parser.go
  - cli/internal/macho/swift5_types.go
  - cli/internal/macho/swift_demangle.go
  - cli/internal/macho/hermes.go
  - cli/internal/macho/heuristics.go
  - cli/internal/macho/parser_test.go
  - cli/internal/macho/hermes_test.go
  - cli/internal/macho/__tests__/fixtures/README.md
  - web/src/routes/builds/[id]/skeleton/+page.svelte
  - web/src/routes/preflight/+page.svelte
  - package.json
autonomous: true
requirements:
  - EXT-IOS-SKELETON
  - EXT-PREFLIGHT
  - EXT-GITHUB-PR
  - EXT-INPUT-BROADCAST
  - EXT-BUILD-ONCE
must_haves:
  truths:
    - "All 4 track scaffolds (server/analysis, server/preflight, server/integrations/github, server/jobs/internal broadcaster+build-once) exist with MODULE.md or stub files referenced by later waves"
    - "All Wave 0 spec stubs from 37-VALIDATION.md exist as files (no MISSING references when later plans run)"
    - "npm deps @octokit/app, @octokit/auth-app, @octokit/webhooks-methods, @plist/parse, bplist-parser, yauzl installed and declared in package.json"
    - "Drizzle migration 0009 adds analyses table + preflight_runs table + pipeline_runs.github_pr_* columns; npx drizzle-kit push applies cleanly to a fresh DB"
    - "cli/internal/macho/ Go package compiles (empty stubs returning errors.New('not implemented')) and parser_test.go + hermes_test.go run (assertions t.Skip() until Wave 1)"
    - "Web stub routes /builds/[id]/skeleton and /preflight render placeholder + load without 500"
  artifacts:
    - path: "server/analysis/MODULE.md"
      provides: "Track A server-side module scaffold (9 H2 sections per MOD-01)"
      contains: "## Purpose"
    - path: "server/preflight/MODULE.md"
      provides: "Track B preflight module scaffold"
      contains: "## Purpose"
    - path: "server/integrations/github/MODULE.md"
      provides: "Track C GitHub plugin scaffold mirroring server/azure"
      contains: "## Purpose"
    - path: "server/preflight/rules/__data__/forbidden-symbols.json"
      provides: "Versioned rule pack with schema_version=1 and rules_updated=2026-05-16"
      contains: "rules_updated"
    - path: "server/db/schema.ts"
      provides: "Schema extension: analyses, preflight_runs tables + pipeline_runs.github_pr_* columns"
      contains: "github_pr_id"
    - path: "server/db/migrations/0009_phase37_analyses_preflight_github.sql"
      provides: "Drizzle migration adding 3 schema deltas in one file"
      contains: "CREATE TABLE analyses"
    - path: "cli/internal/macho/parser.go"
      provides: "Go package stub with exported ParseObjCClasslist signature"
      contains: "package macho"
    - path: "package.json"
      provides: "npm deps for Tracks B+C"
      contains: "@octokit/app"
  key_links:
    - from: "server/analysis/plugin.ts"
      to: "server/index.ts"
      via: "Fastify plugin registration order (Wave 1 will register; Wave 0 only creates the file with a TODO export)"
      pattern: "export default"
    - from: "server/preflight/rules/__data__/forbidden-symbols.json"
      to: "server/preflight/__tests__/rules.spec.ts"
      via: "Test imports rule pack via path"
      pattern: "forbidden-symbols.json"
    - from: "server/db/schema.ts"
      to: "server/integrations/github/internal/module.ts (Wave 1 reads pipeline_runs.github_pr_*)"
      via: "drizzle-orm schema imports"
      pattern: "pipelineRuns"
---

<objective>
Wave 0 substrate for Phase 37: create the entire test/build/CI scaffold so all 4 tracks (A iOS skeleton, B preflight, C GitHub, D parallel patterns) have a place to land code in Wave 1. NO production logic — only MODULE.md scaffolds (9 H2 sections per MOD-01), empty plugins (Fastify factory `createXModule` that returns `null` for now), spec stubs with `it.todo()` placeholders, the rule pack JSON, the Drizzle migration adding tables + columns, the Go package stubs (functions return `errors.New("not implemented")`), web stub routes, and npm deps.

Purpose: Honor the Nyquist gate from `37-VALIDATION.md` — every later task points its `<verify>` at a test command, and that command must resolve before the implementing task starts. This plan resolves all `❌ Wave 0` entries in 37-VALIDATION.md.

Output: ~40 new files + 1 schema extension + 1 migration + 1 package.json bump. Zero functional logic.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-platform-extensions/37-BRIEF.md
@.planning/phases/37-platform-extensions/37-CONTEXT.md
@.planning/phases/37-platform-extensions/37-RESEARCH.md
@.planning/phases/37-platform-extensions/37-VALIDATION.md

# Reference for plugin shape (MIRROR — do not import)
@server/azure/plugin.ts
@server/azure/routes.ts
@server/azure/index.ts
@server/azure/internal/module.ts
@server/jobs/MODULE.md
@server/jobs/index.ts

<interfaces>
<!-- Patterns Wave 0 executor must mirror -->

Phase 16 MOD-01..09 conventions enforced for new modules:
- MODULE.md sections: Purpose / Public API / Events Emitted / Events Consumed / Queue Produced / Queue Consumed / Invariants / Non-Goals / Dependencies (+ Runnable Example footer)
- index.ts barrel: strict 1-line `./internal/module.js` re-export FIRST + named exports of events/schemas surface (NO `export *`)
- plugin.ts: thin Fastify plugin via `fp(asyncFn, { name, dependencies: [...] })` decorating `fastify.{xxxModule}`
- internal/module.ts: `createXxxModule(deps): XxxModule | null` factory (returns null if config absent)
- __tests__/*.spec.ts (NOT .test.ts — MOD-04)

Drizzle schema additions (from 37-RESEARCH.md §Schema Additions):
```typescript
// Add to server/db/schema.ts pipelineRuns table:
githubPrId: text('github_pr_id'),
githubPrUrl: text('github_pr_url'),
githubPrCommentId: text('github_pr_comment_id'),
githubInstallationId: bigint('github_installation_id', { mode: 'number' }),
githubRepoOwner: text('github_repo_owner'),
githubRepoName: text('github_repo_name'),

// New tables:
export const analyses = pgTable('analyses', {
  id: uuid('id').primaryKey().defaultRandom(),
  buildArtifactId: uuid('build_artifact_id'),
  jobId: uuid('job_id').references(() => jobs.id, { onDelete: 'set null' }),
  platform: platformEnum('platform').notNull(),
  payload: jsonb('payload').notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
});

export const preflightRuns = pgTable('preflight_runs', {
  id: uuid('id').primaryKey().defaultRandom(),
  platform: platformEnum('platform').notNull(),
  filename: text('filename').notNull(),
  fileSizeBytes: bigint('file_size_bytes', { mode: 'number' }).notNull(),
  rulesVersion: text('rules_version').notNull(),
  status: text('status').notNull(), // 'pass' | 'pass_with_warnings' | 'blocked'
  blockers: jsonb('blockers').default([]).notNull(),
  warnings: jsonb('warnings').default([]).notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold server tracks A/B/C/D + spec stubs + rule pack JSON</name>
  <files>
    server/analysis/MODULE.md,
    server/analysis/index.ts,
    server/analysis/plugin.ts,
    server/analysis/routes.ts,
    server/analysis/schemas.ts,
    server/analysis/events.ts,
    server/analysis/__tests__/routes.spec.ts,
    server/preflight/MODULE.md,
    server/preflight/index.ts,
    server/preflight/plugin.ts,
    server/preflight/routes.ts,
    server/preflight/schemas.ts,
    server/preflight/events.ts,
    server/preflight/rules/__data__/forbidden-symbols.json,
    server/preflight/__tests__/rules.spec.ts,
    server/preflight/__tests__/privacy-manifest.spec.ts,
    server/integrations/github/MODULE.md,
    server/integrations/github/index.ts,
    server/integrations/github/plugin.ts,
    server/integrations/github/routes.ts,
    server/integrations/github/__tests__/routes.spec.ts,
    server/integrations/github/__tests__/webhook-handler.spec.ts,
    server/integrations/github/__tests__/commenter.spec.ts,
    server/jobs/internal/input-broadcaster.ts,
    server/jobs/internal/build-once-deploy-n.ts,
    server/jobs/__tests__/input-broadcaster.spec.ts,
    server/jobs/__tests__/parallel-deploy.spec.ts
  </files>
  <behavior>
    - Test: `npx vitest run server/analysis server/preflight server/integrations/github server/jobs/__tests__/input-broadcaster server/jobs/__tests__/parallel-deploy` exits 0 with all describe blocks reporting `it.todo` entries (test count > 0 but no failures)
    - Test: Each MODULE.md grep proves exactly 9 H2 sections (`grep -c '^## ' server/analysis/MODULE.md` == 9; same for preflight + github)
    - Test: Each index.ts has zero `export *` statements (`! grep '^export \*' server/{analysis,preflight,integrations/github}/index.ts`)
    - Test: forbidden-symbols.json parses as JSON and has `schema_version: 1`, `rules_updated: "2026-05-16"`, `rules` array length >= 2 (seed with ITMS-91053-USERDEFAULTS + ATT-MISSING-FOR-IDFA from 37-RESEARCH.md §Code Examples)
    - Test: `cat server/integrations/github/plugin.ts | grep -E "dependencies: \['config', 'db', 'pipelines-plugin'\]"` succeeds (mirrors server/azure/plugin.ts:147)
  </behavior>
  <read_first>
    - server/azure/plugin.ts (mirror shape; especially fp() footer with `dependencies: ['config','db','pipelines-plugin']`)
    - server/azure/index.ts (mirror barrel: strict 1-line + named exports, no `export *`)
    - server/azure/internal/module.ts (mirror `createXModule(deps): XModule | null` factory)
    - server/jobs/MODULE.md (mirror 9 H2 section headings exactly)
    - 37-VALIDATION.md §Wave 0 Requirements (lists every spec file path)
    - 37-RESEARCH.md §Code Examples Example 1 (rule pack JSON shape — copy literally for the seed pack)
  </read_first>
  <action>
    Create 27 scaffold files. Each module follows Phase 23/26 conventions exactly:

    **server/analysis/** (Track A server side):
    - `MODULE.md` with 9 H2 sections (Purpose: "Stores iOS skeleton analyses ingested from `device-farm analyze`"; Public API: TODO; Events Emitted: `analysis.ingested` TODO; Events Consumed: none; Queue Produced/Consumed: none; Invariants: payload schema versioned; Non-Goals: extraction itself lives in CLI; Dependencies: `['config', 'db']`)
    - `index.ts`: strict 1-line `export { createAnalysisModule, type AnalysisModule } from './internal/module.js';` + `export { default as analysisPlugin } from './plugin.js';` + `export { skeletonPayloadSchema, type SkeletonPayload } from './schemas.js';`
    - `plugin.ts`: stub Fastify plugin via `fp(async (fastify) => { fastify.decorate('analysisModule', null); }, { name: 'analysis-plugin', dependencies: ['config', 'db'] })`. Add `declare module 'fastify'` block for the decorator type.
    - `routes.ts`: export `registerAnalysisRoutes(app, deps): void` stub with no routes registered yet (Wave 1 adds POST /api/builds/:id/skeleton)
    - `schemas.ts`: Zod `skeletonPayloadSchema` matching 37-BRIEF.md Output JSON shape (schema_version, platform, app, react_native_bundle nullable, stats, candidate_screens[], deep_link_entries[], known_gaps[]). Export `z.infer` type.
    - `events.ts`: stub `ANALYSIS_EVENT_NAMES = { INGESTED: 'analysis.ingested' as const }` (no body yet)
    - `__tests__/routes.spec.ts`: vitest describe('analysis routes', () => { it.todo('POST /api/builds/:id/skeleton stores payload') })

    **server/preflight/** (Track B):
    - `MODULE.md` 9 H2 sections; Purpose: "Greenlight App Store preflight scanner — IPA/APK static analysis against versioned rule pack"; Events Emitted: `preflight.completed` (TODO Wave 1); Dependencies: `['config', 'db']`
    - `index.ts`: 1-line internal/ re-export + plugin + schemas surface
    - `plugin.ts`: same stub shape as analysis. `dependencies: ['config', 'db']`, name `'preflight-plugin'`
    - `routes.ts`: stub `registerPreflightRoutes` no routes yet
    - `schemas.ts`: Zod `preflightReportSchema` (status: 'pass'|'pass_with_warnings'|'blocked'; blockers: array; warnings: array; rules_version: string; platform: 'ios'|'android')
    - `events.ts`: `PREFLIGHT_EVENT_NAMES = { COMPLETED: 'preflight.completed' as const }`
    - `rules/__data__/forbidden-symbols.json`: copy the JSON from 37-RESEARCH.md §Code Examples Example 1 verbatim (schema_version: 1, rules_updated: "2026-05-16", platform: "ios", rules: [ITMS-91053-USERDEFAULTS, ATT-MISSING-FOR-IDFA])
    - `__tests__/rules.spec.ts`: describe('preflight rules', () => { it('rule pack is well-formed JSON with required fields', async () => { const p = await import('../rules/__data__/forbidden-symbols.json'); expect(p.schema_version).toBe(1); expect(p.rules_updated).toBe('2026-05-16'); expect(Array.isArray(p.rules)).toBe(true); expect(p.rules.length).toBeGreaterThanOrEqual(2); }); it.todo('known-bad.ipa returns blockers'); it.todo('known-good.ipa returns pass') })
    - `__tests__/privacy-manifest.spec.ts`: describe('privacy manifest parser', () => { it.todo('detects missing NSPrivacyAccessedAPITypes'); it.todo('matches required_api_type from rule pack') })

    **server/integrations/github/** (Track C — MIRROR server/azure):
    - `MODULE.md` 9 H2 sections; Purpose: "GitHub App webhook receiver + PR commenter (mirrors server/azure for GitHub provider)"; Events Consumed: pipeline.run.started/completed/failed; Dependencies: `['config', 'db', 'pipelines-plugin']`
    - `index.ts`: 1-line `./internal/module.js` re-export + `export { default as githubPlugin } from './plugin.js';` (no Wave 0 schema barrel — Wave 1 adds)
    - `plugin.ts`: stub mirroring server/azure/plugin.ts:144-147 footer — `export default fp(githubPlugin, { name: 'github-plugin', dependencies: ['config', 'db', 'pipelines-plugin'] })`. Inside: `fastify.decorate('githubModule', null)` (Wave 1 wires real factory)
    - `routes.ts`: export `registerGithubRoutes(app, deps): void` stub
    - `__tests__/routes.spec.ts`: describe('github routes', () => { it.todo('rejects forged x-hub-signature-256'); it.todo('accepts valid signature') })
    - `__tests__/webhook-handler.spec.ts`: describe('github webhook handler', () => { it.todo('dispatches pull_request.opened'); it.todo('ignores irrelevant events') })
    - `__tests__/commenter.spec.ts`: describe('github commenter', () => { it.todo('upsert creates new comment when github_pr_comment_id null'); it.todo('upsert edits existing comment') })

    **server/jobs/internal/** (Track D — add to existing module):
    - `input-broadcaster.ts`: export factory `createInputBroadcaster(deps): InputBroadcaster` returning `{ broadcast: async (sessionIds, action) => { throw new Error('not implemented — wave 1') } }`. Type the result as `BroadcastResult[]` per 37-RESEARCH.md Pattern 4.
    - `build-once-deploy-n.ts`: export `runParallelDeploy(deps, job): Promise<ParallelDeployResult>` stub throwing 'not implemented — wave 1'. Result type matches 37-RESEARCH.md Pattern 5 shape.
    - `__tests__/input-broadcaster.spec.ts`: describe('input broadcaster', () => { it.todo('fans out tap to 3 sessions within 100ms'); it.todo('partial failure surfaces per-session error'); it.todo('successful sends preserved on partial failure') })
    - `__tests__/parallel-deploy.spec.ts`: describe('parallel deploy', () => { it.todo('aggregates per-device results'); it.todo('port exhaustion returns 503 with Retry-After'); it.todo('build artifact uploaded once for N devices').skip })

    Use vitest `it.todo()` for all production-pending assertions; this lets Wave 1 flip them to real assertions while satisfying the Nyquist "test exists" gate. Do NOT register the new plugins in `server/index.ts` yet — Wave 1 will (avoids surfacing null decorators to production startup).
  </action>
  <verify>
    <automated>npx vitest run server/analysis server/preflight server/integrations/github server/jobs/__tests__/input-broadcaster server/jobs/__tests__/parallel-deploy 2>&amp;1 | grep -E '(Tests|passed|failed|todo)' &amp;&amp; grep -c '^## ' server/analysis/MODULE.md | grep -q '^9$' &amp;&amp; grep -c '^## ' server/preflight/MODULE.md | grep -q '^9$' &amp;&amp; grep -c '^## ' server/integrations/github/MODULE.md | grep -q '^9$' &amp;&amp; node -e "const p=require('./server/preflight/rules/__data__/forbidden-symbols.json'); if(p.schema_version!==1||p.rules.length&lt;2) process.exit(1)"</automated>
  </verify>
  <done>27 scaffold files exist; each MODULE.md has exactly 9 H2 sections; rule pack JSON parses with schema_version=1 and ≥2 rules; vitest reports it.todo entries for every Wave 0 placeholder; zero functional logic shipped (every stub throws or is no-op).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DB schema extension + migration 0009 + migration test + npm deps</name>
  <files>
    server/db/schema.ts,
    server/db/migrations/0009_phase37_analyses_preflight_github.sql,
    server/db/__tests__/migration-037.spec.ts,
    package.json
  </files>
  <behavior>
    - Test: `npx drizzle-kit push` against a fresh test DB succeeds with the new migration applied (or `npx drizzle-kit generate` produces a diff matching the hand-written 0009 file)
    - Test: `npx vitest run server/db/__tests__/migration-037.spec.ts` proves analyses + preflight_runs tables exist with the documented columns and pipeline_runs has the 6 new github_pr_* columns
    - Test: `cat package.json | jq -r '.dependencies | to_entries[] | select(.key | startswith("@octokit/") or contains("plist") or contains("bplist") or contains("yauzl")) | .key'` returns 6 lines (@octokit/app, @octokit/auth-app, @octokit/webhooks-methods, @plist/parse, bplist-parser, yauzl)
    - Test: `node -e "require('@octokit/webhooks-methods').verify"` does not throw (proves install completed)
  </behavior>
  <read_first>
    - server/db/schema.ts lines 211-230 (pipelineRuns table — extending here)
    - server/db/migrations/0006_pipeline_runs_azure_pr_comment.sql (pattern for ALTER TABLE migrations in this project)
    - 37-RESEARCH.md §Schema Additions (literal SQL for all three schema deltas)
    - 37-VALIDATION.md §Wave 0 Requirements npm deps list (last bullet)
  </read_first>
  <action>
    **1) Extend server/db/schema.ts:**
    - Add 6 columns to existing `pipelineRuns` table after the `azurePrIntegrationId` column (~line 224): `githubPrId: text('github_pr_id')`, `githubPrUrl: text('github_pr_url')`, `githubPrCommentId: text('github_pr_comment_id')`, `githubInstallationId: bigint('github_installation_id', { mode: 'number' })`, `githubRepoOwner: text('github_repo_owner')`, `githubRepoName: text('github_repo_name')`.
    - Add new `analyses` table after the existing tables (per 37-RESEARCH.md §Schema Additions). FK `jobId` references `jobs(id) ON DELETE SET NULL`; `platform` uses the existing platform enum; `payload jsonb`.
    - Add new `preflightRuns` table per 37-RESEARCH.md §Schema Additions. No FK; `platform` uses existing enum; `blockers` + `warnings` are `jsonb` defaulting to `[]`.
    - Update the `bigint` import if needed: `import { ... bigint ... } from 'drizzle-orm/pg-core'`.

    **2) Write migration server/db/migrations/0009_phase37_analyses_preflight_github.sql:**
    ```sql
    ALTER TABLE "pipeline_runs" ADD COLUMN "github_pr_id" text;--&gt; statement-breakpoint
    ALTER TABLE "pipeline_runs" ADD COLUMN "github_pr_url" text;--&gt; statement-breakpoint
    ALTER TABLE "pipeline_runs" ADD COLUMN "github_pr_comment_id" text;--&gt; statement-breakpoint
    ALTER TABLE "pipeline_runs" ADD COLUMN "github_installation_id" bigint;--&gt; statement-breakpoint
    ALTER TABLE "pipeline_runs" ADD COLUMN "github_repo_owner" text;--&gt; statement-breakpoint
    ALTER TABLE "pipeline_runs" ADD COLUMN "github_repo_name" text;--&gt; statement-breakpoint
    CREATE INDEX IF NOT EXISTS "pipeline_runs_github_pr_idx" ON "pipeline_runs" ("github_installation_id","github_pr_id") WHERE "github_pr_id" IS NOT NULL;--&gt; statement-breakpoint
    CREATE TABLE IF NOT EXISTS "analyses" (
      "id" uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL,
      "build_artifact_id" uuid,
      "job_id" uuid,
      "platform" "platform" NOT NULL,
      "payload" jsonb NOT NULL,
      "created_at" timestamp DEFAULT now() NOT NULL,
      CONSTRAINT "analyses_job_id_fkey" FOREIGN KEY ("job_id") REFERENCES "jobs"("id") ON DELETE SET NULL
    );--&gt; statement-breakpoint
    CREATE INDEX IF NOT EXISTS "analyses_job_id_idx" ON "analyses"("job_id");--&gt; statement-breakpoint
    CREATE TABLE IF NOT EXISTS "preflight_runs" (
      "id" uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL,
      "platform" "platform" NOT NULL,
      "filename" text NOT NULL,
      "file_size_bytes" bigint NOT NULL,
      "rules_version" text NOT NULL,
      "status" text NOT NULL,
      "blockers" jsonb DEFAULT '[]'::jsonb NOT NULL,
      "warnings" jsonb DEFAULT '[]'::jsonb NOT NULL,
      "created_at" timestamp DEFAULT now() NOT NULL
    );
    ```
    Then run `npx drizzle-kit generate` to update `server/db/migrations/meta/_journal.json` (or hand-edit if generate refuses).

    **3) Write server/db/__tests__/migration-037.spec.ts:**
    Use the existing DB-gated test pattern (look at one of the `subscriber.spec.ts` files in pipelines/auth for the `DEVICE_FARM_DB_URL` gate). Test:
    - `analyses` table exists with columns id, build_artifact_id, job_id, platform, payload, created_at
    - `preflight_runs` table exists with columns id, platform, filename, file_size_bytes, rules_version, status, blockers, warnings, created_at
    - `pipeline_runs` has the 6 new github_pr_* columns
    - Index `pipeline_runs_github_pr_idx` exists
    Use `db.execute(sql\`SELECT column_name FROM information_schema.columns WHERE table_name = 'analyses'\`)` to introspect.

    **4) Update package.json dependencies:**
    Run `npm install @octokit/app@^15 @octokit/auth-app@^7 @octokit/webhooks-methods@^5 @plist/parse@^1 bplist-parser@^0.3 yauzl@^3` and `npm install -D @types/yauzl`. Commit the resulting package.json + package-lock.json deltas.

    Do NOT register the migration in any plugin yet — Wave 1 will run it via the existing drizzle migration system at boot.
  </action>
  <verify>
    <automated>npx tsc --noEmit server/db/schema.ts &amp;&amp; node -e "require('@octokit/app'); require('@octokit/auth-app'); require('@octokit/webhooks-methods').verify; require('@plist/parse'); require('bplist-parser'); require('yauzl')" &amp;&amp; test -f server/db/migrations/0009_phase37_analyses_preflight_github.sql &amp;&amp; grep -q "CREATE TABLE.*analyses" server/db/migrations/0009_phase37_analyses_preflight_github.sql &amp;&amp; grep -q "github_pr_id" server/db/schema.ts</automated>
  </verify>
  <done>Drizzle schema.ts has 6 new pipeline_runs columns + analyses + preflight_runs tables; migration 0009 SQL file applies cleanly; migration-037.spec.ts proves all columns/tables exist (test gated on DB URL — skips if not configured but file exists); 6 npm deps installed and importable; npx tsc --noEmit passes for the changed schema file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Go macho package stubs + Hermes fixture + web stub routes</name>
  <files>
    cli/internal/macho/parser.go,
    cli/internal/macho/swift5_types.go,
    cli/internal/macho/swift_demangle.go,
    cli/internal/macho/hermes.go,
    cli/internal/macho/heuristics.go,
    cli/internal/macho/parser_test.go,
    cli/internal/macho/hermes_test.go,
    cli/internal/macho/__tests__/fixtures/README.md,
    web/src/routes/builds/[id]/skeleton/+page.svelte,
    web/src/routes/preflight/+page.svelte
  </files>
  <behavior>
    - Test: `cd cli &amp;&amp; go build ./internal/macho/...` succeeds (zero compile errors)
    - Test: `cd cli &amp;&amp; go test ./internal/macho/...` runs (tests skip via t.Skip with "wave 1 implements" message)
    - Test: `npm run web:build` produces output without errors for both new routes
    - Test: `grep -c 'package macho' cli/internal/macho/*.go` returns 5 (all 5 files declare package)
    - Test: Hermes magic bytes constant present: `grep -q '0xC6, 0x1F, 0xBC, 0x03' cli/internal/macho/hermes.go`
  </behavior>
  <read_first>
    - /Users/heicg/Desktop/projects/_reference/app-explorer/app_explorer/skeleton/ios.py:136-172 (otool parser shape) — STUDY-ONLY, do NOT add as dep
    - /Users/heicg/Desktop/projects/_reference/app-explorer/app_explorer/skeleton/react_native.py:19, 138-143 (Hermes magic byte)
    - cli/cmd/doctor.go (shell-out style — exec.Command pattern used throughout this repo)
    - 37-RESEARCH.md §Pattern 3 (Mach-O parsing Go example) and §Code Examples Example 3 (Hermes magic byte Go)
    - 37-RESEARCH.md §Common Pitfalls Pitfall 2 (swift-demangle batch size 2000)
  </read_first>
  <action>
    **Go package cli/internal/macho/** — 5 source files + 2 test files + fixture README:

    **parser.go:**
    ```go
    package macho

    import (
      "errors"
      "regexp"
    )

    type ObjCClass struct {
      MangledName       string
      SuperclassExternal string
    }

    // ParseObjCClasslist parses `otool -ov &lt;binary&gt;` output for the
    // __objc_classlist section, matching app-explorer/skeleton/ios.py:136-172.
    // Wave 0 stub — Wave 1 implements.
    func ParseObjCClasslist(binary string) ([]ObjCClass, error) {
      return nil, errors.New("not implemented — wave 1 (37-01)")
    }

    // Exported regex anchors so tests can verify them once Wave 1 lands.
    var (
      ClassHeaderRe = regexp.MustCompile(`^[0-9a-f]{16} 0x[0-9a-f]+\s*$`)
      NameLineRe    = regexp.MustCompile(`^ {8}name\s+0x[0-9a-f]+\s+(\S+)\s*$`)
      SuperLineRe   = regexp.MustCompile(`^    superclass\s+0x[0-9a-f]+(?:\s+(\S+))?\s*$`)
    )
    ```

    **swift5_types.go:**
    ```go
    package macho

    import "errors"

    type Swift5Type struct {
      MangledName string
      Module      string
    }

    func ParseSwift5Types(binary string) ([]Swift5Type, error) {
      return nil, errors.New("not implemented — wave 1 (37-01)")
    }
    ```

    **swift_demangle.go:**
    ```go
    package macho

    import "errors"

    // SwiftDemangleBatchSize matches app-explorer's SWIFT_DEMANGLE_BATCH = 2000.
    // Larger batches deadlock the pipe (Pitfall 2 in 37-RESEARCH.md).
    const SwiftDemangleBatchSize = 2000

    func DemangleBatch(symbols []string) ([]string, error) {
      return nil, errors.New("not implemented — wave 1 (37-01)")
    }
    ```

    **hermes.go:**
    ```go
    package macho

    import (
      "bytes"
      "io"
      "os"
    )

    // HermesMagic — Hermes bytecode magic bytes (8 bytes).
    // Source: app-explorer/skeleton/react_native.py:19.
    var HermesMagic = []byte{0xC6, 0x1F, 0xBC, 0x03, 0xC1, 0x03, 0x19, 0x1F}

    func IsHermes(bundlePath string) bool {
      f, err := os.Open(bundlePath)
      if err != nil {
        return false
      }
      defer f.Close()
      var buf [8]byte
      if _, err := io.ReadFull(f, buf[:]); err != nil {
        return false
      }
      return bytes.Equal(buf[:], HermesMagic)
    }

    // ExtractHermesScreens runs `strings -a -n 8 &lt;bundle&gt;` + regex for
    // `[A-Z][a-z][A-Za-z]{2,40}?Screen` then filters concatenation artifacts
    // per app-explorer/react_native.py:72-128. Wave 0 stub.
    func ExtractHermesScreens(bundlePath string) ([]string, error) {
      return nil, nil // empty result, not an error — keeps callers simple in Wave 0
    }
    ```

    **heuristics.go:**
    ```go
    package macho

    // Confidence per app-explorer/skeleton/ios.py _SCREEN_HEURISTICS.
    type Confidence string

    const (
      ConfidenceHigh   Confidence = "high"
      ConfidenceMedium Confidence = "medium"
      ConfidenceLow    Confidence = "low"
    )

    // ClassifyName returns the confidence tier for a class name.
    // Wave 0 stub — Wave 1 implements *ViewController -&gt; high etc.
    func ClassifyName(name string) Confidence {
      return ConfidenceLow
    }
    ```

    **parser_test.go:**
    ```go
    package macho

    import "testing"

    func TestParseObjCClasslistStub(t *testing.T) {
      _, err := ParseObjCClasslist("/dev/null")
      if err == nil {
        t.Fatal("expected error from Wave 0 stub")
      }
    }

    func TestRegexAnchorsCompile(t *testing.T) {
      // Wave 1 will replace with real fixture-driven assertions.
      if ClassHeaderRe == nil || NameLineRe == nil || SuperLineRe == nil {
        t.Fatal("regex anchors must be non-nil")
      }
      t.Skip("wave 1 implements real fixture parse (37-01)")
    }
    ```

    **hermes_test.go:**
    ```go
    package macho

    import (
      "os"
      "path/filepath"
      "testing"
    )

    func TestIsHermesPositive(t *testing.T) {
      // Write a temp file with the magic bytes prefix; assert IsHermes returns true.
      tmp := filepath.Join(t.TempDir(), "fake-hermes.hbc")
      if err := os.WriteFile(tmp, append(HermesMagic, 0x00, 0x00), 0o644); err != nil {
        t.Fatal(err)
      }
      if !IsHermes(tmp) {
        t.Fatal("IsHermes should return true for magic-byte prefixed file")
      }
    }

    func TestIsHermesNegative(t *testing.T) {
      tmp := filepath.Join(t.TempDir(), "not-hermes")
      if err := os.WriteFile(tmp, []byte("nothing to see here"), 0o644); err != nil {
        t.Fatal(err)
      }
      if IsHermes(tmp) {
        t.Fatal("IsHermes should return false for non-magic file")
      }
    }

    func TestHermesArtifactsFilter(t *testing.T) {
      t.Skip("wave 1 implements concatenation-artifact filter (Pitfall 3)")
    }
    ```

    **__tests__/fixtures/README.md** (placeholder — Wave 1 commits binary fixtures):
    ```markdown
    # macho test fixtures

    Wave 1 (Plan 37-01) commits:
    - `good.ipa` — small known-good native iOS app (~1MB)
    - `hermes-rn.ipa` — RN+Hermes app for scan tests
    - `known-bad.ipa` — preflight blocker fixture (uses ASIdentifierManager without ATT)

    Fixtures excluded from this Wave 0 plan to keep commit size small.
    ```

    **Web stub routes:**
    - `web/src/routes/builds/[id]/skeleton/+page.svelte`: minimal Svelte 5 page with `&lt;script lang="ts"&gt;let { data } = $props();&lt;/script&gt;&lt;h1&gt;Skeleton — Build {data.buildId ?? '?'}&lt;/h1&gt;&lt;p&gt;Wave 1 (Plan 37-01) renders the screen list.&lt;/p&gt;`. Page should not crash if `data` is empty (use optional chaining).
    - `web/src/routes/preflight/+page.svelte`: minimal Svelte 5 page `&lt;h1&gt;Preflight&lt;/h1&gt;&lt;p&gt;Drag-drop IPA/APK — coming in Plan 37-02.&lt;/p&gt;`. No data loader required.

    Do NOT touch `cli/cmd/analyze.go` (Wave 1 creates it). The Go package above is consumed by that Wave 1 file.
  </action>
  <verify>
    <automated>cd cli &amp;&amp; go build ./internal/macho/... &amp;&amp; go test ./internal/macho/... 2&gt;&amp;1 | grep -E '(PASS|SKIP|FAIL)' &amp;&amp; cd /Users/heicg/Desktop/projects/device-farm &amp;&amp; npm run web:build 2&gt;&amp;1 | tail -20 | grep -qv 'error TS' &amp;&amp; grep -q '0xC6, 0x1F, 0xBC, 0x03' cli/internal/macho/hermes.go &amp;&amp; test -f web/src/routes/builds/'[id]'/skeleton/+page.svelte &amp;&amp; test -f web/src/routes/preflight/+page.svelte</automated>
  </verify>
  <done>cli/internal/macho/ compiles cleanly; 2 IsHermes tests pass (magic-byte detect works in Wave 0 — it's a 10-line function); 2 stub tests pass via t.Skip or expected-error assertions; web build succeeds with both new routes registered; SwiftDemangleBatchSize constant = 2000 (Pitfall 2 enforced); HermesMagic constant matches app-explorer reference bytes.</done>
</task>

</tasks>

<verification>
1. `npx vitest run server/analysis server/preflight server/integrations/github server/jobs/__tests__/input-broadcaster server/jobs/__tests__/parallel-deploy server/db/__tests__/migration-037` — all describe blocks report `todo` or `passed`, zero `failed`
2. `cd cli && go build ./internal/macho/... && go test ./internal/macho/...` — compiles + tests pass (with t.Skip for stubs)
3. `npm run web:build` — succeeds; both new web routes registered
4. `node -e "require('@octokit/app'); require('@octokit/webhooks-methods').verify"` — does not throw
5. `grep -c '^## ' server/{analysis,preflight,integrations/github}/MODULE.md` — all report 9
6. `npx drizzle-kit push` against a fresh test DB applies migration 0009 cleanly
</verification>

<success_criteria>
- All 27 server scaffold files + 8 cli/web files + 1 schema + 1 migration + package.json bump committed
- Wave 0 gap list from 37-VALIDATION.md fully resolved (every file marked ❌ now exists)
- Every Wave 1+ task can reference a real test file path via its `<verify>` block
- Zero functional logic — every stub throws "not implemented" or is a no-op
- `npm test` reports zero new failures (only new `todo` entries)
- Phase 23 module conventions enforced (MOD-01 9 sections, MOD-02 no `export *`, MOD-04 .spec.ts not .test.ts)
- Dep-cruiser: no new violations introduced (`npm run dep-check` count unchanged from pre-37)
</success_criteria>

<output>
After completion, create `.planning/phases/37-platform-extensions/37-00-SUMMARY.md` documenting:
- 27 server scaffold files committed (paths + line counts)
- DB migration 0009 SQL applied to dev DB
- 6 npm deps installed (versions from package-lock.json)
- Go macho package compiles, IsHermes works end-to-end in Wave 0 (only function with real logic)
- Wave 1 unblocked: all 4 track plans (37-01..37-04) can run in parallel
</output>
