---
phase: 04-go-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-04
files_modified:
  - cli/internal/client/jobs.go
  - cli/internal/client/devices.go
  - cli/internal/client/types.go
  - cli/cmd/status.go
  - cli/cmd/status_test.go
  - cli/cmd/devices.go
  - cli/cmd/devices_test.go
  - cli/cmd/cancel.go
  - cli/cmd/cancel_test.go
autonomous: true
requirements:
  - CLI-02
  - CLI-04
  - CLI-05

must_haves:
  truths:
    - "`device-farm status <job-id>` displays job status, platform, duration, and step results in a formatted table"
    - "`device-farm status <job-id> --json` outputs full job JSON"
    - "`device-farm devices` displays a table of all devices with NAME, PLATFORM, STATUS, CURRENT JOB columns"
    - "`device-farm devices --json` outputs device list as JSON array"
    - "`device-farm cancel <job-id>` cancels a job and prints confirmation"
    - "All commands exit 2 on infrastructure errors (server unreachable, bad config)"
    - "Status symbols and colors render correctly for TTY and degrade for non-TTY"
  artifacts:
    - path: "cli/internal/client/types.go"
      provides: "Go structs matching server API response shapes (Job, JobStep, Device)"
    - path: "cli/internal/client/jobs.go"
      provides: "GetJob, ListJobs, CancelJob API methods"
    - path: "cli/internal/client/devices.go"
      provides: "ListDevices API method"
    - path: "cli/cmd/status.go"
      provides: "status command with formatted job display"
    - path: "cli/cmd/status_test.go"
      provides: "Unit tests for status command using httptest"
    - path: "cli/cmd/devices.go"
      provides: "devices command with tabwriter table"
    - path: "cli/cmd/devices_test.go"
      provides: "Unit tests for devices command using httptest"
    - path: "cli/cmd/cancel.go"
      provides: "cancel command with DELETE call"
    - path: "cli/cmd/cancel_test.go"
      provides: "Unit tests for cancel command using httptest"
  key_links:
    - from: "cli/cmd/status.go"
      to: "cli/internal/client/jobs.go"
      via: "GetJob call with resolved server URL and API key"
      pattern: "client\\.GetJob"
    - from: "cli/cmd/devices.go"
      to: "cli/internal/client/devices.go"
      via: "ListDevices call"
      pattern: "client\\.ListDevices"
    - from: "cli/cmd/cancel.go"
      to: "cli/internal/client/jobs.go"
      via: "CancelJob DELETE call"
      pattern: "client\\.CancelJob"
---

<objective>
Implement the simple REST-only commands: status, devices, and cancel. These consume GET/DELETE endpoints and display formatted output. Includes unit tests for all three commands.

Purpose: Delivers three of seven CLI commands that only need HTTP client (no WebSocket). Users can inspect jobs, view device pool, and cancel jobs.
Output: Three working commands with table and JSON output modes, proper exit codes, color/symbol formatting, and unit tests.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-go-cli/04-CONTEXT.md
@.planning/phases/04-go-cli/04-RESEARCH.md
@.planning/phases/04-go-cli/04-01-SUMMARY.md
@.planning/phases/04-go-cli/04-04-SUMMARY.md

<interfaces>
<!-- From Plan 01 - HTTP client and config -->

cli/internal/client/client.go exports:
```go
type Client struct { BaseURL, APIKey string; HTTPClient *http.Client }
func NewClient(baseURL, apiKey string) *Client
func (c *Client) do(ctx context.Context, method, path string, body io.Reader, contentType string) (*http.Response, error)
type ExitError struct { Code int; Message string }
type ProblemDetail struct { Type, Title, Detail, Instance string; Status int }
```

cli/internal/config/resolve.go exports:
```go
func ResolveServerURL(flagValue string) string
func ResolveAPIKey(flagValue string) string
func ResolvePlatform(flagValue string) string
```

<!-- From Plan 04 - Output helpers -->

cli/internal/output/ exports:
```go
func PrintTable(w io.Writer, headers []string, rows [][]string)
func StatusColor(status string) *color.Color
func StatusSymbol(status string, isTTY bool) string
func PrintJSON(w io.Writer, v any) error
```

cli/cmd/root.go exports:
```go
var ServerFlag, APIKeyFlag string
var JSONOutput, NoColor, IsTTY bool
```

Server API responses (from server/api/routes.ts):
- GET /api/jobs/:id -> { id, status, platform, createdAt, startedAt, completedAt, metadata, maestroOutput, result, steps: [{flowName, command, status, durationMs, stepIndex}] }
- GET /api/devices -> [{ name, platform, status, emulatorId, currentJobId }]
- DELETE /api/jobs/:id -> { status: 'cancelled' }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client methods and response types</name>
  <files>
    cli/internal/client/types.go
    cli/internal/client/jobs.go
    cli/internal/client/devices.go
  </files>
  <action>
    1. Create `cli/internal/client/types.go` with Go structs matching server responses:
       - Job struct: ID, Status, Platform, CreatedAt, StartedAt, CompletedAt (string pointers for nullable), Metadata (map[string]any), Result (map[string]any, nullable), Steps []JobStep
       - JobStep struct: FlowName, Command (string pointer), Status, DurationMs (*int), StepIndex int
       - Device struct: Name, Platform, Status, EmulatorID, CurrentJobID (string pointer)
       - JobListResponse struct: Data []Job, Cursor string, HasMore bool
       - CancelResponse struct: Status string
       - Use json struct tags matching server camelCase (e.g., `json:"flowName"`)

    2. Create `cli/internal/client/jobs.go`:
       - GetJob(ctx, jobID string) (*Job, error) -- GET /api/jobs/:id, decode JSON into Job struct
       - GetJobLogs(ctx, jobID string) (string, error) -- GET /api/jobs/:id/logs, return .logs field
       - ListJobs(ctx context.Context, params ListJobsParams) (*JobListResponse, error) -- GET /api/jobs with query params (cursor, limit, status, platform)
       - CancelJob(ctx, jobID string) error -- DELETE /api/jobs/:id
       - ListJobsParams struct: Cursor, Status, Platform string; Limit int
       - NOTE: Do NOT add CreateJob here -- that goes in submit.go (Plan 03)

    3. Create `cli/internal/client/devices.go`:
       - ListDevices(ctx context.Context) ([]Device, error) -- GET /api/devices
  </action>
  <verify>
    <automated>cd /Users/heicg/Desktop/projects/device-farm/cli && go build ./...</automated>
  </verify>
  <done>API client types compile correctly with JSON tags matching server response shape. GetJob, ListJobs, CancelJob, GetJobLogs, ListDevices methods all compile.</done>
</task>

<task type="auto">
  <name>Task 2: Status, devices, and cancel commands with unit tests</name>
  <files>
    cli/cmd/status.go
    cli/cmd/status_test.go
    cli/cmd/devices.go
    cli/cmd/devices_test.go
    cli/cmd/cancel.go
    cli/cmd/cancel_test.go
  </files>
  <action>
    1. Create `cli/cmd/status.go`:
       - `status` command, takes job-id as first arg (required, cobra.ExactArgs(1))
       - Creates client via config.ResolveServerURL/ResolveAPIKey, calls client.GetJob
       - Human-readable output:
         - Header line: "Job {id}" with status symbol + colored status
         - Fields: Platform, Created, Started, Completed (format durations), Device
         - If job has result with summary: print "Results: {passed} passed, {failed} failed, {skipped} skipped"
         - If job has steps: print step table with FLOW, COMMAND, STATUS, DURATION columns
         - Use StatusSymbol and StatusColor for step status rendering
         - Failed steps highlighted in red
       - --json mode: output full Job struct as JSON
       - Exit code: 0 if passed, 1 if failed, 2 on infrastructure error
       - Register on rootCmd in init()

    2. Create `cli/cmd/devices.go`:
       - `devices` command, no args required
       - Creates client, calls client.ListDevices
       - Table output: NAME, PLATFORM, STATUS, CURRENT JOB columns via output.PrintTable
       - Status column uses StatusSymbol + StatusColor (idle->green, running->cyan, error->red, etc.)
       - Current job shows job ID or "-" if nil
       - --json mode: output device array as JSON
       - Register on rootCmd in init()

    3. Create `cli/cmd/cancel.go`:
       - `cancel` command, takes job-id as first arg (required, cobra.ExactArgs(1))
       - Creates client, calls client.CancelJob
       - Success: print "Job {id} cancelled" with yellow warning symbol
       - --json mode: output {"status": "cancelled", "jobId": "..."}
       - Register on rootCmd in init()

    4. Create `cli/cmd/status_test.go` using httptest mock server:
       - TestStatusDisplaysJobDetails: mock GET /api/jobs/:id returning job JSON, verify output contains job ID, status, platform
       - TestStatusWithSteps: mock response with steps array, verify step table rendered
       - TestStatusJSON: verify --json flag outputs valid JSON matching job struct
       - TestStatusServerError: mock 500 response, verify exit code 2
       - Helper: newTestServer(t, handler) that returns httptest.Server + cleanup

    5. Create `cli/cmd/devices_test.go` using httptest mock server:
       - TestDevicesTable: mock GET /api/devices returning device list, verify table headers and device rows
       - TestDevicesJSON: verify --json outputs JSON array
       - TestDevicesEmpty: mock empty array, verify "No devices found" or empty table
       - TestDevicesServerError: mock error, verify exit code 2

    6. Create `cli/cmd/cancel_test.go` using httptest mock server:
       - TestCancelSuccess: mock DELETE /api/jobs/:id returning 200, verify confirmation message
       - TestCancelJSON: verify --json output
       - TestCancelNotFound: mock 404, verify error message
       - TestCancelServerError: mock 500, verify exit code 2

    All commands:
    - Use config.ResolveServerURL(ServerFlag) and config.ResolveAPIKey(APIKeyFlag) for client creation
    - Return ExitError{Code: 2} on client/server errors
    - Respect IsTTY for symbol selection
  </action>
  <verify>
    <automated>cd /Users/heicg/Desktop/projects/device-farm/cli && go build -o bin/device-farm . && go test ./cmd/ -run "TestStatus|TestDevices|TestCancel" -v -count=1 && ./bin/device-farm status --help && ./bin/device-farm devices --help && ./bin/device-farm cancel --help</automated>
  </verify>
  <done>
    - `device-farm status <job-id>` shows job details with colored status, step table, and summary
    - `device-farm devices` shows device table with aligned columns
    - `device-farm cancel <job-id>` sends DELETE and prints confirmation
    - All three commands support --json flag for structured output
    - All three commands show proper help text
    - Unit tests pass for all three commands (TestStatus, TestDevices, TestCancel)
  </done>
</task>

</tasks>

<verification>
- `cd cli && go build ./...` compiles without errors
- `cd cli && go test ./cmd/ -run "TestStatus|TestDevices|TestCancel" -v -count=1` all tests pass
- `./bin/device-farm status --help` shows usage with job-id argument
- `./bin/device-farm devices --help` shows usage
- `./bin/device-farm cancel --help` shows usage with job-id argument
- All commands use resolved config (server URL, API key) from flag/env/config chain
- `./bin/device-farm devices --json` outputs valid JSON
</verification>

<success_criteria>
- Three new commands registered and accessible via compiled binary
- Status command displays job details with step table and colored/symboled status indicators
- Devices command shows kubectl-style aligned device table
- Cancel command sends DELETE and confirms cancellation
- All commands support --json output mode
- Exit codes: 0 success, 2 infrastructure error
- Unit tests pass for status, devices, and cancel commands
</success_criteria>

<output>
After completion, create `.planning/phases/04-go-cli/04-02-SUMMARY.md`
</output>
