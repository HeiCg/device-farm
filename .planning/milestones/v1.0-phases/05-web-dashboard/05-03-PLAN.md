---
phase: 05-web-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/routes/devices/+page.svelte
  - web/src/lib/api/devices.ts
  - web/src/lib/components/devices/DeviceCard.svelte
  - web/src/routes/settings/+page.svelte
  - server/api/routes.ts
autonomous: true
requirements:
  - UI-05
  - UI-06

must_haves:
  truths:
    - "Device grid page shows all emulators/simulators with their current state"
    - "Device states update on the page via periodic polling (5s interval)"
    - "Settings page displays current server configuration"
    - "GET /api/config endpoint returns sanitized server config"
  artifacts:
    - path: "web/src/routes/devices/+page.svelte"
      provides: "Device grid page with live status"
      min_lines: 40
    - path: "web/src/lib/components/devices/DeviceCard.svelte"
      provides: "Device card with state badge"
    - path: "web/src/routes/settings/+page.svelte"
      provides: "Settings page showing config"
      min_lines: 30
    - path: "web/src/lib/api/devices.ts"
      provides: "Device API client"
      exports: ["listDevices", "restartDevice"]
  key_links:
    - from: "web/src/routes/devices/+page.svelte"
      to: "/api/devices"
      via: "listDevices() in onMount + setInterval(5s)"
      pattern: "listDevices.*setInterval|interval"
    - from: "web/src/routes/settings/+page.svelte"
      to: "/api/config"
      via: "apiFetch in onMount"
      pattern: "api.*config"
    - from: "server/api/routes.ts"
      to: "fastify.config"
      via: "GET /config route reading config decoration"
      pattern: "config.*route|GET.*config"
---

<objective>
Build the Device Grid and Settings pages.

Purpose: Device grid gives operators visibility into emulator/simulator fleet health. Settings page shows the current server configuration.
Output: Working Device grid page (UI-05) with polling updates, and Settings page (UI-06) with a new /api/config endpoint.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-dashboard/05-RESEARCH.md
@.planning/phases/05-web-dashboard/05-01-SUMMARY.md

<interfaces>
From web/src/lib/api/client.ts (Plan 01):
```typescript
export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>;
```

From web/src/lib/api/types.ts (Plan 01):
```typescript
export type DeviceState = 'booting' | 'idle' | 'allocated' | 'running' | 'cleanup' | 'error' | 'offline';
export interface Device { id: string; name: string; platform: Platform; state: DeviceState; emulatorId: string; currentJobId: string | null; }
```

From server/config/schema.ts:
```typescript
export type AppConfig = z.infer<typeof configSchema>;
// Config has: server { host, port }, database_url, pool { android: [...], ios: [...] }, storage { ... }, jobs { ... }
```

From server/api/routes.ts:
```typescript
// Existing routes: jobRoutes, deviceRoutes, healthRoute -- all registered at /api prefix
// GET /devices returns Device[]
// POST /devices/:id/restart
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Device API client, DeviceCard component, and Device Grid page</name>
  <files>web/src/lib/api/devices.ts, web/src/lib/components/devices/DeviceCard.svelte, web/src/routes/devices/+page.svelte</files>
  <action>
1. **web/src/lib/api/devices.ts**: Device API functions:
   - `listDevices()`: GET /devices, returns `Device[]`
   - `restartDevice(id: string)`: POST /devices/:id/restart

2. **web/src/lib/components/devices/DeviceCard.svelte**: Card for a single device. Props via `$props()`: `device: Device`, optional `onrestart` callback.
   - Display: device name, platform badge (Android/iOS), state badge with color coding:
     - idle: green, running: blue, booting: yellow, allocated: cyan, cleanup: orange, error: red, offline: gray
   - Show emulatorId in smaller text
   - Show current job ID (truncated, linked to /jobs/{id}) if running
   - Restart button (visible only when state is 'error'): calls onrestart callback
   - Tailwind: bg-white rounded-lg shadow-sm p-4 border border-gray-200, state indicator as colored dot + text

3. **web/src/routes/devices/+page.svelte** (UI-05):
   - On mount: fetch `listDevices()`, store in `$state`
   - Set up 5-second polling interval with `setInterval` -- refetch listDevices and update state
   - Clean up interval on destroy (return cleanup from `onMount` or use `onDestroy`)
   - Render page heading "Devices" with device count
   - Summary bar: counts by state (N idle, N running, N error) as colored badges
   - Grid layout: lg:grid-cols-3, md:grid-cols-2, sm:grid-cols-1
   - Render DeviceCard for each device
   - Restart handler: calls `restartDevice(id)`, then refetches list
   - Loading state on initial load, error display on failure
   - Group devices by platform (Android section, iOS section) with subheadings
  </action>
  <verify>
    <automated>cd /Users/heicg/Desktop/projects/device-farm/web && npm run check 2>&1 | tail -5</automated>
  </verify>
  <done>Device grid page renders all devices grouped by platform with state badges, polls every 5s, restart button shows on errored devices</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/config endpoint and Settings page</name>
  <files>server/api/routes.ts, web/src/routes/settings/+page.svelte</files>
  <action>
1. **server/api/routes.ts**: Add a new `configRoute` function alongside existing routes:
   - `GET /config`: Returns sanitized server config from `fastify.config`
   - Sanitize: Include server (host, port), pool (android count + names, ios count + names), storage (base paths, retention days, max storage), jobs (timeout, queue settings)
   - EXCLUDE: database_url (contains credentials) -- replace with "***" or omit entirely
   - Export the new `configRoute` function
   - Register it in `server/api/plugin.ts` alongside jobRoutes, deviceRoutes, healthRoute: `await fastify.register(configRoute, { prefix: '/api' });`

2. **web/src/routes/settings/+page.svelte** (UI-06):
   - On mount: fetch `apiFetch<any>('/config')` (generic since config shape is flexible)
   - Display config sections in a clean layout:
     - **Server**: host, port in a key-value table
     - **Pool**: Android devices (count + names), iOS devices (count + names)
     - **Storage**: base path, retention days, max storage GB
     - **Jobs**: timeout, queue settings
   - Each section: bg-white rounded-lg shadow-sm p-6 with section heading
   - Key-value display: use `<dl>` or grid with label (text-gray-500) and value (text-gray-900 font-medium)
   - Read-only display only -- no edit functionality
   - Loading/error states
  </action>
  <verify>
    <automated>cd /Users/heicg/Desktop/projects/device-farm && npx tsc --noEmit --pretty 2>&1 | tail -10 && cd web && npm run check 2>&1 | tail -5</automated>
  </verify>
  <done>GET /api/config returns sanitized config (no database_url), Settings page renders all config sections in readable key-value format</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` succeeds
- `npx tsc --noEmit` passes for server (new config route)
- Device grid page renders device cards grouped by platform
- Settings page fetches and displays config sections
- Polling interval is cleaned up on navigation
</verification>

<success_criteria>
Device grid page shows all emulators/simulators with color-coded state badges, polls every 5 seconds for updates, and offers restart for errored devices. Settings page displays sanitized server configuration via a new GET /api/config endpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard/05-03-SUMMARY.md`
</output>
