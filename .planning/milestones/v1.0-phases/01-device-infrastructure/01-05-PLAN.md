---
phase: 01-device-infrastructure
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - server/pool/plugin.ts
autonomous: true
gap_closure: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - INFRA-07
  - INFRA-08

must_haves:
  truths:
    - "Server starts without plugin dependency assertion errors"
    - "TypeScript compiles production code (server/**/*.ts excluding tests) without errors"
  artifacts:
    - path: "server/pool/plugin.ts"
      provides: "Pool plugin with correct dependency name and logger types"
      contains: "dependencies: ['config']"
  key_links:
    - from: "server/pool/plugin.ts"
      to: "server/config/plugin.ts"
      via: "Fastify plugin dependency declaration"
      pattern: "dependencies:\\s*\\['config'\\]"
    - from: "server/pool/plugin.ts"
      to: "server/pool/pool-manager.ts"
      via: "PoolManager constructor receives compatible logger type"
      pattern: "fastify\\.log as pino\\.Logger"
---

<objective>
Fix two blocker gaps found during Phase 1 verification: plugin dependency name mismatch and TypeScript logger type incompatibility in server/pool/plugin.ts.

Purpose: Phase 1 code is functionally correct (74 tests pass) but the server cannot start due to a Fastify plugin dependency assertion, and production TypeScript does not compile due to logger type mismatches. Both issues are in a single file.
Output: server/pool/plugin.ts compiles cleanly and server can start with all plugins wired.
</objective>

<execution_context>
@/Users/heicg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heicg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-device-infrastructure/01-VERIFICATION.md

<interfaces>
<!-- Key types the executor needs to understand the fix -->

From server/config/plugin.ts (line 14 — the name that must be matched):
```typescript
export default fp(async (fastify) => {
  const config = loadConfig();
  fastify.decorate('config', config);
}, { name: 'config' });  // <-- registered as 'config', NOT 'config-plugin'
```

From server/pool/pool-manager.ts (line 17 — constructor signature):
```typescript
constructor(config: AppConfig, processTracker: ProcessTracker, logger: pino.Logger)
```

From server/pool/process-tracker.ts (line 15 — constructor signature):
```typescript
constructor(logger: pino.Logger, execFile?: ExecFileFn)
```

From server/pool/health-checker.ts (line 21 — constructor signature):
```typescript
constructor(poolManager: PoolManager, processTracker: ProcessTracker, logger: pino.Logger)
```

Note: `fastify.log` is typed as `FastifyBaseLogger` which is structurally compatible with `pino.Logger` at runtime but not assignable at the TypeScript level. All three constructors expect `pino.Logger`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix plugin dependency name and logger type casting in pool plugin</name>
  <files>server/pool/plugin.ts</files>
  <action>
Two changes in server/pool/plugin.ts:

1. **Fix plugin dependency name (line 45):** Change `dependencies: ['config-plugin']` to `dependencies: ['config']`. The config plugin registers itself as `'config'` in server/config/plugin.ts, not `'config-plugin'`.

2. **Fix logger type mismatch (lines 20, 21, 31):** Add `import type pino from 'pino';` at the top of the file. Then cast `fastify.log` to `pino.Logger` when passing to constructors. Create a local variable: `const logger = fastify.log as unknown as pino.Logger;` and use it for all three constructor calls (ProcessTracker on line 20, PoolManager on line 21, HealthChecker on line 31). The `as unknown as pino.Logger` double-cast is needed because FastifyBaseLogger and pino.Logger are structurally similar but TypeScript does not consider them directly assignable. This is safe because Fastify's logger IS pino at runtime.

Do NOT change the constructor signatures in pool-manager.ts, process-tracker.ts, or health-checker.ts — the cast in plugin.ts is the minimal fix that keeps those modules correctly typed against pino.
  </action>
  <verify>
    <automated>cd /Users/heicg/Desktop/projects/device-farm && npx tsc --noEmit 2>&1 | grep -c "server/pool/plugin.ts" | xargs -I{} test {} -eq 0 && echo "PASS: No TS errors in plugin.ts" || echo "FAIL: TS errors remain in plugin.ts"</automated>
  </verify>
  <done>
    - `dependencies: ['config']` on line 45 (matches config plugin name)
    - No TypeScript errors in server/pool/plugin.ts when running `npx tsc --noEmit`
    - All existing tests still pass (`npx vitest run` — 74 tests)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` reports zero errors in server/pool/plugin.ts (Gap 2 closed)
2. `npx vitest run` — all 74 existing tests still pass (no regressions)
3. `grep "dependencies.*config" server/pool/plugin.ts` shows `dependencies: ['config']` (Gap 1 closed)
</verification>

<success_criteria>
- server/pool/plugin.ts declares `dependencies: ['config']` matching the config plugin's registered name
- server/pool/plugin.ts compiles without TypeScript errors (logger type properly cast)
- All 74 existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-device-infrastructure/01-05-SUMMARY.md`
</output>
